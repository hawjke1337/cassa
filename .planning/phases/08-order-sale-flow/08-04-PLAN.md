---
phase: 08-order-sale-flow
plan: 04
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - src/actions/sales.ts
  - src/components/pos/return-form.tsx
autonomous: true
requirements:
  - FIN-07
  - FIN-09
  - FIN-10
must_haves:
  truths:
    - "createReturn требует refundMethod и валидирует soft set: refundMethod ∈ set(sale.payments.methods)"
    - "При RETURNED/PARTIALLY_RETURNED Sale с customOrderId — CustomOrder.status синхронизирован: full return → CANCELLED, partial → остаётся COMPLETED"
    - "createReturn транзакция атомарна — mid-tx throw не оставляет Sale.status изменённым (Prisma auto-rollback)"
    - "UI return-form содержит Select refundMethod с опциями из оригинальных методов оплаты"
    - "Wave 0 E2E order-return-sync, return-midway-failure, order-payment-constraints (refundMethod part) → GREEN"
  artifacts:
    - path: "src/actions/sales.ts"
      provides: "hardened createReturn с refundMethod validation + CustomOrder sync"
      contains: "refundMethod"
    - path: "src/components/pos/return-form.tsx"
      provides: "Return form с обязательным Select refundMethod"
      contains: "refundMethod"
  key_links:
    - from: "src/actions/sales.ts createReturn"
      to: "CustomOrder status sync"
      via: "tx.customOrder.update({ status: CANCELLED })"
      pattern: "customOrder\\.update"
    - from: "src/components/pos/return-form.tsx"
      to: "src/actions/sales.ts createReturn"
      via: "form submit → server action"
      pattern: "refundMethod"
---

<objective>
Wave 2 Plan B (parallel с 08-03): hardening createReturn в sales.ts и UI return-form для обязательного refundMethod. Добавить sync CustomOrder status при возврате. Проверить atomicity rollback.

Purpose: Закрыть FIN-07, FIN-09, FIN-10. НЕ трогает orders.ts — parallel с Plan 08-03.
Output: createReturn с refundMethod check, UI Select, Wave 0 тесты GREEN.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-order-sale-flow/08-CONTEXT.md
@.planning/phases/08-order-sale-flow/08-RESEARCH.md
@src/actions/sales.ts
@src/components/pos/return-form.tsx
@prisma/schema.prisma

<interfaces>
From src/actions/sales.ts (existing):
- createReturn signature будет hardened с refundMethod required

**Verified schema facts (after Plan 08-02):**

- `Return.refundMethod`: PaymentMethod (NOT NULL)
- `CustomOrder.cancellationType`: String? (Plan 08-02 added)
- `CustomOrder.cancelReason`: String? (Plan 08-02 added)
- `CustomOrderStatus` enum: reuse CANCELLED (no REFUNDED)
- `OrderStatusHistory` имеет поле `comment String?` (НЕ `reason`) — verified prisma/schema.prisma:616
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hardening createReturn — refundMethod validation + CustomOrder sync</name>
  <files>src/actions/sales.ts</files>
  <behavior>
    - refundMethod: required Zod validation
    - Soft set: refundMethod ∈ set(sale.payments.filter(!isExpense).map(p => p.method))
    - Error: "Метод возврата {X} не совпадает с методами оплаты: {list}"
    - Order sync: full return → CustomOrder.status='CANCELLED' + cancellationType='REFUND' + cancelReason; partial → no change
    - Atomicity: db.$transaction, throw → full rollback
    - НЕ добавлять REFUNDED enum value
  </behavior>
  <read_first>
    - src/actions/sales.ts полностью, особенно lines 581-736 (createReturn)
    - src/actions/sales.ts lines 640-720 (per-unit refund — НЕ ломать)
    - prisma/schema.prisma lines 242-371, 542-625 (verified — cancelReason добавлен в Plan 08-02)
    - prisma/schema.prisma lines 611-623 (OrderStatusHistory.comment, не reason)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md секции "Refund method", "Order ↔ Sale sync"
    - .planning/phases/08-order-sale-flow/08-RESEARCH.md Pitfall 6
    - src/__tests__/e2e/order-return-sync.e2e.test.ts
    - src/__tests__/e2e/return-midway-failure.e2e.test.ts
    - src/__tests__/e2e/order-payment-constraints.e2e.test.ts
    - src/lib/money.ts
    - src/lib/counters.ts
  </read_first>
  <action>
    **Step 1 — Zod schema:** `refundMethod: z.nativeEnum(PaymentMethod)` (REQUIRED)

    **Step 2 — Soft-set validation после load sale (включая customOrder relation):**
    ```typescript
    const sale = await tx.sale.findUniqueOrThrow({
      where: { id: input.saleId },
      include: { items: true, payments: true, customOrder: true },
    })

    const originalMethods = new Set(
      sale.payments.filter(p => !p.isExpense).map(p => p.method)
    )
    if (!originalMethods.has(input.refundMethod)) {
      const methodsList = Array.from(originalMethods).join(', ')
      throw new Error(`Метод возврата ${input.refundMethod} не совпадает с методами оплаты: ${methodsList}`)
    }
    ```

    **Step 3 — FIN-07 sync (после Sale.update):**
    ```typescript
    if (sale.customOrder && isFullReturn) {
      await tx.customOrder.update({
        where: { id: sale.customOrder.id },
        data: {
          status: 'CANCELLED',
          cancellationType: 'REFUND',
          cancelReason: `Возврат продажи ${sale.number}: ${input.reason}`,
        },
      })
      await tx.orderStatusHistory.create({
        data: {
          orderId: sale.customOrder.id,
          status: 'CANCELLED',
          userId,
          comment: `Возврат продажи ${sale.number}: ${input.reason}`,
        },
      })
    }
    ```

    **Step 4 — Return.refundMethod записывается:**
    ```typescript
    refundMethod: input.refundMethod,
    ```

    **Step 5 — Atomicity inline comment (FIN-10).**

    **КРИТИЧНО:**
    - Money через @/lib/money helpers
    - НЕ трогать per-unit refund calculation
    - Использовать `comment` field (не `reason`) в OrderStatusHistory

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint src/actions/sales.ts &amp;&amp; pnpm test:e2e order-return-sync return-midway-failure 2>&amp;1 | tee /tmp/phase8-w2b.log; grep -q "PASS\|✓" /tmp/phase8-w2b.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "refundMethod: z.nativeEnum" src/actions/sales.ts`
    - `grep -q "Метод возврата.*не совпадает с методами оплаты" src/actions/sales.ts`
    - `grep -q "customOrder" src/actions/sales.ts` в createReturn context
    - `grep -q "cancellationType: 'REFUND'" src/actions/sales.ts`
    - `grep -q "cancelReason:" src/actions/sales.ts`
    - `grep -c "REFUNDED" prisma/schema.prisma` = 0
    - `pnpm test:e2e order-return-sync` PASSES
    - `pnpm test:e2e return-midway-failure` PASSES
    - `pnpm test:e2e order-payment-constraints -t "refundMethod"` PASSES
    - `pnpm lint src/actions/sales.ts` passes
    - `pnpm tsc --noEmit` passes
  </acceptance_criteria>
  <done>createReturn hardened, CustomOrder sync, Wave 0 E2E GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Return form UI — Select refundMethod обязательный</name>
  <files>src/components/pos/return-form.tsx</files>
  <read_first>
    - src/components/pos/return-form.tsx полностью
    - src/actions/sales.ts createReturn (новая signature)
    - src/components/ui/select.tsx (shadcn)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md UI decisions
    - CLAUDE.md (Awwwards quality)
  </read_first>
  <action>
    **Step 1 — State:** `useState<PaymentMethod | ''>('')`

    **Step 2 — Derive availableMethods из sale.payments через useMemo (filter !isExpense, unique).**

    **Step 3 — shadcn Select** компонент с опциями availableMethods. Полный JSX см. предыдущую версию плана.

    **Step 4 — Auto-select когда один method (useEffect).**

    **Step 5 — Client validation перед submit; передача refundMethod в createReturn call.**

    **Awwwards:** shadcn Select с анимацией, никаких native `<select>`. helper `methodLabel` для русских названий.

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint src/components/pos/return-form.tsx &amp;&amp; grep -q "refundMethod" src/components/pos/return-form.tsx &amp;&amp; grep -q "Select" src/components/pos/return-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refundMethod" src/components/pos/return-form.tsx` ≥ 4
    - `grep -q "from.*@/components/ui/select" src/components/pos/return-form.tsx`
    - `grep -q "availableMethods\|sale.payments.*method" src/components/pos/return-form.tsx`
    - `grep -q "Метод возврата" src/components/pos/return-form.tsx`
    - `! grep -q "<select\b" src/components/pos/return-form.tsx`
    - `pnpm tsc --noEmit` passes
    - `pnpm lint src/components/pos/return-form.tsx` passes
  </acceptance_criteria>
  <done>UI return form валидирует refundMethod, Awwwards-уровень.</done>
</task>

</tasks>

<verification>
- `pnpm test:e2e order-return-sync return-midway-failure order-payment-constraints` GREEN
- `pnpm test && pnpm test:e2e` не сломан
- `pnpm lint src/actions/sales.ts src/components/pos/return-form.tsx` passes
- `pnpm tsc --noEmit` passes
</verification>

<success_criteria>

- [ ] createReturn требует refundMethod + валидирует
- [ ] CustomOrder sync при full return
- [ ] FIN-10 atomicity подтверждён
- [ ] UI shadcn Select
- [ ] Wave 0 E2E GREEN
      </success_criteria>

<output>
After completion, create `.planning/phases/08-order-sale-flow/08-04-SUMMARY.md` с: createReturn diff, UI changes, E2E status, FIN-07/09/10 closure notes.
</output>
