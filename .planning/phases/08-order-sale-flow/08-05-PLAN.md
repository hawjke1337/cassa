---
phase: 08-order-sale-flow
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/components/orders/order-detail.tsx
autonomous: false
requirements:
  - FIN-04
must_haves:
  truths:
    - "CancelDialog содержит RadioGroup 'Удержать предоплату' (default, HandCoins icon) / 'Вернуть клиенту' (Ban icon)"
    - "При 'Вернуть' показан warning 'Требуется открытая смена. Возврат создаст расход в кассе.'"
    - "Textarea reason обязательный для обоих вариантов"
    - "При prepaidAmount === 0 RadioGroup скрыт"
    - "Submit вызывает cancelOrderWithDecision с правильным prepaymentAction"
    - "Visual QA approved (Awwwards-уровень)"
  artifacts:
    - path: "src/components/orders/order-detail.tsx"
      provides: "Extended CancelDialog with RadioGroup HOLD/REFUND"
      contains: "cancelOrderWithDecision"
  key_links:
    - from: "src/components/orders/order-detail.tsx CancelDialog"
      to: "src/actions/orders.ts cancelOrderWithDecision"
      via: "form submit → server action"
      pattern: "cancelOrderWithDecision"
---

<objective>
Wave 3: UI расширение CancelDialog с RadioGroup HOLD/REFUND. ЭТОТ план содержит ТОЛЬКО UI work + human-verify visual QA. Финальная интеграционная проверка (full suite + invariants + VALIDATION sign-off) — в Plan 08-06.

Purpose: Завершить FIN-04 UI частью — оператор делает явный выбор HOLD/REFUND через RadioGroup. Awwwards-уровень визуальной полировки.
Output: CancelDialog с RadioGroup, visual QA approved.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-order-sale-flow/08-CONTEXT.md
@.planning/phases/08-order-sale-flow/08-VALIDATION.md
@src/components/orders/order-detail.tsx
@src/actions/orders.ts

<interfaces>
From src/actions/orders.ts (Plan 08-03):
```typescript
export type CancelPrepaymentAction = 'HOLD' | 'REFUND'
export async function cancelOrderWithDecision(
  orderId: string,
  options: { prepaymentAction: CancelPrepaymentAction; reason: string }
): Promise<{ success: true }>
```

From lucide-react:

- HandCoins (удержать — positive)
- Ban (вернуть — destructive)
- AlertTriangle (warning)

From shadcn/ui:

- RadioGroup, RadioGroupItem, Label, Dialog, AlertDialog, Textarea, Button
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить CancelDialog RadioGroup HOLD/REFUND</name>
  <files>src/components/orders/order-detail.tsx</files>
  <read_first>
    - src/components/orders/order-detail.tsx lines 1218-1266 (текущий CancelDialog) + imports
    - src/actions/orders.ts (cancelOrderWithDecision signature из Plan 08-03)
    - src/components/ui/radio-group.tsx (shadcn API)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md секция UI decisions (CancelDialog)
    - CLAUDE.md (Awwwards-уровень)
  </read_first>
  <action>
    **Step 1 — Imports:**
    ```tsx
    import { HandCoins, Ban, AlertTriangle } from 'lucide-react'
    import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group'
    import { cancelOrderWithDecision } from '@/actions/orders'
    ```

    **Step 2 — State:**
    ```tsx
    const [prepaymentAction, setPrepaymentAction] = useState<'HOLD' | 'REFUND'>('HOLD')
    const [cancelReason, setCancelReason] = useState('')
    const hasPrepayment = toMoney(order.prepaidAmount).gt(0)
    ```

    **Step 3 — Dialog body** (полный JSX см. предыдущую версию плана):
    - DialogHeader с conditional описанием
    - hasPrepayment ? RadioGroup с двумя label-обёрнутыми RadioGroupItem
    - HOLD: HandCoins emerald-600, default checked
    - REFUND: Ban destructive, on select показывается amber AlertTriangle warning "Требуется открытая смена. Возврат создаст расход в кассе."
    - Textarea reason обязательный
    - DialogFooter с Button disabled если нет reason
    - Submit вызывает cancelOrderWithDecision с правильными опциями, toast.success/error, router.refresh()

    **Step 4 — Удалить старые вызовы `cancelOrder` (если ещё остались) → cancelOrderWithDecision.**

    **Awwwards-уровень:**
    - Плавные transitions на hover/checked state (has-[:checked] selectors)
    - emerald для HOLD, destructive для REFUND
    - Conditional warning render

    **КРИТИЧНО:**
    - При `!hasPrepayment` RadioGroup скрыт; submit с HOLD default
    - Textarea обязательный
    - Никаких native элементов

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint src/components/orders/order-detail.tsx &amp;&amp; grep -q "cancelOrderWithDecision" src/components/orders/order-detail.tsx &amp;&amp; grep -q "RadioGroup" src/components/orders/order-detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cancelOrderWithDecision" src/components/orders/order-detail.tsx` ≥ 1
    - `grep -q "RadioGroup" src/components/orders/order-detail.tsx`
    - `grep -q "HandCoins" src/components/orders/order-detail.tsx`
    - `grep -q "Ban" src/components/orders/order-detail.tsx`
    - `grep -q "Удержать предоплату" src/components/orders/order-detail.tsx`
    - `grep -q "Вернуть клиенту" src/components/orders/order-detail.tsx`
    - `grep -q "Требуется открытая смена. Возврат создаст расход в кассе" src/components/orders/order-detail.tsx`
    - `grep -q "hasPrepayment\|prepaidAmount.*gt" src/components/orders/order-detail.tsx`
    - `grep -c "cancelOrder\b" src/components/orders/order-detail.tsx` = 0 (старая функция не вызывается)
    - `pnpm tsc --noEmit` passes
    - `pnpm lint src/components/orders/order-detail.tsx` passes
  </acceptance_criteria>
  <done>CancelDialog расширен RadioGroup, вызывает cancelOrderWithDecision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA CancelDialog (Awwwards-уровень)</name>
  <files>src/components/orders/order-detail.tsx</files>
  <action>
    Visual/UX QA checkpoint — human-only verification (Awwwards-уровень subjective). Developer запускает dev server, проходит сценарии.
  </action>
  <what-built>
    CancelDialog расширен RadioGroup HOLD/REFUND с icons (HandCoins / Ban), conditional warning, обязательным textarea reason. Интегрирован с cancelOrderWithDecision.
  </what-built>
  <how-to-verify>
    1. `pnpm dev`
    2. Открыть http://localhost:3000/orders
    3. Создать или открыть тестовый заказ с предоплатой (prepaidAmount > 0, status ≠ COMPLETED/CANCELLED)
    4. Нажать "Отменить заказ" → открыть Dialog
    5. **Проверить:**
       - RadioGroup виден, "Удержать предоплату" checked по умолчанию (HandCoins emerald-600)
       - "Вернуть клиенту" (Ban destructive)
       - Hover на каждый option — плавный border/bg переход
       - Клик "Вернуть клиенту" → появляется amber warning "Требуется открытая смена. Возврат создаст расход в кассе."
       - Клик обратно "Удержать" → warning исчезает
       - Textarea обязательный, без него Button disabled
    6. **Без предоплаты:** prepaidAmount=0 → RadioGroup скрыт, только textarea + button
    7. **Submit HOLD:** "Тест HOLD" → toast success → CANCELLED статус
    8. **Submit REFUND без смены:** закрыть смены → toast error "Для возврата предоплаты откройте смену"
    9. **Submit REFUND со сменой:** открыть смену → REFUND → CashOperation в кассовом отчёте
    10. **Responsive:** 375px mobile — dialog scales OK
    11. **Dark mode:** toggle theme — цвета корректны
  </how-to-verify>
  <resume-signal>Type "approved" или опишите визуальные/UX проблемы</resume-signal>
  <verify>
    <automated>MISSING — human-verify checkpoint, blocked until user types "approved"</automated>
  </verify>
  <done>User approved CancelDialog UI визуально OR returned actionable feedback.</done>
</task>

</tasks>

<verification>
- CancelDialog соответствует Awwwards-уровню
- RadioGroup + warning banner работают
- Server integration с cancelOrderWithDecision
- Visual QA approved
</verification>

<success_criteria>

- [ ] FIN-04 UI закрыт — явный выбор через RadioGroup
- [ ] Visual QA approved
- [ ] `pnpm tsc --noEmit` && `pnpm lint` passes для order-detail.tsx
      </success_criteria>

<output>
After completion, create `.planning/phases/08-order-sale-flow/08-05-SUMMARY.md` с: UI diff, Visual QA notes, Handoff to Plan 08-06 (integration gate).
</output>
