---
phase: 08-order-sale-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/helpers/fixtures.ts
  - src/__tests__/helpers/invariants.ts
  - src/actions/orders.ts
  - src/__tests__/e2e/order-completion.e2e.test.ts
  - src/__tests__/e2e/order-cancel.e2e.test.ts
  - src/__tests__/e2e/order-return-sync.e2e.test.ts
  - src/__tests__/e2e/partial-return-per-unit.e2e.test.ts
  - src/__tests__/e2e/order-payment-constraints.e2e.test.ts
  - src/__tests__/e2e/return-midway-failure.e2e.test.ts
  - src/__tests__/unit/compute-per-unit-discount.test.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-07
  - FIN-08
  - FIN-09
  - FIN-10
  - FIN-11
  - FIN-12
must_haves:
  truths:
    - "Все 6 E2E файлов запускаются через pnpm test:e2e и падают с ожидаемым RED (MISSING/Not Implemented)"
    - "compute-per-unit-discount.test.ts существует и падает RED (функция ещё не реализована)"
    - "Fixtures createTestOrderWithPrepayment и createTestShift доступны из helpers/fixtures.ts"
    - "Invariants helper экспортирует assertStockConservation, assertSerialConsistency, assertMoneyConservation, assertShiftConsistency, assertReturnAmountCap, assertOrderSaleLink"
    - "src/actions/orders.ts экспортирует throwing stubs completeOrder и cancelOrderWithDecision (для TypeScript compile)"
  artifacts:
    - path: "src/__tests__/helpers/fixtures.ts"
      provides: "createTestOrderWithPrepayment, createTestShift (ensured)"
      contains: "createTestOrderWithPrepayment"
    - path: "src/__tests__/helpers/invariants.ts"
      provides: "Six invariant assertion helpers"
      exports:
        [
          "assertStockConservation",
          "assertSerialConsistency",
          "assertMoneyConservation",
          "assertShiftConsistency",
          "assertReturnAmountCap",
          "assertOrderSaleLink",
        ]
    - path: "src/actions/orders.ts"
      provides: "Throwing stubs completeOrder + cancelOrderWithDecision (Wave 2 заменит)"
      contains: "not implemented — Wave 2"
    - path: "src/__tests__/e2e/order-completion.e2e.test.ts"
      provides: "RED tests for FIN-01, FIN-02, FIN-03"
      min_lines: 60
    - path: "src/__tests__/e2e/order-cancel.e2e.test.ts"
      provides: "RED tests for FIN-04, FIN-05, FIN-06 (HOLD и REFUND)"
      min_lines: 80
    - path: "src/__tests__/e2e/order-return-sync.e2e.test.ts"
      provides: "RED tests for FIN-07 (Order ↔ Sale sync)"
      min_lines: 40
    - path: "src/__tests__/e2e/partial-return-per-unit.e2e.test.ts"
      provides: "RED tests for FIN-08 per-unit discount refund"
      min_lines: 40
    - path: "src/__tests__/e2e/order-payment-constraints.e2e.test.ts"
      provides: "RED tests for FIN-09 (refundMethod), FIN-11 (shift), FIN-12 (overpay)"
      min_lines: 80
    - path: "src/__tests__/e2e/return-midway-failure.e2e.test.ts"
      provides: "RED test for FIN-10 atomic rollback"
      min_lines: 40
    - path: "src/__tests__/unit/compute-per-unit-discount.test.ts"
      provides: "Unit precision tests для residual pattern"
      min_lines: 50
  key_links:
    - from: "src/__tests__/e2e/*.e2e.test.ts"
      to: "src/__tests__/helpers/fixtures.ts"
      via: "import { createTestOrderWithPrepayment, createTestShift }"
      pattern: "from.*helpers/fixtures"
    - from: "src/__tests__/e2e/*.e2e.test.ts"
      to: "src/__tests__/helpers/invariants.ts"
      via: "import assertStockConservation и др."
      pattern: "from.*helpers/invariants"
    - from: "src/__tests__/e2e/order-completion.e2e.test.ts"
      to: "src/actions/orders.ts (stub)"
      via: "import { completeOrder } — throwing stub"
      pattern: "from.*@/actions/orders"
---

<objective>
Wave 0 RED phase: создать все E2E test файлы, unit test для per-unit discount, фикстуры, invariants helper, и **throwing stubs** для completeOrder/cancelOrderWithDecision (чтобы TypeScript compile прошёл). Тесты ДОЛЖНЫ падать RED — никакой имплементации production кода в этом плане. TDD-first подход обязателен (carried from Phase 7).

Purpose: Обеспечить Nyquist compliance для всего Phase 8 — каждое FIN-\* требование имеет свой автоматизированный тест ДО того, как начнётся имплементация. Тесты служат контрактом для Wave 1-3.
Output: 6 E2E файлов с RED stubs, 1 unit test файл RED, 2 helper файла (фикстуры расширены + новый invariants), throwing stubs в orders.ts.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-order-sale-flow/08-CONTEXT.md
@.planning/phases/08-order-sale-flow/08-RESEARCH.md
@.planning/phases/08-order-sale-flow/08-VALIDATION.md

<!-- Reference test infrastructure from Phase 7 -->

@src/**tests**/helpers/db.ts
@src/**tests**/helpers/fixtures.ts
@src/**tests**/e2e/\_template.e2e.test.ts
@src/**tests**/e2e/sales-decimal.e2e.test.ts

<interfaces>
<!-- Phase 7 helpers (reuse) -->
From src/__tests__/helpers/db.ts:
- `getTestDb()` — schema-per-worker Prisma client
- `cleanDb()` — TRUNCATE all tables between tests

From src/**tests**/helpers/fixtures.ts (existing):

- `createTestStore`, `createTestUser`, `createTestProduct`, `createTestStoreProduct`
- Money values передаются как string чтобы избежать float roundtrip

From src/lib/money.ts:

- `toMoney(value: number | string | Decimal): Decimal`
- `sum(...vals)`, `sub(a, b)`, `mul(a, b)`, `div(a, b)` — все Decimal-safe

**Locked invariant function names** (использовать ТОЧНО эти имена в helpers AND в E2E):

- `assertStockConservation`
- `assertSerialConsistency`
- `assertMoneyConservation`
- `assertShiftConsistency`
- `assertReturnAmountCap` ← было `assertReturnCap`, переименовано для ясности
- `assertOrderSaleLink`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить fixtures + создать invariants helper + throwing stubs в orders.ts</name>
  <files>
    src/__tests__/helpers/fixtures.ts,
    src/__tests__/helpers/invariants.ts,
    src/actions/orders.ts
  </files>
  <read_first>
    - src/__tests__/helpers/fixtures.ts (полностью, чтобы понимать существующий стиль и реэкспорты)
    - src/__tests__/helpers/db.ts (getTestDb, cleanDb API)
    - src/__tests__/e2e/_template.e2e.test.ts (как фикстуры используются)
    - src/__tests__/e2e/sales-decimal.e2e.test.ts (pattern использования Decimal в assertions)
    - src/lib/money.ts (sum/sub/mul/toMoney signatures)
    - src/actions/orders.ts (для добавления stubs — НЕ трогать существующие функции)
    - prisma/schema.prisma lines 242-371 (Sale/SaleItem/Payment/Return), 542-623 (CustomOrder/Items), 1083-1135 (CashOperation/SerialUnit) — поля для assertions
    - .planning/phases/08-order-sale-flow/08-VALIDATION.md (секция Invariants 1-6)
  </read_first>
  <action>
    **Часть A: Fixtures расширение (src/__tests__/helpers/fixtures.ts):**

    1. Если `createTestShift` отсутствует — добавить:
    ```ts
    export async function createTestShift(
      db: PrismaClient,
      opts: { storeId: string; userId: string; status?: 'OPEN' | 'CLOSED'; openingBalance?: string }
    ) {
      return db.shift.create({
        data: {
          storeId: opts.storeId,
          openedById: opts.userId,
          status: opts.status ?? 'OPEN',
          openingBalance: opts.openingBalance ?? '0',
          openedAt: new Date(),
        },
      })
    }
    ```

    2. Добавить `createTestOrderWithPrepayment` (см. полный код в предыдущей версии плана — без изменений).

    **Часть B: Invariants helper (src/__tests__/helpers/invariants.ts) — НОВЫЙ ФАЙЛ:**

    Экспортировать ровно 6 assertion функций с **точными именами** (locked):
    - `assertStockConservation`
    - `assertSerialConsistency`
    - `assertMoneyConservation`
    - `assertShiftConsistency`
    - `assertReturnAmountCap`
    - `assertOrderSaleLink`

    Каждая принимает PrismaClient + scope. Использовать `@/lib/money` для всех сравнений сумм. Никакого `Number(decimal)`.

    ```ts
    import { expect } from 'vitest'
    import type { PrismaClient } from '@/generated/prisma/client'
    import { sum, sub, toMoney } from '@/lib/money'

    export async function assertStockConservation(
      db: PrismaClient,
      opts: { storeId: string; productId: string; initialStock: number }
    ): Promise<void> { /* ... */ }

    export async function assertSerialConsistency(
      db: PrismaClient,
      opts: { storeId: string }
    ): Promise<void> { /* ... */ }

    export async function assertMoneyConservation(
      db: PrismaClient,
      opts: { storeId: string }
    ): Promise<void> { /* ... */ }

    export async function assertShiftConsistency(db: PrismaClient): Promise<void> {
      const nullShiftPayments = await db.payment.count({ where: { shiftId: null } })
      expect(nullShiftPayments).toBe(0)
    }

    export async function assertReturnAmountCap(
      db: PrismaClient,
      opts: { saleId: string }
    ): Promise<void> { /* ... */ }

    export async function assertOrderSaleLink(
      db: PrismaClient,
      opts: { storeId: string }
    ): Promise<void> {
      const orphans = await db.customOrder.findMany({
        where: {
          storeId: opts.storeId,
          OR: [
            { saleId: { not: null }, status: { not: 'COMPLETED' } },
            { saleId: null, status: 'COMPLETED' },
          ],
        },
      })
      expect(orphans).toHaveLength(0)
    }
    ```

    **Часть C: Throwing stubs в src/actions/orders.ts (для TypeScript compile в E2E Wave 0):**

    В конец файла `src/actions/orders.ts` добавить:
    ```ts
    // ============================================================
    // Wave 0 stubs — заменяются реальной имплементацией в Plan 08-03
    // ============================================================
    import type { PaymentMethod } from '@/generated/prisma/client'

    export async function completeOrder(
      _orderId: string,
      _options?: {
        discountAmount?: string | number
        finalPayment?: { method: PaymentMethod; amount: string | number }
      }
    ): Promise<{ saleId: string; saleNumber: string }> {
      throw new Error('completeOrder not implemented — Wave 2')
    }

    export type CancelPrepaymentAction = 'HOLD' | 'REFUND'

    export async function cancelOrderWithDecision(
      _orderId: string,
      _options: { prepaymentAction: CancelPrepaymentAction; reason: string }
    ): Promise<{ success: true }> {
      throw new Error('cancelOrderWithDecision not implemented — Wave 2')
    }
    ```

    Эти stubs **должны быть удалены** в Plan 08-03 Task 2 (acceptance check там).

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; grep -q "export async function createTestShift" src/__tests__/helpers/fixtures.ts &amp;&amp; grep -q "export async function createTestOrderWithPrepayment" src/__tests__/helpers/fixtures.ts &amp;&amp; grep -q "export async function assertStockConservation" src/__tests__/helpers/invariants.ts &amp;&amp; grep -q "export async function assertSerialConsistency" src/__tests__/helpers/invariants.ts &amp;&amp; grep -q "export async function assertMoneyConservation" src/__tests__/helpers/invariants.ts &amp;&amp; grep -q "export async function assertShiftConsistency" src/__tests__/helpers/invariants.ts &amp;&amp; grep -q "export async function assertReturnAmountCap" src/__tests__/helpers/invariants.ts &amp;&amp; grep -q "export async function assertOrderSaleLink" src/__tests__/helpers/invariants.ts &amp;&amp; grep -q "completeOrder not implemented — Wave 2" src/actions/orders.ts &amp;&amp; grep -q "cancelOrderWithDecision not implemented — Wave 2" src/actions/orders.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function createTestShift" src/__tests__/helpers/fixtures.ts` matches
    - `grep -n "export async function createTestOrderWithPrepayment" src/__tests__/helpers/fixtures.ts` matches
    - File `src/__tests__/helpers/invariants.ts` exists
    - All 6 individual greps pass (см. verify automated)
    - `grep -q "completeOrder not implemented — Wave 2" src/actions/orders.ts` matches
    - `grep -q "cancelOrderWithDecision not implemented — Wave 2" src/actions/orders.ts` matches
    - `pnpm lint src/__tests__/helpers/invariants.ts` passes
    - `pnpm tsc --noEmit` passes
  </acceptance_criteria>
  <done>Fixtures + invariants helper + throwing stubs готовы. E2E файлы Task 2/3 могут импортировать из @/actions/orders без TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать первые 3 RED E2E файла (order-completion, order-cancel, order-return-sync)</name>
  <files>
    src/__tests__/e2e/order-completion.e2e.test.ts,
    src/__tests__/e2e/order-cancel.e2e.test.ts,
    src/__tests__/e2e/order-return-sync.e2e.test.ts
  </files>
  <behavior>
    - **order-completion.e2e.test.ts**: FIN-01 (finalAmount = total-discount-prepaid), FIN-02 (stock decrement), FIN-03 (SerialUnit → SOLD). Минимум 3 test cases: full prepayment, partial prepayment, несерийный+серийный микс.
    - **order-cancel.e2e.test.ts**: FIN-04 (явный выбор), FIN-05 (REFUND создаёт CashOperation WITHDRAW + Payment isExpense + prepaidAmount=0), FIN-06 (HOLD сохраняет payments, cancellationType='HOLD', status=CANCELLED). Минимум 4 test cases: HOLD-CASH, REFUND-CASH, REFUND-CARD, REFUND без открытой смены → throw "Для возврата предоплаты откройте смену".
    - **order-return-sync.e2e.test.ts**: FIN-07. Full return Sale → CustomOrder.status=CANCELLED, OrderStatusHistory запись. Partial return → CustomOrder.status остаётся COMPLETED.

    Все тесты должны падать RED с сообщением "not implemented — Wave 2".

  </behavior>
  <read_first>
    - src/__tests__/e2e/_template.e2e.test.ts
    - src/__tests__/e2e/sales-decimal.e2e.test.ts
    - src/__tests__/helpers/db.ts
    - src/__tests__/helpers/fixtures.ts (после Task 1)
    - src/__tests__/helpers/invariants.ts (после Task 1)
    - src/actions/orders.ts (stubs из Task 1)
    - src/actions/sales.ts lines 123-362 (createSale сигнатура — для фикстур)
    - prisma/schema.prisma lines 242-371, 542-623, 1083-1135
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md (UI decisions и error messages)
    - .planning/phases/08-order-sale-flow/08-VALIDATION.md (Test Dimensions Matrix)
  </read_first>
  <action>
    Импортировать stubs:
    ```ts
    import { completeOrder, cancelOrderWithDecision } from '@/actions/orders'
    ```

    Структура каждого файла: `describe`/`it` + `beforeEach(cleanDb)`. ARRANGE через фикстуры → ACT через `await expect(completeOrder(...)).rejects.toThrow(/not implemented — Wave 2/)` (Wave 0 RED), либо позитивная проверка которая запускается ПОСЛЕ Wave 2 GREEN.

    **Точные error messages для будущих Wave 2 expect().rejects.toThrow():**
    - "Откройте кассовую смену"
    - "Для возврата предоплаты откройте смену"
    - "Недостаточно остатка: {name}"
    - "Переплата заказа: уменьшите сумму"
    - "Нельзя завершить из {status}"
    - "Метод возврата {X} не совпадает с методами оплаты:"
    - "Некорректная скидка"

    **Invariants после каждого ACT:** вызывать `assertStockConservation`, `assertSerialConsistency`, `assertMoneyConservation`, `assertShiftConsistency`, `assertReturnAmountCap`, `assertOrderSaleLink`.

    **Decimal assertions:** через `toMoney(actual).eq(toMoney(expected))` или `.toString()`. НЕ `toBeCloseTo`.

    **FIN-01 ключевой test case (см. предыдущую версию плана для полного кода).**

    Wave 0 RED ожидание: тесты падают на `not implemented — Wave 2` из throwing stubs.

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm test:e2e order-completion order-cancel order-return-sync 2>&amp;1 | tee /tmp/phase8-w0-a.log; grep -q "FAIL\|not implemented" /tmp/phase8-w0-a.log &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - 3 файла существуют с `.e2e.test.ts` суффиксом
    - `grep -l "completeOrder" src/__tests__/e2e/order-completion.e2e.test.ts` matches
    - `grep -l "cancelOrderWithDecision" src/__tests__/e2e/order-cancel.e2e.test.ts` matches
    - `grep -l "cancellationType" src/__tests__/e2e/order-cancel.e2e.test.ts` matches
    - `grep -l "Для возврата предоплаты откройте смену" src/__tests__/e2e/order-cancel.e2e.test.ts` matches
    - `pnpm tsc --noEmit` passes (stubs из Task 1 обеспечивают exports)
    - `pnpm test:e2e order-completion` exits non-zero (RED)
    - `pnpm lint src/__tests__/e2e/order-completion.e2e.test.ts src/__tests__/e2e/order-cancel.e2e.test.ts src/__tests__/e2e/order-return-sync.e2e.test.ts` passes
  </acceptance_criteria>
  <done>3 E2E файла с RED state. Импорты из '@/actions/orders' успешно компилируются благодаря Task 1 stubs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Создать оставшиеся 3 RED E2E файла + unit test compute-per-unit-discount</name>
  <files>
    src/__tests__/e2e/partial-return-per-unit.e2e.test.ts,
    src/__tests__/e2e/order-payment-constraints.e2e.test.ts,
    src/__tests__/e2e/return-midway-failure.e2e.test.ts,
    src/__tests__/unit/compute-per-unit-discount.test.ts
  </files>
  <behavior>
    - **partial-return-per-unit.e2e.test.ts**: FIN-08. Заказ 3 позиции × 100₽ со скидкой 99₽ (order-level). Completion → per-unit discount 33/33/33+residual. Частичный возврат 1 из 3 → refund точный.
    - **order-payment-constraints.e2e.test.ts**: FIN-09 (refundMethod required + soft set), FIN-11 (completeOrder без OPEN shift → throw), FIN-12 (overpay → throw "Переплата заказа: уменьшите сумму").
    - **return-midway-failure.e2e.test.ts**: FIN-10. Mid-tx failure → assert Sale/stock/SerialUnit не изменились.
    - **compute-per-unit-discount.test.ts**: 6+ unit cases — totalDiscount=0, простой residual, 3×100₽/100₽ → 33.33+33.33+33.34, 99.99₽/2, mixed quantity, property-based invariant 100 random inputs.
  </behavior>
  <read_first>
    - Всё что в Task 2 read_first
    - src/lib/money.ts (sum, sub, mul, div, toMoney — API)
    - .planning/phases/08-order-sale-flow/08-RESEARCH.md строки 424-448 (Example 2 — reference impl)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md секция Per-unit discount
  </read_first>
  <action>
    **3 E2E файла:** структура как в Task 2. Импорт stubs из @/actions/orders.

    **Unit test compute-per-unit-discount.test.ts:**
    ```ts
    import { computePerUnitDiscount } from '@/lib/orders/discount'
    import { toMoney, mul, sum } from '@/lib/money'
    ```

    Структура (см. предыдущую версию плана для полного кода test cases). Все сравнения через `.toString()` или `.eq()`. Никакого `toBeCloseTo`.

    Тест RED: `Cannot find module '@/lib/orders/discount'` (Wave 1 создаст файл).

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm test:e2e partial-return-per-unit order-payment-constraints return-midway-failure 2>&amp;1 | tee /tmp/phase8-w0-b.log; grep -q "FAIL\|not implemented" /tmp/phase8-w0-b.log &amp;&amp; pnpm test -- compute-per-unit-discount 2>&amp;1 | tee /tmp/phase8-w0-unit.log; grep -q "FAIL\|Cannot find module" /tmp/phase8-w0-unit.log &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 E2E files exist
    - `grep -l "refundMethod" src/__tests__/e2e/order-payment-constraints.e2e.test.ts` matches
    - `grep -l "Переплата заказа" src/__tests__/e2e/order-payment-constraints.e2e.test.ts` matches
    - File `src/__tests__/unit/compute-per-unit-discount.test.ts` exists
    - `grep -c "^\s*it(" src/__tests__/unit/compute-per-unit-discount.test.ts` ≥ 6
    - `grep -l "computePerUnitDiscount" src/__tests__/unit/compute-per-unit-discount.test.ts` matches
    - `grep -l "@/lib/orders/discount" src/__tests__/unit/compute-per-unit-discount.test.ts` matches
    - `! grep -q "toBeCloseTo" src/__tests__/unit/compute-per-unit-discount.test.ts`  # money assertions без toBeCloseTo
    - `pnpm tsc --noEmit` passes
    - `pnpm test -- compute-per-unit-discount` падает RED с "Cannot find module" или "is not a function"
    - `pnpm lint src/__tests__/unit/compute-per-unit-discount.test.ts` passes
  </acceptance_criteria>
  <done>Unit test + 3 оставшихся E2E готовы, все RED, блокируют Wave 1/2 пока не станут GREEN.</done>
</task>

</tasks>

<verification>
- Все 6 E2E файлов существуют и запускаются через `pnpm test:e2e` (RED)
- Unit тест существует и падает RED
- Helpers fixtures расширены, invariants создан
- Throwing stubs в orders.ts позволяют TypeScript компилироваться
- TypeScript компилируется (`pnpm tsc --noEmit`)
- ESLint money-guard не нарушен
- Все 12 FIN-* покрыты минимум одним RED тестом (per VALIDATION.md map)
</verification>

<success_criteria>

- [ ] Wave 0 RED phase complete: 6 E2E + 1 unit test существуют и падают
- [ ] Test dimensions matrix покрыт (item type, prepayment, discount, shift, failure, cancel action)
- [ ] Invariants 1-6 (с locked именами) вызываются хотя бы в одном assertion в E2E
- [ ] Фикстуры `createTestOrderWithPrepayment` и `createTestShift` работают
- [ ] Throwing stubs позволяют TS compile (Wave 2 удалит и заменит)
- [ ] Wave 1 и Wave 2 разблокированы (есть чёткий контракт через failing тесты)
      </success_criteria>

<output>
After completion, create `.planning/phases/08-order-sale-flow/08-01-SUMMARY.md` с разделами: Tests Created, Fixtures Added, Invariants Exported (with locked names), Stubs Added, RED State Confirmation, Next Wave Blocker (Wave 1 schema migrations).
</output>
