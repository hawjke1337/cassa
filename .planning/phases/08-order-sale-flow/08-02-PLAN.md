---
phase: 08-order-sale-flow
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260408_phase8_payment_shift_return_refund_cancellation_type/migration.sql
  - src/lib/orders/discount.ts
autonomous: true
requirements:
  - FIN-06
  - FIN-08
  - FIN-09
  - FIN-11
must_haves:
  truths:
    - "Payment.shiftId NOT NULL на уровне БД, backfilled для существующих записей"
    - "Return.refundMethod NOT NULL на уровне БД, backfilled из оригинальных Payment.method"
    - "CustomOrder.cancellationType String? добавлена как новая колонка (nullable)"
    - "CustomOrder.cancelReason String? добавлена как новая колонка (nullable, для FIN-06 audit trail)"
    - "computePerUnitDiscount pure function экспортируется из @/lib/orders/discount и проходит все Wave 0 unit тесты GREEN"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Updated Sale/Payment/Return/CustomOrder models с новыми constraints + cancelReason"
      contains: "cancelReason"
    - path: "prisma/migrations/20260408_phase8_payment_shift_return_refund_cancellation_type/migration.sql"
      provides: "SQL migration с ALTER TABLE + backfill + NOT NULL"
      contains: 'ALTER TABLE "Payment"'
    - path: "src/lib/orders/discount.ts"
      provides: "computePerUnitDiscount pure function с residual pattern"
      exports: ["computePerUnitDiscount"]
  key_links:
    - from: "src/lib/orders/discount.ts"
      to: "src/lib/money.ts"
      via: "import { sum, sub, mul, div, toMoney }"
      pattern: "from.*lib/money"
    - from: "prisma/schema.prisma"
      to: "БД Postgres"
      via: "prisma migrate deploy"
      pattern: "@db\\.VarChar|NOT NULL"
---

<objective>
Wave 1: foundation-слой для Wave 2 имплементации. Schema migrations (4 изменения) + одна pure function для per-unit discount. Всё должно пройти unit тесты из Wave 0 GREEN без трогания actions.

Purpose: Разблокировать Wave 2 (completeOrder/cancelOrderWithDecision/createReturn refactor) — эти функции зависят от NOT NULL schema, от cancelReason колонки, от cancellationType, и от computePerUnitDiscount helper.
Output: Миграция БД применена, schema.prisma обновлена, новая pure function в src/lib/orders/discount.ts, unit test compute-per-unit-discount.test.ts → GREEN.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-order-sale-flow/08-CONTEXT.md
@.planning/phases/08-order-sale-flow/08-RESEARCH.md
@prisma/schema.prisma
@src/lib/money.ts

<interfaces>
From src/lib/money.ts:
```typescript
export function toMoney(v: number | string | Prisma.Decimal): Decimal
export function sum(...vals: Decimal[]): Decimal
export function sub(a: Decimal, b: Decimal): Decimal
export function mul(a: Decimal, b: Decimal | number): Decimal
export function div(a: Decimal, b: Decimal | number): Decimal
```

Target signature for new function:

```typescript
// src/lib/orders/discount.ts
import type { Decimal } from "@/generated/prisma/client"
export function computePerUnitDiscount(
  items: Array<{ price: Decimal | string; quantity: number }>,
  totalDiscount: Decimal,
): Decimal[]
```

**Schema verified facts (read prisma/schema.prisma:542-592):**

- `CustomOrder` model — НЕТ колонки `cancelReason` сейчас → добавляем в этой миграции
- `CashOpType` enum (line 945) = `WITHDRAW | DEPOSIT` — `WITHDRAW` это "расход" (используется в 08-03)
- `CashOperation.type CashOpType` (line 1087) — поле называется `type`, не `operationType`
- `CashOperation.performedById` (line 1095) — есть
- `OrderStatusHistory` имеет `comment String?` (line 616), а НЕ `reason` — поэтому отдельная колонка `CustomOrder.cancelReason` чище

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migrations — Payment.shiftId, Return.refundMethod, CustomOrder.cancellationType, CustomOrder.cancelReason</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/20260408_phase8_payment_shift_return_refund_cancellation_type/migration.sql
  </files>
  <read_first>
    - prisma/schema.prisma lines 242-371 (Sale/Payment/Return models)
    - prisma/schema.prisma lines 540-625 (CustomOrder/Status/History — cancelReason добавляется)
    - prisma/schema.prisma lines 940-960 (CashOpType enum confirm WITHDRAW)
    - prisma/schema.prisma lines 1083-1135 (Shift/CashOperation models)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md секция Schema migrations (FIN-09, FIN-11)
    - prisma/migrations/ (последние миграции для conventions naming)
  </read_first>
  <action>
    **Step 1 — Обновить prisma/schema.prisma:**

    1. `Payment` model:
    ```prisma
    model Payment {
      // ...
      shiftId   String   // was String? — NOW NOT NULL (FIN-11)
      // ...
    }
    ```

    2. `Return` model:
    ```prisma
    model Return {
      // ...
      refundMethod PaymentMethod  // was PaymentMethod? — NOW NOT NULL (FIN-09)
      // ...
    }
    ```

    3. `CustomOrder` model — добавить ДВЕ новые колонки:
    ```prisma
    model CustomOrder {
      // ... existing fields ...
      cancellationType String?  // "HOLD" | "REFUND" — nullable, имеет смысл только при status=CANCELLED (FIN-06)
      cancelReason     String?  // FIN-06: причина отмены, заполняется из CancelDialog.reason
    }
    ```

    **НЕ трогать `CustomOrderStatus` enum** — reuse `CANCELLED`.

    **Step 2 — Создать migration SQL:**

    Путь: `prisma/migrations/20260408_phase8_payment_shift_return_refund_cancellation_type/migration.sql`

    ```sql
    -- Phase 8 — Order/Sale Flow & Предоплаты
    -- FIN-06, FIN-09, FIN-11

    BEGIN;

    -- =========================================================================
    -- 1) CustomOrder.cancellationType + cancelReason — новые nullable колонки (FIN-06)
    -- =========================================================================
    ALTER TABLE "CustomOrder"
      ADD COLUMN "cancellationType" TEXT,
      ADD COLUMN "cancelReason"     TEXT;

    -- =========================================================================
    -- 2) Payment.shiftId — backfill NULLs, затем SET NOT NULL (FIN-11)
    -- =========================================================================

    -- Backfill через Sale → Shift по createdAt
    UPDATE "Payment" p
    SET "shiftId" = (
      SELECT s.id FROM "Shift" s
      JOIN "Sale" sale ON sale.id = p."saleId"
      WHERE s."storeId" = sale."storeId"
        AND (
          (s."closedAt" IS NOT NULL AND p."createdAt" BETWEEN s."openedAt" AND s."closedAt")
          OR (s."closedAt" IS NULL AND p."createdAt" >= s."openedAt")
        )
      ORDER BY s."openedAt" DESC
      LIMIT 1
    )
    WHERE p."shiftId" IS NULL AND p."saleId" IS NOT NULL;

    -- Backfill через CustomOrder → Shift
    UPDATE "Payment" p
    SET "shiftId" = (
      SELECT s.id FROM "Shift" s
      JOIN "CustomOrder" co ON co.id = p."orderId"
      WHERE s."storeId" = co."storeId"
        AND (
          (s."closedAt" IS NOT NULL AND p."createdAt" BETWEEN s."openedAt" AND s."closedAt")
          OR (s."closedAt" IS NULL AND p."createdAt" >= s."openedAt")
        )
      ORDER BY s."openedAt" DESC
      LIMIT 1
    )
    WHERE p."shiftId" IS NULL AND p."orderId" IS NOT NULL;

    DO $$
    DECLARE null_count INT;
    BEGIN
      SELECT COUNT(*) INTO null_count FROM "Payment" WHERE "shiftId" IS NULL;
      IF null_count > 0 THEN
        RAISE EXCEPTION 'FIN-11 backfill failed: % Payment rows still have NULL shiftId. Manual cleanup required.', null_count;
      END IF;
    END $$;

    ALTER TABLE "Payment" ALTER COLUMN "shiftId" SET NOT NULL;

    -- =========================================================================
    -- 3) Return.refundMethod — backfill from first Sale.Payment.method, SET NOT NULL (FIN-09)
    -- =========================================================================
    UPDATE "Return" r
    SET "refundMethod" = (
      SELECT p."method" FROM "Payment" p
      WHERE p."saleId" = r."saleId"
      ORDER BY p."createdAt" ASC
      LIMIT 1
    )
    WHERE r."refundMethod" IS NULL;

    DO $$
    DECLARE null_count INT;
    BEGIN
      SELECT COUNT(*) INTO null_count FROM "Return" WHERE "refundMethod" IS NULL;
      IF null_count > 0 THEN
        RAISE EXCEPTION 'FIN-09 backfill failed: % Return rows still have NULL refundMethod. Manual cleanup required.', null_count;
      END IF;
    END $$;

    ALTER TABLE "Return" ALTER COLUMN "refundMethod" SET NOT NULL;

    COMMIT;
    ```

    **Step 3 — Применить миграцию локально:**
    ```bash
    pnpm prisma migrate dev --name phase8_payment_shift_return_refund_cancellation_type
    ```

    **Step 4 — Regenerate Prisma client:**
    ```bash
    pnpm prisma generate
    ```

    **НЕ добавлять REFUNDED в CustomOrderStatus enum** — locked decision из CONTEXT.md.

  </action>
  <verify>
    <automated>pnpm prisma validate &amp;&amp; grep -q "cancellationType" prisma/schema.prisma &amp;&amp; grep -q "cancelReason" prisma/schema.prisma &amp;&amp; pnpm prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "shiftId\s\+String\s*$" prisma/schema.prisma` matches (НЕ `String?`) в Payment model
    - `grep -n "refundMethod\s\+PaymentMethod\s*$" prisma/schema.prisma` matches (НЕ `PaymentMethod?`) в Return model
    - `grep -n "cancellationType\s\+String?" prisma/schema.prisma` matches в CustomOrder model
    - `grep -n "cancelReason\s\+String?" prisma/schema.prisma` matches в CustomOrder model
    - Migration file exists: `ls prisma/migrations/*phase8*/migration.sql`
    - `grep -c "ALTER TABLE" prisma/migrations/*phase8*/migration.sql` ≥ 3
    - `grep -c "SET NOT NULL" prisma/migrations/*phase8*/migration.sql` ≥ 2
    - `grep -q "ADD COLUMN \"cancelReason\"" prisma/migrations/*phase8*/migration.sql`
    - `grep -q "RAISE EXCEPTION" prisma/migrations/*phase8*/migration.sql` (backfill guards)
    - `pnpm prisma validate` exits 0
    - `pnpm prisma generate` succeeds
    - `pnpm prisma migrate dev` applied без ошибок
    - `grep -c "REFUNDED" prisma/schema.prisma` = 0
  </acceptance_criteria>
  <done>Schema migration применена с 4 изменениями (Payment.shiftId NN, Return.refundMethod NN, CustomOrder.cancellationType, CustomOrder.cancelReason). Wave 2 разблокирован.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computePerUnitDiscount pure function (GREEN Wave 0 unit test)</name>
  <files>src/lib/orders/discount.ts</files>
  <behavior>
    - Делает Wave 0 unit test `src/__tests__/unit/compute-per-unit-discount.test.ts` GREEN
    - Residual pattern: последняя позиция поглощает rounding drift
    - Precision: используются ТОЛЬКО `@/lib/money` helpers (money-guard ESLint)
    - totalDiscount=0 → все нули (fast path)
    - Распределение по весу price*quantity
  </behavior>
  <read_first>
    - src/lib/money.ts (sum, sub, mul, div, toMoney signatures)
    - src/__tests__/unit/compute-per-unit-discount.test.ts (Wave 0 контракт)
    - .planning/phases/08-order-sale-flow/08-RESEARCH.md строки 424-448 (Example 2 — reference implementation)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md секция "Per-unit discount" (residual pattern)
    - src/lib/motivation-calculation.ts (reference: Decimal arithmetic Phase 7)
  </read_first>
  <action>
    Создать файл `src/lib/orders/discount.ts` с экспортом `computePerUnitDiscount` (полный код см. предыдущую версию плана — без изменений).

    **КРИТИЧНО:**
    - Никаких `Number()` на Decimal значениях
    - Все операции через sum/sub/mul/div helpers
    - `.toDecimalPlaces()` допустимо (MemberExpression на Decimal)

    Запустить Wave 0 unit test: `pnpm test -- compute-per-unit-discount` — должен GREEN.

  </action>
  <verify>
    <automated>pnpm test -- compute-per-unit-discount 2>&amp;1 | tee /tmp/phase8-w1-unit.log; grep -q "PASS\|✓" /tmp/phase8-w1-unit.log &amp;&amp; pnpm lint src/lib/orders/discount.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/orders/discount.ts` exists
    - `grep -n "export function computePerUnitDiscount" src/lib/orders/discount.ts` matches
    - `grep -c "Number(" src/lib/orders/discount.ts` = 0
    - `grep -l "@/lib/money" src/lib/orders/discount.ts` matches
    - `grep -q "residual\|isLast" src/lib/orders/discount.ts`
    - `pnpm test -- compute-per-unit-discount` passes (GREEN)
    - `pnpm lint src/lib/orders/discount.ts` passes
    - `pnpm tsc --noEmit` passes
  </acceptance_criteria>
  <done>Pure function готова, unit тест GREEN, computePerUnitDiscount доступна для Wave 2.</done>
</task>

</tasks>

<verification>
- Schema migration применена без ошибок (4 изменения)
- `pnpm prisma validate` и `pnpm prisma generate` succeed
- Wave 0 unit test compute-per-unit-discount → GREEN
- Wave 0 E2E тесты всё ещё RED
- ESLint money-guard не нарушен
- `pnpm tsc --noEmit` passes
</verification>

<success_criteria>

- [ ] 4 schema changes применены (Payment.shiftId NOT NULL, Return.refundMethod NOT NULL, CustomOrder.cancellationType added, CustomOrder.cancelReason added)
- [ ] Migration SQL содержит backfill с guards
- [ ] computePerUnitDiscount exports и unit test GREEN
- [ ] Wave 2 разблокирован (включая cancelReason для 08-03/08-04)
      </success_criteria>

<output>
After completion, create `.planning/phases/08-order-sale-flow/08-02-SUMMARY.md` с: Schema Diff (4 changes), Migration Backfill Stats, computePerUnitDiscount API, Wave 2 Readiness Checklist.
</output>
