---
phase: 08-order-sale-flow
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - src/lib/stock-helpers.ts
  - src/actions/orders.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-08
  - FIN-11
  - FIN-12
must_haves:
  truths:
    - "completeOrder(orderId, options) атомарно создаёт Sale, декрементит stock, помечает SerialUnits=SOLD, обновляет CustomOrder.status=COMPLETED, finalAmount = totalAmount - discount - prepaidAmount"
    - "cancelOrderWithDecision(orderId, { prepaymentAction: 'HOLD' | 'REFUND' }) реализует явный выбор: HOLD сохраняет payments + cancellationType=HOLD, REFUND создаёт compensating Payment isExpense + CashOperation type=WITHDRAW для CASH"
    - "REFUND без OPEN shift падает с 'Для возврата предоплаты откройте смену'"
    - "completeOrder без OPEN shift падает с 'Откройте кассовую смену'"
    - "Overpay блокируется через 'Переплата заказа: уменьшите сумму' в completeOrder и addOrderPayment"
    - "НИГДЕ не используется payment.deleteMany — только compensating entries"
    - "FOR UPDATE lock на CustomOrder, StoreProduct (несерийные), SerialUnit (серийные) перед любыми мутациями"
    - "Wave 0 throwing stubs (completeOrder/cancelOrderWithDecision) удалены и заменены реальной имплементацией"
    - "Wave 0 E2E order-completion, order-cancel, partial-return-per-unit, order-payment-constraints GREEN (кроме refundMethod частей — это Plan 08-04)"
  artifacts:
    - path: "src/lib/stock-helpers.ts"
      provides: "decrementStockForItems(tx, storeId, items) с FOR UPDATE lock"
      exports: ["decrementStockForItems"]
    - path: "src/actions/orders.ts"
      provides: "completeOrder, cancelOrderWithDecision, hardened addOrderPayment"
      exports: ["completeOrder", "cancelOrderWithDecision"]
  key_links:
    - from: "src/actions/orders.ts"
      to: "src/lib/stock-helpers.ts"
      via: "import { decrementStockForItems }"
      pattern: "from.*stock-helpers"
    - from: "src/actions/orders.ts"
      to: "src/lib/orders/discount.ts"
      via: "import { computePerUnitDiscount }"
      pattern: "from.*orders/discount"
    - from: "src/actions/orders.ts"
      to: "src/lib/money.ts"
      via: "import { sum, sub, mul, toMoney }"
      pattern: "from.*lib/money"
    - from: "src/actions/orders.ts"
      to: "БД через tx.$queryRaw FOR UPDATE"
      via: "pessimistic locking"
      pattern: "FOR UPDATE"
---

<objective>
Wave 2 Plan A (parallel с 08-04): переписать hot-функции в `src/actions/orders.ts` в единый набор атомарных транзакций с FOR UPDATE locking, stock decrement, compensating entries. Удалить Wave 0 throwing stubs и заменить реальной имплементацией.

Purpose: Закрыть 9 из 12 FIN-\* требований за один согласованный рефакторинг. Устранить BUG-023, BUG-024, BUG-025.
Output: Две новые публичные функции (completeOrder, cancelOrderWithDecision), hardened addOrderPayment, shared stock helper. Wave 0 E2E тесты GREEN.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-order-sale-flow/08-CONTEXT.md
@.planning/phases/08-order-sale-flow/08-RESEARCH.md
@src/actions/sales.ts
@src/actions/orders.ts
@src/lib/money.ts
@src/lib/counters.ts
@src/lib/permissions.ts
@prisma/schema.prisma

<interfaces>
From src/lib/money.ts (Phase 7):
- sum, sub, mul, div, toMoney — Decimal-safe helpers

From src/lib/counters.ts:

- getNextNumber(prefix: 'S' | 'R', tx: PrismaTx): Promise<string>

From src/lib/permissions.ts:

- requirePermission(permission: string, storeId: string): Promise<void>

From src/lib/orders/discount.ts (Plan 08-02):

- computePerUnitDiscount(items, totalDiscount): Decimal[]

**Verified schema facts (prisma/schema.prisma):**

- `CashOpType` enum (line 945-948): `{ WITHDRAW, DEPOSIT }` — `WITHDRAW` это правильное значение для расхода/изъятия
- `CashOperation` (line 1083): поле называется `type CashOpType` (НЕ `operationType`); имеет `performedById String` (NOT NULL)
- `CustomOrder.cancelReason String?` (добавлено в Plan 08-02) — используется здесь для записи reason
- `CustomOrder.cancellationType String?` (добавлено в Plan 08-02) — 'HOLD' | 'REFUND'

Reference pattern from src/actions/sales.ts:148-323 (createSale):

```typescript
await tx.$queryRaw`
  SELECT sp."productId", sp.quantity FROM "StoreProduct" sp
  WHERE sp."storeId" = ${storeId} AND sp."productId" = ANY(${productIds}::text[])
  FOR UPDATE OF sp
`
await tx.storeProduct.update({
  where: { storeId_productId: { storeId, productId } },
  data: { quantity: { decrement: qty } },
})
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать shared decrementStockForItems helper</name>
  <files>src/lib/stock-helpers.ts</files>
  <behavior>
    - Принимает (tx, storeId, items[]) где item: { productId?, quantity, serialUnitId? }
    - Фильтрует только несерийные (serialUnitId == null && productId != null)
    - SELECT ... FOR UPDATE на все StoreProduct одной batch-операцией
    - Re-check quantity >= decrement, throw "Недостаточно остатка: {name}" если нет
    - tx.storeProduct.update({ quantity: { decrement } }) для каждого
    - Pure — не создаёт sales, не трогает SerialUnit
  </behavior>
  <read_first>
    - src/actions/sales.ts lines 123-362 (FOR UPDATE pattern, decrement loop)
    - src/lib/stock-helpers.ts (если существует — не дублировать)
    - prisma/schema.prisma lines 1083-1135 (StoreProduct composite unique)
    - .planning/phases/08-order-sale-flow/08-RESEARCH.md Pattern 1 (строки 158-178)
  </read_first>
  <action>
    Добавить в `src/lib/stock-helpers.ts` функцию `decrementStockForItems` (полный код см. предыдущую версию плана — без изменений). Pessimistic FOR UPDATE lock + sufficiency check + decrement.
  </action>
  <verify>
    <automated>grep -n "export async function decrementStockForItems" src/lib/stock-helpers.ts &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint src/lib/stock-helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function decrementStockForItems" src/lib/stock-helpers.ts` matches
    - `grep -q "FOR UPDATE OF sp" src/lib/stock-helpers.ts`
    - `grep -q "Недостаточно остатка" src/lib/stock-helpers.ts`
    - `grep -c "Number(" src/lib/stock-helpers.ts` = 0
    - `pnpm tsc --noEmit` passes
    - `pnpm lint src/lib/stock-helpers.ts` passes
  </acceptance_criteria>
  <done>Shared helper готов для completeOrder и (Phase 9) createSale refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: completeOrder atomic implementation</name>
  <files>src/actions/orders.ts</files>
  <behavior>
    - Удалить Wave 0 throwing stub `completeOrder` из orders.ts
    - Создать новую exported `completeOrder(orderId, options?)` с атомарной транзакцией
    - FIN-01: Sale.finalAmount = sub(sub(totalAmount, discount), prepaidAmount) — LOCKED семантика
    - FIN-02: через decrementStockForItems
    - FIN-03: FOR UPDATE на SerialUnit, re-check IN_STOCK, UPDATE → SOLD + SerialUnitHistory
    - FIN-08: computePerUnitDiscount распределяет order-level discount
    - FIN-11: throw "Откройте кассовую смену" если нет OPEN shift
    - FIN-12: throw "Переплата заказа: уменьшите сумму"
    - Prepayment Payments переносятся под Sale с СОХРАНЕНИЕМ оригинального shiftId, и **только не-isExpense** (compensating entries не цепляем)
    - CustomOrder.status → COMPLETED, saleId set, finalAmount updated, OrderStatusHistory записан

    Wave 0 E2E order-completion.e2e.test.ts → GREEN.

  </behavior>
  <read_first>
    - src/actions/orders.ts (полностью; особое внимание Wave 0 stub завершении файла — он удаляется)
    - src/actions/sales.ts lines 123-362 (createSale — эталон)
    - src/lib/stock-helpers.ts (после Task 1)
    - src/lib/orders/discount.ts (computePerUnitDiscount из Plan 08-02)
    - src/lib/money.ts
    - src/lib/counters.ts
    - src/lib/permissions.ts
    - prisma/schema.prisma lines 242-371, 542-625
    - .planning/phases/08-order-sale-flow/08-RESEARCH.md Example 1 (completeOrder template)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md decisions (finalAmount formula, Prepayment shift, Overpay, Shift)
    - src/__tests__/e2e/order-completion.e2e.test.ts
  </read_first>
  <action>
    **Step 1 — Удалить Wave 0 stub:**
    Найти и удалить функцию `completeOrder` помеченную `// not implemented — Wave 2`. Тип `CancelPrepaymentAction` оставить (нужен для cancelOrderWithDecision).

    **Step 2 — Imports (если отсутствуют):**
    ```typescript
    import { decrementStockForItems } from '@/lib/stock-helpers'
    import { computePerUnitDiscount } from '@/lib/orders/discount'
    import { sum, sub, mul, toMoney } from '@/lib/money'
    import { getNextNumber } from '@/lib/counters'
    ```

    **Step 3 — Реализовать completeOrder** (см. полный шаблон в предыдущей версии плана 08-RESEARCH Example 1).

    Ключевые шаги transaction:
    1. LOCK CustomOrder row
    2. Transition validation (allowedFrom = ['READY_FOR_PICKUP', 'ARRIVED'])
    3. Shift required check (FIN-11)
    4. Discount validation
    5. Prepayment arithmetic — `const finalAmount = sub(netTotal, prepaid)` где `netTotal = sub(totalAmount, discount)`
    6. Final payment + overpay check (FIN-12)
    7. Serial lock + re-check IN_STOCK
    8. Stock decrement через `decrementStockForItems`
    9. Per-unit discount via `computePerUnitDiscount`
    10. Create Sale
    11. **Re-parent prepayment Payments — фильтр isExpense:**
        ```ts
        for (const p of order.payments.filter(p => !p.isExpense)) {
          await tx.payment.update({
            where: { id: p.id },
            data: { saleId: sale.id },
            // shiftId НЕ трогаем — оригинальный prepayment shift сохраняется
          })
        }
        ```
        Compensating entries (isExpense=true) **остаются** привязанными к order.
    12. Create final payment (если есть) с openShift.id
    13. Mark SerialUnits SOLD + SerialUnitHistory
    14. Update CustomOrder + OrderStatusHistory

    **КРИТИЧНО:**
    - Весь код через money helpers
    - НИКАКОГО Number() на Decimal
    - Прохождение `pnpm test:e2e order-completion` GREEN

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint src/actions/orders.ts &amp;&amp; ! grep -q "completeOrder not implemented — Wave 2" src/actions/orders.ts &amp;&amp; pnpm test:e2e order-completion 2>&amp;1 | tee /tmp/phase8-w2-complete.log; grep -q "PASS\|✓" /tmp/phase8-w2-complete.log</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "completeOrder not implemented — Wave 2" src/actions/orders.ts` (Wave 0 stub удалён)
    - `grep -n "^export async function completeOrder" src/actions/orders.ts` matches
    - `grep -q "FOR UPDATE" src/actions/orders.ts`
    - `grep -q "decrementStockForItems" src/actions/orders.ts`
    - `grep -q "computePerUnitDiscount" src/actions/orders.ts`
    - `grep -q "Откройте кассовую смену" src/actions/orders.ts`
    - `grep -q "sub(netTotal, prepaid)" src/actions/orders.ts`  # FIN-01 formula present
    - `grep -q "filter(p => !p.isExpense)" src/actions/orders.ts`  # re-parent only non-compensating
    - `pnpm test:e2e order-completion` PASSES
    - `pnpm lint src/actions/orders.ts` passes
    - `pnpm tsc --noEmit` passes
  </acceptance_criteria>
  <done>completeOrder работает атомарно, Wave 0 stub удалён, order-completion E2E GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: addOrderPayment hardening + deprecate legacy completion paths + caller migration</name>
  <files>src/actions/orders.ts, src/components/orders/order-detail.tsx, src/app/orders/**/*.tsx, src/app/api/orders/**/route.ts</files>
  <behavior>
    - addOrderPayment: убрать `shiftId ?? null` pattern, найти OPEN shift → throw "Откройте кассовую смену" если нет
    - addOrderPayment overpay: throw "Переплата заказа: уменьшите сумму" если sum(existing) + new > totalAmount
    - Payment.create с shiftId: openShift.id (NOT NULL)
    - Deprecate `updateOrderStatus(..., 'COMPLETED')` path и `payAndChangeStatus(..., 'COMPLETED')` path: добавить `@deprecated Use completeOrder instead` в JSDoc
    - Найти все callers старых функций (grep `updateOrderStatus.*COMPLETED`, `payAndChangeStatus.*COMPLETED`) — мигрировать на `completeOrder`. Если < 5 call sites — сделать в этом task.

    Wave 0 E2E order-payment-constraints (overpay, shift parts) → GREEN.

  </behavior>
  <read_first>
    - src/actions/orders.ts (после Task 2) — особенно addOrderPayment (line ~759-803), updateOrderStatus, payAndChangeStatus
    - src/__tests__/e2e/order-payment-constraints.e2e.test.ts
    - src/components/orders/order-detail.tsx (callers UI)
    - src/app/orders/ — найти callers
  </read_first>
  <action>
    **Step 1 — Переписать addOrderPayment:**
    - Загрузить order с payments
    - Найти OPEN shift для order.storeId; если нет → `throw new Error('Откройте кассовую смену')`
    - existingPaid = sum(payments via toMoney)
    - newTotal = sum(existingPaid, toMoney(input.amount))
    - if (newTotal.gt(totalAmount)) throw "Переплата заказа: уменьшите сумму"
    - tx.payment.create({ data: { orderId, method, amount: amount.toString(), shiftId: openShift.id, isExpense: false } })

    **Step 2 — Deprecate legacy completion:**
    Добавить JSDoc на старые функции:
    ```ts
    /**
     * @deprecated Use completeOrder() instead. This path will be removed after Phase 9.
     */
    ```
    Только для veterans `updateOrderStatus(..., 'COMPLETED')` branch и `payAndChangeStatus(..., 'COMPLETED')` branch.

    **Step 3 — Migrate callers:**
    ```bash
    grep -rn "updateOrderStatus.*COMPLETED\|payAndChangeStatus.*COMPLETED" src/
    ```
    Заменить на `completeOrder(orderId, { discountAmount, finalPayment })`. Update UI компоненты в src/components/orders/order-detail.tsx и любые routes.

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint src/actions/orders.ts &amp;&amp; pnpm test:e2e order-payment-constraints 2>&amp;1 | tee /tmp/phase8-w2-pay.log; grep -q "PASS\|✓" /tmp/phase8-w2-pay.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Переплата заказа: уменьшите сумму" src/actions/orders.ts`
    - `grep -q "Откройте кассовую смену" src/actions/orders.ts`
    - `grep -q "@deprecated Use completeOrder" src/actions/orders.ts`
    - `pnpm test:e2e order-payment-constraints -t "overpay"` PASSES
    - `pnpm test:e2e order-payment-constraints -t "shift"` PASSES
    - `pnpm test:e2e partial-return-per-unit` PASSES (или completion-side тест GREEN)
    - `pnpm tsc --noEmit` passes
    - `pnpm lint src/actions/orders.ts` passes
    - Callers grep: `grep -rn "updateOrderStatus.*COMPLETED\|payAndChangeStatus.*COMPLETED" src/` returns 0 OR matches только в deprecated function bodies
  </acceptance_criteria>
  <done>addOrderPayment hardened, legacy paths deprecated, callers мигрированы. Готово к Task 4 (cancelOrderWithDecision).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: cancelOrderWithDecision с HOLD/REFUND выбором</name>
  <files>src/actions/orders.ts</files>
  <behavior>
    - Удалить Wave 0 throwing stub `cancelOrderWithDecision`
    - Создать новую `cancelOrderWithDecision(orderId, { prepaymentAction, reason })`
    - HOLD: payments не трогаем, status='CANCELLED', cancellationType='HOLD', cancelReason=reason
    - REFUND: требует OPEN shift; для каждого не-isExpense Payment создаёт compensating Payment {isExpense:true, shiftId, method, orderId, amount}; для CASH дополнительно CashOperation {type: 'WITHDRAW', shiftId, amount, reason, performedById}; prepaidAmount=0; cancellationType='REFUND'
    - REFUND без OPEN shift → throw "Для возврата предоплаты откройте смену"
    - Старая `cancelOrder` с deleteMany УДАЛЕНА
    - OrderStatusHistory запись (используем `comment` field — schema)
    - FOR UPDATE lock на CustomOrder

    Wave 0 E2E order-cancel.e2e.test.ts → GREEN.

  </behavior>
  <read_first>
    - src/actions/orders.ts (после Task 3) — особенно lines 911-977 cancelOrder который полностью удаляется + Wave 0 stub
    - prisma/schema.prisma lines 945-948 (CashOpType enum: WITHDRAW, DEPOSIT — verified)
    - prisma/schema.prisma lines 1083-1100 (CashOperation: type, shiftId, amount, reason, performedById — verified)
    - prisma/schema.prisma lines 542-625 (CustomOrder с новыми cancellationType + cancelReason)
    - prisma/schema.prisma lines 611-623 (OrderStatusHistory — поле `comment String?`, не `reason`)
    - .planning/phases/08-order-sale-flow/08-RESEARCH.md Pattern 2 Prepayment Decision
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md секция "Предоплата: невозвратная по умолчанию"
    - src/__tests__/e2e/order-cancel.e2e.test.ts
    - src/components/orders/order-detail.tsx CancelDialog (UI обновляется в Plan 08-05)
  </read_first>
  <action>
    **Step 1 — Удалить старый код:**
    - Старая `export async function cancelOrder(...)` (lines ~911-977) — полностью УДАЛИТЬ. НИКАКИХ payment.deleteMany.
    - Wave 0 throwing stub `cancelOrderWithDecision` — УДАЛИТЬ.

    **Step 2 — Реализовать cancelOrderWithDecision:**

    ```typescript
    export async function cancelOrderWithDecision(
      orderId: string,
      options: { prepaymentAction: CancelPrepaymentAction; reason: string }
    ): Promise<{ success: true }> {
      const session = await auth()
      if (!session?.user?.id) throw new Error('Не авторизован')
      const userId = session.user.id

      if (!options.reason || options.reason.trim().length === 0) {
        throw new Error('Укажите причину отмены')
      }
      if (!['HOLD', 'REFUND'].includes(options.prepaymentAction)) {
        throw new Error('Некорректное действие по предоплате')
      }

      return await db.$transaction(async (tx) => {
        // 1) LOCK
        await tx.$queryRaw`SELECT id FROM "CustomOrder" WHERE id = ${orderId} FOR UPDATE`
        const order = await tx.customOrder.findUniqueOrThrow({
          where: { id: orderId },
          include: { payments: true },
        })
        await requirePermission('orders.manage', order.storeId)

        // 2) Transition validation
        if (order.status === 'COMPLETED' || order.status === 'CANCELLED') {
          throw new Error(`Нельзя отменить из ${order.status}`)
        }

        const prepaid = toMoney(order.prepaidAmount)

        if (options.prepaymentAction === 'HOLD') {
          await tx.customOrder.update({
            where: { id: orderId },
            data: {
              status: 'CANCELLED',
              cancellationType: 'HOLD',
              cancelReason: options.reason,
            },
          })
        } else {
          // REFUND
          if (prepaid.gt(0)) {
            const openShift = await tx.shift.findFirst({
              where: { storeId: order.storeId, status: 'OPEN' },
              select: { id: true },
            })
            if (!openShift) {
              throw new Error('Для возврата предоплаты откройте смену')
            }

            const originalPayments = order.payments.filter(p => !p.isExpense)
            for (const p of originalPayments) {
              await tx.payment.create({
                data: {
                  orderId: order.id,
                  method: p.method,
                  amount: p.amount.toString(),
                  isExpense: true,
                  shiftId: openShift.id,
                },
              })
              if (p.method === 'CASH') {
                await tx.cashOperation.create({
                  data: {
                    shiftId: openShift.id,
                    type: 'WITHDRAW',  // verified: CashOpType enum = { WITHDRAW, DEPOSIT }
                    amount: p.amount.toString(),
                    reason: `Возврат предоплаты по заказу ${order.number}`,
                    performedById: userId,  // verified: NOT NULL field
                  },
                })
              }
            }
          }

          await tx.customOrder.update({
            where: { id: orderId },
            data: {
              status: 'CANCELLED',
              cancellationType: 'REFUND',
              prepaidAmount: '0',
              cancelReason: options.reason,
            },
          })
        }

        // 3) OrderStatusHistory (поле называется `comment`, не `reason`)
        await tx.orderStatusHistory.create({
          data: {
            orderId,
            status: 'CANCELLED',
            userId,
            comment: options.reason,
          },
        })

        return { success: true as const }
      })
    }
    ```

    **Step 3 — Migrate callers:**
    `grep -rn "cancelOrder(" src/` — переписать на `cancelOrderWithDecision`. UI вызов в order-detail.tsx CancelDialog — временный stub `prepaymentAction: 'HOLD'` до Plan 08-05.

    **КРИТИЧНО:**
    - НИГДЕ `payment.deleteMany` в orders.ts: проверить `! grep -q "payment\.deleteMany\|payments.*deleteMany" src/actions/orders.ts`
    - `type: 'WITHDRAW'` (verified), не WITHDRAWAL/EXPENSE/OUT
    - Никаких bare `Number()` на money

  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint src/actions/orders.ts &amp;&amp; ! grep -q "cancelOrderWithDecision not implemented — Wave 2" src/actions/orders.ts &amp;&amp; ! grep -q "payment\.deleteMany\|payments.*deleteMany" src/actions/orders.ts &amp;&amp; pnpm test:e2e order-cancel 2>&amp;1 | tee /tmp/phase8-w2-cancel.log; grep -q "PASS\|✓" /tmp/phase8-w2-cancel.log</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "cancelOrderWithDecision not implemented — Wave 2" src/actions/orders.ts` (Wave 0 stub удалён)
    - `grep -n "^export async function cancelOrderWithDecision" src/actions/orders.ts` matches
    - `grep -q "cancellationType: 'HOLD'" src/actions/orders.ts`
    - `grep -q "cancellationType: 'REFUND'" src/actions/orders.ts`
    - `grep -q "type: 'WITHDRAW'" src/actions/orders.ts`  # verified enum member
    - `grep -q "Для возврата предоплаты откройте смену" src/actions/orders.ts`
    - `grep -q "isExpense: true" src/actions/orders.ts`
    - `grep -q "cashOperation.create" src/actions/orders.ts`
    - `grep -q "performedById: userId" src/actions/orders.ts`
    - `grep -q "cancelReason: options.reason" src/actions/orders.ts`
    - `! grep -q "payment\.deleteMany\|payments.*deleteMany" src/actions/orders.ts` (CRITICAL)
    - Старая `export async function cancelOrder` УДАЛЕНА
    - `pnpm test:e2e order-cancel` PASSES
    - `pnpm lint src/actions/orders.ts` passes
    - `pnpm tsc --noEmit` passes
  </acceptance_criteria>
  <done>cancelOrderWithDecision работает с HOLD/REFUND, compensating entries, Wave 0 order-cancel E2E GREEN. Wave 2 Plan A завершён.</done>
</task>

</tasks>

<verification>
- `pnpm test:e2e order-completion order-cancel order-payment-constraints partial-return-per-unit` — все GREEN (кроме refund-method specific parts из 08-04)
- `pnpm test` (unit suite) не сломан
- `pnpm lint src/actions/orders.ts src/lib/stock-helpers.ts` — money-guard compliant
- `pnpm tsc --noEmit` passes
- `! grep -q "payment.deleteMany" src/actions/orders.ts`
- `! grep -q "not implemented — Wave 2" src/actions/orders.ts` (оба stub удалены)
- Все 9 requirements (FIN-01,02,03,04,05,06,08,11,12) закрыты через E2E
</verification>

<success_criteria>

- [ ] decrementStockForItems helper создан
- [ ] completeOrder атомарная имплементация (Task 2)
- [ ] addOrderPayment hardening + legacy deprecation + caller migration (Task 3)
- [ ] cancelOrderWithDecision с HOLD/REFUND (Task 4)
- [ ] FOR UPDATE locking на CustomOrder + StoreProduct + SerialUnit
- [ ] FIN-01 формула locked: finalAmount = totalAmount - discount - prepaidAmount
- [ ] compensating entries вместо deleteMany
- [ ] Wave 0 stubs удалены
- [ ] Wave 0 E2E тесты order-completion, order-cancel, order-payment-constraints GREEN
      </success_criteria>

<output>
After completion, create `.planning/phases/08-order-sale-flow/08-03-SUMMARY.md` с: New exports, Callers migrated, E2E status, Deprecation notes, Phase 9 handoff.
</output>
