---
phase: 08-order-sale-flow
plan: 06
type: execute
wave: 4
depends_on: ["08-05", "08-04"]
files_modified:
  - .planning/phases/08-order-sale-flow/08-VALIDATION.md
autonomous: false
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-07
  - FIN-08
  - FIN-09
  - FIN-10
  - FIN-11
  - FIN-12
must_haves:
  truths:
    - "Весь Phase 8 test suite GREEN: pnpm test && pnpm test:e2e && pnpm lint && pnpm tsc --noEmit"
    - "Все 6 invariants из VALIDATION.md проверены runtime в каждом E2E (manually verified через grep)"
    - "VALIDATION.md sign-off: nyquist_compliant=true, wave_0_complete=true"
    - "Все 12 FIN-* имеют минимум один GREEN test согласно VALIDATION.md test map"
    - "Deferred scope не leaked: REFUNDED enum отсутствует, optimistic locking не добавлен"
  artifacts:
    - path: ".planning/phases/08-order-sale-flow/08-VALIDATION.md"
      provides: "Updated frontmatter с sign-off флагами"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "08-VALIDATION.md frontmatter"
      to: "Phase 8 closure"
      via: "manual sign-off after full suite green"
      pattern: "nyquist_compliant: true"
---

<objective>
Wave 4 Integration Gate: финальный full suite green run + invariants verification + VALIDATION.md sign-off для всего Phase 8. ЭТОТ план НЕ изменяет никакого production кода — только запускает тесты и обновляет VALIDATION metadata.

Purpose: Подтвердить что все 12 FIN-\* requirements закрыты, invariants проверены, scope не leaked. Это последний gate перед `/gsd:verify-work`.
Output: Updated VALIDATION.md с sign-off флагами, документированный test run.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-order-sale-flow/08-CONTEXT.md
@.planning/phases/08-order-sale-flow/08-VALIDATION.md
@.planning/phases/08-order-sale-flow/08-01-SUMMARY.md
@.planning/phases/08-order-sale-flow/08-02-SUMMARY.md
@.planning/phases/08-order-sale-flow/08-03-SUMMARY.md
@.planning/phases/08-order-sale-flow/08-04-SUMMARY.md
@.planning/phases/08-order-sale-flow/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite green run + invariants verification + VALIDATION sign-off</name>
  <files>.planning/phases/08-order-sale-flow/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-order-sale-flow/08-VALIDATION.md (полностью — invariants list, test map, frontmatter)
    - .planning/phases/08-order-sale-flow/08-CONTEXT.md (success criteria)
    - SUMMARYs всех 5 предыдущих планов (08-01..08-05)
  </read_first>
  <action>
    **Step 1 — Запустить полный suite (последовательно, чтобы видеть failures):**
    ```bash
    pnpm test                # unit
    pnpm test:e2e            # e2e real-db
    pnpm lint                # ESLint (включая money-guard)
    pnpm tsc --noEmit        # TypeScript
    ```
    Если что-либо НЕ зелёное — STOP, не sign-off. Открыть issue, вернуться в plan-phase --gaps mode.

    **Step 2 — Проверить каждый FIN-* test GREEN индивидуально:**
    ```bash
    pnpm test:e2e order-completion order-cancel order-return-sync partial-return-per-unit order-payment-constraints return-midway-failure
    pnpm test -- compute-per-unit-discount
    ```

    **Step 3 — Coverage report для Phase 8 файлов (опционально, для документации):**
    ```bash
    pnpm test:coverage -- --coverage.include="src/actions/orders.ts" --coverage.include="src/actions/sales.ts" --coverage.include="src/lib/orders/discount.ts" --coverage.include="src/lib/stock-helpers.ts"
    ```

    **Step 4 — Verify invariants присутствуют в E2E:**
    ```bash
    for inv in assertStockConservation assertSerialConsistency assertMoneyConservation assertShiftConsistency assertReturnAmountCap assertOrderSaleLink; do
      count=$(grep -rc "$inv" src/__tests__/e2e/ | grep -v ":0$" | wc -l)
      echo "$inv: $count files"
    done
    ```
    Каждый invariant должен быть в ≥ 1 E2E file.

    **Step 5 — Deferred scope leakage check:**
    ```bash
    ! grep -q "REFUNDED" prisma/schema.prisma  # REFUNDED enum НЕ добавлен
    ! grep -q "version.*Int.*@default" prisma/schema.prisma | grep -i "customOrder"  # optimistic locking НЕ добавлен
    ! grep -q "CHECK.*saleId.*orderId.*repairId" prisma/migrations/  # exclusive constraint НЕ добавлен
    ```
    Все три должны проходить (deferred to Phase 15).

    **Step 6 — Stubs cleanup verification:**
    ```bash
    ! grep -q "not implemented — Wave 2" src/actions/orders.ts  # Wave 0 stubs удалены в Plan 08-03
    ! grep -q "payment\.deleteMany\|payments.*deleteMany" src/actions/orders.ts  # никакого deleteMany
    ```

    **Step 7 — Обновить .planning/phases/08-order-sale-flow/08-VALIDATION.md frontmatter:**
    ```yaml
    ---
    nyquist_compliant: true
    wave_0_complete: true
    approval: completed
    completed_at: 2026-04-08
    ---
    ```
    Также внутри документа: все статусы тестов из ❌ W0 → ✅.

  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm test:e2e &amp;&amp; pnpm lint &amp;&amp; pnpm tsc --noEmit &amp;&amp; grep -q "nyquist_compliant: true" .planning/phases/08-order-sale-flow/08-VALIDATION.md &amp;&amp; grep -q "wave_0_complete: true" .planning/phases/08-order-sale-flow/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (unit suite)
    - `pnpm test:e2e` exits 0 (all E2E GREEN)
    - `pnpm lint` exits 0 (money-guard + all rules)
    - `pnpm tsc --noEmit` exits 0
    - `grep -q "nyquist_compliant: true" .planning/phases/08-order-sale-flow/08-VALIDATION.md`
    - `grep -q "wave_0_complete: true" .planning/phases/08-order-sale-flow/08-VALIDATION.md`
    - `! grep -q "REFUNDED" prisma/schema.prisma`
    - `! grep -q "not implemented — Wave 2" src/actions/orders.ts`
    - `! grep -q "payment\.deleteMany" src/actions/orders.ts`
    - Все 12 FIN-* имеют ≥ 1 GREEN test (документировано в VALIDATION test map)
    - Все 6 invariants присутствуют ≥ 1 E2E file
  </acceptance_criteria>
  <done>Phase 8 closure gate passed: full suite green, invariants verified, VALIDATION signed off. Phase 8 готова к /gsd:verify-work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 8 closure approval (human gate)</name>
  <files>.planning/phases/08-order-sale-flow/08-VALIDATION.md</files>
  <action>
    Human gate — пользователь подтверждает что full suite green run был выполнен и Phase 8 готова к verify-work. Защита от автоматического sign-off без awareness.
  </action>
  <what-built>
    Полный Phase 8 test suite запущен: unit + e2e + lint + tsc — все GREEN. VALIDATION.md обновлён с sign-off флагами. Все 12 FIN-* requirements закрыты.
  </what-built>
  <how-to-verify>
    1. Прочитать .planning/phases/08-order-sale-flow/08-VALIDATION.md frontmatter — должно быть `nyquist_compliant: true`, `wave_0_complete: true`
    2. Прочитать summaries всех 6 планов
    3. Запустить локально для подтверждения: `pnpm test && pnpm test:e2e` (5-10 минут)
    4. Проверить что ничего из deferred scope не появилось в коммитах Phase 8
    5. Если всё OK — type "approved"
    6. Если нет — описать что не так, вернуться в plan-phase --gaps
  </how-to-verify>
  <resume-signal>Type "approved" или опишите gaps для исправления</resume-signal>
  <verify>
    <automated>MISSING — human-verify checkpoint, blocked until user types "approved"</automated>
  </verify>
  <done>User approved Phase 8 closure. Можно запускать /gsd:verify-work для финальной верификации.</done>
</task>

</tasks>

<verification>
- Полный test suite GREEN
- Invariants проверены
- VALIDATION.md sign-off completed
- Deferred scope не leaked
- Human approval получен
</verification>

<success_criteria>

- [ ] `pnpm test && pnpm test:e2e && pnpm lint && pnpm tsc --noEmit` все GREEN
- [ ] VALIDATION.md: nyquist_compliant=true, wave_0_complete=true
- [ ] Все 12 FIN-\* имеют GREEN tests
- [ ] Все 6 invariants присутствуют в E2E
- [ ] Phase 8 готова к /gsd:verify-work
      </success_criteria>

<output>
After completion, create `.planning/phases/08-order-sale-flow/08-06-SUMMARY.md` с: Full suite results, Coverage stats, Invariants verification, VALIDATION sign-off confirmation, Phase 8 closure summary, Handoff to /gsd:verify-work.
</output>
