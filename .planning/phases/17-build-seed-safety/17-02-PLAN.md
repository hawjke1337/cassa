---
phase: 17-build-seed-safety
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260418_add_must_change_password/migration.sql
  - prisma/seed.ts
  - prisma/__tests__/seed-guard.test.ts
autonomous: true
requirements: [BUILD-02]
must_haves:
  truths:
    - "NODE_ENV=production pnpm prisma db seed БЕЗ SEED_ALLOW_PROD=true завершается exit code 1 с понятным русским error message"
    - "SEED_ALLOW_PROD=true NODE_ENV=production pnpm prisma db seed создаёт ТОЛЬКО permissions + roles + 1 admin user (никаких магазинов/товаров/демо-пользователей)"
    - "Prod-seed admin создаётся с `mustChangePassword=true` и generated temp-password (выводится в stdout один раз)"
    - "Dev-режим (NODE_ENV !== 'production') работает полностью как раньше — все fixtures присутствуют"
    - "User.mustChangePassword Boolean @default(false) в schema; миграция backfill существующих users на false"
    - "seed-guard имеет unit тест (TDD — тест первым, потом guard) — тест в prisma/__tests__/seed-guard.test.ts"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "User.mustChangePassword Boolean field"
      contains: "mustChangePassword"
    - path: "prisma/migrations/20260418_add_must_change_password/migration.sql"
      provides: "ALTER TABLE User + backfill existing users"
      contains: "ALTER TABLE \"User\" ADD COLUMN \"mustChangePassword\""
    - path: "prisma/seed.ts"
      provides: "NODE_ENV guard + branched prod/dev seeding"
      contains: "SEED_ALLOW_PROD"
    - path: "prisma/__tests__/seed-guard.test.ts"
      provides: "TDD-first unit test for seed-guard behavior"
      contains: "NODE_ENV"
  key_links:
    - from: "prisma/seed.ts"
      to: "process.env.NODE_ENV + process.env.SEED_ALLOW_PROD"
      via: "early exit check в начале main()"
      pattern: "NODE_ENV.*production.*SEED_ALLOW_PROD"
    - from: "prisma/seed.ts (prod branch)"
      to: "prisma.user.create({ mustChangePassword: true, ... })"
      via: "bcryptjs hash + crypto.randomBytes temp password"
      pattern: "mustChangePassword:\\s*true"
---

<objective>
Добавить защиту prisma/seed.ts от случайного уничтожения production-данных (BUILD-02). Prod-режим разрешён только с `SEED_ALLOW_PROD=true` и создаёт ТОЛЬКО permissions + role presets + 1 admin user (с `mustChangePassword=true` и generated temp-паролем). Дополнительно: Prisma schema migration добавляет `User.mustChangePassword` поле для будущего force-change-on-first-login flow (UI deferred в Phase 18).

Purpose: Случайный запуск `pnpm prisma db seed` в production environment не должен стереть данные (сейчас — упадёт с constraint violation или перезапишет seed-users). Admin bootstrap через generated temp-password — готов фундамент для secure первого входа.

Output: Schema field `User.mustChangePassword`, миграция с backfill, seed.ts с NODE_ENV guard и branched prod/dev логикой, unit-тест для seed-guard (TDD).
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-build-seed-safety/17-CONTEXT.md

@prisma/schema.prisma
@prisma/seed.ts
@package.json

<interfaces>
<!-- Key contracts for seed-guard + User schema. -->

From prisma/schema.prisma (User model, lines 51-97 — existing fields):
```prisma
model User {
  id        String   @id @default(cuid())
  login     String   @unique @db.VarChar(50)
  password  String   @db.VarChar(255)
  firstName String   @db.VarChar(100)
  lastName  String   @db.VarChar(100)
  phone     String?  @db.VarChar(20)
  isActive  Boolean  @default(true)
  deletedAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  permissionsVersion Int @default(1)
  // ... relations
}
```

New field to add (after permissionsVersion, line ~62):
```prisma
mustChangePassword Boolean @default(false)
```

From src/lib/permissions-list.ts (available in seed.ts):
```typescript
export const PERMISSIONS: Record<string, { code: string; module: string; name: string; description?: string }>
export const ROLE_PRESETS: Record<string, { name: string; description?: string; isSystem: boolean; permissions: string[] }>
```

Existing prod-seed admin creation pattern (prisma/seed.ts:143-178 — reuse as template):
- `bcryptjs.hashSync(password, 10)` для password hash
- `prisma.user.upsert({ where: { login: 'admin' }, update: {}, create: {...} })`
- `prisma.userRole.create({ data: { userId, roleId: ownerRole.id, storeId: null } })` для owner role

Guard pattern (from CONTEXT.md §"NODE_ENV guard mechanism"):
```typescript
if (process.env.NODE_ENV === 'production' && process.env.SEED_ALLOW_PROD !== 'true') {
  console.error('\n❌ Refusing to seed in production without SEED_ALLOW_PROD=true')
  console.error('   To intentionally bootstrap production, run: SEED_ALLOW_PROD=true pnpm prisma db seed\n')
  process.exit(1)
}
```

Test script for TDD (from CONTEXT.md §"TDD для Phase 17"):
```bash
# RED — guard не создан
NODE_ENV=production npx tsx prisma/seed.ts  # exit 1 expected, сейчас проходит и падает на INSERT

# GREEN — после guard
NODE_ENV=production npx tsx prisma/seed.ts              # exit 1, stderr contains 'Refusing'
NODE_ENV=production SEED_ALLOW_PROD=true npx tsx prisma/seed.ts  # creates ONLY admin + perms + roles
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: TDD — unit тест seed-guard BEFORE реализации + schema migration</name>
  <read_first>
    - prisma/seed.ts (весь файл — понимать текущую структуру)
    - prisma/schema.prisma lines 51-97 (User model — куда добавить mustChangePassword)
    - .planning/phases/17-build-seed-safety/17-CONTEXT.md §"Admin bootstrap в prod-seed", §"TDD для Phase 17", §"NODE_ENV guard mechanism"
    - package.json — scripts для typecheck/test
  </read_first>
  <behavior>
    - Test 1 (RED → GREEN): spawn `NODE_ENV=production npx tsx prisma/seed.ts` БЕЗ `SEED_ALLOW_PROD` → exit code === 1, stderr содержит "Refusing" или "Отказ"
    - Test 2 (RED → GREEN): spawn с `SEED_ALLOW_PROD=true NODE_ENV=production npx tsx prisma/seed.ts --dry-run` → guard пропускает (exit может быть !=1 если другая логика падает, но НЕ exit 1 из-за guard). Проверяем по stderr: НЕ содержит "Refusing".
    - Test 3: spawn `NODE_ENV=test npx tsx prisma/seed.ts` → guard пропускает (не production).
    - Test 4 (unit-level, не spawn): import guard функцию/проверку и вызвать напрямую — hassleGuard(env) returns boolean или throws.

    Реализация теста: shell-based spawn через `child_process.spawnSync` + `vitest`. **Claude's discretion** — возможно выбрать in-process mock `process.exit` + env manipulation (проще, быстрее).
  </behavior>
  <files>
    prisma/__tests__/seed-guard.test.ts,
    prisma/schema.prisma,
    prisma/migrations/20260418_add_must_change_password/migration.sql
  </files>
  <action>
**Часть A: Создать TDD unit тест (RED первым):**

1. Создать файл `prisma/__tests__/seed-guard.test.ts` (расширить vitest projects config если нужно — проверить vitest.config.ts поддерживает этот путь; если нет — положить в `src/__tests__/seed-guard.test.ts`).

2. Рекомендуемая структура теста (Claude's discretion на точные детали):
```typescript
import { describe, it, expect } from "vitest"
import { spawnSync } from "node:child_process"
import path from "node:path"

const SEED_SCRIPT = path.resolve(__dirname, "../seed.ts")

describe("seed-guard", () => {
  it("refuses in production without SEED_ALLOW_PROD", () => {
    const result = spawnSync("npx", ["tsx", SEED_SCRIPT], {
      env: { ...process.env, NODE_ENV: "production", SEED_ALLOW_PROD: undefined },
      encoding: "utf-8",
      timeout: 10000,
    })
    expect(result.status).toBe(1)
    expect(result.stderr).toMatch(/Refusing|Отказ/)
  })

  it("allows in production with SEED_ALLOW_PROD=true", () => {
    const result = spawnSync("npx", ["tsx", SEED_SCRIPT, "--dry-run"], {
      env: { ...process.env, NODE_ENV: "production", SEED_ALLOW_PROD: "true", DATABASE_URL: process.env.DATABASE_URL },
      encoding: "utf-8",
      timeout: 30000,
    })
    // Guard не падает с exit 1 из-за production check; может падать позже из-за БД setup, но stderr НЕ содержит "Refusing"
    expect(result.stderr).not.toMatch(/Refusing/)
  })

  it("allows in non-production environments", () => {
    const result = spawnSync("npx", ["tsx", SEED_SCRIPT, "--dry-run"], {
      env: { ...process.env, NODE_ENV: "test", SEED_ALLOW_PROD: undefined },
      encoding: "utf-8",
      timeout: 30000,
    })
    expect(result.stderr).not.toMatch(/Refusing/)
  })
})
```

3. Запустить тест — должен FAIL (RED), так как guard ещё не добавлен. Commit: `test(17-02): add failing test for seed-guard`

**Часть B: Schema migration (запуск на dev + test DB):**

1. Обновить `prisma/schema.prisma` — добавить поле в model User после `permissionsVersion Int @default(1)` (строка 62):
```prisma
  mustChangePassword Boolean @default(false)
```

2. Создать миграцию вручную (избегаем `prisma migrate dev` который может потребовать интерактив):
```bash
mkdir -p prisma/migrations/20260418_add_must_change_password
```

Создать файл `prisma/migrations/20260418_add_must_change_password/migration.sql` со содержимым:
```sql
-- Add mustChangePassword field to User for force-change-on-first-login flow (Phase 17 BUILD-02)
-- UI flow (middleware + settings/password page) deferred to Phase 18.
-- Default false для existing users — они уже ввели реальный пароль через setup или seed.

ALTER TABLE "User" ADD COLUMN "mustChangePassword" BOOLEAN NOT NULL DEFAULT false;

-- Backfill: все существующие users явно получают false (v1.1 prod data preservation)
-- DEFAULT false выше покрывает это, но explicit UPDATE для аудита:
UPDATE "User" SET "mustChangePassword" = false WHERE "mustChangePassword" IS NOT DISTINCT FROM false;
```

Примечание: `NOT NULL DEFAULT false` + UPDATE — идемпотентно. Fresh install создаёт колонку с default, existing rows получают false через DEFAULT clause.

3. Применить миграцию локально:
```bash
pnpm prisma migrate deploy  # применяет на dev DB (DATABASE_URL)
# Для test DB:
dotenv -e .env.test -- pnpm prisma migrate deploy
```

4. Regenerate Prisma client (обязательно после schema change):
```bash
pnpm prisma generate
```

5. Типы в `src/generated/prisma/client` теперь содержат `mustChangePassword` на User — TypeScript это увидит в задаче 2.
  </action>
  <verify>
    <automated>pnpm test -- prisma/__tests__/seed-guard.test.ts 2>&1 | grep -E "(FAIL|PASS)"</automated>
  </verify>
  <acceptance_criteria>
- `test -f prisma/__tests__/seed-guard.test.ts` (file exists) OR `test -f src/__tests__/seed-guard.test.ts` (если Claude выбрал этот путь)
- `grep -c 'NODE_ENV' prisma/__tests__/seed-guard.test.ts` >= 2 (или соответствующий путь)
- `grep -c "mustChangePassword" prisma/schema.prisma` >= 1
- `grep -c "mustChangePassword" prisma/migrations/20260418_add_must_change_password/migration.sql` >= 1
- Migration SQL содержит `ALTER TABLE "User" ADD COLUMN "mustChangePassword"`
- `pnpm prisma validate` exit 0 (schema валидна)
- После `pnpm prisma generate`: `grep -c "mustChangePassword" src/generated/prisma/client/index.d.ts` >= 1 (новое поле в типах)
- Тест в RED-стадии (guard ещё не добавлен): один из тестов FAIL ожидаемо. После задачи 2 — GREEN.
  </acceptance_criteria>
  <done>
Unit-тест seed-guard.test.ts создан и **в RED**-стадии (guard ещё не реализован). Schema migration применена, `mustChangePassword` поле доступно через regenerated Prisma client. Commit: `test(17-02): RED — seed-guard test + User.mustChangePassword schema`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: GREEN — Реализация NODE_ENV guard + branched prod/dev seed + admin bootstrap</name>
  <read_first>
    - prisma/seed.ts (весь файл, сейчас 737 строк — рефакторинг в prod/dev branches)
    - prisma/__tests__/seed-guard.test.ts (RED-тест из Задачи 1 — должен стать GREEN)
    - src/lib/permissions-list.ts — PERMISSIONS, ROLE_PRESETS (seed reuse)
    - src/lib/auth.ts (pattern для hashSync через bcryptjs — уже используется в seed.ts:146)
    - .planning/phases/17-build-seed-safety/17-CONTEXT.md §"Prod-safe seed.ts — содержимое в prod-режиме", §"Admin bootstrap в prod-seed"
  </read_first>
  <behavior>
    - Guard проверка в первых 10 строках main() → exit 1 если production && !SEED_ALLOW_PROD
    - Prod-branch: создаёт ТОЛЬКО permissions + roles + 1 admin с mustChangePassword=true и generated temp-password
    - Dev-branch (существующая логика): создаёт всё — stores, products, categories, seller/senior users, motivation schemes
    - Temp-password выводится в stdout ровно один раз с явным WARNING-блоком
    - RED-тест из Задачи 1 → GREEN после этой реализации
  </behavior>
  <files>
    prisma/seed.ts
  </files>
  <action>
Рефакторить `prisma/seed.ts` в branched структуру:

**1. NODE_ENV guard (первые строки main):**

В начале функции `main()` (prisma/seed.ts:9 — между `async function main() {` и `const pool = ...`), добавить:

```typescript
async function main() {
  // BUILD-02: Prod-safe guard — отказ при NODE_ENV=production без SEED_ALLOW_PROD=true
  if (process.env.NODE_ENV === "production" && process.env.SEED_ALLOW_PROD !== "true") {
    console.error("\n❌ Refusing to seed in production without SEED_ALLOW_PROD=true")
    console.error("   To intentionally bootstrap production, run:")
    console.error("     SEED_ALLOW_PROD=true pnpm prisma db seed\n")
    process.exit(1)
  }

  const isProd = process.env.NODE_ENV === "production"
  // ... rest of main
```

**НЕ throw Error** — чистый stderr без stack trace (читаемо оператору, CONTEXT.md §"NODE_ENV guard mechanism").

**2. Рефакторинг тела main в branched prod/dev:**

Структура:
```typescript
async function main() {
  // Guard (выше)

  const pool = new Pool({ connectionString: process.env.DATABASE_URL })
  const adapter = new PrismaPg(pool)
  const prisma = new PrismaClient({ adapter })

  console.log("Seeding database...\n")

  if (isProd) {
    await seedProduction(prisma)
  } else {
    await seedDevelopment(prisma)
  }

  await pool.end()
}

// Extract существующий код в seedDevelopment
async function seedDevelopment(prisma: PrismaClient) {
  // Текущий код строк 18-729 (stores → permissions → roles → users → categories → brands → products → suppliers → templates → motivation groups/schemes)
  // Оставить как есть — никаких изменений в dev-branch
}

// Новая prod-only функция
async function seedProduction(prisma: PrismaClient) {
  console.log("PRODUCTION SEED — minimal bootstrap (permissions + roles + admin only)\n")

  // 1. Permissions (копия блока из seedDevelopment строки 54-69)
  console.log("Permissions...")
  const permissionRecords: Record<string, { id: string }> = {}
  for (const perm of Object.values(PERMISSIONS)) {
    const record = await prisma.permission.upsert({
      where: { code: perm.code },
      update: { module: perm.module, name: perm.name },
      create: { code: perm.code, module: perm.module, name: perm.name },
    })
    permissionRecords[perm.code] = record
  }
  console.log(`  ${Object.keys(permissionRecords).length} permissions`)

  // 2. Role presets (копия блока строки 76-105 — ТОЛЬКО ROLE_PRESETS, без Старший продавец)
  console.log("Roles...")
  const roleRecords: Record<string, { id: string }> = {}
  for (const [key, preset] of Object.entries(ROLE_PRESETS)) {
    const role = await prisma.role.upsert({
      where: { name: preset.name },
      update: { description: preset.description, isSystem: preset.isSystem },
      create: { name: preset.name, description: preset.description, isSystem: preset.isSystem },
    })
    roleRecords[key] = role

    await prisma.rolePermission.deleteMany({ where: { roleId: role.id } })
    for (const permCode of preset.permissions) {
      const permRecord = permissionRecords[permCode]
      if (!permRecord) continue
      await prisma.rolePermission.create({
        data: { roleId: role.id, permissionId: permRecord.id },
      })
    }
  }

  // 3. Admin user с generated temp-password (используя crypto.randomBytes)
  console.log("Admin user bootstrap...")
  const existingAdmin = await prisma.user.findUnique({ where: { login: "admin" } })
  if (existingAdmin) {
    console.log("  admin user already exists — skipping (NOT overwriting existing prod admin)")
    return
  }

  // Generate temp password: 16 hex chars, human-ish
  const tempPassword = `admin-${randomBytes(8).toString("hex")}`  // e.g. admin-a1b2c3d4e5f6a7b8
  const passwordHash = hashSync(tempPassword, 10)

  const admin = await prisma.user.create({
    data: {
      login: "admin",
      password: passwordHash,
      firstName: "Администратор",
      lastName: "Системный",
      mustChangePassword: true,  // force change on first login (Phase 18 UI)
    },
  })

  // Owner role
  const ownerRole = roleRecords["owner"]
  if (ownerRole) {
    await prisma.userRole.create({
      data: { userId: admin.id, roleId: ownerRole.id, storeId: null },
    })
  }

  // Temp password output — СТРОГО один раз, явно, с WARNING
  console.log("\n" + "=".repeat(70))
  console.log("⚠️  FIRST-RUN ADMIN CREATED — SAVE THIS PASSWORD NOW")
  console.log("=".repeat(70))
  console.log(`  Login:         admin`)
  console.log(`  Temp password: ${tempPassword}`)
  console.log(`  ⚠️  Password expires on first login — MUST be changed immediately.`)
  console.log(`  ⚠️  This message will NOT be shown again. Copy the password now.`)
  console.log("=".repeat(70) + "\n")
}
```

Импорты наверху файла добавить `import { randomBytes } from "node:crypto"` (рядом с существующими imports строки 1-7).

**3. НЕ создавать в prod:** магазины (seed-store-*), demo-пользователи (seller/senior), категории, бренды, товары, supplier'ы, document templates, motivation groups/schemes — это dev-only fixtures (CONTEXT.md §"Prod-safe seed.ts — содержимое в prod-режиме").

**4. CLI-флаг `--dry-run` (опционально, для удобства тестов):** проверить `process.argv.includes("--dry-run")` — если да, и мы прошли guard, exit 0 до пула. Полезно для задачи 1 тестов.

**5. Проверить что тест из Задачи 1 теперь GREEN:**
```bash
pnpm test -- prisma/__tests__/seed-guard.test.ts
```
  </action>
  <verify>
    <automated>pnpm test -- prisma/__tests__/seed-guard.test.ts 2>&1 | grep -E "(PASS|FAIL)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `NODE_ENV=production npx tsx prisma/seed.ts` exits code 1 AND stderr содержит "Refusing"
- `NODE_ENV=production SEED_ALLOW_PROD=true npx tsx prisma/seed.ts --dry-run` exits 0 (или не 1 из-за guard)
- `grep -c 'SEED_ALLOW_PROD' prisma/seed.ts` >= 2 (в guard проверке и комментарии)
- `grep -c 'seedProduction' prisma/seed.ts` >= 2 (function def + call)
- `grep -c 'seedDevelopment' prisma/seed.ts` >= 2
- `grep -c 'mustChangePassword: true' prisma/seed.ts` >= 1
- `grep -c 'randomBytes' prisma/seed.ts` >= 1
- Unit-тест `seed-guard.test.ts` переходит в GREEN (все test cases pass)
- Реальный prod-seed тест (интеграционно, manual): `NODE_ENV=production SEED_ALLOW_PROD=true npx tsx prisma/seed.ts` на чистой test DB → после run: `db.user.count() === 1` (только admin), `db.store.count() === 0`, `db.product.count() === 0`, `db.permission.count() >= 30`, `db.role.count() >= 3`
  </acceptance_criteria>
  <done>
prisma/seed.ts имеет NODE_ENV guard в первых строках main(), branched prod/dev логика, prod-режим создаёт только admin+permissions+roles с generated temp-password. Admin имеет `mustChangePassword=true`. Unit-тест GREEN. Commit: `feat(17-02): prod-safe seed guard + admin bootstrap with mustChangePassword`.
  </done>
</task>

</tasks>

<verification>
**Финальная проверка плана 17-02 (BUILD-02):**

```bash
# 1. Guard refuses without flag
NODE_ENV=production npx tsx prisma/seed.ts
echo "Exit without flag: $?"  # Expected: 1
# stderr check — manual

# 2. Guard allows with flag
NODE_ENV=production SEED_ALLOW_PROD=true npx tsx prisma/seed.ts --dry-run
echo "Exit with flag: $?"  # Expected: 0 (or exit from dry-run, not 1 from guard)

# 3. Dev still works
NODE_ENV=development npx tsx prisma/seed.ts --dry-run
echo "Exit dev: $?"  # Expected: 0 (or dry-run exit)

# 4. Unit test green
pnpm test -- prisma/__tests__/seed-guard.test.ts

# 5. Schema migration applied
grep 'mustChangePassword' prisma/schema.prisma
ls prisma/migrations/20260418_add_must_change_password/

# 6. Typecheck still passes (от Plan 01)
pnpm typecheck
```
</verification>

<success_criteria>
1. `NODE_ENV=production pnpm prisma db seed` БЕЗ `SEED_ALLOW_PROD=true` завершается exit 1 с явным error message в stderr
2. `SEED_ALLOW_PROD=true NODE_ENV=production pnpm prisma db seed` создаёт ТОЛЬКО: permissions (из PERMISSIONS), role presets (из ROLE_PRESETS), 1 admin user с `mustChangePassword=true` и generated temp-password
3. Dev-mode (`NODE_ENV !== production`) работает полностью идентично как раньше — все stores/products/demo-users созданы
4. Prisma schema содержит `User.mustChangePassword Boolean @default(false)`, миграция применена с backfill
5. Unit-тест `prisma/__tests__/seed-guard.test.ts` (TDD — RED в Задаче 1, GREEN в Задаче 2) — все test cases pass
6. Temp-password выводится в stdout ровно один раз с явным warning-блоком (НЕ логируется в файл)
</success_criteria>

<output>
После завершения plan 17-02:
- Создать `.planning/phases/17-build-seed-safety/17-02-SUMMARY.md`
- Commit: `feat(17-02): prod-safe seed guard + User.mustChangePassword migration + admin bootstrap`

Phase 17 progress: 2/3 plans complete (BUILD-01, BUILD-02).
</output>
