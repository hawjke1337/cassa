---
phase: 17-build-seed-safety
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - .husky/pre-push
  - .env.production.example
autonomous: true
requirements: [BUILD-03]
must_haves:
  truths:
    - "pnpm build завершается exit code 0 — production bundle собирается чисто"
    - ".husky/pre-push существует, executable, запускает pnpm typecheck && pnpm build (или только typecheck — Claude's discretion)"
    - "CI gate предотвращает push с failing typecheck/build (регрессии BUILD-01/BUILD-02 невозможны)"
    - ".env.production.example содержит NEW_ADMIN_PASSWORD reference flow для Phase 18"
  artifacts:
    - path: ".husky/pre-push"
      provides: "Pre-push git hook running typecheck"
      contains: "pnpm typecheck"
    - path: ".env.production.example"
      provides: "Documentation of prod seed bootstrap flow"
      contains: "SEED_ALLOW_PROD"
  key_links:
    - from: ".husky/pre-push"
      to: "git push"
      via: "husky hook executes typecheck before push"
      pattern: "typecheck"
---

<objective>
Верифицировать что `pnpm build` собирает production bundle чисто (BUILD-03), настроить husky pre-push hook как CI gate для предотвращения регрессий, документировать prod-seed bootstrap flow в `.env.production.example`.

Purpose: Завершить Phase 17 — кодовая база собирается чисто под NODE_ENV=production (Next.js 16 Turbopack), CI gate блокирует push'и с failing typecheck, документация для оператора будущего deploy'я Phase 18 готова.

Output: Green `pnpm build`, .husky/pre-push executable, .env.production.example с prod-seed reference.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-build-seed-safety/17-CONTEXT.md

@package.json
@.planning/phases/17-build-seed-safety/17-01-SUMMARY.md
@.planning/phases/17-build-seed-safety/17-02-SUMMARY.md

<interfaces>
<!-- Key contracts для CI gate + documentation. -->

From package.json (scripts):
```json
{
  "scripts": {
    "build": "next build",           // Next.js 16 Turbopack prod bundle
    "typecheck": "tsc --noEmit",     // TypeScript проверка (0 errors required)
    "prepare": "husky"                // husky уже инициализирован
  }
}
```

Expected `.husky/pre-push` structure (husky v9 format, no shebang/sourcing needed):
```bash
#!/usr/bin/env sh
pnpm typecheck
```

(husky v9 автоматически использует /bin/sh. File НЕ нуждается в `. "$(dirname -- "$0")/_/husky.sh"` — это v8 pattern.)

Expected `.env.production.example` — reference only, actual `.env.production` НЕ commit'ится:
```bash
# Production environment variables — Phase 17 minimal, full version в Phase 18

# === Required for app boot ===
DATABASE_URL=postgresql://astore:CHANGE_ME@postgres:5432/astore_erp
NEXTAUTH_SECRET=CHANGE_ME  # generate: openssl rand -base64 32
NEXTAUTH_URL=https://erp.example.com

# === Production seed bootstrap (Phase 17 BUILD-02) ===
# ONLY set SEED_ALLOW_PROD=true when INTENTIONALLY bootstrapping a fresh prod DB.
# After first `SEED_ALLOW_PROD=true pnpm prisma db seed`:
#  1. Temp admin password printed to stdout ONCE — copy it to a password manager.
#  2. Login as admin, change password immediately (Phase 18 UI force-flow).
#  3. REMOVE SEED_ALLOW_PROD from environment (not needed again).
# SEED_ALLOW_PROD=true  # keep commented in normal operation

# === Phase 18 preview (full list in DEPLOYMENT.md when ready) ===
# SENTRY_DSN=
# REDIS_URL=
# BACKUP_DESTINATION=
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: Верификация pnpm build + .env.production.example документация</name>
  <read_first>
    - package.json (scripts section)
    - .planning/phases/17-build-seed-safety/17-CONTEXT.md §"BUILD-03 scope: чистый build"
    - .planning/phases/17-build-seed-safety/17-01-SUMMARY.md (чтобы понять что typecheck уже 0 errors)
    - .planning/phases/17-build-seed-safety/17-02-SUMMARY.md (чтобы понять seed-guard готов)
    - Если существует: .env.example, .env.test (чтобы сохранить стиль комментариев)
  </read_first>
  <files>
    .env.production.example
  </files>
  <action>
**Шаг 1: Запустить pnpm build и проверить exit code:**

```bash
pnpm build 2>&1 | tee /tmp/build-phase17.log
echo "Build exit: $?"
```

Ожидается exit 0 (после Plan 01 null-fixes и Plan 02 seed-guard не ломают prod build).

**Если build падает** — прочитать лог, диагностировать:
- Пропущенные null-guards в других файлах (не покрыты Plan 01)? → зафиксить точечно тем же паттерном (`filter(a => x !== null)` или `?? undefined`).
- Server-only imports в client code? → Next.js Turbopack может быть строже чем tsc; использовать `"use server"` / `"use client"` директивы где нужно.
- Unused imports или any-types на критичных boundary? → минимальные точечные фиксы.

**НЕ заниматься** lint cleanup (174 issues) — явно deferred в CONTEXT.md §"BUILD-03 scope: что НЕ покрывается".

**Если build падает с warnings (не errors):**
- Next.js warnings часто — "deprecated API", "large bundle", "missing alt text" — не блокирующие.
- CONTEXT.md BUILD-03 требование: "exit code 0 без warnings". Интерпретация: Next.js сам не exit'ит с !=0 на warnings. Если Claude видит warnings — перечислить их в commit message; блокирующий ли warning — Claude's discretion.
- Critical warnings для устранения: `Module not found`, `Failed to compile`, `Type error` — эти обязательно фиксить.

**Шаг 2: Создать `.env.production.example`:**

Использовать шаблон из `<interfaces>` выше. Файл должен быть в корне репо рядом с `package.json`.

Важно:
- Файл `.env.production.example` — reference/documentation. НЕ создавать `.env.production` с реальными секретами.
- Добавить в `.gitignore` (если ещё нет) entry `.env.production` (звёздочный вариант `.env*.local` если присутствует — проверить `.gitignore`).
- Вверху файла explicit comment: "This is a TEMPLATE. Copy to .env.production and fill actual values. Phase 18 будет содержать полный список (caddy TLS, backup destinations, Sentry DSN, Redis URL)."
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20 && test -f .env.production.example && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `pnpm build` exit code === 0
- `pnpm build 2>&1 | grep -cE "^(Error|Failed to compile|Type error)"` === 0
- `test -f .env.production.example` (file exists)
- `grep -c 'SEED_ALLOW_PROD' .env.production.example` >= 1
- `grep -c 'DATABASE_URL' .env.production.example` >= 1
- `grep -c 'NEXTAUTH_SECRET' .env.production.example` >= 1
- `grep -c 'openssl rand' .env.production.example` >= 1 (инструкция generate)
- `.gitignore` содержит `.env.production` (не шаблон `.env.production.example`)
  </acceptance_criteria>
  <done>
`pnpm build` exit 0 (чистая production сборка). `.env.production.example` создан с reference к prod-seed bootstrap flow и основными ENV переменными. Commit: `build(17-03): verify clean pnpm build + .env.production.example reference`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: husky pre-push hook CI gate</name>
  <read_first>
    - package.json (scripts + "prepare": "husky")
    - ls .husky/ (существующие hooks — pre-commit для lint-staged должен быть)
    - .planning/phases/17-build-seed-safety/17-CONTEXT.md §"CI gate — предотвращение регрессий"
  </read_first>
  <files>
    .husky/pre-push
  </files>
  <action>
**Шаг 1: Проверить husky setup:**

```bash
ls -la .husky/
cat .husky/pre-commit 2>&1 | head -5  # обычно запускает lint-staged
```

Husky уже инициализирован (package.json: `"prepare": "husky"`, CONTEXT.md строка 95). Pre-commit hook скорее всего есть для lint-staged.

**Шаг 2: Создать `.husky/pre-push`:**

Содержимое файла (husky v9 formatting — БЕЗ shebang line `#!/usr/bin/env sh` и БЕЗ sourcing `_/husky.sh` — это v8 pattern):

```sh
pnpm typecheck
```

**Опционально, если Claude решит:** добавить и build (но CONTEXT.md §"CI gate" говорит "не build — слишком медленно для каждого push"):
```sh
pnpm typecheck
pnpm build
```

По CONTEXT.md рекомендация — ТОЛЬКО typecheck (быстро, ловит большинство регрессий BUILD-01). Claude выбирает на основе скорости build'а на своей машине: если `pnpm build` < 30s — можно добавить; иначе оставить только typecheck.

**Шаг 3: Сделать hook executable:**

```bash
chmod +x .husky/pre-push
```

**Шаг 4: Протестировать локально:**

```bash
# Создать локальный commit для теста (НЕ push)
git add -A
git commit -m "test: husky pre-push dry-run" --allow-empty

# Симулировать push:
git push --dry-run origin HEAD  # должен вызвать pre-push hook

# Если hook падает (typecheck errors) — git push заблокирован
# Если typecheck passes — push proceed'ит (dry-run не отправит на сервер)

# НЕ удалять тестовый commit — пусть остаётся в истории Plan 03
```

**Альтернативный тест (если Claude не хочет создавать лишние коммиты):** симулировать hook вручную:
```bash
# Напрямую запустить hook
sh .husky/pre-push
echo "Hook exit: $?"  # Expected: 0 (typecheck passes from Plan 01)
```

**Шаг 5: Tamper-test** — нарочно сломать typecheck и проверить что hook блокирует:

```bash
# Создать temporary TS error
echo "const broken: number = 'string'" > /tmp/test-broken.ts
# (Не добавлять в проект — просто mental check что hook работает)
```

Этот шаг — Claude's discretion; если выполняется — ОБЯЗАТЕЛЬНО откатить изменение.

**Шаг 6: Документация:**

В `.husky/pre-push` добавить comment на первой строке (после shebang-если-есть):
```sh
# Phase 17 BUILD-03: CI gate — блокирует push если pnpm typecheck fails.
# Предотвращает регрессии BUILD-01 (zero typecheck errors).
pnpm typecheck
```
  </action>
  <verify>
    <automated>test -x .husky/pre-push && sh .husky/pre-push</automated>
  </verify>
  <acceptance_criteria>
- `test -f .husky/pre-push` (file exists)
- `test -x .husky/pre-push` (executable)
- `grep -c 'typecheck' .husky/pre-push` >= 1
- `sh .husky/pre-push` exit code === 0 (current state passes)
- Tamper-test (conceptual): если временно сломать typecheck — `sh .husky/pre-push` возвращает !=0 (hook блокирует)
- `.husky/pre-commit` (если существует) — НЕ затронут (lint-staged продолжает работать)
  </acceptance_criteria>
  <done>
.husky/pre-push создан, executable, запускает `pnpm typecheck` перед каждым push. Hook проверен локально (exit 0 на текущем state, !=0 при симулированной регрессии). CI gate работает. Commit: `chore(17-03): add husky pre-push hook — pnpm typecheck CI gate`.
  </done>
</task>

</tasks>

<verification>
**Финальная проверка плана 17-03 (BUILD-03) и всей Phase 17:**

```bash
# 1. BUILD-01 verification (from Plan 01)
pnpm typecheck
echo "typecheck exit: $?"  # Expected: 0

# 2. BUILD-02 verification (from Plan 02)
NODE_ENV=production npx tsx prisma/seed.ts 2>&1 | grep -q "Refusing" && echo "guard OK" || echo "FAIL"

# 3. BUILD-03 verification
pnpm build 2>&1 | tail -5
echo "build exit: $?"  # Expected: 0

# 4. CI gate verification
test -x .husky/pre-push && echo "hook OK" || echo "FAIL"
sh .husky/pre-push
echo "hook test exit: $?"  # Expected: 0

# 5. Documentation
test -f .env.production.example && echo "env template OK" || echo "FAIL"

# 6. All Phase 17 requirements mapped:
grep -E "^\s*-\s*\[\s*[x]\s*\]\s*\*\*BUILD-0[1-3]\*\*" .planning/REQUIREMENTS.md  # After Phase 17 completion
```
</verification>

<success_criteria>
1. `pnpm build` завершается exit code 0 — production bundle Next.js 16 Turbopack собирается без errors
2. `.husky/pre-push` существует, executable, запускает `pnpm typecheck`; блокирует push при failing typecheck
3. `.env.production.example` создан с reference к SEED_ALLOW_PROD bootstrap flow и основным ENV variables (DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL)
4. `.gitignore` содержит `.env.production` (не пропускает реальные секреты в git)
5. Phase 17 completion gate: все 3 success criteria из ROADMAP §"Phase 17" TRUE — typecheck 0, build 0, seed refuses prod, seed-with-flag creates only admin
</success_criteria>

<output>
После завершения plan 17-03:
- Создать `.planning/phases/17-build-seed-safety/17-03-SUMMARY.md`
- Commit: `chore(17-03): clean build verification + husky pre-push CI gate + env.production.example`
- Update `.planning/STATE.md`: Phase 17 → complete, milestone v1.2 Phase 17/19 → Phase 17 done
- Update `.planning/ROADMAP.md`: Phase 17 checkbox → [x]

Phase 17 progress: 3/3 plans complete (BUILD-01, BUILD-02, BUILD-03). Ready to proceed to `/gsd:plan-phase 18` (Secure Deploy Foundation).
</output>
