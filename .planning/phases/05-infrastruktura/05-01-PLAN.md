---
phase: 05-infrastruktura
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/create-sale-integration.test.ts
  - src/__tests__/create-return-integration.test.ts
  - src/__tests__/confirm-receive-integration.test.ts
autonomous: true
requirements:
  - INFRA-01
user_setup: []

must_haves:
  truths:
    - "npx vitest run завершается за < 30 секунд и все тесты зелёные"
    - "Integration тесты покрывают createSale, createReturn, confirmReceive"
    - "Тесты используют mock Prisma client, не реальную БД"
  artifacts:
    - path: "src/__tests__/create-sale-integration.test.ts"
      provides: "Integration тесты для createSale"
      contains: "vi.mock"
    - path: "src/__tests__/create-return-integration.test.ts"
      provides: "Integration тесты для createReturn"
      contains: "vi.mock"
    - path: "src/__tests__/confirm-receive-integration.test.ts"
      provides: "Integration тесты для confirmReceive"
      contains: "vi.mock"
  key_links:
    - from: "src/__tests__/create-sale-integration.test.ts"
      to: "src/actions/sales.ts"
      via: "import createSale"
      pattern: "import.*createSale"
    - from: "src/__tests__/create-return-integration.test.ts"
      to: "src/actions/sales.ts"
      via: "import createReturn"
      pattern: "import.*createReturn"
    - from: "src/__tests__/confirm-receive-integration.test.ts"
      to: "src/actions/inventory.ts"
      via: "import confirmReceive"
      pattern: "import.*confirmReceive"
---

<objective>
Integration тесты для трёх критичных server actions: createSale, createReturn, confirmReceive.

Purpose: INFRA-01 требует покрытие критичной бизнес-логики. Эти три функции -- ядро POS-системы: продажи, возвраты, приёмка товара. Без тестов на них любое изменение может сломать основной бизнес-процесс.
Output: 3 тестовых файла с mock Prisma, покрывающие happy path и edge cases.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastruktura/05-CONTEXT.md
@.planning/phases/05-infrastruktura/05-RESEARCH.md

@src/actions/sales.ts
@src/actions/inventory.ts
@vitest.config.ts
@src/__tests__/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration тест createSale с mock Prisma</name>
  <files>src/__tests__/create-sale-integration.test.ts</files>
  <read_first>src/actions/sales.ts, vitest.config.ts, src/__tests__/ (любой существующий тест как образец мока)</read_first>
  <action>
Создать integration тест для createSale server action с полным mock Prisma client.

Mock setup (повторить для каждого теста):
- vi.mock("@/lib/db") -- mock db с sale.create, storeProduct.findUnique, storeProduct.update, $transaction, $queryRaw
- vi.mock("@/lib/auth") -- mock auth() возвращает { user: { id: "u1", storeId: "s1", name: "Test" } }
- vi.mock("@/lib/permissions") -- mock requirePermission как vi.fn()
- vi.mock("next/cache") -- mock revalidatePath, revalidateTag

Тест-кейсы:
1. Happy path: createSale с 1 товаром, quantity=1, проверить что $transaction вызван, storeProduct.update уменьшает quantity
2. Happy path: createSale с 2 товарами, проверить что оба storeProduct обновлены
3. Edge case: товар с quantity=0 -- должен бросить ошибку "нет в наличии"
4. Edge case: серийный товар (serialTracking=true) -- проверить что serialUnit обновляется
5. Проверить что sale.create вызван с правильными полями (totalAmount, discountAmount, finalAmount)

Паттерн мока $transaction: vi.fn(async (fn) => fn(mockTx)) где mockTx содержит те же методы.
Использовать beforeEach для reset всех моков.
НЕ мокировать бизнес-логику внутри action -- мокировать только I/O (db, auth, permissions).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/create-sale-integration.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
- grep -q "vi.mock.*@/lib/db" src/__tests__/create-sale-integration.test.ts
- grep -q "createSale" src/__tests__/create-sale-integration.test.ts
- grep -c "it\|test(" src/__tests__/create-sale-integration.test.ts возвращает >= 4
- npx vitest run src/__tests__/create-sale-integration.test.ts завершается с exit code 0
  </acceptance_criteria>
  <done>createSale покрыт >= 4 тестами (happy path + edge cases), все зелёные, mock Prisma без реальной БД</done>
</task>

<task type="auto">
  <name>Task 2: Integration тесты createReturn и confirmReceive</name>
  <files>src/__tests__/create-return-integration.test.ts, src/__tests__/confirm-receive-integration.test.ts</files>
  <read_first>src/actions/sales.ts (createReturn), src/actions/inventory.ts (confirmReceive), src/__tests__/create-sale-integration.test.ts (паттерн мока из Task 1)</read_first>
  <action>
Создать два тестовых файла по аналогии с Task 1.

**create-return-integration.test.ts:**
Mock setup: тот же паттерн (db, auth, permissions, next/cache).
Тест-кейсы:
1. Happy path: возврат 1 товара из продажи с 2 товарами -- storeProduct.quantity увеличивается на 1
2. Полный возврат: все товары из продажи -- sale помечается как возвращённая
3. Edge case: попытка вернуть больше чем продано -- ошибка
4. Серийный товар: serialUnit статус меняется обратно на IN_STOCK

**confirm-receive-integration.test.ts:**
Mock setup: тот же паттерн.
Тест-кейсы:
1. Happy path: приёмка 10 единиц нового товара -- storeProduct создаётся с quantity=10
2. Happy path: приёмка к существующему товару -- quantity увеличивается, costPrice пересчитывается (средневзвешенная)
3. Edge case: приёмка серийного товара -- создаются serialUnit записи
4. Проверить что stockReceive.status обновляется на COMPLETED

НЕ дублировать mock setup -- вынести общий mock pattern в describe-level beforeEach.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/create-return-integration.test.ts src/__tests__/confirm-receive-integration.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
- grep -q "createReturn" src/__tests__/create-return-integration.test.ts
- grep -q "confirmReceive" src/__tests__/confirm-receive-integration.test.ts
- npx vitest run src/__tests__/create-return-integration.test.ts завершается с exit code 0
- npx vitest run src/__tests__/confirm-receive-integration.test.ts завершается с exit code 0
  </acceptance_criteria>
  <done>createReturn и confirmReceive покрыты >= 4 тестами каждый, все зелёные, полный тестовый suite < 30 секунд</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
Все тесты (112 существующих + новые) проходят за < 30 секунд.
</verification>

<success_criteria>
- 3 новых тестовых файла с integration тестами
- >= 12 новых тестов (4+ на каждый action)
- Все тесты зелёные, runtime < 30 секунд
- Mock Prisma client, без реальной БД
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastruktura/05-01-SUMMARY.md`
</output>
