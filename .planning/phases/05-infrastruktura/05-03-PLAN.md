---
phase: 05-infrastruktura
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/actions/reports.ts
  - src/__tests__/reports-sql.test.ts
  - next.config.ts
  - src/app/api/health/route.ts
  - Dockerfile
  - docker-compose.yml
  - entrypoint.sh
  - .env.example
  - .prettierrc
  - .husky/pre-commit
  - package.json
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07
user_setup: []

must_haves:
  truths:
    - "getSalesReport и getProfitReport используют SQL-агрегацию (aggregate/groupBy/$queryRaw), а не загружают все записи в RAM"
    - "Docker Compose запускает систему с healthcheck, prisma migrate deploy при старте, env variables без хардкода"
    - "next.config.ts содержит poweredByHeader: false и security headers"
    - "git push не проходит без Prettier форматирования staged файлов"
  artifacts:
    - path: "src/actions/reports.ts"
      provides: "SQL-оптимизированные отчёты"
      contains: "aggregate\\|groupBy\\|\\$queryRaw"
    - path: "next.config.ts"
      provides: "Security headers"
      contains: "poweredByHeader"
    - path: "src/app/api/health/route.ts"
      provides: "Health check endpoint"
      contains: "SELECT 1"
    - path: "Dockerfile"
      provides: "Hardened Docker image"
      contains: "HEALTHCHECK"
    - path: "entrypoint.sh"
      provides: "Container startup script"
      contains: "prisma migrate deploy"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "DATABASE_URL"
    - path: ".prettierrc"
      provides: "Prettier configuration"
      contains: "semi"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook"
      contains: "lint-staged"
  key_links:
    - from: "Dockerfile"
      to: "entrypoint.sh"
      via: "CMD entrypoint"
      pattern: "entrypoint"
    - from: "src/actions/reports.ts"
      to: "@prisma/client"
      via: "SQL aggregation"
      pattern: "aggregate\\|\\$queryRaw"
    - from: ".husky/pre-commit"
      to: "package.json"
      via: "lint-staged config"
      pattern: "lint-staged"
---

<objective>
SQL-оптимизация отчётов, Docker hardening, security headers и CI tooling (Prettier + Husky).

Purpose: INFRA-04 (отчёт за год < 3 сек), INFRA-05 (production-ready Docker), INFRA-06 (security headers), INFRA-07 (code formatting pipeline). Переход от dev-grade к production-grade инфраструктуре.
Output: Оптимизированные отчёты, hardened Docker, security headers, Prettier + Husky pipeline.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastruktura/05-CONTEXT.md
@.planning/phases/05-infrastruktura/05-RESEARCH.md

@src/actions/reports.ts
@next.config.ts
@Dockerfile
@docker-compose.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL-оптимизация отчётов getSalesReport и getProfitReport</name>
  <files>src/actions/reports.ts, src/__tests__/reports-sql.test.ts</files>
  <read_first>src/actions/reports.ts (полностью -- 577 строк), prisma/schema.prisma (модели Sale, SaleItem, Payment, Return, StockReceive)</read_first>
  <action>
Переписать getSalesReport и getProfitReport на SQL-агрегацию. Остальные report функции (getInventoryReport, getSellerReport, getFundReport, getSupplierDebtsReport) оставить как есть -- они либо уже оптимальны, либо не критичны по производительности.

**getSalesReport -- переписать на:**
1. Summary: `db.sale.aggregate({ where, _count: true, _sum: { totalAmount: true, discountAmount: true, finalAmount: true } })`
2. Chart data (group by day/month): `db.$queryRaw` с DATE_TRUNC
3. Payment breakdown: `db.payment.groupBy({ by: ["method"], where: { sale: where }, _sum: { amount: true } })`
4. Top products by quantity: `db.$queryRaw` с JOIN SaleItem + Product + Sale
5. Top products by revenue: то же но ORDER BY revenue DESC

ВАЖНО: Prisma aggregate _sum возвращает Decimal -- всегда обернуть в Number(result._sum.field ?? 0).
ВАЖНО: Использовать Prisma.sql tagged template для параметризации (НЕ string concatenation).
ВАЖНО: Для optional storeId использовать `Prisma.sql` / `Prisma.empty` паттерн.

**getProfitReport -- переписать на:**
1. Sales summary: aggregate _sum finalAmount
2. Returns summary: aggregate _sum для возвратов
3. Cost data: $queryRaw для расчёта себестоимости проданных товаров (JOIN SaleItem на costPrice)
4. Expenses: aggregate write-offs и прочие расходы

Сохранить ТОЧНО тот же формат возвращаемых данных (интерфейс ответа) -- UI не должен ломаться.
Старый код удалить полностью из функций (не комментировать).

**Тест reports-sql.test.ts:**
Mock Prisma и проверить:
1. getSalesReport вызывает aggregate (не findMany)
2. getProfitReport вызывает $queryRaw или aggregate (не findMany с include)
3. Формат ответа getSalesReport совпадает (salesCount, totalRevenue, etc.)
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/reports-sql.test.ts --reporter=verbose && grep -c "findMany" src/actions/reports.ts</automated>
  </verify>
  <acceptance_criteria>
- grep -q "aggregate\|groupBy\|\$queryRaw" src/actions/reports.ts
- grep -c "findMany" src/actions/reports.ts уменьшилось (getSalesReport и getProfitReport не используют findMany с include items/payments)
- npx vitest run src/__tests__/reports-sql.test.ts завершается с exit code 0
- npx tsc --noEmit без ошибок
  </acceptance_criteria>
  <done>getSalesReport и getProfitReport используют SQL-агрегацию, тесты подтверждают формат ответа, TypeScript компилируется</done>
</task>

<task type="auto">
  <name>Task 2: Docker hardening + security headers + Prettier/Husky pipeline</name>
  <files>next.config.ts, src/app/api/health/route.ts, Dockerfile, docker-compose.yml, entrypoint.sh, .env.example, .prettierrc, .husky/pre-commit, package.json</files>
  <read_first>next.config.ts, Dockerfile, docker-compose.yml, package.json</read_first>
  <action>
**Часть A: Security headers (INFRA-06)**

Обновить next.config.ts:
```typescript
import type { NextConfig } from "next"

const securityHeaders = [
  { key: "X-Frame-Options", value: "DENY" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
]

const nextConfig: NextConfig = {
  output: "standalone",
  poweredByHeader: false,
  async headers() {
    return [{ source: "/(.*)", headers: securityHeaders }]
  },
}

export default nextConfig
```

**Часть B: Health endpoint + Docker (INFRA-05)**

Создать src/app/api/health/route.ts:
```typescript
import { db } from "@/lib/db"

export const dynamic = "force-dynamic"

export async function GET() {
  try {
    await db.$queryRaw`SELECT 1`
    return Response.json({ status: "ok" }, { status: 200 })
  } catch {
    return Response.json({ status: "error" }, { status: 503 })
  }
}
```

Создать entrypoint.sh:
```bash
#!/bin/sh
set -e
echo "Running database migrations..."
npx prisma migrate deploy
echo "Starting application..."
exec node server.js
```

Обновить Dockerfile -- добавить после COPY prisma:
```dockerfile
COPY --from=builder /app/node_modules/.prisma ./node_modules/.prisma
COPY --from=builder /app/node_modules/@prisma ./node_modules/@prisma
COPY entrypoint.sh ./
USER root
RUN chmod +x entrypoint.sh
USER nextjs

HEALTHCHECK --interval=30s --timeout=10s --start-period=30s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1

CMD ["./entrypoint.sh"]
```
Удалить старый CMD ["node", "server.js"].

Обновить docker-compose.yml -- app service:
- Добавить NEXTAUTH_SECRET и NEXTAUTH_URL в environment (с placeholder значениями)
- Добавить healthcheck: test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
- Добавить depends_on: db: condition: service_healthy
- Добавить healthcheck на db service: test: ["CMD-SHELL", "pg_isready -U astore"]

Создать .env.example:
```
DATABASE_URL=postgresql://user:password@localhost:5432/astore_erp
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000
NODE_ENV=production
```

**Часть C: Prettier + Husky + lint-staged (INFRA-07)**

Запустить: `npm install -D prettier husky lint-staged`

Создать .prettierrc:
```json
{
  "semi": false,
  "singleQuote": false,
  "printWidth": 100,
  "tabWidth": 2,
  "trailingComma": "all"
}
```

Выбор semi: false -- проверить преобладающий стиль в src/actions/ перед выбором. Если большинство файлов с точкой с запятой -- использовать "semi": true.

Добавить в package.json:
- scripts.prepare: "husky"
- lint-staged config: { "*.{ts,tsx}": ["prettier --write"], "*.{json,md,css}": ["prettier --write"] }

Запустить: `npx husky init`
Записать в .husky/pre-commit: `npx lint-staged`

НЕ форматировать весь проект. Только staged files при коммите.
НЕ добавлять ESLint в pre-commit (per user discretion -- только Prettier для минимизации friction).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && grep -q "poweredByHeader" next.config.ts && echo "headers OK" && test -f src/app/api/health/route.ts && echo "health OK" && grep -q "HEALTHCHECK" Dockerfile && echo "docker OK" && test -f .env.example && echo "env OK" && test -f .prettierrc && echo "prettier OK" && test -f .husky/pre-commit && echo "husky OK" && docker compose config --quiet 2>&1 && echo "compose valid"</automated>
  </verify>
  <acceptance_criteria>
- grep -q "poweredByHeader: false" next.config.ts
- grep -q "X-Frame-Options" next.config.ts
- test -f src/app/api/health/route.ts
- grep -q "HEALTHCHECK" Dockerfile
- grep -q "prisma migrate deploy" entrypoint.sh
- test -f .env.example
- grep -q "DATABASE_URL" .env.example
- test -f .prettierrc
- test -f .husky/pre-commit
- grep -q "lint-staged" .husky/pre-commit
- grep -q "prepare" package.json
- docker compose config --quiet завершается без ошибок
  </acceptance_criteria>
  <done>Security headers в next.config, Docker с healthcheck + entrypoint + env vars, Prettier + Husky + lint-staged pipeline настроен</done>
</task>

</tasks>

<verification>
npx vitest run && npx tsc --noEmit && docker compose config --quiet
Все тесты зелёные, TypeScript компилируется, Docker Compose валиден.
</verification>

<success_criteria>
- getSalesReport и getProfitReport на SQL-агрегации (INFRA-04)
- Docker: healthcheck, prisma migrate deploy, env vars, .env.example (INFRA-05)
- next.config: poweredByHeader false + 4 security headers (INFRA-06)
- Prettier + Husky + lint-staged pipeline (INFRA-07)
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastruktura/05-03-SUMMARY.md`
</output>
