---
phase: 04-zakazy-i-postavshchiki
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/permissions-list.ts
  - src/lib/order-utils.ts
  - src/actions/orders.ts
  - src/components/orders/order-form.tsx
  - src/components/orders/order-detail.tsx
  - src/__tests__/order-net-profit.test.ts
  - src/__tests__/order-costs.test.ts
  - src/__tests__/order-discount.test.ts
  - src/__tests__/order-item-edit.test.ts
autonomous: true
requirements:
  - ORD-01
  - ORD-02
  - ORD-03
  - ORD-04
  - ORD-07
  - ORD-08

must_haves:
  truths:
    - "При создании заказа продавец может выбрать поставщика из выпадающего списка"
    - "После завершения заказа менеджер может ввести закупочную цену и стоимость доставки"
    - "В карточке заказа отображается чистая прибыль (или 'не рассчитана' если нет закупочной цены)"
    - "При вводе закупочных данных сумма долга поставщику обновляется автоматически"
    - "При выдаче заказа можно применить скидку (не хардкод 0)"
    - "Цена товара в заказе редактируема, totalAmount пересчитывается"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "purchasePrice и deliveryCost поля в CustomOrder"
      contains: "purchasePrice"
    - path: "src/lib/order-utils.ts"
      provides: "calculateNetProfit pure function"
      exports: ["calculateNetProfit"]
    - path: "src/lib/permissions-list.ts"
      provides: "orders.manage_costs permission"
      contains: "orders.manage_costs"
    - path: "src/actions/orders.ts"
      provides: "updateOrderCosts, updateOrderItem actions; discountAmount в COMPLETED flow"
      exports: ["updateOrderCosts", "updateOrderItem"]
  key_links:
    - from: "src/actions/orders.ts (updateOrderCosts)"
      to: "prisma.supplierDebt.update"
      via: "transaction"
      pattern: "supplierDebt\\.update"
    - from: "src/actions/orders.ts (COMPLETED flow)"
      to: "sale.create discountAmount"
      via: "extraData?.discountAmount"
      pattern: "discountAmount.*extraData"
    - from: "src/components/orders/order-detail.tsx"
      to: "src/lib/order-utils.ts"
      via: "calculateNetProfit import"
      pattern: "import.*calculateNetProfit.*order-utils"
---

<objective>
Добавить полный цикл управления заказами: выбор поставщика, ввод закупочных данных, расчет чистой прибыли, обновление долга, скидка при выдаче, редактирование цен товаров.

Purpose: Владелец должен видеть реальную прибыль по каждому заказу и контролировать финансовые аспекты заказов.
Output: Миграция БД, серверные actions, UI компоненты для работы с заказами.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-zakazy-i-postavshchiki/04-CONTEXT.md
@.planning/phases/04-zakazy-i-postavshchiki/04-RESEARCH.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From prisma/schema.prisma (CustomOrder, lines 540-577):
```prisma
model CustomOrder {
  id              String            @id @default(cuid())
  number          String            @unique
  storeId         String
  sellerId        String
  status          CustomOrderStatus @default(NEW)
  clientName      String
  clientPhone     String
  clientEmail     String?
  totalAmount     Decimal           @db.Decimal(12, 2)
  prepaidAmount   Decimal           @default(0) @db.Decimal(12, 2)
  finalAmount     Decimal?          @db.Decimal(12, 2)
  saleId          String?           @unique
  supplierId      String?
  supplierCity    String?
  estimatedDays   Int?
  trackingInfo    String?
  comment         String?
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  completedAt     DateTime?
  items           CustomOrderItem[]
  payments        Payment[]
  debt            SupplierDebt?
}
```

From prisma/schema.prisma (SupplierDebt, lines 621-635):
```prisma
model SupplierDebt {
  id         String      @id @default(cuid())
  supplierId String
  orderId    String      @unique
  amount     Decimal     @db.Decimal(12, 2)
  isPaid     Boolean     @default(false)
  paidAt     DateTime?
  comment    String?
  createdAt  DateTime    @default(now())
  updatedAt  DateTime    @updatedAt
}
```

From src/lib/permissions-list.ts (orders section, lines 23-26):
```typescript
ORDERS_VIEW: { code: "orders.view", module: "orders", name: "Просмотр заказов" },
ORDERS_CREATE: { code: "orders.create", module: "orders", name: "Создание заказов" },
ORDERS_MANAGE: { code: "orders.manage", module: "orders", name: "Управление заказами" },
ORDERS_COSTS: { code: "orders.costs", module: "orders", name: "Просмотр закупочных цен заказов" },
```

From src/actions/orders.ts key exports:
```typescript
export async function createOrder(data: { storeId, clientName, clientPhone, clientEmail?, items[], comment? })
export async function updateOrderStatus(orderId, newStatus, comment?, extraData?: { supplierCity?, estimatedDays?, trackingInfo? })
export async function payAndChangeStatus(orderId, payment, newStatus, comment?)
export async function markSupplierDebtPaid(debtId, comment?)
```

Hardcoded discountAmount: 0 at lines 384 and 571 in COMPLETED flow.
SupplierDebt created at ORDERED status (line 435).
createOrder does NOT pass supplierId (line 240-268).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema + permissions + pure utils + tests</name>
  <files>
    prisma/schema.prisma,
    src/lib/permissions-list.ts,
    src/lib/order-utils.ts,
    src/__tests__/order-net-profit.test.ts,
    src/__tests__/order-costs.test.ts,
    src/__tests__/order-discount.test.ts,
    src/__tests__/order-item-edit.test.ts
  </files>
  <read_first>
    prisma/schema.prisma,
    src/lib/permissions-list.ts,
    src/lib/motivation-utils.ts,
    src/__tests__/motivation-utils.test.ts
  </read_first>
  <behavior>
    calculateNetProfit tests:
    - (10000, 0, 7000, 500) => 2500 (стандартный случай)
    - (10000, 1000, 7000, 500) => 1500 (со скидкой)
    - (10000, 0, null, null) => null (нет закупочной цены)
    - (10000, 0, 7000, null) => 3000 (нет доставки)
    - (5000, 0, 6000, 500) => -1500 (убыточный заказ)

    Discount validation tests:
    - discountAmount = 0, totalAmount = 10000 => valid
    - discountAmount = 5000, totalAmount = 10000 => valid
    - discountAmount = -100, totalAmount = 10000 => invalid
    - discountAmount = 15000, totalAmount = 10000 => invalid

    Item price edit + totalAmount recalc tests:
    - items [{price:100, qty:2}, {price:200, qty:1}] => totalAmount = 400
    - update item 0 price to 150 => totalAmount = 500
    - price = 0 => invalid
    - price < 0 => invalid
  </behavior>
  <action>
    1. SCHEMA MIGRATION: Add to CustomOrder model in prisma/schema.prisma:
       ```
       purchasePrice   Decimal?  @db.Decimal(12, 2)
       deliveryCost    Decimal?  @db.Decimal(12, 2)
       ```
       Run: `npx prisma migrate dev --name add-order-purchase-costs`

    2. PERMISSION: Add to permissions-list.ts after ORDERS_COSTS (line 26):
       ```typescript
       ORDERS_MANAGE_COSTS: { code: "orders.manage_costs", module: "orders", name: "Ввод закупочных цен заказов" },
       ```
       Add "orders.manage_costs" to owner role defaults (find the owner permissions array around line 130).

    3. PURE FUNCTION: Create src/lib/order-utils.ts:
       ```typescript
       export function calculateNetProfit(
         totalAmount: number,
         discountAmount: number,
         purchasePrice: number | null,
         deliveryCost: number | null,
       ): number | null {
         if (purchasePrice === null) return null
         return totalAmount - discountAmount - purchasePrice - (deliveryCost ?? 0)
       }

       export function calculateOrderTotalAmount(
         items: Array<{ price: number; quantity: number }>
       ): number {
         return items.reduce((sum, item) => sum + item.price * item.quantity, 0)
       }

       export function validateDiscountAmount(
         discountAmount: number,
         totalAmount: number,
       ): { valid: boolean; error?: string } {
         if (discountAmount < 0) return { valid: false, error: "Скидка не может быть отрицательной" }
         if (discountAmount > totalAmount) return { valid: false, error: "Скидка не может превышать сумму заказа" }
         return { valid: true }
       }
       ```

    4. TESTS (TDD - write first, then implement):
       - src/__tests__/order-net-profit.test.ts: Test calculateNetProfit per behavior spec above
       - src/__tests__/order-discount.test.ts: Test validateDiscountAmount per behavior spec
       - src/__tests__/order-item-edit.test.ts: Test calculateOrderTotalAmount per behavior spec
       - src/__tests__/order-costs.test.ts: Test validation logic for updateOrderCosts (status must be COMPLETED, purchasePrice > 0, Zod schema)
       
       Follow existing test pattern from src/__tests__/motivation-utils.test.ts.

    5. Run tests: RED first (write tests) -> GREEN (implement functions) -> verify all pass.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma migrate status && npx vitest run src/__tests__/order-net-profit.test.ts src/__tests__/order-costs.test.ts src/__tests__/order-discount.test.ts src/__tests__/order-item-edit.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "purchasePrice" prisma/schema.prisma
    - grep -q "deliveryCost" prisma/schema.prisma
    - grep -q "orders.manage_costs" src/lib/permissions-list.ts
    - grep -q "calculateNetProfit" src/lib/order-utils.ts
    - grep -q "calculateOrderTotalAmount" src/lib/order-utils.ts
    - grep -q "validateDiscountAmount" src/lib/order-utils.ts
    - test -f src/__tests__/order-net-profit.test.ts
    - test -f src/__tests__/order-costs.test.ts
    - test -f src/__tests__/order-discount.test.ts
    - test -f src/__tests__/order-item-edit.test.ts
    - npx vitest run src/__tests__/order-net-profit.test.ts --reporter=verbose (all pass)
    - npx vitest run src/__tests__/order-discount.test.ts --reporter=verbose (all pass)
    - npx vitest run src/__tests__/order-item-edit.test.ts --reporter=verbose (all pass)
  </acceptance_criteria>
  <done>
    Миграция применена (purchasePrice, deliveryCost в CustomOrder). Permission orders.manage_costs зарегистрирован. Pure functions calculateNetProfit, calculateOrderTotalAmount, validateDiscountAmount реализованы и протестированы. Все unit-тесты зеленые.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server actions + UI (supplierId, costs, discount, price edit)</name>
  <files>
    src/actions/orders.ts,
    src/components/orders/order-form.tsx,
    src/components/orders/order-detail.tsx
  </files>
  <read_first>
    src/actions/orders.ts,
    src/components/orders/order-form.tsx,
    src/components/orders/order-detail.tsx,
    src/actions/suppliers.ts,
    src/lib/order-utils.ts
  </read_first>
  <action>
    **SERVER ACTIONS (src/actions/orders.ts):**

    1. **createOrder** (ORD-01): Add `supplierId?: string` to data parameter. Pass it to `tx.customOrder.create`:
       ```typescript
       ...(data.supplierId ? { supplier: { connect: { id: data.supplierId } } } : {}),
       ```

    2. **updateOrderCosts** (ORD-02, ORD-04): NEW action:
       ```typescript
       export async function updateOrderCosts(
         orderId: string,
         data: { purchasePrice: number; deliveryCost?: number }
       ) {
         const session = await auth()
         if (!session?.user?.id) throw new Error("Не авторизован")
         
         const order = await db.customOrder.findUnique({
           where: { id: orderId },
           include: { debt: true },
         })
         if (!order) throw new Error("Заказ не найден")
         
         await requirePermission("orders.manage_costs", order.storeId)
         
         if (order.status !== "COMPLETED") {
           throw new Error("Закупочные данные можно ввести только после завершения заказа")
         }
         if (data.purchasePrice <= 0) {
           throw new Error("Закупочная цена должна быть больше 0")
         }
         
         await db.$transaction(async (tx) => {
           await tx.customOrder.update({
             where: { id: orderId },
             data: {
               purchasePrice: data.purchasePrice,
               deliveryCost: data.deliveryCost ?? 0,
             },
           })
           // Update supplier debt amount
           if (order.debt) {
             await tx.supplierDebt.update({
               where: { id: order.debt.id },
               data: { amount: data.purchasePrice + (data.deliveryCost ?? 0) },
             })
           }
         })
         
         revalidatePath(`/orders/${orderId}`)
         return { success: true }
       }
       ```

    3. **updateOrderItem** (ORD-08): NEW action:
       ```typescript
       export async function updateOrderItem(
         itemId: string,
         data: { price?: number; quantity?: number }
       ) {
         // Validate price > 0, quantity > 0
         // Find item -> find order -> requirePermission("orders.manage", order.storeId)
         // Check order.status is editable (NEW or CONFIRMED, not COMPLETED/CANCELLED)
         // Update item in transaction
         // Recalculate totalAmount = SUM(price * quantity) for all items
         // Update customOrder.totalAmount
         // revalidatePath
       }
       ```

    4. **COMPLETED flow** (ORD-07): In BOTH updateOrderStatus (line 374-412) AND payAndChangeStatus (line 562-618):
       - Add `discountAmount?: number` to `extraData` type
       - Replace `discountAmount: 0` with `extraData?.discountAmount ?? 0`
       - Replace `finalAmount: order.totalAmount` with `finalAmount: Number(order.totalAmount) - (extraData?.discountAmount ?? 0)`
       - Add Zod validation: discountAmount >= 0 and discountAmount <= Number(order.totalAmount)

    **UI (src/components/orders/order-form.tsx)** (ORD-01, ORD-08):
    
    5. **Supplier Select** (ORD-01): Add supplier dropdown. Fetch suppliers via getSuppliersList(). Add shadcn Select after client fields. Pass supplierId to createOrder.

    6. **Editable price** (ORD-08): Make price field in order item rows an editable Input instead of static text. On change, call updateOrderItem or update local state if creating new order.

    **UI (src/components/orders/order-detail.tsx)** (ORD-02, ORD-03, ORD-07):

    7. **Net profit display** (ORD-03): Import calculateNetProfit from order-utils. Show "Чистая прибыль: X руб" (green if > 0, red if < 0) or "Прибыль: не рассчитана" (gray) in order card.

    8. **Cost entry modal** (ORD-02): Add button "Ввести закупочные данные" visible when status === "COMPLETED" and purchasePrice is null. Opens Dialog with purchasePrice (required) and deliveryCost (optional) Decimal inputs. Confirmation dialog before submit showing "Закупочная: X, Доставка: Y, Итого долг: Z". Calls updateOrderCosts.

    9. **Discount field** (ORD-07): In the COMPLETED status change flow (wherever the completion form/dialog is), add a discount input field. Pass discountAmount through to updateOrderStatus/payAndChangeStatus via extraData.

    Use existing patterns: shadcn Dialog, Input, Select, Button. sonner toast for success/error.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "supplierId" src/actions/orders.ts (in createOrder)
    - grep -q "updateOrderCosts" src/actions/orders.ts
    - grep -q "updateOrderItem" src/actions/orders.ts
    - grep -q "orders.manage_costs" src/actions/orders.ts
    - grep -q "extraData?.discountAmount" src/actions/orders.ts
    - grep -q "calculateNetProfit" src/components/orders/order-detail.tsx
    - grep -q "getSuppliersList\|suppliers" src/components/orders/order-form.tsx
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>
    createOrder принимает supplierId. updateOrderCosts создан с permission guard orders.manage_costs и обновляет SupplierDebt.amount. updateOrderItem редактирует цену и пересчитывает totalAmount. COMPLETED flow принимает discountAmount из extraData. UI: supplier Select в форме, editable price, net profit display, cost entry modal, discount field. TypeScript компилируется, все тесты зеленые.
  </done>
</task>

</tasks>

<verification>
1. `npx prisma migrate status` -- миграция применена
2. `npx tsc --noEmit` -- TypeScript компилируется без ошибок
3. `npx vitest run --reporter=verbose` -- все тесты зеленые
4. `grep -q "purchasePrice" prisma/schema.prisma` -- поля в схеме
5. `grep -q "updateOrderCosts" src/actions/orders.ts` -- action существует
6. `grep -q "calculateNetProfit" src/lib/order-utils.ts` -- util существует
</verification>

<success_criteria>
- Миграция добавила purchasePrice и deliveryCost в CustomOrder
- Permission orders.manage_costs зарегистрирован
- createOrder принимает supplierId (ORD-01)
- updateOrderCosts обновляет costs и debt (ORD-02, ORD-04)
- calculateNetProfit вычисляет прибыль (ORD-03)
- discountAmount передается при COMPLETED (ORD-07)
- updateOrderItem позволяет редактировать цену (ORD-08)
- Все unit-тесты зеленые
- TypeScript компилируется
</success_criteria>

<output>
After completion, create `.planning/phases/04-zakazy-i-postavshchiki/04-01-SUMMARY.md`
</output>
