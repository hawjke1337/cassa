---
phase: 03-skhema-bd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
autonomous: true
requirements:
  - DB-01
  - DB-07

must_haves:
  truths:
    - "Запросы Sale по магазину и дате используют индекс (не Seq Scan)"
    - "Запросы Payment по смене используют индекс"
    - "Supplier.inn уникален на уровне БД"
    - "Category name+parentId уникальна (нет дублей на одном уровне)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "30+ новых @@index и 2 @@unique"
      contains: "@@index([storeId, createdAt])"
    - path: "prisma/migrations/*_add_indexes_and_unique/migration.sql"
      provides: "Миграция с CREATE INDEX и UNIQUE"
  key_links:
    - from: "prisma/schema.prisma"
      to: "PostgreSQL"
      via: "prisma migrate dev"
      pattern: "@@index"
---

<objective>
Добавить отсутствующие индексы на часто используемые запросы и уникальные constraints на Supplier.inn и Category[name, parentId].

Purpose: Производительность запросов (DB-01) и предотвращение дубликатов (DB-07).
Output: Обновленный schema.prisma + применённая миграция.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-skhema-bd/03-CONTEXT.md
@.planning/phases/03-skhema-bd/03-RESEARCH.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить @@index на все приоритетные таблицы</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, .planning/phases/03-skhema-bd/03-CONTEXT.md</read_first>
  <action>
Добавить @@index() директивы в prisma/schema.prisma для следующих моделей.

НЕ добавлять индексы на таблицы < 100 строк (Role, Permission, Counter, Brand) -- решение из CONTEXT.md.

Порядок полей в составных индексах: equality-фильтр первым, range/sort вторым.

Конкретные индексы для добавления (проверить, что ещё не существуют в schema):

**Sale** (уже есть @@index([shiftId])):
```
@@index([storeId, createdAt])
@@index([sellerId])
```

**SaleItem** (нет индексов):
```
@@index([saleId])
@@index([productId])
```

**Payment** (уже есть @@index([shiftId])):
```
@@index([saleId])
@@index([type])
```

**StockReceive** (нет индексов):
```
@@index([storeId, createdAt])
@@index([supplierId])
```

**Repair** (нет индексов):
```
@@index([storeId, status])
@@index([customerId])
```

**CustomOrder** (нет индексов):
```
@@index([storeId, status])
@@index([customerId])
```

**Return** (уже есть @@index([shiftId])):
```
@@index([saleId])
@@index([storeId, createdAt])
```

**StockTransfer** (нет индексов):
```
@@index([fromStoreId])
@@index([toStoreId])
```

**InventoryAudit** (нет индексов):
```
@@index([storeId])
```

**StockWriteOff** (нет индексов):
```
@@index([storeId])
```

**SupplierDebt** (нет индексов):
```
@@index([supplierId])
```

**Payroll** (нет индексов):
```
@@index([userId, periodStart])
@@index([storeId])
```

**MotivationAssignment** (нет индексов):
```
@@index([schemeId])
```

**RepairStatusHistory** (нет индексов):
```
@@index([repairId])
```

**OrderStatusHistory** (нет индексов):
```
@@index([orderId])
```

**ReturnItem** (нет индексов):
```
@@index([returnId])
@@index([saleItemId])
```

**PriceHistory** (нет индексов):
```
@@index([productId])
@@index([storeId])
```

Примечание: SerialUnit, SerialUnitHistory, WarrantyClaim, Customer, Shift, TradeIn, InventoryAuditSerial, CashOperation -- уже имеют нужные индексы в текущей schema.

После добавления всех @@index запустить `npx prisma validate` для проверки.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
- grep -c "@@index" prisma/schema.prisma показывает 40+ (было ~15, добавлено ~28)
- npx prisma validate проходит без ошибок
  </acceptance_criteria>
  <done>Все 28+ индексов добавлены в schema.prisma, prisma validate проходит</done>
</task>

<task type="auto">
  <name>Task 2: Добавить @@unique constraints и применить миграцию</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
Добавить уникальные constraints:

1. **Category** -- добавить `@@unique([name, parentId])` для предотвращения дубликатов на одном уровне.
   Убедиться что parentId nullable (для корневых категорий) -- уже nullable в текущей schema.

2. **Supplier** -- добавить `@@unique` на поле `inn`:
   ```
   inn String? @unique
   ```
   Заменить текущее `inn String?` на `inn String? @unique`.

После добавления:
1. Запустить `npx prisma validate`
2. Применить миграцию: `npx prisma migrate dev --name add_indexes_and_unique_constraints`
3. Убедиться что миграция применилась без ошибок

Если миграция падает из-за дублирующихся данных в Category или Supplier.inn:
- Для Category: найти дубли через SQL и объединить/переименовать
- Для Supplier.inn: найти дубли и очистить (inn=null для дублей)
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma validate && npx prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
- grep "@@unique(\[name, parentId\])" prisma/schema.prisma возвращает строку в модели Category
- grep "@unique" prisma/schema.prisma | grep "inn" возвращает строку в модели Supplier
- npx prisma migrate status показывает все миграции применены
  </acceptance_criteria>
  <done>@@unique на Category[name,parentId] и Supplier.inn добавлены, миграция применена</done>
</task>

</tasks>

<verification>
- `npx prisma validate` проходит без ошибок
- `npx prisma migrate status` показывает все миграции applied
- Количество @@index в schema.prisma >= 40
- Category имеет @@unique([name, parentId])
- Supplier.inn имеет @unique
</verification>

<success_criteria>
Все приоритетные индексы (DB-01) и уникальные constraints (DB-07) добавлены в schema.prisma и применены через миграцию. Prisma validate проходит чисто.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skhema-bd/03-01-SUMMARY.md`
</output>
