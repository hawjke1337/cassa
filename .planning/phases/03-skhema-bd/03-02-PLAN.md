---
phase: 03-skhema-bd
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - prisma/schema.prisma
  - src/lib/db.ts
autonomous: true
requirements:
  - DB-02
  - DB-03
  - DB-04
  - DB-05

must_haves:
  truths:
    - "Удаление SaleItem каскадно происходит при удалении Sale"
    - "Удаление Product с продажами запрещено (Restrict)"
    - "PriceHistory.storeId ссылается на Store как FK"
    - "PriceHistory.changedBy ссылается на User как FK"
    - "Soft-deleted Product не появляется в findMany без явного запроса"
    - "Все 51 модель имеют updatedAt с @updatedAt"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "onDelete на всех FK, deletedAt на 5 моделей, updatedAt на 35+ моделей, PriceHistory FK"
      contains: "onDelete: Cascade"
    - path: "src/lib/db.ts"
      provides: "$extends soft delete фильтрация"
      contains: "$extends"
    - path: "prisma/migrations/*_add_on_delete_soft_delete_updated_at/migration.sql"
      provides: "Миграция с ALTER TABLE для FK, новых полей"
  key_links:
    - from: "src/lib/db.ts"
      to: "prisma/schema.prisma"
      via: "$extends query hooks для soft delete моделей"
      pattern: "SOFT_DELETE_MODELS"
    - from: "prisma/schema.prisma"
      to: "PostgreSQL"
      via: "prisma migrate dev"
      pattern: "onDelete"
---

<objective>
Определить onDelete правила на все FK, добавить PriceHistory FK на Store/User, soft delete (deletedAt) на справочники с $extends фильтрацией, и updatedAt на все модели.

Purpose: Целостность данных на уровне СУБД (DB-02, DB-03), мягкое удаление (DB-04), аудит изменений (DB-05).
Output: Обновленные schema.prisma и db.ts + применённая миграция.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-skhema-bd/03-CONTEXT.md
@.planning/phases/03-skhema-bd/03-RESEARCH.md
@prisma/schema.prisma
@src/lib/db.ts

<interfaces>
<!-- Текущий db.ts экспортирует -->
From src/lib/db.ts:
```typescript
export const db = globalForPrisma.prisma ?? createPrismaClient()
```
<!-- Все server actions импортируют { db } from '@/lib/db' -->
<!-- $extends меняет тип db -- нужно обновить globalForPrisma типизацию -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить onDelete правила, PriceHistory FK, deletedAt, updatedAt в schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, .planning/phases/03-skhema-bd/03-CONTEXT.md, .planning/phases/03-skhema-bd/03-RESEARCH.md</read_first>
  <action>
Модифицировать prisma/schema.prisma в 4 блока изменений:

**Блок A: onDelete правила (DB-02)**

Добавить `onDelete: Cascade` на следующие relation fields (если ещё нет):
- SaleItem.sale -> Sale (onDelete: Cascade)
- ReturnItem.return_ -> Return (onDelete: Cascade)
- StockReceiveItem.receive -> StockReceive -- уже есть Cascade
- StockTransferItem.transfer -> StockTransfer -- уже есть Cascade
- InventoryAuditItem.audit -> InventoryAudit -- уже есть Cascade
- StockWriteOffItem.writeOff -> StockWriteOff -- уже есть Cascade
- CustomOrderItem.order -> CustomOrder -- уже есть Cascade
- OrderStatusHistory.order -> CustomOrder -- уже есть Cascade
- RepairStatusHistory.repair -> Repair -- уже есть Cascade
- RolePermission.role -> Role -- уже есть Cascade
- RolePermission.permission -> Permission -- уже есть Cascade
- UserRole.user -> User -- уже есть Cascade
- UserRole.role -> Role -- уже есть Cascade
- MotivationGroupProduct.group -> MotivationGroup -- уже есть Cascade
- MotivationGroupProduct.product -> Product -- уже есть Cascade
- SerialUnitHistory.serialUnit -> SerialUnit (onDelete: Cascade)
- StockTransferItemSerial.transferItem -> StockTransferItem -- уже есть Cascade
- InventoryAuditSerial.audit -> InventoryAudit -- уже есть Cascade
- InventoryAuditSerial.auditItem -> InventoryAuditItem -- уже есть Cascade
- MotivationAssignment.scheme -> MotivationScheme (onDelete: Cascade)
- MotivationAssignment.user -> User (onDelete: Cascade)
- MotivationAssignment.store -> Store (onDelete: Cascade)

Добавить `onDelete: Restrict` на:
- Product.category -> Category (onDelete: Restrict) -- нельзя удалить категорию с товарами
- StoreProduct.store -> Store (onDelete: Restrict)
- StoreProduct.product -> Product (onDelete: Restrict)
- Sale.store -> Store (onDelete: Restrict)
- Sale.seller -> User (onDelete: Restrict)
- SaleItem.product -> Product (onDelete: Restrict) -- nullable, но если есть -- Restrict
- Payment.sale -> Sale (onDelete: Restrict) -- nullable
- Payment.order -> CustomOrder (onDelete: Restrict)
- Payment.repair -> Repair (onDelete: Restrict)
- Return.sale -> Sale (onDelete: Restrict)
- Return.processedBy -> User (onDelete: Restrict)
- StockReceive.store -> Store (onDelete: Restrict)
- StockReceive.receivedBy -> User (onDelete: Restrict)
- StockTransfer.fromStore -> Store (onDelete: Restrict)
- StockTransfer.toStore -> Store (onDelete: Restrict)
- StockTransfer.createdBy -> User (onDelete: Restrict)
- InventoryAudit.store -> Store (onDelete: Restrict)
- InventoryAudit.createdBy -> User (onDelete: Restrict)
- StockWriteOff.store -> Store (onDelete: Restrict)
- StockWriteOff.createdBy -> User (onDelete: Restrict)
- CustomOrder.store -> Store (onDelete: Restrict)
- CustomOrder.seller -> User (onDelete: Restrict)
- Repair.store -> Store (onDelete: Restrict)
- Repair.createdBy -> User (onDelete: Restrict)
- SupplierDebt.supplier -> Supplier (onDelete: Restrict)
- SupplierDebt.order -> CustomOrder (onDelete: Restrict)
- TradeIn.customer -> Customer -- уже есть Restrict
- SerialUnit.product -> Product (onDelete: Restrict)
- SerialUnit.store -> Store (onDelete: Restrict)
- Shift.store -> Store (onDelete: Restrict)
- Shift.openedBy -> User (onDelete: Restrict)
- CashOperation.shift -> Shift (onDelete: Restrict)
- CashOperation.performedBy -> User (onDelete: Restrict)
- Payroll.user -> User (onDelete: Restrict)
- Payroll.store -> Store (onDelete: Restrict)
- Payroll.scheme -> MotivationScheme (onDelete: Restrict)
- PriceLabelTemplate.store -> Store (onDelete: Restrict)
- PriceLabelTemplate.createdBy -> User (onDelete: Restrict)
- DocumentTemplate.store -> Store (onDelete: Restrict)
- DocumentTemplate.createdBy -> User (onDelete: Restrict)
- MotivationScheme.createdBy -> User (onDelete: Restrict)
- WarrantyClaim.store -> Store (onDelete: Restrict)
- WarrantyClaim.createdBy -> User (onDelete: Restrict)
- SerialUnitHistory.store -> Store (onDelete: Restrict)
- SerialUnitHistory.performedBy -> User (onDelete: Restrict)

Добавить `onDelete: SetNull` на:
- Sale.shift -> Shift (onDelete: SetNull) -- продажа может существовать без смены
- Product.brand -> Brand (onDelete: SetNull) -- товар может быть без бренда
- StockReceive.supplier -> Supplier (onDelete: SetNull) -- приход может быть без поставщика
- Payment.shift -> Shift (onDelete: SetNull)
- Payment.store -> Store (onDelete: SetNull)
- Return.shift -> Shift (onDelete: SetNull)
- Repair.master -> User (onDelete: SetNull) -- опциональный мастер
- CustomOrder.supplier -> Supplier (onDelete: SetNull)
- CustomOrder.sale -> Sale (onDelete: SetNull)
- CashOperation.fund -> Fund (onDelete: SetNull)
- CashOperation.supplier -> Supplier (onDelete: SetNull)
- Shift.closedBy -> User (onDelete: SetNull)
- Fund.store -> Store (onDelete: SetNull)
- MotivationScheme.store -> Store (onDelete: SetNull)
- MotivationScheme.approvedBy -> User (onDelete: SetNull)
- MotivationScheme.parentScheme -> MotivationScheme (onDelete: SetNull)
- UserRole.store -> Store (onDelete: SetNull) -- глобальная роль может не иметь магазина
- UserStore.store -> Store (onDelete: Cascade) -- нет смысла без магазина
- UserStore.user -> User (onDelete: Cascade)
- SerialUnit.receiveItem -> StockReceiveItem (onDelete: SetNull)
- SerialUnit.deviceRecord -> DeviceRecord (onDelete: SetNull)
- Repair.deviceRecord -> DeviceRecord (onDelete: SetNull)
- Repair.serialUnit -> SerialUnit (onDelete: SetNull)
- TradeIn.sale -> Sale (onDelete: SetNull)
- TradeIn.product -> Product (onDelete: SetNull)
- TradeIn.repair -> Repair (onDelete: SetNull)
- TradeIn.payout -> Payment (onDelete: SetNull)
- TradeIn.deviceRecord -> DeviceRecord (onDelete: SetNull)
- WarrantyClaim.serialUnit -> SerialUnit (onDelete: SetNull)
- WarrantyClaim.repair -> Repair (onDelete: SetNull)
- WarrantyClaim.deviceRecord -> DeviceRecord (onDelete: SetNull)
- WarrantyClaim.customer -> Customer (onDelete: SetNull)
- DeviceRecord.customer -> Customer (onDelete: SetNull)
- SaleItem.serialUnit -> SerialUnit (onDelete: SetNull)
- StockWriteOffItem.serialUnit -> SerialUnit (onDelete: SetNull)
- CustomOrderItem.serialUnit -> SerialUnit (onDelete: SetNull)
- CustomOrderItem.product -> Product (onDelete: SetNull) -- nullable
- StockTransferItemSerial.serialUnit -> SerialUnit (onDelete: Restrict)

**Блок B: PriceHistory FK (DB-03)**

Заменить в модели PriceHistory:
```
storeId   String
changedBy String
```
на:
```
store     Store    @relation(fields: [storeId], references: [id], onDelete: Restrict)
storeId   String
changedByUser User @relation("PriceHistoryChangedBy", fields: [changedBy], references: [id], onDelete: Restrict)
changedBy String
```

Добавить в модели Store:
```
priceHistory PriceHistory[]
```

Добавить в модели User:
```
priceHistoryChanges PriceHistory[] @relation("PriceHistoryChangedBy")
```

**Блок C: Soft delete (DB-04)**

Добавить `deletedAt DateTime?` поле на 5 моделей:
- Product: `deletedAt DateTime?`
- Supplier: `deletedAt DateTime?`
- Customer: `deletedAt DateTime?`
- Store: `deletedAt DateTime?`
- User: `deletedAt DateTime?`

НЕ добавлять на транзакционные таблицы (Sale, Payment, Return) -- решение из CONTEXT.md.

Добавить @@index([deletedAt]) на каждую из 5 моделей для производительности фильтрации.

**Блок D: updatedAt (DB-05)**

Добавить `updatedAt DateTime @updatedAt` на ВСЕ модели, которые ещё не имеют updatedAt. Проверить каждую модель schema. Модели, которые уже имеют updatedAt: Store, User, Product, Supplier, Customer, CustomOrder, Repair, WarrantyClaim, PriceLabelTemplate, DocumentTemplate, MotivationGroup, MotivationScheme, Shift, DeviceRecord, SerialUnit, TradeIn.

Добавить updatedAt на: Role, Permission, RolePermission, UserRole, UserStore, Category, Brand, StoreProduct, PriceHistory, Counter, Sale, SaleItem, Payment, Return, ReturnItem, StockReceive, StockReceiveItem, StockTransfer, StockTransferItem, InventoryAudit, InventoryAuditItem, StockWriteOff, StockWriteOffItem, CustomOrderItem, OrderStatusHistory, SupplierDebt, RepairStatusHistory, MotivationGroupProduct, MotivationAssignment, Payroll, Fund, CashOperation, SerialUnitHistory, StockTransferItemSerial, InventoryAuditSerial.

После всех изменений запустить `npx prisma validate`.

ВАЖНО: Перед миграцией проверить orphaned PriceHistory записи:
```sql
SELECT count(*) FROM "PriceHistory" WHERE "storeId" NOT IN (SELECT id FROM "Store");
SELECT count(*) FROM "PriceHistory" WHERE "changedBy" NOT IN (SELECT id FROM "User");
```
Если есть orphaned записи -- очистить перед миграцией.

Применить миграцию: `npx prisma migrate dev --name add_on_delete_soft_delete_updated_at`
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma validate && npx prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
- grep -c "onDelete:" prisma/schema.prisma показывает 60+ (было ~17)
- grep -c "deletedAt" prisma/schema.prisma показывает 5+ строк
- grep -c "@updatedAt" prisma/schema.prisma показывает 51 (все модели)
- grep "changedByUser" prisma/schema.prisma возвращает строку в PriceHistory
- npx prisma validate проходит без ошибок
- npx prisma migrate status показывает все миграции applied
  </acceptance_criteria>
  <done>Все FK имеют onDelete правила, PriceHistory FK на Store/User, deletedAt на 5 моделей, updatedAt на все модели, миграция применена</done>
</task>

<task type="auto">
  <name>Task 2: Добавить $extends soft delete фильтрацию в db.ts</name>
  <files>src/lib/db.ts</files>
  <read_first>src/lib/db.ts, .planning/phases/03-skhema-bd/03-RESEARCH.md</read_first>
  <action>
КРИТИЧНО: Использовать $extends, НЕ $use -- $use удален в Prisma 7.

Заменить содержимое src/lib/db.ts на:

```typescript
import { PrismaClient } from '@/generated/prisma/client'
import { PrismaPg } from '@prisma/adapter-pg'
import pg from 'pg'

const SOFT_DELETE_MODELS = ['Product', 'Supplier', 'Customer', 'Store', 'User'] as const

function createPrismaClient() {
  const pool = new pg.Pool({
    connectionString: process.env.DATABASE_URL!,
    max: 20,
    idleTimeoutMillis: 30000,
    connectionTimeoutMillis: 5000,
  })
  const adapter = new PrismaPg(pool)
  const base = new PrismaClient({ adapter })

  return base.$extends({
    name: 'softDelete',
    query: {
      $allModels: {
        async findMany({ model, args, query }) {
          if (SOFT_DELETE_MODELS.includes(model as any)) {
            args.where = { deletedAt: null, ...args.where }
          }
          return query(args)
        },
        async findFirst({ model, args, query }) {
          if (SOFT_DELETE_MODELS.includes(model as any)) {
            args.where = { deletedAt: null, ...args.where }
          }
          return query(args)
        },
        async findFirstOrThrow({ model, args, query }) {
          if (SOFT_DELETE_MODELS.includes(model as any)) {
            args.where = { deletedAt: null, ...args.where }
          }
          return query(args)
        },
        async count({ model, args, query }) {
          if (SOFT_DELETE_MODELS.includes(model as any)) {
            args.where = { deletedAt: null, ...args.where }
          }
          return query(args)
        },
      },
    },
  })
}

const globalForPrisma = globalThis as unknown as {
  prisma: ReturnType<typeof createPrismaClient> | undefined
}

export const db = globalForPrisma.prisma ?? createPrismaClient()

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = db
```

Ключевые моменты:
- findUnique НЕ фильтруется -- если код знает ID, он может проверить deletedAt явно
- Порядок spread: `{ deletedAt: null, ...args.where }` -- пользовательский where может переопределить (для admin доступа через `{ deletedAt: { not: null } }`)
- globalForPrisma типизирован как `ReturnType<typeof createPrismaClient>` -- не PrismaClient напрямую
- SOFT_DELETE_MODELS const assertion для type safety
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/lib/db.ts 2>&1 || echo "TypeCheck done (warnings ok for isolated file)"</automated>
  </verify>
  <acceptance_criteria>
- grep "extends" src/lib/db.ts возвращает строку с "$extends"
- grep "SOFT_DELETE_MODELS" src/lib/db.ts возвращает массив из 5 моделей
- grep -v "\\$use" src/lib/db.ts -- файл НЕ содержит $use
- grep "ReturnType" src/lib/db.ts подтверждает правильную типизацию
  </acceptance_criteria>
  <done>db.ts использует $extends для автоматической фильтрации soft-deleted записей из Product, Supplier, Customer, Store, User</done>
</task>

</tasks>

<verification>
- `npx prisma validate` проходит без ошибок
- `npx prisma migrate status` показывает все миграции applied
- `grep -c "onDelete:" prisma/schema.prisma` >= 60
- `grep "$extends" src/lib/db.ts` -- soft delete extension настроен
- `grep -c "@updatedAt" prisma/schema.prisma` == 51
</verification>

<success_criteria>
Все FK имеют onDelete правила (DB-02). PriceHistory ссылается на Store и User через FK (DB-03). Product, Supplier, Customer, Store, User имеют deletedAt + автоматическая фильтрация через $extends (DB-04). Все модели имеют updatedAt (DB-05). Миграции применены.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skhema-bd/03-02-SUMMARY.md`
</output>
