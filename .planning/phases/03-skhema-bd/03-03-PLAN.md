---
phase: 03-skhema-bd
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - prisma/migrations
autonomous: true
requirements:
  - DB-06

must_haves:
  truths:
    - "Невозможно вставить StoreProduct.quantity < 0 через любой путь"
    - "Невозможно вставить SaleItem.price < 0 или quantity <= 0"
    - "Невозможно вставить Payment.amount <= 0"
    - "Невозможно вставить StockReceiveItem.quantity <= 0 или costPrice < 0"
  artifacts:
    - path: "prisma/migrations/*_add_check_constraints/migration.sql"
      provides: "CHECK constraints на уровне PostgreSQL"
      contains: "ADD CONSTRAINT chk_"
  key_links:
    - from: "prisma/migrations/*_add_check_constraints/migration.sql"
      to: "PostgreSQL"
      via: "prisma migrate dev"
      pattern: "CHECK"
---

<objective>
Добавить CHECK constraints на уровне PostgreSQL для защиты от невалидных price, quantity и amount значений.

Purpose: Двойная защита -- Zod на уровне приложения (Phase 1/2) + CHECK на уровне СУБД (DB-06).
Output: Raw SQL миграция с data cleanup + CHECK constraints.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-skhema-bd/03-CONTEXT.md
@.planning/phases/03-skhema-bd/03-RESEARCH.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Проверить существующие данные и создать data cleanup</name>
  <files>prisma/migrations</files>
  <read_first>prisma/schema.prisma, .planning/phases/03-skhema-bd/03-RESEARCH.md</read_first>
  <action>
Перед добавлением CHECK constraints необходимо проверить существующие данные на нарушения.

1. Выполнить SQL-запросы для поиска невалидных данных (через psql или Prisma $queryRaw):

```sql
SELECT 'StoreProduct.quantity<0' as issue, count(*) as cnt FROM "StoreProduct" WHERE quantity < 0
UNION ALL SELECT 'StoreProduct.sellPrice<0', count(*) FROM "StoreProduct" WHERE "sellPrice" < 0
UNION ALL SELECT 'StoreProduct.costPrice<0', count(*) FROM "StoreProduct" WHERE "costPrice" < 0
UNION ALL SELECT 'SaleItem.price<0', count(*) FROM "SaleItem" WHERE price < 0
UNION ALL SELECT 'SaleItem.costPrice<0', count(*) FROM "SaleItem" WHERE "costPrice" < 0
UNION ALL SELECT 'SaleItem.quantity<=0', count(*) FROM "SaleItem" WHERE quantity <= 0
UNION ALL SELECT 'Payment.amount<=0', count(*) FROM "Payment" WHERE amount <= 0
UNION ALL SELECT 'StockReceiveItem.quantity<=0', count(*) FROM "StockReceiveItem" WHERE quantity <= 0
UNION ALL SELECT 'StockReceiveItem.costPrice<0', count(*) FROM "StockReceiveItem" WHERE "costPrice" < 0
UNION ALL SELECT 'Return.amount<0', count(*) FROM "Return" WHERE amount < 0
UNION ALL SELECT 'ReturnItem.quantity<=0', count(*) FROM "ReturnItem" WHERE quantity <= 0;
```

Можно использовать MCP PostgreSQL сервер для выполнения запроса.

2. Создать пустую миграцию:
```bash
npx prisma migrate dev --create-only --name add_check_constraints
```

3. Отредактировать созданный файл `prisma/migrations/YYYYMMDDHHMMSS_add_check_constraints/migration.sql`:

Добавить следующий SQL:

```sql
-- ============================================================
-- Data Cleanup: исправить невалидные данные перед CHECK constraints
-- ============================================================

-- StoreProduct: отрицательные остатки -> 0
UPDATE "StoreProduct" SET quantity = 0 WHERE quantity < 0;
UPDATE "StoreProduct" SET "sellPrice" = 0 WHERE "sellPrice" < 0;
UPDATE "StoreProduct" SET "costPrice" = 0 WHERE "costPrice" < 0;

-- SaleItem: невалидные значения
UPDATE "SaleItem" SET price = 0 WHERE price < 0;
UPDATE "SaleItem" SET "costPrice" = 0 WHERE "costPrice" < 0;
UPDATE "SaleItem" SET quantity = 1 WHERE quantity <= 0;

-- Payment: невалидные суммы
UPDATE "Payment" SET amount = 0.01 WHERE amount <= 0;

-- StockReceiveItem
UPDATE "StockReceiveItem" SET quantity = 1 WHERE quantity <= 0;
UPDATE "StockReceiveItem" SET "costPrice" = 0 WHERE "costPrice" < 0;

-- Return
UPDATE "Return" SET amount = 0 WHERE amount < 0;
UPDATE "ReturnItem" SET quantity = 1 WHERE quantity <= 0;

-- ============================================================
-- CHECK Constraints
-- ============================================================

-- StoreProduct
ALTER TABLE "StoreProduct" ADD CONSTRAINT chk_store_product_quantity_gte0
  CHECK (quantity >= 0);
ALTER TABLE "StoreProduct" ADD CONSTRAINT chk_store_product_sell_price_gte0
  CHECK ("sellPrice" >= 0);
ALTER TABLE "StoreProduct" ADD CONSTRAINT chk_store_product_cost_price_gte0
  CHECK ("costPrice" >= 0);

-- SaleItem
ALTER TABLE "SaleItem" ADD CONSTRAINT chk_sale_item_price_gte0
  CHECK (price >= 0);
ALTER TABLE "SaleItem" ADD CONSTRAINT chk_sale_item_cost_price_gte0
  CHECK ("costPrice" >= 0);
ALTER TABLE "SaleItem" ADD CONSTRAINT chk_sale_item_quantity_gt0
  CHECK (quantity > 0);

-- Payment
ALTER TABLE "Payment" ADD CONSTRAINT chk_payment_amount_gt0
  CHECK (amount > 0);

-- StockReceiveItem
ALTER TABLE "StockReceiveItem" ADD CONSTRAINT chk_stock_receive_item_quantity_gt0
  CHECK (quantity > 0);
ALTER TABLE "StockReceiveItem" ADD CONSTRAINT chk_stock_receive_item_cost_price_gte0
  CHECK ("costPrice" >= 0);

-- Return
ALTER TABLE "Return" ADD CONSTRAINT chk_return_amount_gte0
  CHECK (amount >= 0);

-- ReturnItem
ALTER TABLE "ReturnItem" ADD CONSTRAINT chk_return_item_quantity_gt0
  CHECK (quantity > 0);
```

Формат именования: `chk_{table_snake}_{column_snake}_{rule}` -- решение из RESEARCH.md.

4. Применить миграцию:
```bash
npx prisma migrate dev
```

Если миграция падает -- посмотреть на какой строке, исправить данные и повторить.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
- ls prisma/migrations/*check_constraints*/migration.sql существует
- grep -c "ADD CONSTRAINT chk_" prisma/migrations/*check_constraints*/migration.sql показывает 11
- npx prisma migrate status показывает все миграции applied
  </acceptance_criteria>
  <done>Data cleanup выполнен, 11 CHECK constraints добавлены и применены</done>
</task>

<task type="auto">
  <name>Task 2: Верифицировать CHECK constraints работают</name>
  <files>prisma/migrations</files>
  <read_first>prisma/migrations/*check_constraints*/migration.sql</read_first>
  <action>
Проверить что CHECK constraints действительно работают, выполнив SQL-запросы которые ДОЛЖНЫ упасть.

Использовать MCP PostgreSQL сервер или psql для выполнения:

```sql
-- Должен упасть: quantity < 0
DO $$
BEGIN
  INSERT INTO "StoreProduct" (id, "storeId", "productId", quantity, "sellPrice", "costPrice")
  VALUES ('test-chk-1', (SELECT id FROM "Store" LIMIT 1), (SELECT id FROM "Product" LIMIT 1), -1, 100, 50);
  RAISE EXCEPTION 'CHECK constraint did NOT fire for negative quantity';
EXCEPTION WHEN check_violation THEN
  RAISE NOTICE 'OK: quantity >= 0 constraint works';
END $$;

-- Должен упасть: payment amount <= 0
DO $$
BEGIN
  INSERT INTO "Payment" (id, method, amount)
  VALUES ('test-chk-2', 'CASH', 0);
  RAISE EXCEPTION 'CHECK constraint did NOT fire for zero amount';
EXCEPTION WHEN check_violation THEN
  RAISE NOTICE 'OK: amount > 0 constraint works';
END $$;

-- Должен упасть: SaleItem quantity <= 0
DO $$
BEGIN
  INSERT INTO "SaleItem" (id, "saleId", quantity, price, "costPrice", total)
  VALUES ('test-chk-3', (SELECT id FROM "Sale" LIMIT 1), 0, 100, 50, 100);
  RAISE EXCEPTION 'CHECK constraint did NOT fire for zero quantity';
EXCEPTION WHEN check_violation THEN
  RAISE NOTICE 'OK: quantity > 0 constraint works';
END $$;
```

Все 3 блока должны вывести "OK:" сообщения, а не EXCEPTION.

Если какой-то тест не проходит -- constraint не применился, нужно разобраться.

Также проверить что валидные данные проходят:
```sql
-- Валидная вставка не должна падать (cleanup после теста)
-- Просто убедиться что существующие данные в порядке
SELECT count(*) as total_store_products FROM "StoreProduct" WHERE quantity >= 0;
```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma validate && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- Все 3 DO-блока в PostgreSQL завершаются с "OK:" сообщениями (constraint сработал)
- npx prisma validate проходит
- Существующие тесты не сломаны (npx vitest run)
  </acceptance_criteria>
  <done>CHECK constraints верифицированы -- невалидные данные отклоняются на уровне СУБД</done>
</task>

</tasks>

<verification>
- `npx prisma migrate status` -- все миграции applied
- CHECK constraints работают (негативные тесты отклоняются)
- `npx prisma validate` проходит
- `npx vitest run` -- существующие тесты зеленые
</verification>

<success_criteria>
11 CHECK constraints (DB-06) добавлены через raw SQL миграцию. Data cleanup выполнен перед constraints. Невалидные price/quantity/amount отклоняются на уровне PostgreSQL. Двойная защита: Zod (app) + CHECK (DB).
</success_criteria>

<output>
After completion, create `.planning/phases/03-skhema-bd/03-03-SUMMARY.md`
</output>
