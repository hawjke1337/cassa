---
phase: 15-data-integrity-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/new_varchar_limits_cascade/migration.sql
autonomous: true
requirements: [DATA2-03, DATA2-06]

must_haves:
  truths:
    - "Все текстовые поля без @db.VarChar имеют лимиты"
    - "Удаление User (soft delete) не каскадно удаляет UserRole/UserStore/MotivationAssignment"
    - "История ролей и назначений сохраняется после soft delete пользователя"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "VarChar limits on all text fields + SetNull cascades"
      contains: "@db.VarChar"
  key_links:
    - from: "prisma/schema.prisma"
      to: "UserRole/UserStore/MotivationAssignment"
      via: "onDelete: SetNull"
      pattern: "onDelete: SetNull"
---

<objective>
Add @db.VarChar(N) limits to all unbounded text fields in the schema and change User cascade behavior from Cascade to SetNull for UserRole, UserStore, and MotivationAssignment.

Purpose: Prevent unbounded text storage and preserve role/assignment history when users are soft-deleted.
Output: Updated schema.prisma with varchar limits + cascade safety migration.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-data-integrity-hardening/15-CONTEXT.md
@.planning/phases/15-data-integrity-hardening/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @db.VarChar(N) to all unbounded text fields</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (full file — need to audit every String field)
  </read_first>
  <action>
NOTE: Plan 01 has already modified prisma/schema.prisma (CHECK constraints + SerialUnit index). Read the current state before editing.

Audit every `String` field in schema.prisma. For fields that already have `@db.VarChar(N)` — skip. For fields without limits, add appropriate `@db.VarChar(N)`:

**Limits by field type:**

| Field Pattern | Limit | Rationale |
|---|---|---|
| name, firstName, lastName, clientName | 100 | Person/entity names |
| login | 50 | Username |
| password | 255 | bcrypt hash |
| phone, clientPhone | 20 | International phone format |
| email, clientEmail | 255 | RFC 5321 |
| address | 500 | Full address |
| city, supplierCity | 100 | City name |
| website | 500 | URL |
| inn | 12 | Russian INN (10 or 12 digits) |
| number (Sale, Return, etc.) | 50 | Document numbers |
| comment, reason | 1000 | Free-text comments |
| diagnosis, workDone, defectDescription | 2000 | Repair descriptions |
| deviceCondition | 500 | Device state description |
| deviceType, deviceBrand, deviceModel | 100 | Device identifiers |
| deviceSerial, devicePassword | 100 | Device credentials |
| trackingInfo | 500 | Tracking data |
| imei, imei2 | 15 | IMEI is exactly 15 digits |
| serialNumber | 100 | Serial numbers vary |
| rejectionReason | 1000 | Motivation scheme rejection |
| relatedDocument | 255 | Reference document ID |
| field (PriceHistory) | 50 | Field name |
| passportSeries | 4 | Russian passport series |
| passportNumber | 6 | Russian passport number |
| passportIssuedBy | 500 | Passport issuing authority |
| fundName (Fund.name) | 100 | Fund name |
| description (MotivationScheme) | 1000 | Scheme description |

Fields to SKIP (already typed or not String):
- All `@id @default(cuid())` fields — these are cuid-length, skip
- All relation FK fields (saleId, orderId, etc.) — cuid, skip
- All enum fields — already typed
- All Json fields (formula) — not String

Apply `@db.VarChar(N)` after the type annotation, e.g.:
```prisma
name String @db.VarChar(100)
comment String? @db.VarChar(1000)
```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm prisma validate` passes with no errors
    - prisma/schema.prisma contains `@db.VarChar(100)` on Store.name
    - prisma/schema.prisma contains `@db.VarChar(1000)` on Sale.comment
    - prisma/schema.prisma contains `@db.VarChar(2000)` on Repair.defectDescription
    - prisma/schema.prisma contains `@db.VarChar(20)` on Customer.phone
    - prisma/schema.prisma contains `@db.VarChar(15)` on SerialUnit.imei
    - No unbounded String fields remain (except id/FK cuid fields and Json fields)
  </acceptance_criteria>
  <done>All text fields in schema.prisma have explicit @db.VarChar(N) limits</done>
</task>

<task type="auto">
  <name>Task 2: Change User cascade to SetNull + migration</name>
  <files>prisma/schema.prisma, prisma/migrations/new_varchar_cascade/migration.sql</files>
  <read_first>
    - prisma/schema.prisma (lines 132-154 — UserRole, UserStore models)
    - prisma/schema.prisma (lines 942-956 — MotivationAssignment model)
    - src/lib/db.ts (lines 1-30 — soft delete middleware)
  </read_first>
  <action>
1. **Update prisma/schema.prisma:**

In UserRole model (line ~134):
```prisma
user    User    @relation(fields: [userId], references: [id], onDelete: SetNull)
userId  String?
```
Change `userId String` to `userId String?` (nullable for SetNull).
Change `onDelete: Cascade` to `onDelete: SetNull`.

In UserStore model (line ~147):
```prisma
user    User   @relation(fields: [userId], references: [id], onDelete: SetNull)
userId  String?
```
Change `userId String` to `userId String?` (nullable for SetNull).
Change `onDelete: Cascade` to `onDelete: SetNull`.

In MotivationAssignment model (line ~946):
```prisma
user      User              @relation(fields: [userId], references: [id], onDelete: SetNull)
userId    String?
```
Change `userId String` to `userId String?` (nullable for SetNull).
Change `onDelete: Cascade` to `onDelete: SetNull`.

Update the `@@unique` constraints that include userId — they now need to handle nullable userId:
- UserRole: `@@unique([userId, roleId, storeId])` — keep as-is (Prisma handles nullable in unique)
- UserStore: `@@unique([userId, storeId])` — keep as-is
- MotivationAssignment: `@@unique([userId, storeId, startDate])` — keep as-is

2. **Run migration:**
```bash
pnpm prisma migrate dev --name varchar_limits_cascade_safety
```

This migration will:
- Add VARCHAR limits to all text columns (from Task 1)
- Change FK constraints on UserRole.userId, UserStore.userId, MotivationAssignment.userId from CASCADE to SET NULL
- Make userId nullable in those 3 tables
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm prisma migrate status && pnpm prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `onDelete: SetNull` for UserRole.user relation
    - prisma/schema.prisma contains `onDelete: SetNull` for UserStore.user relation
    - prisma/schema.prisma contains `onDelete: SetNull` for MotivationAssignment.user relation
    - prisma/schema.prisma contains `userId  String?` (nullable) in UserRole
    - prisma/schema.prisma contains `userId  String?` (nullable) in UserStore
    - prisma/schema.prisma contains `userId  String?` (nullable) in MotivationAssignment
    - Migration applied successfully (no pending migrations)
  </acceptance_criteria>
  <done>User cascade changed to SetNull — soft deleting a User preserves role/assignment records with userId=null instead of cascading deletion</done>
</task>

</tasks>

<verification>
- `pnpm prisma validate` passes
- `pnpm prisma migrate status` shows no pending migrations
- grep confirms no unbounded String fields remain in schema
- grep confirms SetNull on UserRole, UserStore, MotivationAssignment user relations
</verification>

<success_criteria>
All text fields have VarChar limits. User deletion preserves UserRole/UserStore/MotivationAssignment records (SetNull instead of Cascade).
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-integrity-hardening/15-02-SUMMARY.md`
</output>
