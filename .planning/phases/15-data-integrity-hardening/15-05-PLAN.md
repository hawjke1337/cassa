---
phase: 15-data-integrity-hardening
plan: 05
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - prisma/schema.prisma
  - src/actions/motivation.ts
  - src/actions/repairs.ts
  - src/actions/device-records.ts
  - src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts
autonomous: true
requirements: [DATA2-10, DATA2-11, DATA2-12]

must_haves:
  truths:
    - "Два пользователя одновременно редактируют MotivationScheme — второй получает ошибку 'Данные были изменены другим пользователем'"
    - "При повторном ремонте устройства с тем же IMEI создаётся новый Repair привязанный к существующему DeviceRecord"
    - "MotivationScheme.formula проходит валидацию перед сохранением"
    - "При применении формулы к MotivationAssignment сохраняется snapshot формулы на момент применения"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "version field on MotivationScheme"
      contains: "version Int @default(1)"
    - path: "src/actions/motivation.ts"
      provides: "Optimistic locking on MotivationScheme update + formula snapshot on assignment"
      contains: "version"
    - path: "src/actions/repairs.ts"
      provides: "DeviceRecord deduplication in createRepair"
      contains: "findFirst"
    - path: "src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts"
      provides: "E2E tests for locking and dedup"
      min_lines: 80
  key_links:
    - from: "src/actions/motivation.ts"
      to: "prisma/schema.prisma"
      via: "version field WHERE clause"
      pattern: "version.*expectedVersion\\|WHERE.*version"
    - from: "src/actions/repairs.ts"
      to: "DeviceRecord"
      via: "findFirst by imei/imei2/serialNumber"
      pattern: "deviceRecord.*findFirst\\|findFirst.*imei"
---

<objective>
Add optimistic locking (version field) to MotivationScheme, formula validation on update, formula snapshot storage when applying to assignments, and DeviceRecord deduplication in createRepair.

Purpose: Prevent concurrent edit conflicts on MotivationScheme, ensure formula integrity via snapshots, and eliminate duplicate DeviceRecords for the same physical device.
Output: Schema update + action logic + E2E tests.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-data-integrity-hardening/15-CONTEXT.md
@.planning/phases/15-data-integrity-hardening/15-02-SUMMARY.md
@.planning/phases/15-data-integrity-hardening/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MotivationScheme version field + optimistic locking + formula validation + snapshot</name>
  <files>prisma/schema.prisma, src/actions/motivation.ts</files>
  <read_first>
    - prisma/schema.prisma (lines 919-960 — MotivationScheme and MotivationAssignment models)
    - src/actions/motivation.ts (find updateMotivationScheme and any assignment-apply functions)
  </read_first>
  <action>
NOTE: Plans 01 and 02 have already modified prisma/schema.prisma (CHECK constraints, VarChar limits, SetNull cascades). Read the current state before editing.

1. **Add version field to MotivationScheme in schema.prisma:**

In MotivationScheme model (after `updatedAt`):
```prisma
version     Int          @default(1)
```

Also check MotivationAssignment model — if it does NOT have a `formulaSnapshot Json?` field, add one:
```prisma
formulaSnapshot Json?   // Snapshot формулы на момент применения
```

Run migration: `pnpm prisma migrate dev --name add_motivation_version_snapshot`

2. **Implement optimistic locking in update action:**

In `src/actions/motivation.ts`, find the function that updates MotivationScheme (likely `updateMotivationScheme` or similar). Modify it:

```typescript
export async function updateMotivationScheme(
  id: string,
  data: { name?: string; formula?: unknown; description?: string },
  expectedVersion: number
) {
  // Formula validation (DATA2-10)
  if (data.formula !== undefined) {
    validateMotivationFormula(data.formula)
  }

  // Optimistic locking (DATA2-11)
  const result = await db.motivationScheme.updateMany({
    where: { id, version: expectedVersion },
    data: {
      ...data,
      version: { increment: 1 },
    },
  })

  if (result.count === 0) {
    throw new Error('Данные были изменены другим пользователем. Обновите страницу и попробуйте снова.')
  }

  return db.motivationScheme.findUnique({ where: { id } })
}
```

3. **Add formula validation function:**

```typescript
function validateMotivationFormula(formula: unknown): void {
  if (formula === null || formula === undefined) {
    throw new Error('Формула мотивации не может быть пустой')
  }
  if (typeof formula !== 'object') {
    throw new Error('Формула мотивации должна быть объектом JSON')
  }
  // Additional structural validation based on expected formula shape
}
```

4. **Formula snapshot on assignment application (DATA2-10):**

Find the function that applies/assigns a MotivationScheme to a MotivationAssignment (e.g., `createMotivationAssignment`, `assignMotivationScheme`, or similar). When creating or updating a MotivationAssignment, store the current formula as a snapshot:

```typescript
// When creating/applying assignment:
const scheme = await db.motivationScheme.findUnique({ where: { id: schemeId } })
await db.motivationAssignment.create({
  data: {
    ...assignmentData,
    schemeId: scheme.id,
    formulaSnapshot: scheme.formula, // snapshot at time of assignment
  },
})
```

This ensures that if the formula is later changed, existing assignments retain the formula that was active when they were created.

5. **Update any form/UI that calls updateMotivationScheme** to pass `expectedVersion` from the loaded scheme data. Search for usages and add the version parameter.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm prisma validate && grep -n "version\|formulaSnapshot\|expectedVersion" src/actions/motivation.ts | head -15</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `version     Int          @default(1)` in MotivationScheme
    - prisma/schema.prisma contains `formulaSnapshot` in MotivationAssignment (if not already present)
    - src/actions/motivation.ts contains "expectedVersion" parameter
    - src/actions/motivation.ts contains "Данные были изменены другим пользователем"
    - src/actions/motivation.ts contains formula validation logic
    - src/actions/motivation.ts contains "formulaSnapshot" when creating assignments
    - Migration applied (pnpm prisma migrate status clean)
  </acceptance_criteria>
  <done>MotivationScheme has version-based optimistic locking, formula validation on update, and formula snapshot stored when applied to assignments</done>
</task>

<task type="auto">
  <name>Task 2: DeviceRecord deduplication in createRepair + E2E tests</name>
  <files>src/actions/repairs.ts, src/actions/device-records.ts, src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts</files>
  <read_first>
    - src/actions/repairs.ts (find createRepair function — how DeviceRecord is created)
    - src/actions/device-records.ts (full — existing device record actions)
    - prisma/schema.prisma (lines 1244-1261 — DeviceRecord model)
  </read_first>
  <action>
NOTE: Plan 03 has already modified src/actions/repairs.ts (phone normalization + IMEI validation). Read the current state before editing.

1. **Create findOrCreateDeviceRecord helper** in `src/actions/device-records.ts`:

```typescript
/**
 * DATA2-12: Ищет существующий DeviceRecord по IMEI/serial.
 * Если найден — обновляет customerId и возвращает существующий.
 * Если не найден — создаёт новый.
 */
export async function findOrCreateDeviceRecord(
  tx: PrismaTransaction,
  data: {
    imei?: string | null
    imei2?: string | null
    serialNumber?: string | null
    deviceType: string
    brand?: string | null
    model?: string | null
    customerId?: string | null
  }
): Promise<string> {
  // Search by all identifiers for maximum dedup coverage
  const identifiers: any[] = []
  if (data.imei) identifiers.push({ imei: data.imei })
  if (data.imei2) identifiers.push({ imei2: data.imei2 })
  if (data.serialNumber) identifiers.push({ serialNumber: data.serialNumber })

  let existing: DeviceRecord | null = null
  if (identifiers.length > 0) {
    existing = await tx.deviceRecord.findFirst({
      where: { OR: identifiers },
    })
  }

  if (existing) {
    // Update customerId to current customer
    if (data.customerId && existing.customerId !== data.customerId) {
      await tx.deviceRecord.update({
        where: { id: existing.id },
        data: { customerId: data.customerId },
      })
    }
    return existing.id
  }

  // Create new DeviceRecord
  const created = await tx.deviceRecord.create({
    data: {
      imei: data.imei || null,
      imei2: data.imei2 || null,
      serialNumber: data.serialNumber || null,
      deviceType: data.deviceType,
      brand: data.brand || null,
      model: data.model || null,
      customerId: data.customerId || null,
    },
  })
  return created.id
}
```

2. **Update createRepair in `src/actions/repairs.ts`:**

Replace any direct `db.deviceRecord.create()` with `findOrCreateDeviceRecord()`. Pass the repair's device identifiers (imei from deviceSerial, deviceBrand, deviceModel, etc.) to the helper.

3. **Create E2E test file** `src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts`:

```typescript
describe('Optimistic Locking & DeviceRecord Dedup (E2E)', () => {
  describe('MotivationScheme optimistic locking', () => {
    it('allows update with correct version', async () => {
      // Create scheme with version=1
      // Update with expectedVersion=1 -> success, version becomes 2
    })

    it('rejects update with stale version', async () => {
      // Create scheme with version=1
      // Update with expectedVersion=1 -> success (v=2)
      // Update again with expectedVersion=1 -> error "Данные были изменены"
    })

    it('stores formula snapshot when applying to assignment', async () => {
      // Create scheme with formula={...}
      // Create assignment -> formulaSnapshot matches scheme.formula
      // Update scheme formula -> assignment.formulaSnapshot unchanged
    })
  })

  describe('DeviceRecord deduplication', () => {
    it('reuses existing DeviceRecord when IMEI matches', async () => {
      // Create repair with IMEI X -> creates DeviceRecord
      // Create second repair with same IMEI X -> reuses DeviceRecord
      // Assert: only 1 DeviceRecord exists with IMEI X
      // Assert: both repairs reference same deviceRecordId
    })

    it('creates new DeviceRecord when no match found', async () => {
      // Create repair with IMEI X -> creates DeviceRecord
      // Create repair with IMEI Y -> creates new DeviceRecord
      // Assert: 2 DeviceRecords exist
    })

    it('matches by imei2 when imei differs', async () => {
      // Create DeviceRecord with imei2=Z
      // Create repair with imei2=Z -> reuses existing
    })
  })
})
```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/device-records.ts contains "findOrCreateDeviceRecord" function
    - src/actions/device-records.ts contains `findFirst` with OR query on imei/imei2/serialNumber
    - src/actions/repairs.ts contains "findOrCreateDeviceRecord" import
    - src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts exists with >= 80 lines
    - E2E tests pass including formula snapshot test
  </acceptance_criteria>
  <done>DeviceRecord deduplication by IMEI/serial in createRepair works — one device = one DeviceRecord with full repair history. Optimistic locking on MotivationScheme prevents lost concurrent edits. Formula snapshots preserved on assignments.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/__tests__/e2e/optimistic-lock-dedup.e2e.test.ts` passes
- `pnpm prisma validate` passes
- Schema has version field on MotivationScheme
- Schema has formulaSnapshot on MotivationAssignment
- createRepair uses findOrCreateDeviceRecord instead of direct create
</verification>

<success_criteria>
Concurrent MotivationScheme edits produce clear version conflict error. Formula validation prevents invalid MotivationScheme saves. Formula snapshot is stored when applying to assignments. Repeat repairs on same device reuse existing DeviceRecord.
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-integrity-hardening/15-05-SUMMARY.md`
</output>
