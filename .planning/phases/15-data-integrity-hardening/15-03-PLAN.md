---
phase: 15-data-integrity-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/phone-utils.ts
  - src/lib/imei-utils.ts
  - src/lib/validations/serial.ts
  - src/actions/repairs.ts
  - src/actions/sales.ts
  - src/actions/orders.ts
  - src/actions/trade-in.ts
  - src/actions/customers.ts
  - src/actions/stores.ts
  - src/actions/users.ts
  - src/actions/suppliers.ts
  - src/__tests__/e2e/phone-imei-normalization.e2e.test.ts
autonomous: true
requirements: [DATA2-05, DATA2-08]

must_haves:
  truths:
    - "Телефон сохраняется в нормализованном формате +7XXXXXXXXXX"
    - "IMEI проходит Luhn-валидацию при приёмке, обновлении, trade-in, импорте"
    - "Невалидный IMEI отклоняется с понятной ошибкой"
    - "Телефон без цифр или слишком короткий отклоняется"
  artifacts:
    - path: "src/lib/phone-utils.ts"
      provides: "normalizePhone() function"
      exports: ["normalizePhone", "isValidPhone"]
      min_lines: 20
    - path: "src/__tests__/e2e/phone-imei-normalization.e2e.test.ts"
      provides: "E2E tests for normalization"
      min_lines: 60
  key_links:
    - from: "src/actions/repairs.ts"
      to: "src/lib/phone-utils.ts"
      via: "import { normalizePhone }"
      pattern: "normalizePhone"
    - from: "src/actions/repairs.ts"
      to: "src/lib/imei-utils.ts"
      via: "import { isValidImei }"
      pattern: "isValidImei"
---

<objective>
Create phone normalization utility and apply IMEI validation to all entry points (receive, update, trade-in, import). Apply normalizePhone to all Customer/User/Store/Supplier/Repair/CustomOrder create/update actions.

Purpose: Ensure consistent phone format for search/dedup and prevent invalid IMEI from entering the system.
Output: Phone normalization lib + IMEI validation at all entry points + E2E tests.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-data-integrity-hardening/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create normalizePhone + extend IMEI validation</name>
  <files>src/lib/phone-utils.ts, src/lib/imei-utils.ts, src/lib/validations/serial.ts</files>
  <read_first>
    - src/lib/imei-utils.ts (full — existing IMEI validation)
    - src/lib/validations/serial.ts (full — existing serial validation schemas)
  </read_first>
  <action>
1. **Create `src/lib/phone-utils.ts`:**

```typescript
/**
 * Нормализация телефона в формат +7XXXXXXXXXX (Россия).
 * Принимает: 89001234567, +79001234567, 8(900)123-45-67, 9001234567
 * Возвращает: +79001234567
 * Если невалидный — возвращает null.
 */
export function normalizePhone(raw: string): string | null {
  // Strip all non-digit characters
  const digits = raw.replace(/\D/g, '')

  // Handle Russian phone formats
  if (digits.length === 11 && digits.startsWith('8')) {
    return '+7' + digits.slice(1)
  }
  if (digits.length === 11 && digits.startsWith('7')) {
    return '+' + digits
  }
  if (digits.length === 10) {
    // Assume Russian mobile without prefix
    return '+7' + digits
  }

  return null
}

/**
 * Проверка валидности телефона (после нормализации).
 */
export function isValidPhone(raw: string): boolean {
  return normalizePhone(raw) !== null
}

/**
 * Нормализация с выбросом ошибки для server actions.
 */
export function normalizePhoneOrThrow(raw: string, fieldName = 'phone'): string {
  const normalized = normalizePhone(raw)
  if (!normalized) {
    throw new Error(`Невалидный номер телефона в поле "${fieldName}": ${raw}`)
  }
  return normalized
}
```

2. **Extend `src/lib/imei-utils.ts`** — add a throwing variant:

```typescript
export function validateImeiOrThrow(imei: string, fieldName = 'IMEI'): string {
  const cleaned = imei.replace(/\s/g, '')
  if (!isValidImei(cleaned)) {
    throw new Error(`Невалидный ${fieldName}: ${imei}. IMEI должен содержать 15 цифр и пройти проверку Luhn.`)
  }
  return cleaned
}
```

3. **Update `src/lib/validations/serial.ts`** — add phone validation to Zod schemas if not present:
- Add `import { isValidPhone } from '@/lib/phone-utils'`
- Add phone validation refinement where phone fields are validated
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/lib/phone-utils.ts src/lib/imei-utils.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/phone-utils.ts exists and exports normalizePhone, isValidPhone, normalizePhoneOrThrow
    - src/lib/phone-utils.ts contains `+7` format logic
    - src/lib/imei-utils.ts exports validateImeiOrThrow
    - src/lib/imei-utils.ts contains "Невалидный" error message
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>normalizePhone() and validateImeiOrThrow() utilities ready for use in server actions</done>
</task>

<task type="auto">
  <name>Task 2: Wire phone normalization into repair-flow actions + E2E tests</name>
  <files>src/actions/repairs.ts, src/actions/orders.ts, src/actions/trade-in.ts, src/__tests__/e2e/phone-imei-normalization.e2e.test.ts</files>
  <read_first>
    - src/actions/repairs.ts (find createRepair — phone and IMEI handling)
    - src/actions/orders.ts (find createOrder or similar — clientPhone)
    - src/actions/trade-in.ts (find IMEI usage)
  </read_first>
  <action>
1. **Apply normalizePhone** in repair-flow server actions:

For each action file, add `import { normalizePhoneOrThrow } from '@/lib/phone-utils'` and wrap phone inputs:

- `src/actions/repairs.ts` — createRepair: `clientPhone = normalizePhoneOrThrow(clientPhone, 'Телефон клиента')`
- `src/actions/orders.ts` — createOrder/updateOrder: `clientPhone = normalizePhoneOrThrow(data.clientPhone, 'Телефон клиента')`

2. **Apply IMEI validation** at repair-flow entry points:

- `src/actions/repairs.ts` — createRepair: validate deviceSerial if IMEI format `validateImeiOrThrow(imei)`
- `src/actions/trade-in.ts` — createTradeIn: validate IMEI field if present
- Stock receive actions — validate IMEI on serial unit creation
- Import actions — validate IMEI during import

Add `import { validateImeiOrThrow } from '@/lib/imei-utils'` and call before any DB write with IMEI data.

NOTE: Only validate fields that are explicitly IMEI (not generic serial numbers). Check if the category is serialized with IMEI identifier type before validating.

3. **Create E2E test** `src/__tests__/e2e/phone-imei-normalization.e2e.test.ts`:

```typescript
describe('Phone & IMEI Normalization (E2E)', () => {
  describe('normalizePhone', () => {
    it('normalizes 89001234567 to +79001234567')
    it('normalizes +7(900)123-45-67 to +79001234567')
    it('normalizes 9001234567 to +79001234567')
    it('rejects empty string')
    it('rejects too short number')
  })

  describe('IMEI validation in repairs', () => {
    it('accepts valid IMEI (passes Luhn check)')
    it('rejects invalid IMEI (fails Luhn check)')
  })

  describe('Phone normalization in actions', () => {
    it('stores normalized phone when creating customer')
    it('stores normalized phone when creating repair')
  })
})
```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/phone-imei-normalization.e2e.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/repairs.ts contains "normalizePhoneOrThrow"
    - src/actions/orders.ts contains "normalizePhoneOrThrow"
    - src/actions/repairs.ts contains "validateImeiOrThrow" or "isValidImei"
    - src/__tests__/e2e/phone-imei-normalization.e2e.test.ts exists with >= 60 lines
    - E2E tests pass
  </acceptance_criteria>
  <done>Phone normalization and IMEI validation applied at repair-flow entry points, proven by E2E tests</done>
</task>

<task type="auto">
  <name>Task 3: Wire phone normalization into entity-management actions</name>
  <files>src/actions/customers.ts, src/actions/stores.ts, src/actions/users.ts, src/actions/suppliers.ts</files>
  <read_first>
    - src/actions/customers.ts (find phone field usage in create/update)
    - src/actions/stores.ts (find phone field usage)
    - src/actions/users.ts (find phone field usage)
    - src/actions/suppliers.ts (find phone field usage)
  </read_first>
  <action>
Apply `import { normalizePhoneOrThrow } from '@/lib/phone-utils'` to each entity-management action file and wrap phone inputs:

- `src/actions/customers.ts` — createCustomer/updateCustomer: `phone = normalizePhoneOrThrow(phone)`
- `src/actions/stores.ts` — createStore/updateStore: if phone provided, `phone = normalizePhoneOrThrow(phone, 'Телефон магазина')`
- `src/actions/users.ts` — createUser/updateUser: if phone provided (optional field), `phone = phone ? normalizePhoneOrThrow(phone) : undefined`
- `src/actions/suppliers.ts` — createSupplier/updateSupplier: if phone provided, `phone = phone ? normalizePhoneOrThrow(phone, 'Телефон поставщика') : undefined`

For optional phone fields (where phone may be empty string or undefined), guard with:
```typescript
if (data.phone && data.phone.trim()) {
  data.phone = normalizePhoneOrThrow(data.phone.trim(), 'Телефон')
}
```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && grep -l "normalizePhoneOrThrow" src/actions/customers.ts src/actions/stores.ts src/actions/users.ts src/actions/suppliers.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 4 entity-management action files contain "normalizePhoneOrThrow"
    - grep returns 4 (all files have the import)
    - `npx tsc --noEmit` passes for modified files
  </acceptance_criteria>
  <done>Phone normalization applied at all entity-management entry points — customers, stores, users, suppliers all normalize phone before storage</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/__tests__/e2e/phone-imei-normalization.e2e.test.ts` passes
- grep confirms normalizePhone imported in all action files with phone fields
- grep confirms IMEI validation at repair/trade-in/receive entry points
</verification>

<success_criteria>
Every phone field is normalized to +7XXXXXXXXXX format before storage. Every IMEI passes Luhn validation before storage. Invalid inputs are rejected with clear error messages.
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-integrity-hardening/15-03-SUMMARY.md`
</output>
