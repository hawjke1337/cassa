---
phase: 15-data-integrity-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/timezone.ts
  - src/lib/__tests__/timezone.test.ts
  - src/actions/reports.ts
  - src/actions/dashboard.ts
  - src/actions/shifts.ts
  - src/components/reports/date-range-picker.tsx
autonomous: true
requirements: [DATA2-04]

must_haves:
  truths:
    - "Отчёт за 14 апреля показывает продажи с 13 апреля 21:00 UTC по 14 апреля 20:59 UTC (00:00-23:59 MSK)"
    - "Dashboard за сегодня использует MSK boundaries"
    - "Фильтрация смен по дате использует MSK boundaries"
  artifacts:
    - path: "src/lib/timezone.ts"
      provides: "MSK timezone conversion helpers"
      exports: ["mskStartOfDay", "mskEndOfDay", "toMskDateRange"]
      min_lines: 25
    - path: "src/lib/__tests__/timezone.test.ts"
      provides: "Unit tests for MSK boundary correctness"
      min_lines: 30
  key_links:
    - from: "src/actions/reports.ts"
      to: "src/lib/timezone.ts"
      via: "import { toMskDateRange }"
      pattern: "toMskDateRange"
    - from: "src/actions/dashboard.ts"
      to: "src/lib/timezone.ts"
      via: "import { mskStartOfDay }"
      pattern: "mskStartOfDay"
---

<objective>
Create MSK timezone utilities and apply them to all report/dashboard date filtering so that "today" and date range queries use Moscow time (UTC+3) boundaries.

Purpose: Ensure that a sale at 23:30 MSK (20:30 UTC) appears in the correct day's report, not the next day.
Output: Timezone utility lib + unit tests + updated report/dashboard/shift queries.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-data-integrity-hardening/15-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MSK timezone utility + unit tests</name>
  <files>src/lib/timezone.ts, src/lib/__tests__/timezone.test.ts</files>
  <read_first>
    - src/actions/reports.ts (find date filtering patterns — how dateFrom/dateTo are used in queries)
    - src/actions/dashboard.ts (find date filtering patterns)
  </read_first>
  <behavior>
    - mskStartOfDay(2026-04-14) returns 2026-04-13T21:00:00.000Z (UTC)
    - mskEndOfDay(2026-04-14) returns 2026-04-14T20:59:59.999Z (UTC)
    - toMskDateRange(apr14, apr14) returns { gte: apr13 21:00 UTC, lte: apr14 20:59 UTC }
    - mskStartOfDay(2026-01-01) returns 2025-12-31T21:00:00.000Z (year boundary)
    - mskEndOfDay at month boundary works correctly
  </behavior>
  <action>
1. **Create unit test first** `src/lib/__tests__/timezone.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { mskStartOfDay, mskEndOfDay, toMskDateRange, mskToday } from '../timezone'

describe('MSK timezone utilities', () => {
  it('mskStartOfDay converts April 14 MSK midnight to April 13 21:00 UTC', () => {
    const date = new Date(2026, 3, 14) // April 14, 2026
    const result = mskStartOfDay(date)
    expect(result.toISOString()).toBe('2026-04-13T21:00:00.000Z')
  })

  it('mskEndOfDay converts April 14 MSK 23:59:59 to April 14 20:59:59 UTC', () => {
    const date = new Date(2026, 3, 14)
    const result = mskEndOfDay(date)
    expect(result.toISOString()).toBe('2026-04-14T20:59:59.999Z')
  })

  it('handles year boundary: Jan 1 MSK midnight = Dec 31 21:00 UTC', () => {
    const date = new Date(2026, 0, 1) // Jan 1, 2026
    const result = mskStartOfDay(date)
    expect(result.toISOString()).toBe('2025-12-31T21:00:00.000Z')
  })

  it('toMskDateRange returns correct gte/lte pair', () => {
    const from = new Date(2026, 3, 14)
    const to = new Date(2026, 3, 14)
    const range = toMskDateRange(from, to)
    expect(range.gte.toISOString()).toBe('2026-04-13T21:00:00.000Z')
    expect(range.lte.toISOString()).toBe('2026-04-14T20:59:59.999Z')
  })

  it('mskToday returns a valid range for current day', () => {
    const today = mskToday()
    expect(today.gte).toBeInstanceOf(Date)
    expect(today.lte).toBeInstanceOf(Date)
    expect(today.gte < today.lte).toBe(true)
  })
})
```

2. **Create `src/lib/timezone.ts`:**

```typescript
/**
 * Timezone utilities for MSK (UTC+3).
 * Все магазины в одном часовом поясе — Москва.
 * БД хранит timestamps в UTC.
 * При фильтрации по дате: конвертируем MSK boundaries в UTC.
 */

const MSK_OFFSET_HOURS = 3

/**
 * Начало дня по MSK в UTC.
 * Например, 14 апреля 00:00 MSK = 13 апреля 21:00 UTC
 */
export function mskStartOfDay(date: Date): Date {
  const year = date.getFullYear()
  const month = date.getMonth()
  const day = date.getDate()
  return new Date(Date.UTC(year, month, day, 0 - MSK_OFFSET_HOURS, 0, 0, 0))
}

/**
 * Конец дня по MSK в UTC.
 * Например, 14 апреля 23:59:59.999 MSK = 14 апреля 20:59:59.999 UTC
 */
export function mskEndOfDay(date: Date): Date {
  const year = date.getFullYear()
  const month = date.getMonth()
  const day = date.getDate()
  return new Date(Date.UTC(year, month, day, 23 - MSK_OFFSET_HOURS, 59, 59, 999))
}

/**
 * Конвертирует пару дат (dateFrom, dateTo) в UTC range по MSK.
 */
export function toMskDateRange(dateFrom: Date, dateTo: Date): { gte: Date; lte: Date } {
  return {
    gte: mskStartOfDay(dateFrom),
    lte: mskEndOfDay(dateTo),
  }
}

/**
 * Текущий момент "сегодня" по MSK — start и end.
 */
export function mskToday(): { gte: Date; lte: Date } {
  const now = new Date()
  return toMskDateRange(now, now)
}
```

Important: Use hardcoded UTC+3 offset (no DST in Russia since 2014). Do NOT use Intl.DateTimeFormat or timezone libraries — unnecessary for a fixed offset.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/lib/__tests__/timezone.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/timezone.ts exists with >= 25 lines
    - src/lib/__tests__/timezone.test.ts exists with >= 30 lines
    - src/lib/timezone.ts exports mskStartOfDay, mskEndOfDay, toMskDateRange, mskToday
    - All unit tests pass, including year boundary edge case
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>MSK timezone utility with proven correctness via unit tests</done>
</task>

<task type="auto">
  <name>Task 2: Apply MSK timezone to all report/dashboard/shift date filters</name>
  <files>src/actions/reports.ts, src/actions/dashboard.ts, src/actions/shifts.ts, src/components/reports/date-range-picker.tsx</files>
  <read_first>
    - src/actions/reports.ts (full — find all createdAt gte/lte queries)
    - src/actions/dashboard.ts (full — find date filtering)
    - src/actions/shifts.ts (find shift filtering by date)
    - src/components/reports/date-range-picker.tsx (find how dates are sent to server)
  </read_first>
  <action>
1. **`src/actions/reports.ts`** — find all instances where date range is used for Prisma queries. Replace direct Date usage with `toMskDateRange`:

Before (typical pattern):
```typescript
createdAt: {
  gte: dateFrom,
  lte: dateTo,
}
```

After:
```typescript
import { toMskDateRange } from '@/lib/timezone'
// ...
const range = toMskDateRange(dateFrom, dateTo)
createdAt: {
  gte: range.gte,
  lte: range.lte,
}
```

Apply to ALL report functions: getSalesReport, getProfitReport, getCashReport, getSellerReport, etc.

2. **`src/actions/dashboard.ts`** — find "today" filtering. Replace with:
```typescript
import { mskToday } from '@/lib/timezone'
const today = mskToday()
// Use today.gte and today.lte for queries
```

3. **`src/actions/shifts.ts`** — find shift date filtering. Apply `toMskDateRange` for shift queries filtered by date.

4. **`src/components/reports/date-range-picker.tsx`** — verify that the DateRangePicker sends dates as midnight-to-midnight of the selected days. The server-side `toMskDateRange` handles the UTC conversion, so the client should send plain Date objects (start of selected day, end of selected day). If the client already strips time — no change needed. If client sends timestamps — adjust to send date-only.

5. **Also check and update if date filtering exists in:**
- `src/actions/audit.ts` — audit log filtering
- `src/components/settings/audit-log-table.tsx` — audit table date params
- `src/actions/trade-in.ts` — trade-in date filtering

For raw SQL queries (if any exist in reports), use:
```sql
WHERE "createdAt" >= $1 AND "createdAt" <= $2
```
with the MSK-adjusted UTC dates passed as parameters.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && echo "=== MSK imports ===" && grep -rn "toMskDateRange\|mskStartOfDay\|mskToday\|mskEndOfDay" src/actions/ src/components/reports/ 2>/dev/null | wc -l && echo "=== Unguarded date boundaries ===" && grep -rn "gte:\s*new Date\|lte:\s*new Date\|gte:\s*dateFrom\|lte:\s*dateTo" src/actions/reports.ts src/actions/dashboard.ts src/actions/shifts.ts 2>/dev/null | grep -v "range\.\|msk\|Msk" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/reports.ts contains `import.*from '@/lib/timezone'`
    - src/actions/reports.ts contains "toMskDateRange"
    - src/actions/dashboard.ts contains "mskToday" or "toMskDateRange"
    - grep for timezone imports in src/actions/ returns >= 3 matches
    - grep for unguarded `gte: new Date` or `gte: dateFrom` patterns in reports/dashboard/shifts returns 0 matches (all go through timezone.ts)
    - `npx tsc --noEmit` passes for all modified files
  </acceptance_criteria>
  <done>All report/dashboard/shift date filtering uses MSK timezone boundaries — a sale at 23:30 MSK correctly appears in the right day's report</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lib/__tests__/timezone.test.ts` passes — proves MSK boundary math is correct
- grep confirms timezone.ts imports in reports.ts, dashboard.ts, shifts.ts
- grep for unguarded date boundary patterns returns 0 — no bypass of timezone.ts
- `npx tsc --noEmit` passes for all modified files
</verification>

<success_criteria>
Date filtering in reports and dashboard uses Moscow timezone boundaries. A sale at 23:30 MSK on April 14 appears in the April 14 report, not April 15. Unit tests prove boundary correctness including edge cases.
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-integrity-hardening/15-04-SUMMARY.md`
</output>
