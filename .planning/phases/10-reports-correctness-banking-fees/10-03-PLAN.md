---
phase: 10-reports-correctness-banking-fees
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/actions/fee-settings.ts
  - src/components/settings/fee-settings-form.tsx
  - src/components/pos/payment-dialog.tsx
  - src/components/reports/profit-report.tsx
  - src/components/dashboard/dashboard-content.tsx
autonomous: true
requirements: [FEE-01, FEE-03, FEE-04, FEE-05]

must_haves:
  truths:
    - "Admin can configure fee rates per payment method per store"
    - "POS shows fee breakdown when non-CASH payment selected"
    - "Profit report displays returns, banking fees, trade-in expenses, and net profit"
    - "Dashboard shows gross profit and net profit as separate cards"
  artifacts:
    - path: "src/actions/fee-settings.ts"
      provides: "getFeeSettings + saveFeeSettings server actions"
      exports: ["getFeeSettings", "saveFeeSettings"]
    - path: "src/components/settings/fee-settings-form.tsx"
      provides: "Fee settings form with rate inputs and live example"
    - path: "src/components/pos/payment-dialog.tsx"
      provides: "Updated payment dialog with fee display"
    - path: "src/components/reports/profit-report.tsx"
      provides: "Extended profit report with returns, fees, trade-in cards"
    - path: "src/components/dashboard/dashboard-content.tsx"
      provides: "Gross + net profit stat cards"
  key_links:
    - from: "src/components/settings/fee-settings-form.tsx"
      to: "src/actions/fee-settings.ts"
      via: "getFeeSettings + saveFeeSettings calls"
      pattern: "getFeeSettings|saveFeeSettings"
    - from: "src/components/pos/payment-dialog.tsx"
      to: "src/actions/fee-settings.ts"
      via: "getFeeSettings to get current rates"
      pattern: "getFeeSettings|calcBankingFee"
    - from: "src/components/reports/profit-report.tsx"
      to: "src/actions/reports.ts"
      via: "getProfitReport extended return with returnsTotal, bankingFees, tradeInExpenses"
      pattern: "returnsTotal|bankingFees|tradeInExpenses"
    - from: "src/components/dashboard/dashboard-content.tsx"
      to: "src/actions/dashboard.ts"
      via: "todayGrossProfit + todayNetProfit from getDashboardData"
      pattern: "todayGrossProfit|todayNetProfit"
---

<objective>
Fee settings CRUD + POS fee display + updated profit report and dashboard UIs to show extended financial data (returns, banking fees, trade-in expenses, gross vs net profit).

Purpose: Complete the user-facing experience for banking fees (configuration, POS display) and show corrected financial data in all reports and dashboard.
Output: Fee settings page, POS fee display, extended profit report, dual-profit dashboard.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reports-correctness-banking-fees/10-CONTEXT.md
@.planning/phases/10-reports-correctness-banking-fees/10-UI-SPEC.md
@.planning/phases/10-reports-correctness-banking-fees/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From prisma/schema.prisma (PaymentFeeConfig):
```prisma
model PaymentFeeConfig {
  id       String        @id @default(cuid())
  storeId  String
  method   PaymentMethod
  feeRate  Decimal       @db.Decimal(5, 4)
  @@unique([storeId, method])
}
```

From src/lib/money.ts:

```typescript
export function calcBankingFee(
  amount: DecimalLike,
  rate: DecimalLike,
): { fee: Prisma.Decimal; total: Prisma.Decimal }
```

From src/actions/reports.ts (getProfitReport extended return):

```typescript
return {
  revenue,
  returnsTotal,
  adjustedRevenue,
  cogs,
  grossProfit,
  grossMargin,
  writeOffsTotal,
  bankingFees,
  tradeInExpenses,
  netProfit,
  categoryBreakdown,
}
```

From src/actions/dashboard.ts (getDashboardData extended return):

```typescript
// New fields: todayGrossProfit, todayGrossMargin, todayNetProfit,
//   todayBankingFees, todayTradeInExpenses, todayReturns
```

From src/components/dashboard/stat-card.tsx:

```typescript
// StatCard props: { title, value, icon, description?, change?, trend? }
```

From src/components/pos/payment-dialog.tsx:

```typescript
// METHODS array: { value: PaymentMethodType, label: string, icon: ReactNode }
// State: activeMethod, isCombined, combinedAmounts
// getTotal() returns cart total
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fee settings CRUD + UI</name>
  <files>src/actions/fee-settings.ts, src/components/settings/fee-settings-form.tsx</files>
  <read_first>
    - prisma/schema.prisma (PaymentFeeConfig model — added in Plan 01)
    - src/components/settings/store-form.tsx (pattern for settings forms)
    - src/lib/money.ts (calcBankingFee signature)
    - src/lib/permissions.ts (requirePermission pattern)
    - src/app/(dashboard)/settings/page.tsx (settings page structure — where to add fee settings)
    - .planning/phases/10-reports-correctness-banking-fees/10-UI-SPEC.md (Fee Settings Form layout)
  </read_first>
  <action>
**1. Create `src/actions/fee-settings.ts`:**

```typescript
"use server"

import { db } from "@/lib/db"
import { requirePermission } from "@/lib/permissions"
import { auth } from "@/lib/auth"

const DEFAULT_RATES: Record<string, string> = {
  CASH: "0",
  CARD: "0.02",
  SBP: "0.007",
  TRANSFER: "0.01",
  CREDIT: "0.03",
}

export async function getFeeSettings(storeId: string) {
  await requirePermission("settings.edit", storeId)
  const session = await auth()
  if (!session?.user?.id) throw new Error("Не авторизован")

  const configs = await db.paymentFeeConfig.findMany({
    where: { storeId },
  })

  // Return all methods with defaults for missing ones
  const methods = ["CASH", "CARD", "SBP", "TRANSFER", "CREDIT"] as const
  return methods.map((method) => {
    const config = configs.find((c) => c.method === method)
    return {
      method,
      feeRate: config ? Number(config.feeRate).toString() : DEFAULT_RATES[method],
    }
  })
}

export async function saveFeeSettings(
  storeId: string,
  rates: Array<{ method: string; feeRate: string }>,
) {
  await requirePermission("settings.edit", storeId)
  const session = await auth()
  if (!session?.user?.id) throw new Error("Не авторизован")

  await db.$transaction(
    rates.map((r) =>
      db.paymentFeeConfig.upsert({
        where: { storeId_method: { storeId, method: r.method as any } },
        create: { storeId, method: r.method as any, feeRate: r.feeRate },
        update: { feeRate: r.feeRate },
      }),
    ),
  )

  return { success: true }
}

// Used by POS to get rates for fee calculation
export async function getStoreFeeRates(storeId: string) {
  const configs = await db.paymentFeeConfig.findMany({
    where: { storeId },
  })
  const methods = ["CASH", "CARD", "SBP", "TRANSFER", "CREDIT"] as const
  const rates: Record<string, number> = {}
  for (const method of methods) {
    const config = configs.find((c) => c.method === method)
    rates[method] = config ? Number(config.feeRate) : Number(DEFAULT_RATES[method])
  }
  return rates
}
```

**2. Create `src/components/settings/fee-settings-form.tsx`:**

"use client" component. Props: `{ storeId: string }`

Layout per UI-SPEC:

- Card with title "Комиссии по методам оплаты"
- CardDescription: "Укажите процент комиссии банка для каждого метода оплаты. Расчёт по формуле обратного процента (стандарт эквайринга)."
- Table with columns: Метод оплаты | Ставка, % | Пример (10 000 р)
- Each row: method label | Input type="number" step="0.01" min="0" max="99" width="80px" | computed example
- CASH row: input disabled, value=0, row background `bg-muted/50`
- Example column: `calcBankingFee(10000, rate/100)` computed on client via imported calcBankingFee
- "Сохранить ставки" button right-aligned in CardFooter
- Save: call saveFeeSettings, toast "Ставки комиссий сохранены" on success, "Не удалось сохранить ставки. Попробуйте ещё раз." on error
- useEffect: load getFeeSettings on mount, populate state
- Rate inputs: store as percentage (user enters 2.00 for 2%), convert to decimal for API (0.02)
- Accessibility: `aria-label="Ставка комиссии для {method label}"` on each input

Imports: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, Table, TableHeader, TableRow, TableHead, TableBody, TableCell, Input, Button, Skeleton from shadcn. calcBankingFee from @/lib/money. getFeeSettings, saveFeeSettings from @/actions/fee-settings. toast from sonner. Loader2 from lucide-react.

**3. Wire fee-settings-form into settings page:**

- Read `src/app/(dashboard)/settings/page.tsx` to understand structure
- Add FeeSettingsForm component below existing settings sections, conditionally rendered when storeId is available
  </action>
  <verify>
  <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/actions/fee-settings.ts src/components/settings/fee-settings-form.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria> - src/actions/fee-settings.ts file exists - fee-settings.ts contains `export async function getFeeSettings(` - fee-settings.ts contains `export async function saveFeeSettings(` - fee-settings.ts contains `export async function getStoreFeeRates(` - fee-settings.ts contains `db.paymentFeeConfig.upsert(` - fee-settings.ts DEFAULT_RATES contains CASH: '0', CARD: '0.02', SBP: '0.007', TRANSFER: '0.01', CREDIT: '0.03' - src/components/settings/fee-settings-form.tsx file exists - fee-settings-form.tsx contains `Комиссии по методам оплаты` - fee-settings-form.tsx contains `Сохранить ставки` - fee-settings-form.tsx contains `calcBankingFee` import - fee-settings-form.tsx contains `aria-label` on inputs - TypeScript compilation passes
  </acceptance_criteria>
  <done>Fee settings CRUD with defaults, FeeSettingsForm with live example calculation, integrated into settings page</done>
  </task>

<task type="auto">
  <name>Task 2: POS payment dialog fee display</name>
  <files>src/components/pos/payment-dialog.tsx</files>
  <read_first>
    - src/components/pos/payment-dialog.tsx (full file — understand current flow, METHODS, activeMethod, combined payments, getTotal())
    - src/actions/fee-settings.ts (getStoreFeeRates signature — created in Task 1)
    - src/lib/money.ts (calcBankingFee signature)
    - src/hooks/use-current-store.ts (currentStoreId)
    - .planning/phases/10-reports-correctness-banking-fees/10-UI-SPEC.md (POS Payment Dialog layout)
    - .planning/phases/10-reports-correctness-banking-fees/10-CONTEXT.md (FEE-03 decisions)
  </read_first>
  <action>
In `payment-dialog.tsx`:

1. **Add imports:**

   ```typescript
   import { getStoreFeeRates } from "@/actions/fee-settings"
   import { calcBankingFee } from "@/lib/money"
   ```

2. **Add state for fee rates:**

   ```typescript
   const [feeRates, setFeeRates] = useState<Record<string, number>>({})
   ```

3. **Load fee rates on dialog open:**

   ```typescript
   useEffect(() => {
     if (open && currentStoreId) {
       getStoreFeeRates(currentStoreId)
         .then(setFeeRates)
         .catch(() => {})
     }
   }, [open, currentStoreId])
   ```

4. **Compute fee for current selection:**
   - For single method (not combined): if `activeMethod !== 'CASH'` and `feeRates[activeMethod] > 0`:
     ```typescript
     const rate = feeRates[activeMethod] ?? 0
     const total = getTotal()
     const { fee: methodFee, total: totalWithFee } =
       rate > 0
         ? calcBankingFee(total, rate)
         : { fee: { toFixed: () => "0" }, total: { toFixed: () => String(total) } }
     ```
   - For combined mode: compute fee per non-CASH method with amount > 0

5. **Display fee breakdown** (below the total amount, above the pay button):
   - When method is not CASH and fee > 0:
     ```tsx
     <div className="space-y-1 rounded-md bg-muted/50 p-3">
       <div className="flex justify-between text-sm">
         <span className="text-muted-foreground">Цена товара</span>
         <span>{formatMoney(total)}</span>
       </div>
       <div className="flex justify-between text-sm">
         <span className="text-orange-500">Комиссия банка</span>
         <span className="text-orange-500">{formatMoney(Number(methodFee.toFixed(2)))}</span>
       </div>
       <Separator className="my-1" />
       <div className="flex justify-between text-lg font-semibold">
         <span>Итого к оплате</span>
         <span>{formatMoney(Number(totalWithFee.toFixed(2)))}</span>
       </div>
     </div>
     ```
   - For CASH: no fee display, show total as before

6. **Update the pay button text** to include fee when applicable:
   - Non-CASH: `Оплатить ${formatMoney(Number(totalWithFee.toFixed(2)))}`
   - CASH: keep current behavior

7. **For combined payments:** show per-method fee breakdown for each non-CASH method that has amount > 0, then total fee sum.

8. **Record feeAmount on sale creation:** When calling createSale, pass feeAmount per payment. This requires checking if createSale accepts feeAmount — if not, the feeAmount will need to be saved separately after sale creation, or createSale needs a minor extension. Read createSale to determine approach. At minimum, note: Payment.feeAmount is saved when the payment is created in the sale flow.

Note: The fee display uses CSS transition per UI-SPEC — `transition-all duration-150` on the fee breakdown container, `opacity-0` when hidden, `opacity-100` when shown.
</action>
<verify>
<automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/components/pos/payment-dialog.tsx 2>&1 | head -20</automated>
</verify>
<acceptance_criteria> - payment-dialog.tsx contains `import { getStoreFeeRates }` from fee-settings - payment-dialog.tsx contains `import { calcBankingFee }` from money - payment-dialog.tsx contains `Цена товара` text - payment-dialog.tsx contains `Комиссия банка` text with `text-orange-500` - payment-dialog.tsx contains `Итого к оплате` text - payment-dialog.tsx contains `feeRates` state - payment-dialog.tsx contains `getStoreFeeRates` call in useEffect - TypeScript compilation passes
</acceptance_criteria>
<done>POS payment dialog shows fee breakdown when non-CASH method selected, with reverse percentage calculation, real-time updates on method change</done>
</task>

<task type="auto">
  <name>Task 3: Profit report + dashboard UI updates</name>
  <files>src/components/reports/profit-report.tsx, src/components/dashboard/dashboard-content.tsx</files>
  <read_first>
    - src/components/reports/profit-report.tsx (full file — current summary cards layout, how data is consumed)
    - src/components/dashboard/dashboard-content.tsx (full file — StatCard usage, grid layout)
    - src/components/dashboard/stat-card.tsx (StatCard props interface)
    - .planning/phases/10-reports-correctness-banking-fees/10-UI-SPEC.md (Profit Report layout, Dashboard layout)
    - .planning/phases/10-reports-correctness-banking-fees/10-01-SUMMARY.md (what API returns now)
  </read_first>
  <action>
**1. Update `profit-report.tsx`:**

Update to use extended API response fields. Change summary cards from current 2-row layout to 3-row layout per UI-SPEC:

Row 1 (grid-cols-5 on lg):

- "Выручка" — `data.revenue`, normal color
- "Возвраты" — `data.returnsTotal`, `text-red-500`, negative sign prefix
- "Себестоимость" — `data.cogs`, normal
- "Валовая прибыль" — `data.grossProfit`, conditional green/red
- "Маржа" — `data.grossMargin` + "%"

Row 2 (grid-cols-3):

- "Списания" — `data.writeOffsTotal`, `text-orange-500`
- "Комиссии банка" — `data.bankingFees`, `text-orange-500`
- "Trade-in расходы" — `data.tradeInExpenses`, `text-orange-500`

Row 3 (grid-cols-2):

- "Чистая прибыль" — `data.netProfit`, `text-2xl font-semibold`, conditional green/red
- "Чистая маржа" — computed: `data.adjustedRevenue > 0 ? (data.netProfit / data.adjustedRevenue * 100).toFixed(1) : '0.0'` + "%"

Each summary card uses Card/CardHeader/CardTitle/CardContent pattern (existing in this file).

Update the data type to match new return shape:

```typescript
type ProfitData = {
  revenue: number
  returnsTotal: number
  adjustedRevenue: number
  cogs: number
  grossProfit: number
  grossMargin: number
  writeOffsTotal: number
  bankingFees: number
  tradeInExpenses: number
  netProfit: number
  categoryBreakdown: Array<{
    name: string
    revenue: number
    cogs: number
    profit: number
    margin: number
  }>
}
```

**2. Update `dashboard-content.tsx`:**

Per UI-SPEC, change row 1 from 4-card grid to two rows:

- Row 1 (lg:grid-cols-3): Продаж сегодня | Выручка сегодня | Средний чек
- Row 2 (lg:grid-cols-3): Валовая прибыль | Чистая прибыль | Маржа (use grossMargin)

Update data consumption:

- Replace `data.todayProfit` with `data.todayGrossProfit`
- Replace `data.todayMargin` with `data.todayGrossMargin`
- Add new StatCard for "Чистая прибыль": value=`data.todayNetProfit`, description="После комиссий и расходов"
- "Валовая прибыль" StatCard: value in `text-blue-600` via className on the value
- "Чистая прибыль" StatCard: value in `text-green-600` (positive) or `text-red-600` (negative)

Update DashboardData type to match new return shape from dashboard.ts.

Icon assignments:

- Валовая прибыль: TrendingUp (already imported)
- Чистая прибыль: DollarSign (already imported)
  </action>
  <verify>
  <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/components/reports/profit-report.tsx src/components/dashboard/dashboard-content.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria> - profit-report.tsx contains `returnsTotal` data field usage - profit-report.tsx contains `bankingFees` data field usage - profit-report.tsx contains `tradeInExpenses` data field usage - profit-report.tsx contains text "Возвраты" with `text-red-500` - profit-report.tsx contains text "Комиссии банка" with `text-orange-500` - profit-report.tsx contains text "Trade-in расходы" with `text-orange-500` - profit-report.tsx contains text "Чистая прибыль" - dashboard-content.tsx contains `todayGrossProfit` field usage - dashboard-content.tsx contains `todayNetProfit` field usage - dashboard-content.tsx contains text "Валовая прибыль" - dashboard-content.tsx contains text "Чистая прибыль" - dashboard-content.tsx contains text "После комиссий и расходов" - TypeScript compilation passes
  </acceptance_criteria>
  <done>Profit report shows 3-row summary with returns/fees/trade-in, dashboard shows dual gross/net profit cards</done>
  </task>

</tasks>

<verification>
- `npx tsc --noEmit` on all modified files — no errors
- Fee settings form visible on settings page
- POS dialog shows fee for non-CASH methods
- Profit report shows extended summary
- Dashboard shows gross and net profit cards
</verification>

<success_criteria>

1. FeeSettingsForm allows configuring per-store fee rates with live example calculation
2. POS payment dialog shows "Цена товара / Комиссия банка / Итого к оплате" for non-CASH
3. Profit report displays 3 rows: revenue+returns+COGS+gross, writeoffs+fees+tradein, net profit
4. Dashboard shows gross profit (blue) and net profit (green/red) as separate cards
   </success_criteria>

<output>
After completion, create `.planning/phases/10-reports-correctness-banking-fees/10-03-SUMMARY.md`
</output>
