---
phase: 16-inventory-edge-cases-ux-polish
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/pos/payment-dialog.tsx
  - src/components/pos/pos-interface.tsx
  - src/components/pos/return-form.tsx
  - src/components/pos/close-shift-dialog.tsx
  - src/hooks/use-critical-toast.ts
  - src/lib/form-validation.ts
  - src/components/orders/order-payment-form.tsx
  - src/__tests__/e2e/ux-polish.e2e.test.ts
  - src/__tests__/payment-aggregation.test.ts
autonomous: true
requirements: [UX2-01, UX2-02, UX2-03, UX2-04, UX2-05, UX2-06, UX2-07, UX2-13]
status: draft

must_haves:
  truths:
    - "createReturn: перед отправкой показывается AlertDialog 'Подтвердите возврат' с суммой"
    - "PaymentDialog кнопка 'Оплатить' дизейблится сразу при клике через useRef lock + visible spinner"
    - "Повторный клик на 'Оплатить' во время in-flight запроса ничего не делает (lock)"
    - "PaymentDialog генерирует idempotencyKey (crypto.randomUUID()) при каждом открытии"
    - "Server createSale с уже существующим idempotencyKey возвращает существующую Sale без создания дубля"
    - "Refresh во время оплаты + повторный submit → idempotencyKey защищает от дубля"
    - "Корзина POS полностью заблокирована (aria-disabled + opacity) пока открыт PaymentDialog"
    - "closeShift: при discrepancy != 0 показывается AlertDialog с суммой расхождения"
    - "Критичные ошибки (sale, payment, return, closeShift) показывают toast с кнопкой 'Повторить'"
    - "Order payment < remaining balance: показывается warning (inline или AlertDialog)"
    - "Inline валидация: невалидные поля форм получают красную рамку + helper text"
  artifacts:
    - path: "src/components/pos/payment-dialog.tsx"
      provides: "PaymentDialog с ref-lock, idempotencyKey state, toast retry"
      contains: "useRef"
    - path: "src/components/pos/pos-interface.tsx"
      provides: "POS с cartLocked prop на корзину"
      contains: "cartLocked"
    - path: "src/components/pos/return-form.tsx"
      provides: "Return form с AlertDialog confirm"
      contains: "AlertDialog"
    - path: "src/components/pos/close-shift-dialog.tsx"
      provides: "CloseShift с AlertDialog при discrepancy"
      contains: "AlertDialog"
    - path: "src/hooks/use-critical-toast.ts"
      provides: "Helper hook criticalToast.error(msg, { retry: fn })"
      exports: ["useCriticalToast"]
      min_lines: 20
    - path: "src/__tests__/e2e/ux-polish.e2e.test.ts"
      provides: "E2E для UX2-02, UX2-06"
      min_lines: 100
  key_links:
    - from: "src/components/pos/payment-dialog.tsx"
      to: "src/actions/sales.ts"
      via: "createSale(input) передаёт idempotencyKey"
      pattern: "idempotencyKey"
    - from: "src/actions/sales.ts"
      to: "prisma.sale"
      via: "findUnique({ idempotencyKey }) перед созданием"
      pattern: "idempotencyKey"
    - from: "src/components/pos/pos-interface.tsx"
      to: "src/components/pos/payment-dialog.tsx"
      via: "paymentDialogOpen state поднят в POS и передаётся как cartLocked"
      pattern: "cartLocked"
---

<objective>
Закрыть UX2 требования по защите платёжного/возвратного flow: AlertDialog для createReturn и closeShift discrepancy, ref-lock double-click protection в PaymentDialog, idempotency-key (client UUID + server check), блокировка корзины во время оплаты, toast с "Повторить" для критичных операций, inline валидация форм, warning при оплате заказа меньше остатка.

Purpose: Убрать накопленный UX долг из QA — защитить от дублирующих операций, дать оператору чёткое подтверждение критичных действий, ускорить восстановление при ошибках.
Output: Обновлённые UI компоненты POS + server-side idempotency check + shared hooks + E2E тесты.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-CONTEXT.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-RESEARCH.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-VALIDATION.md
</context>

<interfaces>
<!-- Ключевые паттерны, которые будут установлены в этом плане -->

Ref-lock (UX2-02):
```typescript
const lockRef = useRef(false)
async function handleSubmit() {
  if (lockRef.current) return
  lockRef.current = true
  setLoading(true)
  try {
    await createSale({ ...data, idempotencyKey })
  } finally {
    lockRef.current = false
    setLoading(false)
  }
}
```

Idempotency-key (UX2-06):
```typescript
// Client: регенерация при каждом открытии диалога
const [idempotencyKey, setIdempotencyKey] = useState<string>('')
useEffect(() => {
  if (open) setIdempotencyKey(crypto.randomUUID())
}, [open])

// Server (src/actions/sales.ts → createSale):
if (input.idempotencyKey) {
  const existing = await tx.sale.findUnique({ where: { idempotencyKey: input.idempotencyKey } })
  if (existing) return formatSaleResult(existing)
}
// ... proceed with creation, include idempotencyKey in Sale.create
```

Critical toast (UX2-05):
```typescript
// src/hooks/use-critical-toast.ts
export function useCriticalToast() {
  return {
    error(title: string, opts: { description?: string; retry: () => void }) {
      toast.error(title, {
        description: opts.description,
        action: { label: 'Повторить', onClick: opts.retry },
        duration: 10000,
      })
    }
  }
}
```

Зависимость от Plan 01: поле `Sale.idempotencyKey` уже добавлено в schema (Task 1 of 16-01). **Не трогать schema.prisma.** Только использовать поле.
**ВНИМАНИЕ:** План 02 начинается в той же волне что и План 01, но пишет в разные файлы. Миграция schema.prisma будет применена первым таском Plan 01; executor Plan 02 должен дождаться migrate перед использованием idempotencyKey. Если wave-coordinator сериализует — см. note в `depends_on`.
</interfaces>

<coordination_note>
**Wave 2 — зависит от Plan 01.** Plan 01 и Plan 02 оба модифицируют `src/actions/sales.ts`:
- Plan 01 Task 4 добавляет logQuantityChange вызовы в createSale/createReturn
- Plan 02 Task 1 добавляет idempotencyKey check в createSale
Чтобы избежать merge conflict, Plan 02 вынесен в wave 2 с `depends_on: ["16-01"]`. Executor Plan 02 начинает работу после завершения Plan 01 (миграция schema применена, logQuantityChange интегрирован). Поле `Sale.idempotencyKey` уже существует после Plan 01 Task 1.
</coordination_note>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): E2E стабы для UX2-02 и UX2-06 + unit стаб для UX2-13</name>
  <files>src/__tests__/e2e/ux-polish.e2e.test.ts</files>
  <read_first>
    - src/__tests__/e2e/reports-correctness.e2e.test.ts (pattern)
  </read_first>
  <action>
Создать `src/__tests__/e2e/ux-polish.e2e.test.ts`:

```typescript
describe('UX2-02: PaymentDialog double-click protection', () => {
  it.todo('два параллельных createSale с одним idempotencyKey → одна Sale')
  it.todo('ref-lock не позволяет второму submit отправить запрос (client-side)')
})

describe('UX2-06: Idempotency key', () => {
  it.todo('createSale с новым idempotencyKey → создаёт Sale')
  it.todo('createSale повторно с тем же idempotencyKey → возвращает существующую Sale')
  it.todo('createSale с null idempotencyKey → создаёт Sale (fallback)')
  it.todo('dialog close + open → новый idempotencyKey сгенерирован')
})

describe('UX2-13: Order payment < remaining warning', () => {
  it.todo('payOrder с amount < remaining → warning returned в response')
  it.todo('payOrder с amount === remaining → без warning')
})
```

Минимум 100 строк (раскрыть todo описаниями + fixture setup).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/ux-polish.e2e.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Файл существует, содержит todo для UX2-02/06/13, vitest parsing ok</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: PaymentDialog — ref-lock + idempotencyKey + server check + toast retry</name>
  <files>src/components/pos/payment-dialog.tsx, src/actions/sales.ts, src/hooks/use-critical-toast.ts</files>
  <read_first>
    - src/components/pos/payment-dialog.tsx (вся текущая логика)
    - src/actions/sales.ts (createSale — сигнатура input, транзакция)
    - prisma/schema.prisma (Sale.idempotencyKey — добавлено в Plan 01)
  </read_first>
  <behavior>
    - При открытии dialog (open=true) генерируется новый idempotencyKey через crypto.randomUUID()
    - handleSubmit проверяет lockRef.current — если true, return
    - Button 'Оплатить' показывает Loader2 spinner + текст "Обработка..." при loading=true + disabled
    - createSale server action принимает idempotencyKey в input, при существующей Sale возвращает её
    - При ошибке критичной — criticalToast.error с кнопкой "Повторить" вызывающей handleSubmit
  </behavior>
  <action>
1. **src/hooks/use-critical-toast.ts** (создать новый файл):
```typescript
import { toast } from 'sonner'

export function useCriticalToast() {
  return {
    error(title: string, opts: { description?: string; retry: () => void | Promise<void> }) {
      toast.error(title, {
        description: opts.description,
        action: { label: 'Повторить', onClick: () => { void opts.retry() } },
        duration: 10000,
      })
    }
  }
}
```

2. **src/components/pos/payment-dialog.tsx:**
   - `const [idempotencyKey, setIdempotencyKey] = useState<string>('')`
   - `const lockRef = useRef(false)`
   - `useEffect(() => { if (open) { setIdempotencyKey(crypto.randomUUID()); lockRef.current = false } }, [open])`
   - В handleSubmit:
     ```ts
     if (lockRef.current) return
     lockRef.current = true
     setLoading(true)
     try {
       const result = await createSale({ ...form, idempotencyKey })
       onSuccess(result)
     } catch (error) {
       criticalToast.error('Ошибка оплаты', { description: error.message, retry: handleSubmit })
     } finally {
       lockRef.current = false
       setLoading(false)
     }
     ```
   - Button:
     ```tsx
     <Button onClick={handleSubmit} disabled={loading}>
       {loading ? <><Loader2 className="animate-spin" /> Обработка...</> : 'Оплатить'}
     </Button>
     ```

3. **src/actions/sales.ts → createSale:**
   - Добавить `idempotencyKey: z.string().uuid().optional()` в input Zod schema
   - В начале транзакции:
     ```ts
     if (input.idempotencyKey) {
       const existing = await tx.sale.findUnique({
         where: { idempotencyKey: input.idempotencyKey },
         include: { /* same includes as the returned Sale */ }
       })
       if (existing) return formatSaleResult(existing)
     }
     ```
   - В `tx.sale.create({ data: { ...saleData, idempotencyKey: input.idempotencyKey } })`

4. Реализовать E2E UX2-02 и UX2-06 тесты (из Task 0):
   - UX2-06: два вызова createSale с одним idempotencyKey → saleA.id === saleB.id, Sale.count === 1
   - UX2-06: новый idempotencyKey → новая Sale
   - UX2-02: симулировать gotten race condition через Promise.all([createSale(same key), createSale(same key)]) → одна запись (idempotencyKey @unique в Prisma обеспечит constraint)
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/ux-polish.e2e.test.ts -t "UX2-02|UX2-06" --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>PaymentDialog защищён ref-lock + spinner + idempotencyKey; server возвращает existing Sale при повторе; E2E UX2-02/UX2-06 зелёные</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cart locking (UX2-04) + Return AlertDialog (UX2-01) + CloseShift AlertDialog (UX2-03)</name>
  <files>src/components/pos/pos-interface.tsx, src/components/pos/return-form.tsx, src/components/pos/close-shift-dialog.tsx</files>
  <read_first>
    - src/components/pos/pos-interface.tsx
    - src/components/pos/return-form.tsx
    - src/components/pos/close-shift-dialog.tsx
    - src/components/ui/alert-dialog.tsx (shadcn)
  </read_first>
  <behavior>
    - pos-interface: paymentDialogOpen state управляется в родителе; когда true — корзина получает prop cartLocked=true
    - Cart при cartLocked=true: opacity-50 + pointer-events-none + aria-disabled='true', все кнопки +/- / remove disabled
    - return-form: submit → AlertDialog с текстом 'Будет оформлен возврат на сумму {amount}', buttons 'Подтвердить возврат' (destructive) / 'Отмена'
    - close-shift-dialog: при расчётном расхождении != 0 перед final submit → AlertDialog 'Подтвердите расхождение {amount}₽?' (destructive) / 'Отмена'
  </behavior>
  <action>
1. **src/components/pos/pos-interface.tsx:**
   - Поднять state `paymentDialogOpen` в этот компонент (вместо локального в PaymentDialog)
   - Передать `<PaymentDialog open={paymentDialogOpen} onOpenChange={setPaymentDialogOpen} ... />`
   - Передать `<Cart ... locked={paymentDialogOpen} />` (или через context)
   - В Cart компоненте:
     ```tsx
     <div className={cn(locked && 'opacity-50 pointer-events-none')} aria-disabled={locked}>
       {/* cart items */}
     </div>
     ```

2. **src/components/pos/return-form.tsx:**
   ```tsx
   const [showConfirm, setShowConfirm] = useState(false)
   
   // Replace direct submit with:
   <Button onClick={() => setShowConfirm(true)}>Оформить возврат</Button>
   
   <AlertDialog open={showConfirm} onOpenChange={setShowConfirm}>
     <AlertDialogContent>
       <AlertDialogHeader>
         <AlertDialogTitle>Подтвердите возврат</AlertDialogTitle>
         <AlertDialogDescription>
           Будет оформлен возврат на сумму {formatMoney(refundAmount)}
         </AlertDialogDescription>
       </AlertDialogHeader>
       <AlertDialogFooter>
         <AlertDialogCancel>Отмена</AlertDialogCancel>
         <AlertDialogAction
           className="bg-destructive text-destructive-foreground"
           onClick={handleConfirmReturn}
         >
           Подтвердить возврат
         </AlertDialogAction>
       </AlertDialogFooter>
     </AlertDialogContent>
   </AlertDialog>
   ```
   Также использовать useCriticalToast при ошибке createReturn (UX2-05: возврат — критичная операция).

3. **src/components/pos/close-shift-dialog.tsx:**
   - Вычислить `discrepancy = actualCash - expectedCash`
   - При submit: если `discrepancy !== 0` → открыть AlertDialog
   - AlertDialog: 'Подтвердите расхождение {formatMoney(discrepancy)}?'
   - Только после confirm → вызвать closeShift server action (с useCriticalToast на ошибку)

4. Manual-only тесты (UX2-01, UX2-03, UX2-04) — задокументировать в SUMMARY какие шаги выполнить.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm lint -- src/components/pos/pos-interface.tsx src/components/pos/return-form.tsx src/components/pos/close-shift-dialog.tsx && pnpm tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Cart визуально+функционально заблокирован при открытом PaymentDialog; return требует AlertDialog; closeShift с discrepancy требует AlertDialog; типы валидны, lint чистый</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inline validation helper (UX2-07) + Order payment < remaining warning (UX2-13) + critical toast wiring</name>
  <files>src/lib/form-validation.ts, src/components/orders/order-payment-form.tsx, src/__tests__/payment-aggregation.test.ts</files>
  <read_first>
    - src/lib/utils.ts (существующие хелперы)
    - src/components/orders/order-payment-form.tsx (или найти существующий order payment компонент)
    - src/actions/orders.ts (payOrder action — если есть)
    - src/components/ui/form.tsx (shadcn FormMessage)
  </read_first>
  <behavior>
    - form-validation.ts: экспортирует `fieldErrorClass(hasError: boolean)` → возвращает 'border-destructive focus-visible:ring-destructive' для true
    - Все формы в POS/orders/inventory где есть react-hook-form — используют FormMessage + fieldErrorClass
    - order-payment-form: при вводе amount < remainingBalance показывает inline warning (Alert variant=warning) с текстом 'Оплата меньше остатка ({formatMoney(remaining-amount)}). Продолжить?'
    - order-payment-form: submit не блокируется, но warning виден до submit
  </behavior>
  <action>
1. **src/lib/form-validation.ts:**
```typescript
import { cn } from '@/lib/utils'

export function fieldErrorClass(hasError: boolean | undefined): string {
  return cn(hasError && 'border-destructive focus-visible:ring-destructive')
}

export function helperTextClass(hasError: boolean | undefined): string {
  return cn(hasError ? 'text-destructive' : 'text-muted-foreground', 'text-xs')
}
```

2. **src/components/orders/order-payment-form.tsx** (или создать если нет):
   - Use react-hook-form + zod schema с `amount: z.number().positive()`
   - Получить `remainingBalance` как prop
   - ```tsx
     const amount = watch('amount')
     const underpaid = amount > 0 && amount < remainingBalance
     
     {underpaid && (
       <Alert variant="warning">
         <AlertTriangle />
         <AlertTitle>Оплата меньше остатка</AlertTitle>
         <AlertDescription>
           Остаток после оплаты: {formatMoney(remainingBalance - amount)}. Убедитесь что клиент доплатит позже.
         </AlertDescription>
       </Alert>
     )}
     ```
   - Использовать fieldErrorClass на <Input className={fieldErrorClass(errors.amount)} />
   - FormMessage для helper text

3. Применить fieldErrorClass в payment-dialog.tsx, return-form.tsx, receive-form.tsx, trade-in-form.tsx (из Plan 01) — если там есть react-hook-form поля без inline error.

4. **src/__tests__/payment-aggregation.test.ts** (Wave 0 stub + implementation для UX2-15 который будет в Plan 03 — но агрегация логика общая):
```typescript
import { aggregatePaymentsByMethod } from '@/lib/receipts'

describe('Payment aggregation by method (UX2-15)', () => {
  it('groups 3 cash payments into single entry', () => {
    const result = aggregatePaymentsByMethod([
      { method: 'CASH', amount: 25000 },
      { method: 'CASH', amount: 30000 },
      { method: 'CASH', amount: 20000 },
    ])
    expect(result).toEqual([{ method: 'CASH', amount: 75000 }])
  })
  
  it('separates trade-in from other methods', () => { ... })
  it('preserves order: CASH, CARD, SBP, TRANSFER, CREDIT, TRADE_IN', () => { ... })
})
```

Создать `src/lib/receipts.ts` с `aggregatePaymentsByMethod(payments) → Array<{method, amount}>` (Object.groupBy или Map.reduce). Это используется Plan 03 Task 1 (receipt view).

5. Unit test UX2-13: создать тест в ux-polish.e2e.test.ts → payOrder возвращает warning в response когда amount < remaining (server-side) или проверить prop-based warning через React Testing Library (client-side).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/payment-aggregation.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>fieldErrorClass применён в критичных формах; order-payment показывает warning при underpayment; aggregatePaymentsByMethod покрыт unit тестами (готов для Plan 03)</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/__tests__/e2e/ux-polish.e2e.test.ts src/__tests__/payment-aggregation.test.ts` — все зелёные
- `pnpm lint` — без ошибок в изменённых файлах
- `pnpm tsc --noEmit` — типы корректны
- Manual: открыть PaymentDialog, два быстрых клика → один запрос; корзина серая при открытом dialog; close-shift с расхождением → AlertDialog
</verification>

<success_criteria>
ROADMAP criterion 4 (createReturn AlertDialog, PaymentDialog ref-lock, cart block) выполнен.
ROADMAP criterion 6 частично (toast 'Повторить' для критичных) выполнен.
ROADMAP criterion 7 частично (double-click → один Sale; refresh → idempotencyKey) выполнен.
UX2-01, UX2-02, UX2-03, UX2-04, UX2-05, UX2-06, UX2-07, UX2-13 закрыты.
</success_criteria>

<commit_strategy>
- Task 0: `test(16-02): add ux-polish test stubs` (Wave 0)
- Task 1: `feat(16-02): PaymentDialog ref-lock + idempotency-key + critical toast hook` + `test(16-02): UX2-02/06 E2E`
- Task 2: `feat(16-02): cart lock during payment + return/closeShift AlertDialog`
- Task 3: `feat(16-02): inline validation helper + order underpay warning + aggregatePaymentsByMethod helper` + `test(16-02): payment aggregation unit`
</commit_strategy>

<output>
After completion, create `.planning/phases/16-inventory-edge-cases-ux-polish/16-02-SUMMARY.md` with diff of PaymentDialog, screenshot-level description of cart lock, list of forms updated with inline validation, and exported helpers (useCriticalToast, fieldErrorClass, aggregatePaymentsByMethod).
</output>
