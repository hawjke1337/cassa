---
phase: 16-inventory-edge-cases-ux-polish
plan: 05
type: execute
wave: 5
depends_on: [16-04]
files_modified:
  - src/components/catalog/category-form.tsx
  - src/components/pos/pos-interface.tsx
  - src/lib/imei-utils.ts
  - src/components/serial/imei-scanner-input.tsx
  - src/actions/trade-in.ts
autonomous: true
gap_closure: true
requirements: [INV-01, UX2-17, UX2-08, INV-06]

must_haves:
  truths:
    - "Администратор открывает Редактировать Аксессуары → видит переключатель isSerialized (не задизейблен для admin), поле выбора identifierType (IMEI/SN/BOTH), и при попытке сохранить isSerialized=true — появляется AlertDialog с полем причины и кнопка Сохранить активна только после ввода причины"
    - "Клик по категории в POS-сетке (при пустом поиске) фильтрует товары этой категории и показывает кнопку 'Все категории' для возврата к сетке"
    - "При viewport 768px корзина скрыта (hidden), плавающая кнопка 'Корзина' с суммой видна в правом нижнем углу, клик открывает Sheet"
    - "Receive/Trade-in для категории identifierType='SN': ввод '982893192939' принимается без ошибки; для категории identifierType='IMEI': ввод того же значения отклоняется с ошибкой Luhn+15 цифр"
  artifacts:
    - path: "src/components/catalog/category-form.tsx"
      provides: "CategoryForm с identifierType selector + admin override AlertDialog"
      contains: "identifierType"
    - path: "src/components/pos/pos-interface.tsx"
      provides: "POS с category filter state + back-button + mobile Sheet"
      contains: "selectedCategoryId"
    - path: "src/lib/imei-utils.ts"
      provides: "validateSerialOrThrow с параметром identifierType"
      exports: ["validateSerialOrThrow", "isValidImei"]
    - path: "src/components/serial/imei-scanner-input.tsx"
      provides: "ImeiScannerInput без Luhn-валидации при identifierType=SN"
      contains: "identifierType === 'SN'"
    - path: "src/actions/trade-in.ts"
      provides: "createTradeIn без обязательной IMEI-валидации deviceImei"
      contains: "validateSerialOrThrow"
  key_links:
    - from: "pos-interface.tsx onSelect (CategoryGrid)"
      to: "selectedCategoryId state + searchPosProducts categoryId param"
      via: "setSelectedCategoryId(cat.id) + useEffect dependency on selectedCategoryId"
      pattern: "selectedCategoryId"
    - from: "ImeiScannerInput identifierType prop"
      to: "validate() Luhn branch"
      via: "identifierType !== 'SN' condition"
      pattern: "identifierType.*SN"
    - from: "trade-in.ts createTradeIn"
      to: "validateSerialOrThrow"
      via: "passes identifierType from product's category"
      pattern: "validateSerialOrThrow"
---

<objective>
UAT gap closure: fix 4 regressions found via Playwright UAT on 2026-04-14.

Gap 1 (BLOCKER, INV-01): CategoryForm не показывает identifierType selector и некорректно блокирует admin override.
Gap 2 (major, UX2-17): Клик по категории в POS не фильтрует товары и нет back-button.
Gap 3 (major, UX2-08): POS responsive: at 768px корзина сжимается вместо Sheet (mobile trigger не работает).
Gap 4 (major, INV-06/Category.identifierType): Валидация серийников игнорирует identifierType — SN-категории отклоняют любой ввод как невалидный IMEI.

Purpose: Восстановить работоспособность admin category override, POS category filter, POS mobile layout и serial validation по типу категории.
Output: 4 исправленных файла + 1 обновлённый lib-файл.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-CONTEXT.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-UAT.md
</context>

<interfaces>
<!-- Существующие контракты, которые executor использует напрямую — без исследования кодовой базы. -->

### src/components/catalog/category-form.tsx (текущее состояние — читать ПЕРЕД правкой)
```typescript
export interface CategoryFormValues {
  name: string
  parentId?: string | null
  isSerialized: boolean
  identifierType?: "IMEI" | "SN" | "BOTH" | null
  forceOverride?: boolean
  forceReason?: string
}

export interface CategoryFormProps {
  initialValues?: Partial<CategoryFormValues> & { id?: string }
  isAdmin: boolean
  onSubmit: (values: CategoryFormValues) => Promise<void> | void
  onCancel?: () => void
}
```
Дефект: в JSX формы НЕТ рендера `identifierType` selector.
Кнопка Switch: `disabled={hasExistingSerials && !isAdmin}` — правильно для admin, но guardTooltip выглядит как disabled.

### src/lib/imei-utils.ts (текущий публичный API)
```typescript
export function isValidImei(imei: string): boolean  // Luhn + 15 digits
export function validateImeiOrThrow(imei: string, fieldName?: string): string  // throws if not valid IMEI
```
Нужно добавить:
```typescript
export function validateSerialOrThrow(
  value: string,
  identifierType: "IMEI" | "SN" | "BOTH",
  fieldName?: string
): string
// IMEI: 15 digits + Luhn; SN: non-empty string (trim); BOTH: same as SN (any non-empty)
```

### src/components/serial/imei-scanner-input.tsx (текущий контракт)
```typescript
interface ImeiScannerInputProps {
  identifierType: "IMEI" | "SN" | "BOTH"
  fieldName: "imei" | "imei2" | "serialNumber"
  value: string
  onChange: (value: string) => void
  onEnter?: () => void
  autoFocus?: boolean
  placeholder?: string
}
```
Дефект: `validate()` применяет Luhn когда `identifierType !== "SN"` — т.е. для "BOTH" поле imei валидируется как настоящий IMEI.
Фикс: для `identifierType === "IMEI"` → Luhn+15digits; для `"BOTH"` → только 15digits no Luhn required (Dual-SIM может иметь нестандартные IMEI); для `"SN"` → never show in ImeiScannerInput (showImei=false в parent), но добавить guard.

### src/actions/trade-in.ts (вызов validateImeiOrThrow)
Строка 139-141:
```typescript
if (parsed.deviceImei && parsed.deviceImei.trim()) {
  parsed.deviceImei = validateImeiOrThrow(parsed.deviceImei.trim(), "IMEI устройства")
}
```
Дефект: `deviceImei` — поле из формы trade-in, которое заполняет оператор. Форма не знает identifierType категории.
Фикс: Trade-in форма принимает любой непустой серийник. Заменить `validateImeiOrThrow` на `isValidImei`-check с предупреждением только если похоже на IMEI (15 цифр) но не проходит Luhn.

### src/components/pos/pos-interface.tsx (fragment — CategoryGrid wiring)
Строка 345: `<CategoryGrid categories={categories} onSelect={(cat) => setSearch(cat.name)} />`
Дефект: `onSelect` только устанавливает текстовый search, а `searchPosProducts` ищет по имени — но API не поддерживает поиск по categoryId. Кнопки возврата нет.
Текущий state: `search`, `categories`, `mobileCartOpen`. НЕТ: `selectedCategoryId`.

### src/actions/sales.ts — searchPosProducts signature
```typescript
export async function searchPosProducts(
  storeId: string,
  query: string
): Promise<PosProduct[]>
```
Нужно проверить — принимает ли уже `categoryId`? Если нет, нужно добавить параметр.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (BLOCKER): Добавить identifierType selector в CategoryForm + диагностика admin switch</name>
  <read_first>src/components/catalog/category-form.tsx</read_first>
  <files>src/components/catalog/category-form.tsx</files>
  <action>
Прочитать файл полностью (уже прочитан выше). Внести следующие конкретные изменения:

**1. Добавить identifierType selector в JSX формы (после блока Switch, перед кнопками):**

После закрывающего `</div>` блока со Switch (строка ~133), добавить условный блок:

```tsx
{isSerialized ? (
  <div className="space-y-2">
    <Label>Тип идентификатора</Label>
    <div className="flex gap-2">
      {(["IMEI", "SN", "BOTH"] as const).map((type) => (
        <button
          key={type}
          type="button"
          onClick={() => setIdentifierType(type)}
          className={`rounded-lg border px-3 py-1.5 text-sm transition-colors ${
            identifierType === type
              ? "border-primary bg-primary text-primary-foreground"
              : "hover:bg-muted"
          }`}
        >
          {type === "IMEI" ? "IMEI (телефоны)" : type === "SN" ? "SN (аксессуары)" : "IMEI + SN (dual)"}
        </button>
      ))}
    </div>
    {!identifierType && (
      <p className="text-xs text-destructive">Выберите тип идентификатора</p>
    )}
  </div>
) : null}
```

**2. Исправить `handleSubmit` — заблокировать сохранение без identifierType когда isSerialized=true:**

В `handleSubmit` перед проверкой `isAttemptingFlip` добавить:
```tsx
if (isSerialized && !identifierType) return  // validation: identifierType required
```

**3. Исправить Switch disabled logic — убрать visual confusion для admin:**

Изменить отображение guardTooltip для admin — вместо "Нельзя: ..." показывать "Внимание: N единиц" (не запрет, а предупреждение):
```tsx
{guardTooltip && !isAdmin ? (
  <p className="text-xs text-muted-foreground">{guardTooltip}</p>
) : guardTooltip && isAdmin ? (
  <p className="text-xs text-amber-600">⚠ {guardTooltip} — admin override доступен</p>
) : null}
```

Switch остаётся: `disabled={hasExistingSerials && !isAdmin}` — это уже корректно.

**4. Убедиться, что identifierType передаётся в handleForceConfirm (строка ~102-112) — уже есть, не менять.**

**5. Исправить тип кнопки Сохранить — заблокировать если isSerialized=true и identifierType не выбран:**
```tsx
<Button type="submit" disabled={pending || (isSerialized && !identifierType)}>
  Сохранить
</Button>
```
  </action>
  <acceptance_criteria>
    - `grep -n "identifierType" src/components/catalog/category-form.tsx` возвращает строки с JSX-рендером selector (не только интерфейс и state)
    - `grep -n "IMEI.*SN.*BOTH\|SN.*IMEI\|BOTH.*SN" src/components/catalog/category-form.tsx` находит строку с тремя вариантами
    - `grep -n "isSerialized && !identifierType" src/components/catalog/category-form.tsx` — guard в handleSubmit присутствует
    - `grep -n "admin override доступен\|amber" src/components/catalog/category-form.tsx` — admin-specific tooltip присутствует
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "category-form" || echo "no category-form errors"</automated>
  </verify>
  <done>CategoryForm рендерит IMEI/SN/BOTH selector при isSerialized=true; admin видит предупреждение вместо запрета; кнопка Сохранить задизейблена без identifierType выбора; TSC не ругается на category-form.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: POS category click — фильтр по categoryId + back-button + responsive mobile Sheet fix</name>
  <read_first>src/components/pos/pos-interface.tsx, src/actions/sales.ts</read_first>
  <files>src/components/pos/pos-interface.tsx, src/actions/sales.ts</files>
  <action>
Прочитать оба файла перед правками.

**Шаг A — проверить searchPosProducts в sales.ts:**

Найти сигнатуру `searchPosProducts`. Если параметр `categoryId?: string` уже есть — использовать. Если нет — добавить:
- В параметры функции: `categoryId?: string`
- В where clause Prisma: если `categoryId` передан, добавить `storeProduct: { product: { categoryId } }` (или аналогичный where по структуре модели)

**КРИТИЧНО: обновить early-return guard.** Текущий guard `if (!search || search.trim().length === 0) return []` (строка ~106) отклоняет вызов `searchPosProducts(storeId, "", categoryId)` с пустой строкой поиска, не доходя до Prisma-фильтра. Изменить guard на:
```typescript
if (!search?.trim() && !categoryId) return []
```
Это позволяет category-only запросам (пустой search + непустой categoryId) пройти до Prisma.

**Шаг B — добавить state в pos-interface.tsx:**

В блок `useState` (после `const [mobileCartOpen, setMobileCartOpen] = useState(false)`, примерно строка 83) добавить:
```tsx
const [selectedCategoryId, setSelectedCategoryId] = useState<string | null>(null)
```

**Шаг C — обновить debounced search useEffect (строки ~121-187):**

В условии `if (!search.trim() || !currentStoreId)` — добавить обработку selectedCategoryId:
```tsx
if (!search.trim() && !selectedCategoryId) {
  setProducts([])
  setSearching(false)
  return
}
```

Добавить отдельный useEffect для загрузки товаров по категории:
```tsx
useEffect(() => {
  if (!selectedCategoryId || !currentStoreId) return
  setSearching(true)
  searchPosProducts(currentStoreId, "", selectedCategoryId)
    .then(setProducts)
    .catch(() => setProducts([]))
    .finally(() => setSearching(false))
}, [selectedCategoryId, currentStoreId])
```

**Шаг D — обновить CategoryGrid onSelect (строка 345):**

Заменить:
```tsx
<CategoryGrid categories={categories} onSelect={(cat) => setSearch(cat.name)} />
```
На:
```tsx
<CategoryGrid
  categories={categories}
  onSelect={(cat) => {
    setSelectedCategoryId(cat.id)
    setSearch("")
  }}
/>
```

**Шаг E — добавить back-button над product list (строки ~325-345, перед CategoryGrid):**

В блоке `<div className="border-b px-3 py-2">` добавить кнопку "Все категории" когда selectedCategoryId активен. Добавить ПЕРЕД заголовком "Результаты поиска" / "Категории":
```tsx
{selectedCategoryId && !search.trim() && (
  <div className="flex items-center gap-2 border-b px-3 py-2">
    <Button
      variant="ghost"
      size="sm"
      onClick={() => {
        setSelectedCategoryId(null)
        setProducts([])
      }}
    >
      ← Все категории
    </Button>
    <span className="text-sm text-muted-foreground">
      {categories.find((c) => c.id === selectedCategoryId)?.name ?? "Категория"}
    </span>
  </div>
)}
```

**Шаг F — исправить SheetTrigger синтаксис (строки ~595-608):**

Текущий код (строки 595-608) использует `SheetTrigger render={<Button>}` — это нестандартный Radix patten. В shadcn/ui SheetTrigger не принимает `render` prop. Заменить на:
```tsx
<SheetTrigger asChild>
  <Button
    size="lg"
    className="rounded-full shadow-lg"
    aria-label={`Открыть корзину (${itemCount} товаров, к оплате ${formatMoney(finalAmount)})`}
    disabled={paymentOpen}
  >
    <ShoppingCart aria-hidden className="size-5" />
    <span className="ml-2">
      {itemCount} · {formatMoney(finalAmount)}
    </span>
  </Button>
</SheetTrigger>
```

Это фиксирует Gap 3 (mobile cart Sheet не открывался из-за неверного SheetTrigger API).
  </action>
  <acceptance_criteria>
    - `grep -n "selectedCategoryId" src/components/pos/pos-interface.tsx | wc -l` — не менее 5 вхождений (state объявление + onSelect + useEffect + back-button + condition)
    - `grep -n "Все категории" src/components/pos/pos-interface.tsx` — back-button присутствует
    - `grep -n "SheetTrigger asChild" src/components/pos/pos-interface.tsx` — SheetTrigger использует asChild
    - `grep -n "categoryId" src/actions/sales.ts` — параметр добавлен
    - `grep -n "search?.trim.*categoryId\|categoryId.*search?.trim" src/actions/sales.ts` — обновлённый guard присутствует
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "pos-interface|sales\.ts" || echo "no pos errors"</automated>
  </verify>
  <done>
    - Клик на категорию в POS устанавливает selectedCategoryId и загружает товары через searchPosProducts(storeId, "", categoryId)
    - Early-return guard в sales.ts пропускает category-only запросы (пустой search + непустой categoryId)
    - Back-button "← Все категории" виден при активной категории
    - SheetTrigger использует asChild синтаксис — mobile Sheet корректно открывается
    - TSC не ругается на pos-interface.tsx и sales.ts
  </done>
</task>

<task type="auto">
  <name>Task 3: Исправить IMEI/SN валидацию — validateSerialOrThrow + ImeiScannerInput + trade-in</name>
  <read_first>src/lib/imei-utils.ts, src/components/serial/imei-scanner-input.tsx, src/actions/trade-in.ts</read_first>
  <files>src/lib/imei-utils.ts, src/components/serial/imei-scanner-input.tsx, src/actions/trade-in.ts</files>
  <action>
Прочитать все три файла перед правками.

**Шаг A — src/lib/imei-utils.ts: добавить validateSerialOrThrow:**

Добавить новую экспортируемую функцию после существующих (сохранить `isValidImei` и `validateImeiOrThrow` без изменений — они используются в orders.ts и других местах):

```typescript
/**
 * Валидация серийного идентификатора с учётом типа категории.
 * - IMEI: 15 цифр + Luhn algorithm
 * - SN: любая непустая строка (серийный номер аксессуара, не IMEI)
 * - BOTH: любая непустая строка (dual-SIM: оба поля могут быть IMEI или SN)
 */
export function validateSerialOrThrow(
  value: string,
  identifierType: "IMEI" | "SN" | "BOTH",
  fieldName = "Серийный номер",
): string {
  const cleaned = value.replace(/\s/g, "")
  if (!cleaned) {
    throw new Error(`${fieldName}: значение не может быть пустым`)
  }
  if (identifierType === "IMEI") {
    if (!isValidImei(cleaned)) {
      throw new Error(
        `Невалидный ${fieldName}: ${value}. IMEI должен содержать 15 цифр и пройти проверку Luhn.`,
      )
    }
  }
  // SN and BOTH: accept any non-empty string
  return cleaned
}
```

**Шаг B — src/components/serial/imei-scanner-input.tsx: исправить validate():**

Текущая логика (строки 28-47): валидирует как IMEI когда `identifierType !== "SN"`. Для `BOTH` это некорректно — IMEI поле для dual-SIM может содержать нестандартные серийники.

Заменить функцию `validate`:
```typescript
const validate = (v: string) => {
  if (!v) {
    setError("")
    return
  }
  // Only apply strict IMEI validation (Luhn + 15 digits) for identifierType="IMEI"
  // For "BOTH": show soft warning if looks like IMEI format but fails (non-blocking)
  if (identifierType === "IMEI" && (fieldName === "imei" || fieldName === "imei2")) {
    if (!/^\d{15}$/.test(v)) {
      setError("IMEI должен содержать 15 цифр")
      return
    }
    if (!isValidImei(v)) {
      setError("Некорректная контрольная сумма IMEI (Luhn)")
      return
    }
  }
  // SN or BOTH: no format restriction, any non-empty string is valid
  setError("")
}
```

**Шаг C — src/actions/trade-in.ts: заменить unconditional validateImeiOrThrow:**

Текущий код (строки 139-141):
```typescript
if (parsed.deviceImei && parsed.deviceImei.trim()) {
  parsed.deviceImei = validateImeiOrThrow(parsed.deviceImei.trim(), "IMEI устройства")
}
```

Trade-in форма собирает `deviceImei` как универсальное поле для любого серийника (может быть IMEI телефона или SN аксессуара). Заменить на мягкую валидацию:
```typescript
if (parsed.deviceImei && parsed.deviceImei.trim()) {
  const trimmed = parsed.deviceImei.trim()
  // Soft validation: if it looks like IMEI (15 digits), check Luhn; otherwise accept as SN
  if (/^\d{15}$/.test(trimmed) && !isValidImei(trimmed)) {
    throw new Error(
      `Невалидный IMEI: ${trimmed}. 15-значный номер должен пройти проверку Luhn. Если это серийный номер (не IMEI), введите его в другом формате.`,
    )
  }
  parsed.deviceImei = trimmed
}
```

Обновить импорт: удалить `validateImeiOrThrow` из деструктуры и оставить только `isValidImei`:
```typescript
import { isValidImei } from '@/lib/imei-utils'
```
  </action>
  <acceptance_criteria>
    - `grep -n "validateSerialOrThrow" src/lib/imei-utils.ts` — функция объявлена и экспортирована
    - `grep -n "identifierType === .IMEI." src/components/serial/imei-scanner-input.tsx` — условие Luhn применяется только для IMEI
    - `grep -n "isValidImei.*trimmed\|trimmed.*isValidImei" src/actions/trade-in.ts` — мягкая валидация с isValidImei
    - `grep -n "validateImeiOrThrow" src/actions/trade-in.ts` — строка с unconditional вызовом УДАЛЕНА (не должно быть `validateImeiOrThrow` в trade-in.ts)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "imei-utils|imei-scanner|trade-in" || echo "no serial validation errors"</automated>
  </verify>
  <done>
    - validateSerialOrThrow экспортирована из imei-utils.ts с поддержкой IMEI/SN/BOTH
    - ImeiScannerInput применяет Luhn только для identifierType="IMEI"
    - trade-in.ts: SN-значение '982893192939' (12 цифр) принимается без ошибки; 15-значное число с неверной Luhn отклоняется с понятным сообщением
    - TSC чистый по всем трём файлам
  </done>
</task>

</tasks>

<verification>
После выполнения всех задач проверить:

```bash
cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp"

# 1. TSC clean (только целевые файлы)
npx tsc --noEmit 2>&1 | grep -E "category-form|pos-interface|imei-utils|imei-scanner|trade-in|sales\." | head -20

# 2. CategoryForm — identifierType selector присутствует
grep -c "identifierType" src/components/catalog/category-form.tsx

# 3. POS — selectedCategoryId state и back-button
grep -n "selectedCategoryId\|Все категории\|SheetTrigger asChild" src/components/pos/pos-interface.tsx

# 4. IMEI utils — новая функция
grep -n "validateSerialOrThrow" src/lib/imei-utils.ts

# 5. Trade-in — мягкая валидация, validateImeiOrThrow удалён
grep -n "isValidImei\|validateImeiOrThrow" src/actions/trade-in.ts

# 6. sales.ts — обновлённый guard
grep -n "categoryId\|search?.trim" src/actions/sales.ts
```
</verification>

<success_criteria>
- Gap 1 закрыт: CategoryForm рендерит IMEI/SN/BOTH selector; admin может флипнуть isSerialized и видит AlertDialog с полем причины
- Gap 2 закрыт: selectedCategoryId state + категорийный фильтр + "← Все категории" back-button; early-return guard в sales.ts пропускает category-only запросы
- Gap 3 закрыт: SheetTrigger asChild — mobile floating cart button открывает Sheet корректно
- Gap 4 закрыт: SN '982893192939' (12 цифр) принимается в trade-in; IMEI с неверной Luhn отклоняется с понятным сообщением
- npx tsc --noEmit без новых ошибок в изменённых файлах
</success_criteria>

## Deferred from this plan

**Gap 5 — Форма заказа: dropdown/autocomplete существующих контрагентов (test 16)**
- Статус UAT: major, "при заказе нет выпадающего списка с контрагентами"
- Причина отсрочки: функциональность принадлежит модулю Заказов, а не Phase 16 (Inventory Edge Cases & UX Polish). Требует CustomerCombobox компонент, API endpoint search-customers, интеграцию в order form.
- Целевая фаза: UX2-18 (новое требование) или отдельная "Orders UX" фаза после v1.1 завершения.
- Не реализовывать в рамках этого плана.

<output>
После выполнения создать `.planning/phases/16-inventory-edge-cases-ux-polish/16-05-SUMMARY.md`
</output>
