---
phase: 16-inventory-edge-cases-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260414_inventory_edge_cases/migration.sql
  - src/lib/store-product-history.ts
  - src/actions/audit.ts
  - src/actions/inventory.ts
  - src/actions/catalog.ts
  - src/actions/trade-in.ts
  - src/actions/sales.ts
  - src/components/inventory/audit-detail.tsx
  - src/components/inventory/audit-filters.tsx
  - src/components/catalog/category-form.tsx
  - src/components/inventory/receive-form.tsx
  - src/components/inventory/trade-in-form.tsx
  - src/__tests__/e2e/inventory-edge-cases.e2e.test.ts
  - src/__tests__/trade-in-edge-cases.test.ts
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-08, INV-09]
status: draft

must_haves:
  truths:
    - "Обычный пользователь не может сменить Category.isSerialized если существуют SerialUnit — сервер возвращает ошибку"
    - "Админ может принудительно сменить Category.isSerialized с записью в AuditLog"
    - "Audit close: ненайденные серийники получают статус MISSING"
    - "Audit close: если серийник был MISSING в предыдущем завершённом audit и снова не найден → WRITTEN_OFF"
    - "Audit close: expectedQty пересчитывается на момент закрытия (учитывает продажи/возвраты в процессе)"
    - "StoreProductHistory заполняется при createSale, createReturn, confirmReceive, confirmTransferSent/Received, closeAudit (surplus/shortage), createWriteOff, completeOrder (для не-серийных товаров)"
    - "Stock Transfer блокируется с ошибкой если товара нет на складе-источнике (sourceSp=null)"
    - "Receive: sellPrice обязательное поле при создании нового StoreProduct — пустое значение отклоняется"
    - "Trade-In: agreedPrice=0 показывает warning и помечает запись как 'Бесплатный приём'; создание не блокируется"
    - "Audit page: toggle 'В т.ч. удалённые' показывает soft-deleted StoreProduct с визуальным отличием"
    - "Trade-In: создание возможно со статусом IN_STOCK или PENDING по выбору оператора"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "StoreProductHistory model, MISSING enum, StockChangeReason enum, Sale.idempotencyKey"
      contains: "model StoreProductHistory"
    - path: "prisma/migrations/20260414_inventory_edge_cases/migration.sql"
      provides: "Schema migration SQL"
      contains: "StoreProductHistory"
    - path: "src/lib/store-product-history.ts"
      provides: "logQuantityChange(tx, storeProductId, before, after, reason, userId) helper"
      exports: ["logQuantityChange"]
      min_lines: 20
    - path: "src/actions/audit.ts"
      provides: "closeAudit с MISSING/WRITTEN_OFF логикой и пересчётом expectedQty"
      contains: "MISSING"
    - path: "src/actions/catalog.ts"
      provides: "updateCategory с guard и admin override"
      contains: "isSerialized"
    - path: "src/__tests__/e2e/inventory-edge-cases.e2e.test.ts"
      provides: "E2E тесты для INV-01..06, INV-08"
      min_lines: 150
  key_links:
    - from: "src/actions/sales.ts"
      to: "src/lib/store-product-history.ts"
      via: "logQuantityChange вызывается в транзакции createSale"
      pattern: "logQuantityChange"
    - from: "src/actions/inventory.ts"
      to: "src/lib/store-product-history.ts"
      via: "logQuantityChange в confirmReceive/confirmTransferSent/confirmTransferReceived/createWriteOff"
      pattern: "logQuantityChange"
    - from: "src/actions/audit.ts"
      to: "prisma.serialUnit"
      via: "update status=MISSING для ненайденных, WRITTEN_OFF если был MISSING в предыдущем audit"
      pattern: "status.*MISSING"
    - from: "src/actions/catalog.ts"
      to: "prisma.auditLog"
      via: "Запись AuditLog при admin force override isSerialized"
      pattern: "auditLog\\.create"
---

<objective>
Закрыть все inventory edge cases фазы 16 (INV-01..09): миграция схемы (StoreProductHistory, MISSING enum, Sale.idempotencyKey), переработка closeAudit (MISSING → WRITTEN_OFF + пересчёт expectedQty), guard + admin override для Category.isSerialized, валидация Stock Transfer, обязательный sellPrice в Receive, warning для Trade-In agreedPrice=0, фильтр soft-deleted в audit, trade-in со статусом IN_STOCK/PENDING.

Purpose: Устранить последние пробелы в inventory core flow — обеспечить полный audit trail изменений количества, корректную обработку пропавших серийников, невозможность создать inconsistent состояние.
Output: Prisma миграция + переработанные server actions + UI компоненты + E2E тесты.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-CONTEXT.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-RESEARCH.md
@.planning/phases/16-inventory-edge-cases-ux-polish/16-VALIDATION.md
</context>

<interfaces>
<!-- Ключевые контракты, которые нужны исполнителю. -->

StoreProductHistory (новая модель Prisma):
```prisma
enum StockChangeReason {
  SALE
  RETURN
  RECEIVE
  TRANSFER_OUT
  TRANSFER_IN
  AUDIT_SURPLUS
  AUDIT_SHORTAGE
  WRITE_OFF
  ORDER_COMPLETE
}

model StoreProductHistory {
  id              String            @id @default(cuid())
  storeProduct    StoreProduct      @relation(fields: [storeProductId], references: [id], onDelete: Cascade)
  storeProductId  String
  quantityBefore  Int
  quantityAfter   Int
  reason          StockChangeReason
  user            User              @relation(fields: [userId], references: [id], onDelete: Restrict)
  userId          String
  createdAt       DateTime          @default(now())

  @@index([storeProductId, createdAt])
  @@index([userId])
}
```

SerialUnitStatus добавить:
```
MISSING       // не найден при audit (первый раз)
```

Sale добавить:
```
idempotencyKey  String?   @unique @db.VarChar(36)
```

Helper API:
```typescript
// src/lib/store-product-history.ts
export async function logQuantityChange(
  tx: Prisma.TransactionClient,
  params: {
    storeProductId: string
    quantityBefore: number
    quantityAfter: number
    reason: StockChangeReason
    userId: string
  }
): Promise<void>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test stubs + Schema migration — StoreProductHistory + MISSING + Sale.idempotencyKey + helper</name>
  <files>src/__tests__/e2e/inventory-edge-cases.e2e.test.ts, src/__tests__/trade-in-edge-cases.test.ts, prisma/schema.prisma, prisma/migrations/20260414_inventory_edge_cases/migration.sql, src/lib/store-product-history.ts</files>
  <read_first>
    - src/__tests__/e2e/reports-correctness.e2e.test.ts (first 60 lines — E2E pattern)
    - src/__tests__/e2e/data-integrity-constraints.e2e.test.ts (Phase 15 — fixture pattern)
    - prisma/schema.prisma (найти model StoreProduct, SerialUnit, Sale, SerialUnitStatus enum)
    - src/lib/db.ts (Prisma client pattern, TransactionClient type)
  </read_first>
  <behavior>
    - Test stub файлы существуют с it.todo заглушками для всех INV-01..09 + UX2-11
    - StoreProductHistory создаётся успешно через prisma.storeProductHistory.create
    - SerialUnit.status может быть установлен в 'MISSING'
    - Sale с дубликатом idempotencyKey отклоняется на уровне БД (@unique)
    - logQuantityChange вставляет запись и не падает при повторном вызове в той же транзакции
  </behavior>
  <action>
**Шаг 0 (test stubs — выполнить ПЕРЕД изменениями schema):** Создать тест-скелеты (failing/pending) для всех требований INV-01..09:

**src/__tests__/e2e/inventory-edge-cases.e2e.test.ts** (минимум 150 строк, один describe на требование):
- `describe('INV-01: Category.isSerialized guard')` — it.todo('обычный пользователь не может сменить если есть SerialUnit'), it.todo('админ может force change с AuditLog')
- `describe('INV-02: Audit MISSING → WRITTEN_OFF')` — it.todo('первый miss → MISSING'), it.todo('второй подряд miss → WRITTEN_OFF'), it.todo('MISSING → найден в след audit → IN_STOCK')
- `describe('INV-03: Audit expected qty at close')` — it.todo('продажа между open и close учтена в expectedQty')
- `describe('INV-04: StoreProductHistory')` — it.todo('createSale пишет AUDIT_SHORTAGE не пишется, SALE пишется'), it.todo('confirmReceive пишет RECEIVE'), it.todo('audit close пишет AUDIT_SURPLUS/AUDIT_SHORTAGE')
- `describe('INV-05: Transfer null sourceSp')` — it.todo('transfer с несуществующим товаром на источнике → ошибка')
- `describe('INV-06: Receive sellPrice mandatory')` — it.todo('новый StoreProduct без sellPrice отклоняется')
- `describe('INV-08: Soft-deleted в audit')` — it.todo('toggle показывает deletedAt != null')

**src/__tests__/trade-in-edge-cases.test.ts** (unit, минимум 60 строк):
- `describe('INV-07: Trade-in agreedPrice=0')` — it.todo('не блокирует создание'), it.todo('помечает как Бесплатный приём')
- `describe('INV-09: Trade-in status choice')` — it.todo('создаётся с IN_STOCK'), it.todo('создаётся с PENDING')
- `describe('UX2-11: Single price field')` — it.todo('form schema имеет только agreedPrice')

Все it.todo должны реально существовать как заглушки. Использовать fixture pattern из data-integrity-constraints.e2e.test.ts. Зафиксировать stubs коммитом `test(16-01): add inventory edge cases test stubs` перед переходом к schema.

**Шаг 1. prisma/schema.prisma** изменения:

   a) Добавить enum `StockChangeReason` (SALE, RETURN, RECEIVE, TRANSFER_OUT, TRANSFER_IN, AUDIT_SURPLUS, AUDIT_SHORTAGE, WRITE_OFF, ORDER_COMPLETE).

   b) Добавить model `StoreProductHistory` (см. `<interfaces>` выше) — storeProductId, quantityBefore, quantityAfter, reason, userId, createdAt, индексы.

   c) Добавить обратные relations:
      - в `StoreProduct`: `quantityHistory StoreProductHistory[]`
      - в `User`: `quantityChangeLogs StoreProductHistory[]`

   d) В enum `SerialUnitStatus` добавить значение `MISSING` (после `WRITTEN_OFF`, до `IN_REPAIR` если есть).

   e) В model `Sale` добавить поле `idempotencyKey String? @unique @db.VarChar(36)`.

**Шаг 2.** Запустить `pnpm prisma migrate dev --name inventory_edge_cases` — создать миграцию в `prisma/migrations/20260414_inventory_edge_cases/migration.sql`. Проверить что SQL содержит:
   - CREATE TABLE "StoreProductHistory"
   - ALTER TYPE "SerialUnitStatus" ADD VALUE 'MISSING'
   - ALTER TABLE "Sale" ADD COLUMN "idempotencyKey"
   - CREATE UNIQUE INDEX на Sale.idempotencyKey

**Шаг 3. Создать src/lib/store-product-history.ts**:
```typescript
import type { Prisma, StockChangeReason } from '@prisma/client'

export async function logQuantityChange(
  tx: Prisma.TransactionClient,
  params: {
    storeProductId: string
    quantityBefore: number
    quantityAfter: number
    reason: StockChangeReason
    userId: string
  }
): Promise<void> {
  // Skip if no actual change (defensive — avoid noise)
  if (params.quantityBefore === params.quantityAfter) return
  await tx.storeProductHistory.create({ data: params })
}
```

**Шаг 4.** Запустить `pnpm prisma generate`.

**Важно:** не смешивать с изменениями action-файлов — они будут в Task 2-5.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm prisma validate && pnpm prisma migrate status</automated>
  </verify>
  <done>Test stub файлы (inventory-edge-cases.e2e.test.ts + trade-in-edge-cases.test.ts) существуют с todo для всех INV-требований, vitest парсит без падений; schema.prisma содержит StoreProductHistory + StockChangeReason + MISSING + Sale.idempotencyKey; миграция применена; helper logQuantityChange экспортируется</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: closeAudit rework — MISSING, WRITTEN_OFF, пересчёт expectedQty, StoreProductHistory</name>
  <files>src/actions/audit.ts, src/components/inventory/audit-detail.tsx, src/components/inventory/audit-filters.tsx</files>
  <read_first>
    - src/actions/audit.ts (вся текущая логика open/close/count)
    - prisma/schema.prisma (InventoryAudit, InventoryAuditSerial, InventoryAuditItem models)
    - src/components/inventory/audit-detail.tsx (текущий UI)
    - src/lib/store-product-history.ts (помощник из Task 1)
  </read_first>
  <behavior>
    - closeAudit: ненайденный серийник, которого не было в previous completed audit с MISSING → status = MISSING
    - closeAudit: ненайденный серийник, который УЖЕ был MISSING в previous completed audit → status = WRITTEN_OFF + SerialUnitEvent запись
    - closeAudit: найденный серийник с status=MISSING → возвращается в IN_STOCK
    - closeAudit: expectedQty пересчитывается через re-query StoreProduct.quantity ВНУТРИ транзакции после FOR UPDATE
    - closeAudit: разница actualQty - recomputedExpected логируется в StoreProductHistory (AUDIT_SURPLUS или AUDIT_SHORTAGE)
    - audit-filters: toggle 'showDeleted' переключает фильтр deletedAt в query
  </behavior>
  <action>
1. **src/actions/audit.ts → closeAudit:**

   a) Внутри транзакции после открытия audit:
      - `SELECT ... FOR UPDATE` по всем StoreProduct из audit items
      - Для каждого item: recomputedExpected = (актуальный StoreProduct.quantity на момент закрытия), NOT items.expectedQty (старое значение)
      - discrepancy = actualQty - recomputedExpected
      - Если discrepancy != 0: `logQuantityChange(tx, { storeProductId, quantityBefore: currentQty, quantityAfter: actualQty, reason: discrepancy > 0 ? 'AUDIT_SURPLUS' : 'AUDIT_SHORTAGE', userId: session.userId })` + UPDATE StoreProduct.quantity = actualQty

   b) Для серийников (InventoryAuditSerial где found=false):
      ```ts
      const prevAudit = await tx.inventoryAudit.findFirst({
        where: { storeId, status: 'COMPLETED', id: { not: audit.id } },
        orderBy: { closedAt: 'desc' },
        include: { serials: { where: { serialUnitId: { in: notFoundIds } } } }
      })
      for (const s of notFoundSerials) {
        const wasPrevMissing = prevAudit?.serials.find(
          ps => ps.serialUnitId === s.serialUnitId && ps.resultStatus === 'MISSING'
        )
        const newStatus = wasPrevMissing ? 'WRITTEN_OFF' : 'MISSING'
        await tx.serialUnit.update({
          where: { id: s.serialUnitId },
          data: { status: newStatus }
        })
        await tx.serialUnitEvent.create({
          data: { serialUnitId: s.serialUnitId, event: newStatus === 'WRITTEN_OFF' ? 'WRITTEN_OFF' : 'MISSING', userId, auditId: audit.id }
        })
      }
      ```
   c) Для found=true серийников с текущим status=MISSING: вернуть в IN_STOCK, добавить SerialUnitEvent(MISSING_RESOLVED) — если enum позволяет; иначе просто status update с причиной в комментарии.

   d) InventoryAuditSerial.resultStatus сохраняет MISSING/WRITTEN_OFF/FOUND — может потребоваться расширение enum (добавить MISSING в InventoryAuditSerialStatus если его нет).

2. **src/components/inventory/audit-filters.tsx** (создать или обновить):
   - Checkbox `<Checkbox checked={showDeleted} onCheckedChange={setShowDeleted} />` + label "В т.ч. удалённые"
   - Прокинуть в query параметр audit page

3. **src/components/inventory/audit-detail.tsx:**
   - Визуально помечать deleted товары (серый/opacity-50 + badge "Удалён")
   - Показать колонку "Статус серийника" с новыми MISSING/WRITTEN_OFF значениями

4. Server action `getAuditDetail(auditId, { includeDeleted: boolean })` — условно пропускать фильтр `deletedAt: null` в запросе items.

5. Реализовать tests в inventory-edge-cases.e2e.test.ts:
   - INV-02: создать audit, не найти серийник → MISSING; закрыть; создать второй audit, снова не найти → WRITTEN_OFF
   - INV-03: открыть audit, создать Sale в процессе, закрыть audit → expectedQty отражает Sale
   - INV-04: audit close с discrepancy → StoreProductHistory имеет запись AUDIT_SURPLUS/SHORTAGE
   - INV-08: создать StoreProduct, soft-delete, запустить audit с showDeleted=false (не видно), потом true (видно)
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/inventory-edge-cases.e2e.test.ts -t "INV-02|INV-03|INV-04|INV-08" --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>E2E тесты INV-02, INV-03, INV-04 (audit-часть), INV-08 зелёные; closeAudit пересчитывает expectedQty внутри транзакции с FOR UPDATE; MISSING → WRITTEN_OFF логика работает</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Category.isSerialized guard + admin override + AuditLog</name>
  <files>src/actions/catalog.ts, src/components/catalog/category-form.tsx</files>
  <read_first>
    - src/actions/catalog.ts (текущий updateCategory, createCategory)
    - src/components/catalog/category-form.tsx
    - src/lib/permissions.ts (requirePermission pattern)
    - prisma/schema.prisma (Category, Product, SerialUnit)
  </read_first>
  <behavior>
    - updateCategory: обычный пользователь меняет isSerialized при наличии SerialUnits → ошибка 'CATEGORY_HAS_SERIAL_UNITS'
    - updateCategory: админ с параметром forceOverride=true + reason → success + AuditLog запись
    - updateCategory: нет связанных SerialUnit → смена разрешена всем с правом
    - UI: обычный пользователь видит disabled toggle с tooltip "Нельзя: есть N серийных товаров"
    - UI: админ видит AlertDialog с предупреждением и полем reason
  </behavior>
  <action>
1. **src/actions/catalog.ts → updateCategory:**
```ts
const existingSerialCount = await db.serialUnit.count({
  where: { product: { categoryId: id } }
})

if (input.isSerialized !== undefined && existingSerialCount > 0 && !input.forceOverride) {
  throw new ActionError('CATEGORY_HAS_SERIAL_UNITS', `Нельзя: есть ${existingSerialCount} серийных товаров`)
}

if (input.isSerialized !== undefined && input.forceOverride) {
  await requirePermission('admin.all') // или существующее role-based
  if (!input.forceReason) throw new ActionError('FORCE_REASON_REQUIRED')
  await db.auditLog.create({
    data: {
      userId, action: 'CATEGORY_ISSERIALIZED_FORCE_CHANGE',
      targetType: 'Category', targetId: id,
      details: { oldValue: oldIsSerialized, newValue: input.isSerialized, reason: input.forceReason, affectedSerialUnits: existingSerialCount }
    }
  })
}
```

2. **src/components/catalog/category-form.tsx:**
   - Получить count через server action `getCategorySerialCount(categoryId)`
   - Если > 0 и user не admin: `<Switch disabled />` + Tooltip
   - Если > 0 и user admin: AlertDialog при change с Textarea для reason + confirm "Подтверждаю принудительную смену"

3. E2E тест INV-01 в inventory-edge-cases.e2e.test.ts:
   - Создать category + product + SerialUnit
   - Попытка updateCategory isSerialized flip обычным юзером → ошибка
   - Попытка admin с forceOverride=true, reason='test' → success + AuditLog.count > 0
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/inventory-edge-cases.e2e.test.ts -t "INV-01" --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Guard блокирует обычного пользователя; admin override с reason + AuditLog работает; E2E INV-01 зелёный</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Transfer validation + Receive sellPrice + StoreProductHistory в inventory actions</name>
  <files>src/actions/inventory.ts, src/actions/sales.ts, src/components/inventory/receive-form.tsx</files>
  <read_first>
    - src/actions/inventory.ts (createTransfer, confirmTransferSent, confirmTransferReceived, confirmReceive, createWriteOff)
    - src/actions/sales.ts (createSale — только места изменяющие StoreProduct.quantity)
    - src/components/inventory/receive-form.tsx (текущая форма + sellPriceFallback)
    - src/lib/store-product-history.ts
  </read_first>
  <behavior>
    - createTransfer: если sourceStoreProduct не существует → ActionError('PRODUCT_NOT_IN_SOURCE_STORE')
    - confirmReceive: новый StoreProduct создаётся только если sellPrice > 0 передан явно; иначе ошибка
    - createSale / createReturn / confirmReceive / confirmTransferSent / confirmTransferReceived / createWriteOff / completeOrder: каждое изменение StoreProduct.quantity у НЕ-серийного товара сопровождается logQuantityChange
    - Для серийных товаров history не пишется (SerialUnit tracking уже есть)
  </behavior>
  <action>
1. **src/actions/inventory.ts:**

   a) `createTransfer`: перед созданием transfer проверить:
      ```ts
      const sourceSp = await tx.storeProduct.findFirst({
        where: { productId, storeId: sourceStoreId, deletedAt: null }
      })
      if (!sourceSp) throw new ActionError('PRODUCT_NOT_IN_SOURCE_STORE', 'Товар не найден на складе-источнике')
      if (sourceSp.quantity < quantity) throw new ActionError('INSUFFICIENT_STOCK')
      ```

   b) `confirmReceive`: изменить контракт — если создаётся новый StoreProduct, требовать `sellPrice` в input (Zod: `sellPrice: z.number().positive()`). Убрать fallback на costPrice*markup.

   c) Везде где меняется StoreProduct.quantity и товар не-серийный:
      ```ts
      const isSerialized = storeProduct.product.category.isSerialized
      if (!isSerialized) {
        await logQuantityChange(tx, {
          storeProductId: sp.id,
          quantityBefore: oldQty,
          quantityAfter: newQty,
          reason: 'RECEIVE', // или TRANSFER_OUT/TRANSFER_IN/WRITE_OFF соответственно
          userId
        })
      }
      ```

2. **src/actions/sales.ts → createSale:** после декремента StoreProduct.quantity для не-серийного товара вызвать logQuantityChange(reason='SALE'). В `createReturn` — reason='RETURN'.

3. **src/actions/orders.ts → completeOrder** (если существует) — reason='ORDER_COMPLETE'.

4. **src/components/inventory/receive-form.tsx:**
   - Для новых товаров добавить обязательное поле `<Input type="number" name="sellPrice" required min={0.01} />`
   - Валидация react-hook-form: sellPrice.positive()
   - Убрать любой auto-calc через costPrice*markup

5. E2E тесты:
   - INV-05: createTransfer с productId которого нет в source store → ошибка
   - INV-06: confirmReceive нового товара без sellPrice → ошибка; с sellPrice=1500 → StoreProduct.sellPrice === 1500
   - INV-04: createSale не-серийного → StoreProductHistory.count() +1 с reason=SALE
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/e2e/inventory-edge-cases.e2e.test.ts -t "INV-04|INV-05|INV-06" --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Transfer валидация блокирует null sourceSp; Receive требует sellPrice; StoreProductHistory пишется на всех изменениях quantity не-серийных; E2E INV-04/05/06 зелёные</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Trade-In — agreedPrice=0 warning + IN_STOCK/PENDING выбор + single price field (UX2-11)</name>
  <files>src/actions/trade-in.ts, src/components/inventory/trade-in-form.tsx</files>
  <read_first>
    - src/actions/trade-in.ts (createTradeIn, текущая обработка estimatedPrice/agreedPrice)
    - src/components/inventory/trade-in-form.tsx
    - prisma/schema.prisma (TradeIn model)
  </read_first>
  <behavior>
    - createTradeIn: agreedPrice=0 разрешён, но SerialUnit/StoreProduct помечается флагом isFreePickup=true (или через отсутствие CashOperation выплаты)
    - createTradeIn: operator выбирает status из {IN_STOCK, PENDING}; оба варианта проходят
    - trade-in-form: одно поле "Цена выкупа" (agreedPrice), без estimatedPrice
    - trade-in-form: при agreedPrice=0 показывает inline warning "Бесплатный приём — уверены?" (не блокирует submit)
    - Reports: trade-in записи с agreedPrice=0 помечены "Бесплатный приём"
  </behavior>
  <action>
1. **src/actions/trade-in.ts → createTradeIn:**
   - Zod schema: убрать estimatedPrice, оставить `agreedPrice: z.number().min(0)` (разрешить 0)
   - Добавить `status: z.enum(['IN_STOCK', 'PENDING']).default('PENDING')` в input
   - При agreedPrice=0: НЕ создавать CashOperation(WITHDRAW) — просто SerialUnit/StoreProduct с соответствующим статусом
   - Проверить VALID_TRANSITIONS — если нужно, разрешить инициализацию сразу с IN_STOCK (поле initialStatus)

2. **src/components/inventory/trade-in-form.tsx:**
   - Удалить field estimatedPrice
   - Добавить RadioGroup "Статус товара": [PENDING (проверка), IN_STOCK (готов к продаже)]
   - При agreedPrice === 0 показать `<Alert>Бесплатный приём — уверены?</Alert>` (не блок, просто визуально)

3. Unit тесты в trade-in-edge-cases.test.ts:
   - INV-07: createTradeIn({ agreedPrice: 0 }) → успех, CashOperation не создана
   - INV-09: createTradeIn({ status: 'IN_STOCK' }) → SerialUnit.status === 'IN_STOCK'
   - UX2-11: form schema validates (только agreedPrice, без estimatedPrice)

4. Reports: в src/actions/reports.ts (если есть агрегация trade-in) — добавить флаг/колонку "Бесплатный приём" при agreedPrice=0. Если существующий report не трогаем — минимум пометить в карточке trade-in UI.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && pnpm vitest run src/__tests__/trade-in-edge-cases.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>Trade-in с agreedPrice=0 проходит без выплаты; operator выбирает IN_STOCK или PENDING; form содержит только agreedPrice; все unit тесты trade-in-edge-cases зелёные</done>
</task>

</tasks>

<verification>
- `pnpm prisma migrate status` — миграция применена
- `pnpm prisma validate` — схема валидна
- `pnpm vitest run src/__tests__/e2e/inventory-edge-cases.e2e.test.ts src/__tests__/trade-in-edge-cases.test.ts` — все зелёные
- `pnpm test` — полный прогон без регрессий
- Manual: открыть audit, закрыть, посмотреть MISSING серийники в UI
</verification>

<success_criteria>
ROADMAP criterion 1 (Category.isSerialized guard + MISSING/WRITTEN_OFF) выполнен.
ROADMAP criterion 2 (audit recomputed expectedQty + StoreProductHistory) выполнен.
ROADMAP criterion 3 (Receive sellPrice, Transfer null validation, soft-deleted visible) выполнен.
ROADMAP criterion 7 частично (audit с продажами → корректный expectedQty) выполнен.
Все 9 INV-* требований закрыты.
</success_criteria>

<commit_strategy>
- Task 1: `test(16-01): add inventory edge cases test stubs` + `feat(16-01): add StoreProductHistory + MISSING + Sale.idempotencyKey schema`
- Task 2: `feat(16-01): rework closeAudit with MISSING→WRITTEN_OFF and recomputed expectedQty` + `test(16-01): INV-02/03/04/08 E2E`
- Task 3: `feat(16-01): Category.isSerialized guard with admin override` + `test(16-01): INV-01 E2E`
- Task 4: `feat(16-01): Transfer validation + mandatory Receive sellPrice + StoreProductHistory logging` + `test(16-01): INV-04/05/06 E2E`
- Task 5: `feat(16-01): trade-in single price field + agreedPrice=0 warning + IN_STOCK option` + `test(16-01): INV-07/09 UX2-11`
</commit_strategy>

<output>
After completion, create `.planning/phases/16-inventory-edge-cases-ux-polish/16-01-SUMMARY.md` with schema diff, list of modified actions, StoreProductHistory coverage map (which actions log which reasons), and test results.
</output>
