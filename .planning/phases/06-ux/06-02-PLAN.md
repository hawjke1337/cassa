---
phase: 06-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/validations/sales.ts
  - src/actions/sales.ts
  - src/components/pos/payment-dialog.tsx
  - src/components/pos/return-form.tsx
  - src/components/pos/sales-history.tsx
  - src/actions/document-templates.ts
  - src/lib/document-variables.ts
  - src/lib/default-document-templates.ts
  - src/app/(dashboard)/print/return/[id]/page.tsx
autonomous: true
requirements: [UX-03, UX-06, UX-07, UX-08, UX-09]

must_haves:
  truths:
    - "При оплате наличными в чеке и БД фиксируется полученная сумма и сдача"
    - "Двойной клик на Подтвердить оплату не создает дубль продажи"
    - "Продавец может добавить комментарий к продаже"
    - "Возврат можно распечатать как акт возврата"
    - "Продавец видит историю продаж текущей смены в POS"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "cashReceived, changeAmount поля в Sale; RETURN_ACT в DocumentType"
      contains: "cashReceived"
    - path: "src/components/pos/payment-dialog.tsx"
      provides: "cashReceived передается в createSale, кнопка disabled при loading"
      contains: "cashReceived"
    - path: "src/components/pos/sales-history.tsx"
      provides: "Компонент истории продаж текущей смены"
      exports: ["SalesHistory"]
    - path: "src/app/(dashboard)/print/return/[id]/page.tsx"
      provides: "Печатная форма возврата"
  key_links:
    - from: "src/components/pos/payment-dialog.tsx"
      to: "src/actions/sales.ts"
      via: "createSale с cashReceived/changeAmount"
      pattern: "cashReceived"
    - from: "src/actions/document-templates.ts"
      to: "prisma/schema.prisma"
      via: "getDocumentData RETURN_ACT case"
      pattern: "RETURN_ACT"
---

<objective>
POS оплата и продажи: сдача в БД/чеке, защита от двойного клика, комментарий, печать возврата, история продаж.

Purpose: Кассовые операции полноценны -- сдача фиксируется, возврат печатается, история доступна из POS.
Output: Миграция БД, обновленные payment-dialog и sales actions, новые компоненты sales-history и print/return.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux/06-CONTEXT.md
@.planning/phases/06-ux/06-RESEARCH.md

<interfaces>
From src/lib/validations/sales.ts:
```typescript
export const createSaleSchema = z.object({
  storeId: z.string().min(1),
  items: z.array(createSaleItemSchema).min(1),
  payments: z.array(z.object({
    method: z.enum(["CASH", "CARD", "SBP", "TRANSFER", "CREDIT"]),
    amount: z.number().positive(),
  })).min(1),
  comment: z.string().optional(),
})
export type CreateSaleInput = z.infer<typeof createSaleSchema>
```

From src/components/pos/payment-dialog.tsx:

```typescript
// Line 46: const [cashReceived, setCashReceived] = useState("")
// Line 55: const [loading, setLoading] = useState(false)
// Line 62: const cashAmount = Number(cashReceived) || 0
// Line 103: async function handleConfirm() -- calls createSale
// Line 304-318: Button already has disabled={loading || ...} and loading spinner
```

From src/actions/sales.ts:

```typescript
export async function createSale(rawData: CreateSaleInput)
// Line 251: comment: data.comment || null
export async function searchSaleByNumber(number: string)
```

From prisma/schema.prisma Sale model:

```prisma
model Sale {
  id, number, storeId, sellerId, type, status,
  totalAmount Decimal, discountAmount Decimal, finalAmount Decimal,
  comment String?,
  shiftId String?,
  createdAt, updatedAt, items[], payments[], returns[], customOrder?, tradeIns[]
}
enum DocumentType { SALE_RECEIPT, ORDER_FORM, RECEIVE_DOC, WRITE_OFF_DOC, REPAIR_RECEIPT, REPAIR_DELIVERY, TRADE_IN_CONTRACT }
```

From src/actions/document-templates.ts:

```typescript
export async function getDocumentData(type: DocumentType, entityId: string)
// Has cases for each DocumentType -- need to add RETURN_ACT case
```

From src/components/pos/return-form.tsx:

```html
<!-- Line 397-406: native <select> for refundMethod with options CASH, CARD, SBP, TRANSFER, CREDIT -->
```

Print route pattern: src/app/(dashboard)/print/sale/[id]/page.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + sales action + payment dialog (cashReceived, comment, double-click)</name>
  <files>prisma/schema.prisma, src/lib/validations/sales.ts, src/actions/sales.ts, src/components/pos/payment-dialog.tsx</files>
  <read_first>prisma/schema.prisma (Sale model ~line 242), src/lib/validations/sales.ts, src/actions/sales.ts (createSale function), src/components/pos/payment-dialog.tsx</read_first>
  <action>
1. Schema migration (UX-03):
   - В prisma/schema.prisma, модель Sale, добавить ПЕРЕД comment:
     ```
     cashReceived   Decimal?  @db.Decimal(12, 2)
     changeAmount   Decimal?  @db.Decimal(12, 2)
     ```
   - В enum DocumentType добавить RETURN_ACT (для Task 2)
   - Запустить: npx prisma migrate dev --name add-cash-received-and-return-act

2. Zod schema (UX-03):
   - В src/lib/validations/sales.ts добавить в createSaleSchema:
     ```
     cashReceived: z.number().min(0).optional(),
     changeAmount: z.number().min(0).optional(),
     ```

3. createSale action (UX-03):
   - В src/actions/sales.ts, функция createSale:
     - В db.sale.create data добавить: cashReceived: data.cashReceived ?? null, changeAmount: data.changeAmount ?? null
     - В возвращаемый объект добавить cashReceived и changeAmount

4. PaymentDialog обновление (UX-03 + UX-06 + UX-08):
   - cashReceived УЖЕ есть как state (строка 46) -- нужно ПЕРЕДАТЬ в createSale
   - В handleConfirm, при вызове createSale добавить:
     - cashReceived: activeMethod === "CASH" ? cashAmount : undefined
     - changeAmount: activeMethod === "CASH" && cashAmount > finalAmount ? cashAmount - finalAmount : undefined
   - Comment: добавить state const [comment, setComment] = useState("")
     - В JSX: Textarea (из shadcn) перед кнопкой подтверждения, placeholder="Комментарий к продаже (необязательно)"
     - В handleConfirm: передать comment в createSale если не пустой
   - Double-click (UX-08): loading state УЖЕ реализован (строка 55, 129, 304-318). Проверить что:
     - setLoading(true) вызывается ДО async вызова (уже на строке 129)
     - Button disabled={loading || ...} (уже на строке 306)
     - Если все на месте -- UX-08 уже реализован, просто подтвердить
       </action>
       <verify>
       <automated>npx prisma migrate status && npx tsc --noEmit && grep "cashReceived" prisma/schema.prisma src/lib/validations/sales.ts src/actions/sales.ts src/components/pos/payment-dialog.tsx | wc -l</automated>
       </verify>
       <acceptance_criteria>
   - grep "cashReceived" prisma/schema.prisma returns match
   - grep "changeAmount" prisma/schema.prisma returns match
   - grep "RETURN_ACT" prisma/schema.prisma returns match
   - grep "cashReceived" src/lib/validations/sales.ts returns match
   - grep "cashReceived" src/actions/sales.ts returns match
   - grep "comment" src/components/pos/payment-dialog.tsx returns match for Textarea
   - npx tsc --noEmit exits 0
     </acceptance_criteria>
     <done>Sale хранит cashReceived/changeAmount, PaymentDialog передает их в createSale, комментарий доступен, кнопка защищена от двойного клика</done>
     </task>

<task type="auto">
  <name>Task 2: История продаж + печать возврата + native select замена</name>
  <files>src/components/pos/sales-history.tsx, src/actions/sales.ts, src/components/pos/pos-interface.tsx, src/actions/document-templates.ts, src/lib/document-variables.ts, src/lib/default-document-templates.ts, src/app/(dashboard)/print/return/[id]/page.tsx, src/components/pos/return-form.tsx</files>
  <read_first>src/actions/sales.ts (searchSaleByNumber, getSale), src/actions/document-templates.ts (getDocumentData), src/app/(dashboard)/print/sale/[id]/page.tsx (print route pattern), src/components/pos/return-form.tsx (native select at line 397), src/lib/document-variables.ts, src/lib/default-document-templates.ts</read_first>
  <action>
1. Server action для истории продаж (UX-09):
   - В src/actions/sales.ts добавить:
     ```typescript
     export async function getSalesByShift(shiftId: string) {
       // requirePermission("sales.view", ...)
       // db.sale.findMany({ where: { shiftId }, orderBy: { createdAt: "desc" }, take: 20,
       //   include: { items: { include: { product: true } }, payments: true, seller: true } })
       // Вернуть массив с number, createdAt, finalAmount, seller.name, itemCount
     }
     ```

2. Компонент истории продаж (UX-09):
   - Создать src/components/pos/sales-history.tsx:
     - "use client"
     - Использовать shadcn Sheet (side="left") -- не блокирует POS полностью
     - Props: open, onOpenChange, shiftId
     - Загружает getSalesByShift(shiftId) при open
     - Поле поиска по номеру (searchSaleByNumber уже есть с exact match)
     - Список: номер, дата, сумма, продавец
     - Клик на продажу: router.push(`/pos?sale=${sale.id}`) или открыть деталь в модале

3. Подключить в pos-interface.tsx (UX-09):
   - Добавить кнопку "История" (иконка History из lucide-react) рядом с другими action-кнопками
   - State: const [historyOpen, setHistoryOpen] = useState(false)
   - Передать shiftId из текущей смены (если доступен)
   - <SalesHistory open={historyOpen} onOpenChange={setHistoryOpen} shiftId={currentShiftId} />

4. Печатная форма возврата (UX-07):
   - В src/actions/document-templates.ts, функция getDocumentData:
     - Добавить case "RETURN_ACT": загрузить Return с items, sale, store, seller
     - Вернуть переменные: returnNumber, returnDate, saleNumber, items (name, qty, price), totalAmount, sellerName, storeName
   - В src/lib/document-variables.ts: добавить RETURN_ACT переменные (по аналогии с SALE_RECEIPT)
   - В src/lib/default-document-templates.ts: добавить дефолтный шаблон RETURN_ACT
   - Создать src/app/(dashboard)/print/return/[id]/page.tsx:
     - По аналогии с src/app/(dashboard)/print/sale/[id]/page.tsx
     - getDocumentData("RETURN_ACT", params.id)
     - DocumentRenderer с данными

5. Native select замена (UX-15):
   - src/components/pos/return-form.tsx строка 397-406:
     - Заменить <select>/<option> на shadcn <Select>/<SelectTrigger>/<SelectValue>/<SelectContent>/<SelectItem>
     - value={refundMethod} onValueChange={setRefundMethod}
     - Опции: CASH (Наличные), CARD (Карта), SBP (СБП), TRANSFER (Перевод), CREDIT (Рассрочка)
       </action>
       <verify>
       <automated>npx tsc --noEmit && test -f src/components/pos/sales-history.tsx && test -f src/app/\(dashboard\)/print/return/\[id\]/page.tsx && grep -c "SelectTrigger" src/components/pos/return-form.tsx</automated>
       </verify>
       <acceptance_criteria>
   - test -f src/components/pos/sales-history.tsx (file exists)
   - test -f "src/app/(dashboard)/print/return/[id]/page.tsx" (file exists)
   - grep "getSalesByShift" src/actions/sales.ts returns match
   - grep "RETURN_ACT" src/actions/document-templates.ts returns match
   - grep "SelectTrigger" src/components/pos/return-form.tsx returns match (no more native select)
   - grep -c "<select" src/components/pos/return-form.tsx returns 0 (native select removed)
   - npx tsc --noEmit exits 0
     </acceptance_criteria>
     <done>История продаж доступна через Sheet в POS, возврат можно распечатать через /print/return/[id], native select заменен на shadcn Select в return-form</done>
     </task>

</tasks>

<verification>
npx tsc --noEmit && npx vitest run --reporter=verbose
</verification>

<success_criteria>

- cashReceived и changeAmount сохраняются в Sale при оплате наличными
- Комментарий к продаже передается из PaymentDialog в createSale
- Кнопка "Подтвердить" disabled при loading (двойной клик невозможен)
- История продаж текущей смены отображается в Sheet (последние 20)
- Акт возврата печатается через /print/return/[id]
- Native select в return-form заменен на shadcn Select
- TypeScript компилируется без ошибок
  </success_criteria>

<output>
After completion, create `.planning/phases/06-ux/06-02-SUMMARY.md`
</output>
