---
phase: 06-ux
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - src/components/dashboard/dashboard-content.tsx
  - src/actions/dashboard.ts
  - src/components/layout/header.tsx
  - src/lib/format.ts
  - src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx
  - src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx
  - src/__tests__/format-duration.test.ts
  - src/__tests__/dashboard-metrics.test.ts
autonomous: true
requirements: [UX-10, UX-11, UX-12, UX-13, UX-14, UX-15]

must_haves:
  truths:
    - "Дашборд показывает прибыль/маржу за сегодня"
    - "Дашборд показывает количество готовых ремонтов"
    - "Время смены отображается в формате Xч Yм"
    - "Все основные маршруты имеют breadcrumbs"
    - "Ошибка загрузки дашборда показывается через toast, а не молча проглатывается"
    - "Native select в trade-in-detail заменен на shadcn Select"
  artifacts:
    - path: "src/components/dashboard/dashboard-content.tsx"
      provides: "Карточки прибыли, маржи, готовых ремонтов; toast при ошибке"
      contains: "readyRepairsCount"
    - path: "src/lib/format.ts"
      provides: "formatDuration утилита"
      exports: ["formatDuration"]
    - path: "src/components/layout/header.tsx"
      provides: "Breadcrumbs для всех 14+ маршрутов"
      contains: "pageTitles"
    - path: "src/__tests__/dashboard-metrics.test.ts"
      provides: "Тесты для profit/margin логики дашборда"
  key_links:
    - from: "src/components/dashboard/dashboard-content.tsx"
      to: "src/actions/dashboard.ts"
      via: "getDashboardData (readyRepairsCount уже возвращается)"
      pattern: "readyRepairsCount"
    - from: "src/components/dashboard/dashboard-content.tsx"
      to: "src/actions/reports.ts"
      via: "getProfitReport для прибыли/маржи"
      pattern: "getProfitReport"
    - from: "src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx"
      to: "src/lib/format.ts"
      via: "import formatDuration"
      pattern: "formatDuration"
---

<objective>
Дашборд и навигация: прибыль/маржа, готовые ремонты, время смены, breadcrumbs, toast ошибки, shadcn Select.

Purpose: Владелец видит ключевые метрики (прибыль, ремонты); продавец видит breadcrumbs на всех страницах; UI консистентен (shadcn Select вместо native).
Output: Обновленные dashboard-content.tsx, header.tsx, format.ts, shift-detail-client.tsx, trade-in-detail-client.tsx, новые тесты.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux/06-CONTEXT.md
@.planning/phases/06-ux/06-RESEARCH.md

<interfaces>
From src/components/dashboard/dashboard-content.tsx:
```typescript
import { StatCard } from "./stat-card"
export function DashboardContent() {
  // useEffect fetches getDashboardData(currentStoreId)
  // Currently 5 StatCards: sales count, revenue, avg check, orders count, active repairs
  // readyRepairsCount is returned by getDashboardData but NOT displayed
}
```

From src/actions/dashboard.ts:

```typescript
export async function getDashboardData(storeId: string) {
  // Line 79: const readyRepairsCount = await db.repair.count({ where: { status: "READY", storeId } })
  // Line 176: returns { ..., readyRepairsCount }
}
```

From src/actions/reports.ts:

```typescript
export async function getProfitReport(params: { storeId, dateFrom, dateTo, ... })
// Returns SQL-optimized profit data
```

From src/components/layout/header.tsx:

```typescript
const pageTitles: Record<string, string> = {
  "/": "Главная",
  "/pos": "Касса",
  "/catalog": "Каталог",
  "/inventory": "Склад",
  "/orders": "Заказы",
  "/suppliers": "Поставщики",
  "/reports": "Отчёты",
  "/settings": "Настройки",
}
// Missing: /customers, /repairs, /shifts, /trade-in, /warranty, /motivation
```

From src/lib/format.ts:

```typescript
export function formatMoney(amount: number): string
export function formatDate(date: Date | string): string
// formatDuration does NOT exist yet
```

From src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx:

```typescript
// Line 150: inline duration calculation:
// `${Math.round((new Date(shift.closedAt).getTime() - new Date(shift.openedAt).getTime()) / 60000)} мин`
// This is the exact location where formatDuration should be applied
```

From src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx:

```html
<!-- Line 590-598: native <select> for category selection -->
```

From src/components/dashboard/stat-card.tsx:

```typescript
// StatCard component -- takes title, value, icon, description props
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatDuration + dashboard-metrics тесты + breadcrumbs + native select замена</name>
  <files>src/lib/format.ts, src/__tests__/format-duration.test.ts, src/__tests__/dashboard-metrics.test.ts, src/components/layout/header.tsx, src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx</files>
  <read_first>src/lib/format.ts, src/components/layout/header.tsx, src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx (around line 590), src/actions/dashboard.ts</read_first>
  <behavior>
    - formatDuration(0) === "0м"
    - formatDuration(45) === "45м"
    - formatDuration(60) === "1ч"
    - formatDuration(90) === "1ч 30м"
    - formatDuration(125) === "2ч 5м"
    - Dashboard metrics: getDashboardData returns profit and margin fields (stub test)
  </behavior>
  <action>
1. formatDuration (UX-14):
   - В src/lib/format.ts добавить:
     ```typescript
     export function formatDuration(minutes: number): string {
       if (minutes < 60) return `${minutes}м`
       const h = Math.floor(minutes / 60)
       const m = minutes % 60
       return m > 0 ? `${h}ч ${m}м` : `${h}ч`
     }
     ```
   - Тест ПЕРВЫМ: src/__tests__/format-duration.test.ts с 5 случаями из behavior

2. Dashboard metrics тест scaffold (Wave 0 для UX-10):
   - Создать src/**tests**/dashboard-metrics.test.ts:
     - Тест: profit calculation (revenue - cost = profit)
     - Тест: margin calculation (profit / revenue \* 100), edge case revenue === 0 -> margin 0
     - Тесты могут быть unit-тестами для вспомогательной функции calculateMargin
       которую Task 2 создаст в dashboard-content.tsx или actions/dashboard.ts
     - Пометить skip если функция еще не существует: it.skip или предусмотреть import

3. Breadcrumbs (UX-11):
   - В src/components/layout/header.tsx расширить pageTitles:
     ```typescript
     const pageTitles: Record<string, string> = {
       "/": "Главная",
       "/pos": "Касса",
       "/catalog": "Каталог",
       "/inventory": "Склад",
       "/orders": "Заказы",
       "/suppliers": "Поставщики",
       "/customers": "Клиенты",
       "/repairs": "Ремонты",
       "/shifts": "Смены",
       "/trade-in": "Trade-in",
       "/warranty": "Гарантия",
       "/motivation": "Мотивация",
       "/reports": "Отчёты",
       "/settings": "Настройки",
       "/my": "Профиль",
     }
     ```
   - getPageTitle уже обрабатывает вложенные routes через first segment match -- проверить что работает для /repairs/[id], /orders/[id] и т.д.

4. Native select замена в trade-in-detail (UX-15):
   - src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx строки 590-598:
     - Заменить <select>/<option> на shadcn <Select>/<SelectTrigger>/<SelectValue>/<SelectContent>/<SelectItem>
     - Сохранить существующую логику value/onChange (перевести на onValueChange)
     - Импортировать Select компоненты из "@/components/ui/select"
       </action>
       <verify>
       <automated>npx vitest run src/**tests**/format-duration.test.ts --reporter=verbose && npx tsc --noEmit</automated>
       </verify>
       <acceptance_criteria>

- grep "formatDuration" src/lib/format.ts returns match
- test -f src/**tests**/dashboard-metrics.test.ts (Wave 0 scaffold exists)
- grep "/customers" src/components/layout/header.tsx returns match
- grep "/repairs" src/components/layout/header.tsx returns match
- grep "/shifts" src/components/layout/header.tsx returns match
- grep "/trade-in" src/components/layout/header.tsx returns match
- grep "/warranty" src/components/layout/header.tsx returns match
- grep "/motivation" src/components/layout/header.tsx returns match
- grep "SelectTrigger" src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx returns match
- grep -c "<select" src/app/(dashboard)/trade-in/[id]/trade-in-detail-client.tsx returns 0
- npx vitest run src/**tests**/format-duration.test.ts exits 0
- npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>formatDuration работает корректно с тестами, dashboard-metrics.test.ts scaffold создан, breadcrumbs покрывают все 14+ маршрутов, native select заменен в trade-in-detail</done>
  </task>

<task type="auto">
  <name>Task 2: Dashboard -- прибыль/маржа, готовые ремонты, toast ошибки, время смены</name>
  <files>src/components/dashboard/dashboard-content.tsx, src/actions/dashboard.ts, src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx</files>
  <read_first>src/components/dashboard/dashboard-content.tsx, src/actions/dashboard.ts, src/actions/reports.ts (getProfitReport signature), src/components/dashboard/stat-card.tsx, src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx (around line 150)</read_first>
  <action>
1. Dashboard action расширение (UX-10):
   - В src/actions/dashboard.ts, функция getDashboardData:
     - Добавить вызов getProfitReport с dateFrom=начало сегодня, dateTo=конец сегодня, storeId
     - Вернуть profit (number) и margin (number, %) в объекте результата
     - Если getProfitReport уже вызывается -- проверить и использовать
     - Margin = (profit / revenue) * 100, или 0 если revenue === 0

2. Dashboard UI обновление (UX-10 + UX-12 + UX-13 + UX-14):
   - В src/components/dashboard/dashboard-content.tsx:

   a) Прибыль/маржа (UX-10):
   - Добавить StatCard "Прибыль" с иконкой TrendingUp из lucide-react
   - Значение: formatMoney(data.profit)
   - Description: `Маржа ${data.margin.toFixed(1)}%`
   - Цвет: зелёный текст если profit > 0, красный если < 0

   b) Готовые ремонты (UX-13):
   - Добавить StatCard "Готовые ремонты" с иконкой Wrench из lucide-react
   - Значение: data.readyRepairsCount (данные УЖЕ возвращаются из getDashboardData, line 176)
   - onClick или Link: /repairs?status=READY

   c) Время смены (UX-14):
   - В src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx, строка 150:
     - БЫЛО: `${Math.round((new Date(shift.closedAt).getTime() - new Date(shift.openedAt).getTime()) / 60000)} мин`
     - СТАЛО: `formatDuration(Math.round((new Date(shift.closedAt).getTime() - new Date(shift.openedAt).getTime()) / 60000))`
     - Добавить import { formatDuration } from "@/lib/format"

   d) Toast при ошибке (UX-12):
   - Обернуть getDashboardData вызов в try/catch
   - При ошибке: toast.error("Ошибка загрузки данных дашборда")
   - НЕ сбрасывать data в null -- показывать частичные данные если были
   - Import toast from "sonner"

   e) Layout карточек:
   - Первый ряд (4 карточки): Продажи, Выручка, Ср.чек, Прибыль
   - Второй ряд (3 карточки): Заказы, Ремонты в работе, Готовые ремонты
   - Grid: lg:grid-cols-4 для первого ряда

3. Активировать dashboard-metrics тесты:
   - Обновить src/**tests**/dashboard-metrics.test.ts (из Task 1):
     - Убрать skip, импортировать реальную функцию расчета margin
     - Убедиться что тесты проходят
       </action>
       <verify>
       <automated>npx tsc --noEmit && grep -q "readyRepairsCount" src/components/dashboard/dashboard-content.tsx && grep -q "profit" src/components/dashboard/dashboard-content.tsx && grep -q "formatDuration" src/app/\(dashboard\)/shifts/\[id\]/shift-detail-client.tsx && grep -q "toast.error" src/components/dashboard/dashboard-content.tsx && echo "PASS"</automated>
       </verify>
       <acceptance_criteria>

- grep "profit" src/components/dashboard/dashboard-content.tsx returns match
- grep "margin" src/components/dashboard/dashboard-content.tsx returns match
- grep "readyRepairsCount" src/components/dashboard/dashboard-content.tsx returns match (StatCard using it)
- grep "toast.error" src/components/dashboard/dashboard-content.tsx returns match
- grep "formatDuration" src/app/(dashboard)/shifts/[id]/shift-detail-client.tsx returns match
- npx vitest run src/**tests**/dashboard-metrics.test.ts exits 0
- npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Дашборд показывает прибыль/маржу, количество готовых ремонтов, время смены в формате Xч Yм в shift-detail, ошибки через toast, тесты dashboard-metrics проходят</done>
  </task>

</tasks>

<verification>
npx tsc --noEmit && npx vitest run --reporter=verbose
</verification>

<success_criteria>

- Дашборд показывает прибыль за сегодня с маржой в процентах
- Дашборд показывает количество готовых ремонтов с ссылкой на /repairs?status=READY
- Время смены в shift-detail-client.tsx в формате "Xч Yм" вместо "N мин"
- Breadcrumbs покрывают все 14+ маршрутов (customers, repairs, shifts, trade-in, warranty, motivation, my)
- Ошибка загрузки дашборда показывается через toast
- Native select в trade-in-detail заменен на shadcn Select
- formatDuration проходит все тесты
- dashboard-metrics тесты проходят
- TypeScript компилируется без ошибок
  </success_criteria>

<output>
After completion, create `.planning/phases/06-ux/06-03-SUMMARY.md`
</output>
