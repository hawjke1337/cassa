---
phase: 06-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-cart.ts
  - src/components/pos/pos-interface.tsx
  - src/lib/barcode.ts
  - src/app/(dashboard)/inventory/stock-overview-client.tsx
  - src/__tests__/barcode-ean.test.ts
  - src/__tests__/cart-persist.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-04, UX-05]

must_haves:
  truths:
    - "Поиск в POS не лагает при быстром вводе (debounce >= 300ms)"
    - "Поиск на складе имеет debounce >= 300ms"
    - "Корзина POS сохраняется при обновлении страницы"
    - "Escape в открытом PaymentDialog не очищает корзину"
    - "Сканирование EAN-13 штрихкода автоматически добавляет товар в корзину"
  artifacts:
    - path: "src/hooks/use-cart.ts"
      provides: "Zustand persist middleware для корзины"
      contains: "persist"
    - path: "src/lib/barcode.ts"
      provides: "EAN-13 валидация (checksum)"
      exports: ["isValidEAN13"]
    - path: "src/components/pos/pos-interface.tsx"
      provides: "Escape guard, EAN-13 handler"
      contains: "isValidEAN13"
    - path: "src/app/(dashboard)/inventory/stock-overview-client.tsx"
      provides: "Debounce на поиске склада"
      contains: "setTimeout"
  key_links:
    - from: "src/hooks/use-cart.ts"
      to: "localStorage"
      via: "Zustand persist middleware"
      pattern: "persist.*astore-pos-cart"
    - from: "src/components/pos/pos-interface.tsx"
      to: "src/lib/barcode.ts"
      via: "import isValidEAN13"
      pattern: "isValidEAN13"
---

<objective>
POS UX: debounce проверка (POS + склад), персистентная корзина, Escape fix, EAN-13 штрихкод.

Purpose: Продавец работает без лагов, корзина не теряется при обновлении, Escape не создает конфликтов, штрихкод-сканер работает с EAN-13.
Output: Обновленные use-cart.ts, pos-interface.tsx, stock-overview-client.tsx, новый barcode.ts с тестами.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux/06-CONTEXT.md
@.planning/phases/06-ux/06-RESEARCH.md

<interfaces>
<!-- Executor needs these existing interfaces -->

From src/hooks/use-cart.ts:

```typescript
export interface CartItem {
  productId: string; name: string; sku: string; price: number;
  costPrice: number; quantity: number; discount: number;
  maxStock: number; total: number;
  serialUnitId?: string | null; imei?: string | null;
}

interface CartState {
  items: CartItem[]
  addItem: (product: Omit<CartItem, "quantity" | "discount" | "total">) => void
  removeItem: (productId: string, serialUnitId?: string | null) => void
  updateQuantity: (productId: string, quantity: number, serialUnitId?: string | null) => void
  applyDiscount: (productId: string, discount: number, serialUnitId?: string | null) => void
  clearCart: () => void
  getTotal: () => number; getDiscountTotal: () => number; getItemCount: () => number
}

export const useCart = create<CartState>()((set, get) => ({ ... }))
```

From src/components/pos/pos-interface.tsx (key state):

```typescript
const [paymentOpen, setPaymentOpen] = useState(false)
const [clearConfirmOpen, setClearConfirmOpen] = useState(false)
const [serialPickerProduct, setSerialPickerProduct] = useState<PosProduct | null>(null)

// handleKeyDown at line 136 -- Escape at line 149 opens clearConfirmOpen WITHOUT checking dialog states
// debounceRef at line 74 -- already uses setTimeout pattern for search
```

From src/app/(dashboard)/inventory/stock-overview-client.tsx:

```typescript
// Line 66: const [search, setSearch] = useState("")
// Line 80: search: search || undefined  -- passed directly to server action, NO debounce
// Line 92: useEffect depends on [currentStoreId, search, categoryId, page]
// Line 197: value={search} onChange={(e) => setSearch(e.target.value)}
```

From src/actions/sales.ts:

```typescript
export async function searchPosProducts(storeId: string, search: string)
// Already searches by barcode field (line 25): { barcode: { contains: search, mode: "insensitive" } }
export async function searchByImeiForPos(storeId: string, imei: string)
```

From prisma/schema.prisma:

```prisma
model Product {
  barcode     String?  @unique
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EAN-13 валидация + Zustand persist + тесты</name>
  <files>src/lib/barcode.ts, src/hooks/use-cart.ts, src/__tests__/barcode-ean.test.ts, src/__tests__/cart-persist.test.ts</files>
  <read_first>src/hooks/use-cart.ts, src/lib/imei-utils.ts</read_first>
  <behavior>
    - isValidEAN13("4006381333931") === true (valid EAN-13)
    - isValidEAN13("4006381333930") === false (wrong checksum)
    - isValidEAN13("123456789012") === false (12 digits)
    - isValidEAN13("12345678901234") === false (14 digits)
    - isValidEAN13("abcdefghijklm") === false (non-numeric)
    - Cart persist: useCart.persist.getOptions().name === "astore-pos-cart"
    - Cart persist: storage is localStorage (default)
  </behavior>
  <action>
1. Создать src/lib/barcode.ts:
   - export function isValidEAN13(code: string): boolean
   - Проверка: ровно 13 цифр (/^\d{13}$/)
   - Checksum: sum of digits[i] * (i % 2 === 0 ? 1 : 3), result % 10 === 0
   - Это чистая функция без side effects

2. Обновить src/hooks/use-cart.ts:
   - Добавить import { persist } from "zustand/middleware"
   - Обернуть create в persist middleware: create<CartState>()(persist((set, get) => ({ ...existing... }), { name: "astore-pos-cart", version: 1 }))
   - НЕ менять ни одну существующую функцию (addItem, removeItem, и т.д.)
   - НЕ добавлять sync между вкладками

3. Написать тесты ПЕРВЫМИ (TDD):
   - src/**tests**/barcode-ean.test.ts: тесты isValidEAN13 (6 случаев из behavior)
   - src/**tests**/cart-persist.test.ts: проверить что persist настроен (name, version)
     </action>
     <verify>
     <automated>npx vitest run src/**tests**/barcode-ean.test.ts src/**tests**/cart-persist.test.ts --reporter=verbose</automated>
     </verify>
     <acceptance_criteria>

- grep "isValidEAN13" src/lib/barcode.ts returns match
- grep "persist" src/hooks/use-cart.ts returns match
- grep "astore-pos-cart" src/hooks/use-cart.ts returns match
- npx vitest run src/**tests**/barcode-ean.test.ts exits 0
- npx vitest run src/**tests**/cart-persist.test.ts exits 0
  </acceptance_criteria>
  <done>isValidEAN13 проходит 6+ тест-кейсов, корзина использует Zustand persist с ключом "astore-pos-cart"</done>
  </task>

<task type="auto">
  <name>Task 2: Escape guard + EAN-13 auto-add + debounce проверка (POS + склад)</name>
  <files>src/components/pos/pos-interface.tsx, src/app/(dashboard)/inventory/stock-overview-client.tsx</files>
  <read_first>src/components/pos/pos-interface.tsx, src/lib/barcode.ts, src/actions/sales.ts, src/app/(dashboard)/inventory/stock-overview-client.tsx</read_first>
  <action>
1. Escape guard (UX-04):
   В handleKeyDown (строка 136), блок "Escape" (строка 149):
   БЫЛО:
   ```
   } else if (e.key === "Escape") {
     e.preventDefault()
     if (items.length > 0) {
       setClearConfirmOpen(true)
     }
   }
   ```
   СТАЛО:
   ```
   } else if (e.key === "Escape") {
     e.preventDefault()
     // Guard: не очищать корзину если открыт диалог
     if (paymentOpen || serialPickerProduct || clearConfirmOpen) return
     if (items.length > 0) {
       setClearConfirmOpen(true)
     }
   }
   ```
   Добавить paymentOpen, serialPickerProduct, clearConfirmOpen в deps useCallback.

2. EAN-13 handler (UX-05):
   - Добавить import { isValidEAN13 } from "@/lib/barcode"
   - В обработчике поиска (рядом с существующей IMEI-логикой), ПЕРЕД текстовым поиском:
     - Если search.trim() длиной 13 цифр и isValidEAN13(search.trim()):
       - Вызвать searchPosProducts(currentStoreId, search.trim())
       - Если найден ровно 1 результат с barcode === search.trim(): auto-add в корзину через handleAddProduct
       - toast.success(`${product.name} добавлен по штрихкоду`)
       - setSearch("")
       - return (не продолжать обычный поиск)

3. Debounce проверка POS (UX-01):
   - pos-interface.tsx уже имеет debounceRef (строка 74) с setTimeout (строка 94)
   - Проверить что timeout >= 300ms. Если < 300ms -- увеличить до 300ms
   - НЕ устанавливать новые пакеты (use-debounce не нужен)

4. Debounce на складе (UX-01):
   - В src/app/(dashboard)/inventory/stock-overview-client.tsx поиск НЕ имеет debounce:
     - search state (строка 66) напрямую передается в useEffect (строка 92)
   - Добавить debounce паттерн аналогично POS:
     - Добавить const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null)
     - Добавить const [debouncedSearch, setDebouncedSearch] = useState("")
     - В onChange поля поиска: setSearch(value) для UI, debounce 300ms для setDebouncedSearch
     - В useEffect (строка 80) заменить search на debouncedSearch
   - Заказы (orders-page-client.tsx): страница НЕ имеет поля поиска вообще -- debounce не применим
     </action>
     <verify>
     <automated>npx tsc --noEmit && grep -q "paymentOpen" src/components/pos/pos-interface.tsx && grep -q "isValidEAN13" src/components/pos/pos-interface.tsx && grep -q "debounce\|setTimeout\|debouncedSearch" src/app/\(dashboard\)/inventory/stock-overview-client.tsx && echo "PASS"</automated>
     </verify>
     <acceptance_criteria>

- grep -q "isValidEAN13" src/components/pos/pos-interface.tsx (EAN-13 handler present)
- grep -q "paymentOpen" in Escape block of handleKeyDown (guard present)
- grep -q "debouncedSearch" src/app/(dashboard)/inventory/stock-overview-client.tsx (debounce present)
- npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Escape не очищает корзину при открытом диалоге; EAN-13 автоматически добавляет товар; debounce >= 300ms в POS и на складе; заказы не имеют поля поиска (debounce не применим)</done>
  </task>

</tasks>

<verification>
npx tsc --noEmit && npx vitest run src/__tests__/barcode-ean.test.ts src/__tests__/cart-persist.test.ts --reporter=verbose
</verification>

<success_criteria>

- Корзина сохраняется в localStorage под ключом "astore-pos-cart"
- isValidEAN13 корректно валидирует контрольную цифру
- Escape в открытом PaymentDialog не вызывает setClearConfirmOpen
- EAN-13 штрихкод автоматически добавляет товар в корзину
- Debounce поиска >= 300ms в POS и на складе
- Заказы не имеют поля поиска (подтверждено grep)
- TypeScript компилируется без ошибок
  </success_criteria>

<output>
After completion, create `.planning/phases/06-ux/06-01-SUMMARY.md`
</output>
