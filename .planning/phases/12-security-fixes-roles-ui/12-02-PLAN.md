---
phase: 12-security-fixes-roles-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/audit.ts
  - src/actions/audit.ts
  - src/actions/roles.ts
  - src/app/(dashboard)/settings/audit-log/page.tsx
  - src/components/settings/audit-log-table.tsx
  - src/components/settings/inline-audit-history.tsx
  - src/__tests__/e2e/audit-log.e2e.test.ts
autonomous: true
requirements:
  - SEC2-10

must_haves:
  truths:
    - "AuditLog записи создаются при изменении ролей и привилегий"
    - "Владелец видит структурированный лог на /settings/audit-log"
    - "Лог фильтруется по дате, типу действия, пользователю, типу сущности"
    - "Настройка retention периода доступна в UI"
    - "Inline audit history отображается на страницах ролей и пользователей"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "AuditLog model with indexes"
      contains: "model AuditLog"
    - path: "src/lib/audit.ts"
      provides: "createAuditEntry helper function"
      exports: ["createAuditEntry", "getAuditLogs"]
    - path: "src/actions/audit.ts"
      provides: "Server actions for audit log page"
      exports: ["fetchAuditLogs", "runAuditCleanup", "fetchEntityAuditLogs"]
    - path: "src/app/(dashboard)/settings/audit-log/page.tsx"
      provides: "Audit log settings page"
      min_lines: 30
    - path: "src/components/settings/audit-log-table.tsx"
      provides: "Filterable audit log table component"
      min_lines: 50
    - path: "src/components/settings/inline-audit-history.tsx"
      provides: "Reusable inline audit history component for entity pages"
      min_lines: 30
  key_links:
    - from: "src/lib/audit.ts"
      to: "prisma/schema.prisma"
      via: "db.auditLog.create"
      pattern: "auditLog\\.create"
    - from: "src/app/(dashboard)/settings/audit-log/page.tsx"
      to: "src/components/settings/audit-log-table.tsx"
      via: "component import"
      pattern: "AuditLogTable"
    - from: "src/components/settings/inline-audit-history.tsx"
      to: "src/actions/audit.ts"
      via: "fetchEntityAuditLogs server action"
      pattern: "fetchEntityAuditLogs"
---

<objective>
AuditLog infrastructure: Prisma model, helper function, dedicated UI page with filters, inline audit history on entity pages.

Purpose: Implement SEC2-10 -- structured audit log for all entity changes, with dedicated /settings/audit-log page filterable by date, action, user, entity. Owner-only access. Plus inline audit history on relevant entity pages (roles, users) per user decision.
Output: AuditLog model in schema, createAuditEntry helper, audit log page with table and filters, inline audit history component, E2E test.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-security-fixes-roles-ui/12-CONTEXT.md
@.planning/phases/12-security-fixes-roles-ui/12-RESEARCH.md

<interfaces>
From prisma/schema.prisma (existing models):
```prisma
model User {
  id        String   @id @default(cuid())
  // ... existing fields
}
model Store {
  id        String   @id @default(cuid())
  // ... existing fields
}
```

From src/lib/db.ts:

```typescript
export const db: ExtendedPrismaClient
// Usage: db.auditLog.create({ data: { ... } })
```

From src/app/(dashboard)/settings/layout.tsx:

```typescript
// Settings layout with sidebar navigation
// New routes need to be added to sidebar nav items
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditLog Prisma model + createAuditEntry helper + migration</name>
  <files>
    prisma/schema.prisma,
    src/lib/audit.ts
  </files>
  <read_first>
    prisma/schema.prisma,
    src/lib/db.ts,
    src/lib/permissions.ts
  </read_first>
  <action>
**A. prisma/schema.prisma -- Add AuditLog model**

Add at the end of the schema file, before any closing comments:

```prisma
model AuditLog {
  id        String   @id @default(cuid())
  action    String   // CREATE, UPDATE, DELETE, ROLE_CHANGE, PERMISSION_CHANGE
  entity    String   // Sale, User, Role, Customer, Store, etc.
  entityId  String
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  storeId   String?
  store     Store?   @relation(fields: [storeId], references: [id])
  changes   Json?    // { field: { old: value, new: value } }
  metadata  Json?    // additional context (IP, user agent, etc.)
  createdAt DateTime @default(now())

  @@index([entity, entityId])
  @@index([userId])
  @@index([storeId])
  @@index([createdAt])
  @@index([action])
}
```

Also add the reverse relation to User model:

```prisma
auditLogs AuditLog[]
```

And to Store model:

```prisma
auditLogs AuditLog[]
```

Run migration:

```bash
npx prisma db push
```

**B. src/lib/audit.ts -- Create audit entry helper**

```typescript
import { db } from "@/lib/db"
import type { Prisma } from "@/generated/prisma/client"

type PrismaTransaction = Parameters<Parameters<typeof db.$transaction>[0]>[0]

export type AuditAction = "CREATE" | "UPDATE" | "DELETE" | "ROLE_CHANGE" | "PERMISSION_CHANGE"

export interface AuditEntryParams {
  action: AuditAction
  entity: string
  entityId: string
  userId: string
  storeId?: string | null
  changes?: Record<string, { old: unknown; new: unknown }>
  metadata?: Record<string, unknown>
  tx?: PrismaTransaction
}

export async function createAuditEntry(params: AuditEntryParams): Promise<void> {
  const client = params.tx ?? db
  await (client as any).auditLog.create({
    data: {
      action: params.action,
      entity: params.entity,
      entityId: params.entityId,
      userId: params.userId,
      storeId: params.storeId ?? undefined,
      changes: params.changes ?? (Prisma.JsonNull as any),
      metadata: params.metadata ?? (Prisma.JsonNull as any),
    },
  })
}

export async function getAuditLogs(filters: {
  entity?: string
  entityId?: string
  action?: string
  userId?: string
  storeId?: string
  dateFrom?: Date
  dateTo?: Date
  page?: number
  pageSize?: number
}) {
  const where: any = {}

  if (filters.entity) where.entity = filters.entity
  if (filters.entityId) where.entityId = filters.entityId
  if (filters.action) where.action = filters.action
  if (filters.userId) where.userId = filters.userId
  if (filters.storeId) where.storeId = filters.storeId
  if (filters.dateFrom || filters.dateTo) {
    where.createdAt = {}
    if (filters.dateFrom) where.createdAt.gte = filters.dateFrom
    if (filters.dateTo) where.createdAt.lte = filters.dateTo
  }

  const page = filters.page ?? 1
  const pageSize = filters.pageSize ?? 50

  const [items, total] = await Promise.all([
    db.auditLog.findMany({
      where,
      include: { user: { select: { id: true, name: true, email: true } } },
      orderBy: { createdAt: "desc" },
      take: pageSize,
      skip: (page - 1) * pageSize,
    }),
    db.auditLog.count({ where }),
  ])

  return { items, total, page, pageSize, totalPages: Math.ceil(total / pageSize) }
}

export async function cleanupAuditLogs(retentionDays: number): Promise<number> {
  const cutoff = new Date()
  cutoff.setDate(cutoff.getDate() - retentionDays)

  const result = await db.auditLog.deleteMany({
    where: { createdAt: { lt: cutoff } },
  })

  return result.count
}
```

  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma db push --accept-data-loss 2>&1 | tail -10 && npx tsc --noEmit --pretty 2>&1 | tail -10 && echo "--- GREP CHECKS ---" && grep -c "model AuditLog" prisma/schema.prisma && grep -c "createAuditEntry" src/lib/audit.ts && grep -c "getAuditLogs" src/lib/audit.ts && grep -c "cleanupAuditLogs" src/lib/audit.ts</automated>
  </verify>
  <done>
    AuditLog model exists in schema with 5 indexes, migration applied. createAuditEntry helper supports both standalone and transactional calls. getAuditLogs supports filtering + pagination. cleanupAuditLogs supports retention cleanup. TypeScript compiles. Grep checks confirm all functions present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit log UI page + inline audit history on entity pages + E2E test</name>
  <files>
    src/actions/audit.ts,
    src/app/(dashboard)/settings/audit-log/page.tsx,
    src/components/settings/audit-log-table.tsx,
    src/components/settings/inline-audit-history.tsx,
    src/__tests__/e2e/audit-log.e2e.test.ts
  </files>
  <read_first>
    src/lib/audit.ts,
    src/app/(dashboard)/settings/users/page.tsx,
    src/app/(dashboard)/settings/layout.tsx,
    src/app/(dashboard)/settings/users/[id]/page.tsx,
    src/lib/permissions-list.ts
  </read_first>
  <action>
**A. src/actions/audit.ts -- Server actions for audit log page + inline history**

```typescript
"use server"
import { auth } from "@/lib/auth"
import { requirePermission } from "@/lib/permissions"
import { getAuditLogs, cleanupAuditLogs } from "@/lib/audit"

export async function fetchAuditLogs(filters: {
  entity?: string
  action?: string
  userId?: string
  storeId?: string
  dateFrom?: string // ISO string from client
  dateTo?: string
  page?: number
}) {
  const session = await auth()
  if (!session?.user?.id) throw new Error("Не авторизован")

  // Owner-only access to full audit log
  await requirePermission("settings.manage")

  return getAuditLogs({
    ...filters,
    dateFrom: filters.dateFrom ? new Date(filters.dateFrom) : undefined,
    dateTo: filters.dateTo ? new Date(filters.dateTo) : undefined,
  })
}

export async function fetchEntityAuditLogs(params: {
  entity: string
  entityId: string
  page?: number
  pageSize?: number
}) {
  const session = await auth()
  if (!session?.user?.id) throw new Error("Не авторизован")

  // Any authenticated user can see audit history for entities they can access
  // (permission check is on the entity page itself, not here)
  return getAuditLogs({
    entity: params.entity,
    entityId: params.entityId,
    page: params.page,
    pageSize: params.pageSize ?? 10,
  })
}

export async function runAuditCleanup(retentionDays: number) {
  const session = await auth()
  if (!session?.user?.id) throw new Error("Не авторизован")
  await requirePermission("settings.manage")

  if (retentionDays < 30) throw new Error("Минимальный период хранения: 30 дней")
  if (retentionDays > 3650) throw new Error("Максимальный период хранения: 10 лет")

  const deleted = await cleanupAuditLogs(retentionDays)
  return { deleted }
}
```

**B. src/components/settings/audit-log-table.tsx -- Filterable table component**

Client component (`"use client"`). Use shadcn/ui Table, Select, Input (date), Button.

Filters row at top:

- Date range: two date inputs (from/to)
- Action type: Select with options CREATE, UPDATE, DELETE, ROLE_CHANGE, PERMISSION_CHANGE
- Entity type: Select with options (Sale, User, Role, Customer, Store, Order, etc.)
- User: Select populated from available users in log entries
- Clear filters button

Table columns: Дата | Действие | Сущность | ID | Пользователь | Изменения

Each row shows:

- `createdAt` formatted as "DD.MM.YYYY HH:mm"
- `action` as badge (CREATE=green, UPDATE=blue, DELETE=red, ROLE_CHANGE=yellow, PERMISSION_CHANGE=yellow)
- `entity` as text
- `entityId` truncated to 8 chars
- `user.name` or `user.email`
- `changes` as expandable JSON (click to expand, show old->new values)

Pagination at bottom: "Showing 1-50 of 234" with prev/next buttons.

Retention settings section at bottom of page:

- Input: "Период хранения (дней)" with number input, default 365
- Button: "Очистить старые записи" with confirmation dialog
- After cleanup, show toast: "Удалено {count} записей"

Props:

```typescript
interface AuditLogTableProps {
  initialData: Awaited<ReturnType<typeof fetchAuditLogs>>
}
```

Use `useTransition` for filter changes. Call `fetchAuditLogs` server action on filter change with debounce.

**C. src/app/(dashboard)/settings/audit-log/page.tsx -- Page component**

Server component. Fetch initial audit logs with no filters (page 1, 50 items). Pass to AuditLogTable.

```typescript
import { fetchAuditLogs } from "@/actions/audit"
import { AuditLogTable } from "@/components/settings/audit-log-table"

export default async function AuditLogPage() {
  const data = await fetchAuditLogs({})
  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-2xl font-bold tracking-tight">Журнал аудита</h2>
        <p className="text-muted-foreground">История всех изменений в системе</p>
      </div>
      <AuditLogTable initialData={data} />
    </div>
  )
}
```

**D. Update settings layout navigation**

Read `src/app/(dashboard)/settings/layout.tsx`. Add navigation item for audit-log:

```typescript
{ title: "Журнал аудита", href: "/settings/audit-log", icon: FileText }
```

Use `FileText` icon from lucide-react (or `ScrollText` if available).

**E. src/components/settings/inline-audit-history.tsx -- Inline audit history for entity pages**

Per user decision: "both dedicated /settings/audit-log page AND inline history on relevant entity pages".

Create a reusable client component (`"use client"`) that shows recent audit log entries for a specific entity inline on its detail page.

```typescript
"use client"

import { useEffect, useState, useTransition } from "react"
import { fetchEntityAuditLogs } from "@/actions/audit"
import { Badge } from "@/components/ui/badge"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { History } from "lucide-react"

interface InlineAuditHistoryProps {
  entity: string // "Role", "User", etc.
  entityId: string // The specific entity ID
  title?: string // Optional custom title, defaults to "История изменений"
}

export function InlineAuditHistory({
  entity,
  entityId,
  title = "История изменений",
}: InlineAuditHistoryProps) {
  // State for audit entries
  // Fetch on mount via fetchEntityAuditLogs({ entity, entityId })
  // Display as a compact card with:
  //   - Card header: History icon + title
  //   - List of recent entries (last 10) showing:
  //     - Date (relative: "2 часа назад", "вчера", etc.)
  //     - Action badge (same color scheme as audit-log-table)
  //     - User name
  //     - Changes summary (e.g., "name: Кассир -> Старший кассир")
  //   - "Показать все" button that links to /settings/audit-log?entity={entity}&entityId={entityId}
  //   - Empty state: "Нет записей" with muted text
  // Use useTransition for loading state
  // Pagination: show first 10, "Загрузить еще" button for next page
}
```

This component will be used in Plan 03 on /settings/roles and /settings/users/[id] pages by importing and adding:

```tsx
<InlineAuditHistory entity="Role" entityId={roleId} />
<InlineAuditHistory entity="User" entityId={userId} />
```

**F. src/__tests__/e2e/audit-log.e2e.test.ts -- E2E test**

Following existing E2E test patterns:

1. Test: `createAuditEntry` creates record in DB -> verify with `db.auditLog.findFirst`
2. Test: `getAuditLogs` with filters returns correct results
3. Test: `getAuditLogs` pagination works (create 60 entries, page 1 returns 50, page 2 returns 10)
4. Test: `cleanupAuditLogs` deletes entries older than retention period
5. Test: `fetchAuditLogs` without settings.manage permission -> error
6. Test: `fetchEntityAuditLogs` returns entries filtered by entity+entityId
   </action>
   <verify>
   <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/e2e/audit-log.e2e.test.ts --reporter=verbose 2>&1 | tail -20 && echo "--- COMPONENT CHECKS ---" && test -f src/components/settings/inline-audit-history.tsx && grep -c "InlineAuditHistory" src/components/settings/inline-audit-history.tsx && grep -c "fetchEntityAuditLogs" src/actions/audit.ts</automated>
   </verify>
   <done>
   AuditLog page at /settings/audit-log shows filterable table with pagination. Filters: date range, action type, entity type, user. Retention cleanup via UI. Owner-only access enforced. InlineAuditHistory component created for embedding on entity pages. fetchEntityAuditLogs server action supports entity-specific queries. E2E tests verify all behaviors including inline history queries.
   </done>
   </task>

</tasks>

<verification>
- `npx prisma db push` succeeds (AuditLog table exists)
- `npx tsc --noEmit` passes
- `npx vitest run src/__tests__/e2e/audit-log.e2e.test.ts` all green
- `/settings/audit-log` page loads and shows table
- `InlineAuditHistory` component exists and exports correctly
- `fetchEntityAuditLogs` server action exists in src/actions/audit.ts
</verification>

<success_criteria>
AuditLog model in schema, createAuditEntry helper works in standalone and transactional mode, /settings/audit-log page renders with filters and pagination, retention cleanup works, InlineAuditHistory component ready for use on entity pages, E2E tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/12-security-fixes-roles-ui/12-02-SUMMARY.md`
</output>
