---
phase: 12-security-fixes-roles-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/lib/rate-limit.ts
  - src/actions/sales.ts
  - src/actions/reports.ts
  - src/actions/orders.ts
  - src/actions/cash-operations.ts
  - src/actions/shifts.ts
  - src/actions/settings.ts
  - src/lib/permissions-list.ts
  - src/hooks/use-rate-limit-toast.ts
  - src/__tests__/e2e/security-hardening.e2e.test.ts
autonomous: true
requirements:
  - SEC2-01
  - SEC2-02
  - SEC2-03
  - SEC2-04
  - SEC2-05
  - SEC2-06
  - SEC2-07
  - SEC2-08
  - SEC2-09

must_haves:
  truths:
    - "getSale с чужим storeId выбрасывает ошибку доступа"
    - "findUnique для soft-deleted записи возвращает null"
    - "Write-операции блокируются rate limiter после превышения лимита"
    - "updateUserRoles запрещает пользователю менять свои роли"
    - "closeShift с discrepancy > 1000 руб требует shifts.override_discrepancy"
    - "updateOrderStatus при скидке > 30% требует pos.discount_high"
    - "createCashOperation с amount > 500000 блокируется"
    - "При rate limit ошибке пользователь видит toast с обратным отсчётом"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "findUnique + findUniqueOrThrow soft delete extension"
      contains: "findUnique"
    - path: "src/lib/rate-limit.ts"
      provides: "Generic write rate limiting"
      exports: ["checkWriteRateLimit", "recordWriteAttempt"]
    - path: "src/lib/permissions-list.ts"
      provides: "New permission shifts.override_discrepancy"
      contains: "shifts.override_discrepancy"
    - path: "src/hooks/use-rate-limit-toast.ts"
      provides: "Client-side hook for rate limit toast with countdown"
      exports: ["useRateLimitToast"]
    - path: "src/__tests__/e2e/security-hardening.e2e.test.ts"
      provides: "E2E tests for all SEC2-01..09"
      min_lines: 100
  key_links:
    - from: "src/actions/sales.ts"
      to: "src/lib/permissions.ts"
      via: "requirePermission('pos.sell', sale.storeId)"
      pattern: "requirePermission.*sale\\.storeId"
    - from: "src/lib/db.ts"
      to: "SOFT_DELETE_MODELS"
      via: "findUnique post-query filter on deletedAt"
      pattern: "findUnique.*deletedAt"
    - from: "src/actions/*.ts"
      to: "src/lib/rate-limit.ts"
      via: "checkWriteRateLimit call at top of write actions"
      pattern: "checkWriteRateLimit"
    - from: "src/hooks/use-rate-limit-toast.ts"
      to: "sonner"
      via: "toast.error with countdown timer"
      pattern: "toast\\.error.*countdown"
---

<objective>
Security hardening: IDOR fixes, soft delete bypass closure, rate limiting, business rule guards, rate limit toast with countdown.

Purpose: Close 9 security vulnerabilities (SEC2-01..09) identified in v1.0 audit. These are server-side fixes in existing code -- no new UI pages. Plus client-side rate limit toast with countdown timer per user decision.
Output: Hardened server actions with storeId checks, findUnique soft delete, write rate limiting, business rule guards, rate limit toast hook, E2E tests.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-security-fixes-roles-ui/12-CONTEXT.md
@.planning/phases/12-security-fixes-roles-ui/12-RESEARCH.md

<interfaces>
<!-- Existing patterns executor must follow -->

From src/lib/db.ts:

```typescript
const SOFT_DELETE_MODELS = ["Product", "Supplier", "Customer", "Store", "User"] as const
// $extends with findMany, findFirst, findFirstOrThrow, count
// MISSING: findUnique, findUniqueOrThrow
```

From src/lib/rate-limit.ts:

```typescript
export function checkRateLimit(username: string): { allowed: boolean; retryAfterMs?: number }
export function recordFailedAttempt(username: string): void
export function clearAttempts(username: string): void
```

From src/lib/permissions.ts:

```typescript
export async function requirePermission(code: PermissionCode, storeId?: string): Promise<void>
export async function checkPermission(code: PermissionCode, storeId?: string): Promise<boolean>
```

From src/lib/permissions-list.ts:

```typescript
export const PERMISSIONS = {
  POS_SELL: { code: "pos.sell", module: "pos", name: "Продажа" },
  POS_DISCOUNT_HIGH: { code: "pos.discount_high", module: "pos", name: "Скидка свыше 30%" },
  // ... ~50 permissions total, each with { code, module, name }
}
export type PermissionCode = ...
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IDOR fixes + soft delete findUnique + rate limiting + business guards</name>
  <files>
    src/lib/db.ts,
    src/lib/rate-limit.ts,
    src/lib/permissions-list.ts,
    src/actions/sales.ts,
    src/actions/reports.ts,
    src/actions/orders.ts,
    src/actions/cash-operations.ts,
    src/actions/shifts.ts,
    src/actions/settings.ts
  </files>
  <read_first>
    src/lib/db.ts,
    src/lib/rate-limit.ts,
    src/lib/permissions.ts,
    src/lib/permissions-list.ts,
    src/actions/sales.ts,
    src/actions/reports.ts,
    src/actions/orders.ts,
    src/actions/cash-operations.ts,
    src/actions/shifts.ts,
    src/actions/settings.ts
  </read_first>
  <action>
**A. src/lib/db.ts -- Add findUnique + findUniqueOrThrow to $extends (SEC2-02)**

Add two new methods inside the existing `$extends({ query: { $allModels: { ... } } })` block, AFTER the existing `count` method:

```typescript
async findUnique({ model, args, query }) {
  if (SOFT_DELETE_MODELS.includes(model as any)) {
    const result = await query(args)
    if (result && (result as any).deletedAt !== null) {
      return null
    }
    return result
  }
  return query(args)
},
async findUniqueOrThrow({ model, args, query }) {
  if (SOFT_DELETE_MODELS.includes(model as any)) {
    const result = await query(args)
    if (result && (result as any).deletedAt !== null) {
      throw new Error(`${model} not found`)
    }
    return result
  }
  return query(args)
},
```

IMPORTANT: findUnique does NOT support arbitrary where conditions (only unique fields). Cannot inject `deletedAt: null` into where. Must use post-query filter pattern.

**B. src/lib/rate-limit.ts -- Add generic write rate limiting (SEC2-06)**

Add BELOW existing login rate limit code (keep existing exports intact):

```typescript
const writeAttempts = new Map<string, { count: number; windowStart: number }>()

interface RateLimitConfig {
  maxRequests: number
  windowMs: number
}

const WRITE_RATE_LIMITS: Record<string, RateLimitConfig> = {
  "pos.sell": { maxRequests: 30, windowMs: 60_000 },
  "pos.return": { maxRequests: 10, windowMs: 60_000 },
  "inventory.receive": { maxRequests: 20, windowMs: 60_000 },
  "orders.create": { maxRequests: 15, windowMs: 60_000 },
  "cash.operation": { maxRequests: 10, windowMs: 60_000 },
  "settings.roles": { maxRequests: 10, windowMs: 60_000 },
  default: { maxRequests: 20, windowMs: 60_000 },
}

export function checkWriteRateLimit(
  userId: string,
  action: string,
): { allowed: boolean; retryAfterMs?: number } {
  const config = WRITE_RATE_LIMITS[action] ?? WRITE_RATE_LIMITS["default"]
  const key = `${userId}:${action}`
  const now = Date.now()
  const entry = writeAttempts.get(key)

  if (!entry || now - entry.windowStart > config.windowMs) {
    return { allowed: true }
  }

  if (entry.count >= config.maxRequests) {
    return { allowed: false, retryAfterMs: config.windowMs - (now - entry.windowStart) }
  }

  return { allowed: true }
}

export function recordWriteAttempt(userId: string, action: string): void {
  const config = WRITE_RATE_LIMITS[action] ?? WRITE_RATE_LIMITS["default"]
  const key = `${userId}:${action}`
  const now = Date.now()
  const entry = writeAttempts.get(key)

  if (!entry || now - entry.windowStart > config.windowMs) {
    writeAttempts.set(key, { count: 1, windowStart: now })
  } else {
    entry.count++
  }
}

// Cleanup stale write entries every 30 minutes
if (typeof setInterval !== "undefined") {
  setInterval(
    () => {
      const now = Date.now()
      for (const [key, entry] of writeAttempts) {
        if (now - entry.windowStart > 300_000) {
          writeAttempts.delete(key)
        }
      }
    },
    30 * 60 * 1000,
  )
}
```

**C. src/lib/permissions-list.ts -- Add new permission (SEC2-07)**

Add to PERMISSIONS object in the Shifts section:

```typescript
SHIFTS_OVERRIDE_DISCREPANCY: { code: "shifts.override_discrepancy", module: "shifts", name: "Подтверждение большого расхождения" },
```

Also add to the PermissionCode type union.

**D. src/actions/sales.ts -- IDOR fix (SEC2-01)**

Find `getSale` function. After the `findUnique` call and null check, add BEFORE returning data:

```typescript
await requirePermission("pos.sell", sale.storeId)
```

Find ALL other exported functions that accept an entity ID and fetch by it (e.g., `getSaleDetails`, any function with `saleId` param). Add the same pattern: fetch entity -> requirePermission with entity.storeId.

Also add rate limiting to `createSale`: after auth check, before business logic:

```typescript
const rateCheck = checkWriteRateLimit(session.user.id, "pos.sell")
if (!rateCheck.allowed) {
  throw new Error(
    `Слишком много запросов. Повторите через ${Math.ceil(rateCheck.retryAfterMs! / 1000)} сек.`,
  )
}
recordWriteAttempt(session.user.id, "pos.sell")
```

Add import: `import { checkWriteRateLimit, recordWriteAttempt } from "@/lib/rate-limit"`

**E. src/actions/reports.ts -- IDOR fix (SEC2-03)**

Find ALL report functions (getSalesReport, getInventoryReport, etc.). After auth check, verify storeId access:

```typescript
if (storeId) {
  await requirePermission("reports.full", storeId)
}
```

If function already has requirePermission but WITHOUT storeId param, add storeId as second argument.

**F. src/actions/orders.ts -- Discount + price guards (SEC2-04, SEC2-09)**

1. In `updateOrderStatus` (or the function that completes an order): when status changes to COMPLETED, calculate total discount percentage. If > 30%:

```typescript
const discountPercent = (totalDiscount / subtotal) * 100
if (discountPercent > 30) {
  await requirePermission("pos.discount_high", order.storeId)
}
```

2. In `updateOrderItem` (or function that changes line item price): calculate price change percentage from original:

```typescript
const priceChange = (Math.abs(newPrice - originalPrice) / originalPrice) * 100
if (priceChange > 30) {
  await requirePermission("pos.discount_high", order.storeId)
}
```

Also add rate limiting to `createOrder`:

```typescript
const rateCheck = checkWriteRateLimit(session.user.id, "orders.create")
if (!rateCheck.allowed)
  throw new Error(
    `Слишком много запросов. Повторите через ${Math.ceil(rateCheck.retryAfterMs! / 1000)} сек.`,
  )
recordWriteAttempt(session.user.id, "orders.create")
```

**G. src/actions/cash-operations.ts -- Amount limit + fund balance (SEC2-05)**

In `createCashOperation`, after auth check and validation:

```typescript
const MAX_CASH_OPERATION = 500_000 // 500k rub hard cap
if (Math.abs(amount) > MAX_CASH_OPERATION) {
  throw new Error(
    `Сумма кассовой операции не может превышать ${MAX_CASH_OPERATION.toLocaleString("ru-RU")} руб.`,
  )
}

// For withdrawals, check fund balance
if (amount < 0 || type === "WITHDRAWAL") {
  const fund = await db.fund.findUnique({ where: { id: fundId } })
  if (fund && Number(fund.balance) < Math.abs(amount)) {
    throw new Error(`Недостаточно средств в фонде. Баланс: ${fund.balance} руб.`)
  }
}
```

Also add rate limiting with key `"cash.operation"`.

**H. src/actions/shifts.ts -- Discrepancy approval (SEC2-07)**

In `closeShift`, after calculating discrepancy, before closing:

```typescript
const DISCREPANCY_THRESHOLD = 1000 // rub
if (Math.abs(discrepancy) > DISCREPANCY_THRESHOLD) {
  const canOverride = await checkPermission("shifts.override_discrepancy", shift.storeId)
  if (!canOverride) {
    throw new Error(
      `Расхождение ${Math.abs(discrepancy)} руб. превышает допустимое (${DISCREPANCY_THRESHOLD} руб.). Требуется подтверждение старшего.`,
    )
  }
}
```

Import `checkPermission` from `@/lib/permissions`.

**I. src/actions/settings.ts -- Self-role change prevention (SEC2-08)**

In `updateUserRoles`, after auth check and before any modification:

```typescript
const session = await auth()
if (userId === session?.user?.id) {
  throw new Error("Нельзя изменять свои собственные роли")
}
```

  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit --pretty 2>&1 | tail -20 && echo "--- GREP CHECKS ---" && grep -c "findUnique\b" src/lib/db.ts && grep -c "checkWriteRateLimit" src/lib/rate-limit.ts && grep -c "shifts.override_discrepancy" src/lib/permissions-list.ts && grep -c "requirePermission.*storeId" src/actions/sales.ts && grep -c "Нельзя изменять свои" src/actions/settings.ts && grep -c "DISCREPANCY_THRESHOLD" src/actions/shifts.ts && grep -c "MAX_CASH_OPERATION" src/actions/cash-operations.ts && grep -c "discount_high" src/actions/orders.ts</automated>
  </verify>
  <done>
    All 9 security fixes (SEC2-01..09) implemented: IDOR checks on getSale/reports, findUnique soft delete bypass closed, write rate limiting on all write actions, self-role change blocked, shift discrepancy approval required, order discount/price guards in place, cash operation limits enforced. TypeScript compiles. Grep checks confirm all patterns present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rate limit toast with countdown timer + E2E security tests</name>
  <files>
    src/hooks/use-rate-limit-toast.ts,
    src/__tests__/e2e/security-hardening.e2e.test.ts
  </files>
  <read_first>
    src/lib/rate-limit.ts,
    src/__tests__/e2e/concurrency-locking.e2e.test.ts,
    src/lib/test-utils.ts
  </read_first>
  <action>
**A. src/hooks/use-rate-limit-toast.ts -- Rate limit toast with countdown (per user decision: "Toast notification with countdown timer on rate limit hit")**

Create a client-side utility hook that intercepts rate limit errors from server actions and displays a sonner toast with a live countdown timer.

````typescript
"use client"

import { toast } from "sonner"
import { useCallback, useRef } from "react"

/**
 * Extracts retryAfterMs from rate limit error messages.
 * Server actions throw errors like: "Слишком много запросов. Повторите через 45 сек."
 */
function parseRateLimitError(error: unknown): number | null {
  if (!(error instanceof Error)) return null
  const match = error.message.match(/Повторите через (\d+) сек/)
  if (!match) return null
  return parseInt(match[1], 10) * 1000
}

/**
 * Hook that wraps server action calls to display rate limit toast with countdown.
 *
 * Usage:
 * ```tsx
 * const { withRateLimitToast } = useRateLimitToast()
 * const handleSave = () => withRateLimitToast(() => createSale(data))
 * ```
 */
export function useRateLimitToast() {
  const countdownRef = useRef<NodeJS.Timeout | null>(null)

  const showCountdownToast = useCallback((retryAfterMs: number) => {
    let remaining = Math.ceil(retryAfterMs / 1000)
    const toastId = toast.error(`Слишком много запросов. Подождите ${remaining} сек.`, {
      duration: retryAfterMs + 1000,
      id: "rate-limit-countdown",
    })

    // Clear any existing countdown
    if (countdownRef.current) clearInterval(countdownRef.current)

    countdownRef.current = setInterval(() => {
      remaining--
      if (remaining <= 0) {
        if (countdownRef.current) clearInterval(countdownRef.current)
        toast.success("Можно продолжать", { id: "rate-limit-countdown", duration: 2000 })
        return
      }
      toast.error(`Слишком много запросов. Подождите ${remaining} сек.`, {
        id: "rate-limit-countdown",
        duration: remaining * 1000 + 1000,
      })
    }, 1000)
  }, [])

  const withRateLimitToast = useCallback(
    async <T>(action: () => Promise<T>): Promise<T | undefined> => {
      try {
        return await action()
      } catch (error) {
        const retryAfterMs = parseRateLimitError(error)
        if (retryAfterMs !== null) {
          showCountdownToast(retryAfterMs)
          return undefined
        }
        throw error // Re-throw non-rate-limit errors
      }
    },
    [showCountdownToast],
  )

  return { withRateLimitToast, showCountdownToast }
}
````

Export `parseRateLimitError` as well for testing.

**B. src/__tests__/e2e/security-hardening.e2e.test.ts -- E2E tests for SEC2-01..09**

Create E2E test file following existing E2E patterns from `concurrency-locking.e2e.test.ts` (real DB, test fixtures, cleanup).

Tests to write:

1. **SEC2-01: IDOR getSale** -- Create sale in store A, attempt getSale from user with access only to store B -> expect error "Нет доступа" or permission error.

2. **SEC2-02: findUnique soft delete** -- Soft-delete a Customer (set deletedAt), then `db.customer.findUnique({ where: { id } })` -> expect null. Also test `findUniqueOrThrow` -> expect Error.

3. **SEC2-03: Reports IDOR** -- Call report function with storeId user doesn't have access to -> expect permission error.

4. **SEC2-04: Discount > 30% check** -- Attempt to complete order with > 30% discount without pos.discount_high permission -> expect error.

5. **SEC2-05: Cash operation limit** -- Attempt createCashOperation with amount > 500000 -> expect error "не может превышать".

6. **SEC2-06: Rate limiting** -- Call checkWriteRateLimit 31 times with same userId + "pos.sell" action within 60s -> 31st call returns `{ allowed: false }`.

7. **SEC2-07: Shift discrepancy** -- Close shift with discrepancy > 1000 without shifts.override_discrepancy permission -> expect error "Требуется подтверждение".

8. **SEC2-08: Self-role change** -- Call updateUserRoles with userId === session.user.id -> expect error "Нельзя изменять свои".

9. **SEC2-09: Price cap > 30%** -- Update order item price by more than 30% without pos.discount_high -> expect error.

Each test: setup fixtures -> call action -> assert error/success -> cleanup.

For rate limit test (SEC2-06), this can be a unit-level test within the same file since it tests pure function logic.
</action>
<verify>
<automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/e2e/security-hardening.e2e.test.ts --reporter=verbose 2>&1 | tail -30 && echo "--- HOOK CHECK ---" && test -f src/hooks/use-rate-limit-toast.ts && grep -c "countdown" src/hooks/use-rate-limit-toast.ts && grep -c "parseRateLimitError" src/hooks/use-rate-limit-toast.ts</automated>
</verify>
<done>
E2E test file covers all 9 SEC2 requirements with at least 9 test cases. All tests pass against real database. Rate limit toast hook exists with countdown timer functionality using sonner, parseRateLimitError utility exported for testing.
</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes without new errors
- `npx vitest run src/__tests__/e2e/security-hardening.e2e.test.ts` all green
- `grep -r "checkWriteRateLimit" src/actions/` shows rate limiting applied to write actions
- `grep "findUnique" src/lib/db.ts` shows soft delete extension covers findUnique
- `grep "countdown" src/hooks/use-rate-limit-toast.ts` shows toast with countdown
</verification>

<success_criteria>
All 9 SEC2-01..09 requirements implemented and verified with E2E tests. Rate limit toast with countdown timer available as hook. No regressions in existing test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/12-security-fixes-roles-ui/12-01-SUMMARY.md`
</output>
