---
phase: 14-payroll-employee-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - src/app/(dashboard)/my/motivation/my-motivation-client.tsx
  - src/components/motivation/earnings-breakdown.tsx
  - src/components/motivation/payroll-history.tsx
autonomous: false
requirements:
  - PAYROLL-04
  - PAYROLL-03

must_haves:
  truths:
    - "Расшифровка комиссии группируется по сменам в складные секции (accordion)"
    - "Каждая смена показывает: дату, количество продаж, итого комиссию"
    - "Раскрытие смены показывает список продаж с товарами: товар, цена, прибыль, %, сумма комиссии"
    - "Таблица payroll истории показывает: период, тип, сумма, статус"
    - "Клик по строке payroll показывает расшифровку (EarningsBreakdown)"
    - "Продажи без смены показаны в группе 'Вне смен'"
  artifacts:
    - path: "src/components/motivation/payroll-history.tsx"
      provides: "Payroll history table component"
      min_lines: 60
    - path: "src/components/motivation/earnings-breakdown.tsx"
      provides: "Shift-grouped commission breakdown"
      contains: "shiftId"
    - path: "src/app/(dashboard)/my/motivation/my-motivation-client.tsx"
      provides: "Integration of payroll history + shift breakdown"
      contains: "PayrollHistory"
  key_links:
    - from: "src/app/(dashboard)/my/motivation/my-motivation-client.tsx"
      to: "src/components/motivation/payroll-history.tsx"
      via: "import PayrollHistory"
      pattern: "import.*PayrollHistory"
    - from: "src/app/(dashboard)/my/motivation/my-motivation-client.tsx"
      to: "src/actions/motivation-payroll.ts"
      via: "getMyPayrolls call"
      pattern: "getMyPayrolls"
    - from: "src/components/motivation/earnings-breakdown.tsx"
      to: "SaleCommission.shiftId"
      via: "groupBy shiftId for accordion"
      pattern: "shiftId"
---

<objective>
Build the UI for payroll history table and shift-grouped commission breakdown in the employee dashboard (PAYROLL-03, PAYROLL-04).

Purpose: Give employees a clear view of their earnings grouped by shifts with full item-level breakdown, plus a history of all payroll records.

Output: PayrollHistory component, shift-grouped EarningsBreakdown, integrated into /my/motivation page
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-payroll-employee-dashboard/14-CONTEXT.md
@.planning/phases/14-payroll-employee-dashboard/14-01-SUMMARY.md
@.planning/phases/14-payroll-employee-dashboard/14-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — extended SaleCommission with shift fields -->
```typescript
interface SaleCommission {
  saleId: string
  saleNumber: string
  date: string
  shiftId: string | null
  shiftDate: string | null
  shiftNumber: string | null
  items: SaleCommissionItem[]
  totalCommission: number
}
```

<!-- From Plan 02 — getMyPayrolls return type -->
```typescript
// Return type of getMyPayrolls(storeId: string):
Array<{
  id: string
  periodStart: string      // ISO date
  periodEnd: string        // ISO date
  isAdvance: boolean
  shiftsCount: number
  dailyTotal: number
  commissions: number
  crossBonuses: number
  repairBonuses: number
  returns: number
  totalAmount: number
  status: "DRAFT" | "CONFIRMED" | "PAID"
  schemeName: string
  breakdown: unknown       // JSON with full earnings data
  createdAt: string
}>
```

<!-- Existing EarningsBreakdown component props -->
```typescript
export interface EarningsResultForBreakdown {
  dailyRate: { shiftsCount: number; ratePerShift: number; total: number }
  commissions: SaleCommission[]
  crossSellBonuses: CrossSellBonusResult[]
  repairBonuses: RepairBonusResult[]
  returnDeductions: ReturnDeduction[]
  totals: { daily: number; commissions: number; crossBonuses: number; repairBonuses: number; returns: number; total: number }
}

export function EarningsBreakdown({ data }: { data: EarningsResultForBreakdown })
```

<!-- Existing UI patterns used in the dashboard -->
```typescript
// from my-motivation-client.tsx:
import { useCurrentStore } from "@/hooks/use-current-store"
import { getUserStores } from "@/actions/stores"
import { getMyEarnings } from "@/actions/motivation-calculation"
import { getPayrollPdfData } from "@/actions/motivation-payroll"
import { EarningsBreakdown } from "@/components/motivation/earnings-breakdown"
import { pdf } from "@react-pdf/renderer"
import { PayrollPdfDocument } from "@/components/motivation/payroll-pdf-document"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shift-grouped EarningsBreakdown + PayrollHistory component</name>
  <files>
    src/components/motivation/earnings-breakdown.tsx,
    src/components/motivation/payroll-history.tsx
  </files>
  <read_first>
    - src/components/motivation/earnings-breakdown.tsx (full file — current rendering, accordion pattern)
    - src/app/(dashboard)/my/motivation/my-motivation-client.tsx (full file — how EarningsBreakdown is used)
    - src/actions/motivation-payroll.ts (getMyPayrolls, getPayrollPdfData signatures)
    - src/components/motivation/payroll-pdf-document.tsx (PDF component for download)
  </read_first>
  <action>
**Part A: Shift-grouped breakdown in earnings-breakdown.tsx**

Modify `EarningsBreakdown` to group commissions by shift when rendering the commissions section.

1. Add a helper function to group commissions by shiftId:
```typescript
interface ShiftGroup {
  shiftId: string | null
  shiftDate: string | null
  shiftNumber: string | null
  sales: SaleCommission[]
  totalCommission: number
  salesCount: number
}

function groupByShift(commissions: SaleCommission[]): ShiftGroup[] {
  const map = new Map<string, ShiftGroup>()
  for (const c of commissions) {
    const key = c.shiftId ?? "__no_shift__"
    const existing = map.get(key)
    if (existing) {
      existing.sales.push(c)
      existing.totalCommission += c.totalCommission
      existing.salesCount++
    } else {
      map.set(key, {
        shiftId: c.shiftId,
        shiftDate: c.shiftDate,
        shiftNumber: c.shiftNumber,
        sales: [c],
        totalCommission: c.totalCommission,
        salesCount: 1,
      })
    }
  }
  // Sort: shifts with dates first (newest first), then "no shift" last
  return Array.from(map.values()).sort((a, b) => {
    if (!a.shiftDate && b.shiftDate) return 1
    if (a.shiftDate && !b.shiftDate) return -1
    if (a.shiftDate && b.shiftDate) return b.shiftDate.localeCompare(a.shiftDate)
    return 0
  })
}
```

2. Replace the flat commissions list rendering with a two-level accordion:
- **Level 1: Shift accordion** — trigger shows: shift number + date, sales count, total commission
  - For shiftId=null, show "Вне смен" as label
  - Date formatted as `DD.MM.YYYY` using `new Date(shiftDate).toLocaleDateString("ru-RU")`
- **Level 2 (inside shift): Sales list** — each sale is an existing expandable section showing items table
  - Keep the existing per-sale item rendering (product, price, cost, profit, %, commission)

Use `useState<Set<string>>` for tracking which shifts are expanded (accordion behavior via ChevronDown/ChevronRight icons, already imported). Pattern:
```tsx
const [expandedShifts, setExpandedShifts] = useState<Set<string>>(new Set())
const toggleShift = (key: string) => {
  setExpandedShifts(prev => {
    const next = new Set(prev)
    next.has(key) ? next.delete(key) : next.add(key)
    return next
  })
}
```

Shift header styling (Tailwind):
```
bg-muted/50 rounded-lg px-4 py-3 cursor-pointer hover:bg-muted/80 transition-colors
flex items-center justify-between
```

Inside expanded shift: render existing per-sale breakdown (items table with columns: Товар, Цена, Себест., Прибыль, Ставка, Комиссия).

3. Keep backward compatibility: if commissions have no shiftId data (all null), render as a single unnamed group (no visual change from before for clients that haven't updated).

**Part B: PayrollHistory component**

Create `src/components/motivation/payroll-history.tsx`:

```typescript
"use client"

import { useState } from "react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { FileDown, ChevronDown, ChevronRight } from "lucide-react"

type PayrollRecord = {
  id: string
  periodStart: string
  periodEnd: string
  isAdvance: boolean
  totalAmount: number
  status: "DRAFT" | "CONFIRMED" | "PAID"
  schemeName: string
  breakdown: unknown
  shiftsCount: number
  dailyTotal: number
  commissions: number
  crossBonuses: number
  repairBonuses: number
  returns: number
  createdAt: string
}

interface PayrollHistoryProps {
  payrolls: PayrollRecord[]
  onDownloadPdf: (payrollId: string) => void
  loading?: boolean
}
```

Component renders a table with columns:
- **Период**: `formatPeriod(periodStart, periodEnd)` — e.g., "01.03.2026 — 31.03.2026"
- **Тип**: Badge showing "Аванс" (isAdvance=true, variant="outline") or "Итого" (variant="default")
- **Сумма**: `formatMoney(totalAmount)` using `Intl.NumberFormat("ru-RU", { style: "currency", currency: "RUB", maximumFractionDigits: 0 })`
- **Статус**: Badge — DRAFT="Черновик" (variant="secondary"), CONFIRMED="Подтверждён" (variant="default"), PAID="Выплачен" (variant="default" + green color className="bg-green-100 text-green-800")
- **PDF**: Button with FileDown icon, calls `onDownloadPdf(payroll.id)`, only shown for CONFIRMED/PAID status

Click on a row (not the PDF button) toggles an expanded section below the row showing a summary:
- Смены: {shiftsCount}, Ставка: {dailyTotal} руб, Комиссия: {commissions} руб
- Кросс-бонусы: {crossBonuses}, Ремонт: {repairBonuses}, Возвраты: -{returns}

Empty state: "Нет расчётных листов за выбранный период" centered text with muted color.

Table uses standard HTML `<table>` with Tailwind classes (matching existing patterns in the app — NOT shadcn Table component unless already used in this page).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit --pretty 2>&1 | grep -E "earnings-breakdown|payroll-history|my-motivation" | head -10; echo "---"; test -f src/components/motivation/payroll-history.tsx && echo "payroll-history.tsx EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/motivation/payroll-history.tsx` exists with at least 60 lines
    - `payroll-history.tsx` contains `export function PayrollHistory` or `export default function PayrollHistory`
    - `payroll-history.tsx` contains `PayrollHistoryProps`
    - `payroll-history.tsx` contains `Черновик` and `Подтверждён` and `Выплачен` (status labels)
    - `payroll-history.tsx` contains `Аванс` and `Итого` (type labels)
    - `payroll-history.tsx` contains `onDownloadPdf`
    - `earnings-breakdown.tsx` contains `groupByShift` function
    - `earnings-breakdown.tsx` contains `ShiftGroup` interface
    - `earnings-breakdown.tsx` contains `Вне смен` (label for null-shift sales)
    - `earnings-breakdown.tsx` contains `shiftId` in its rendering logic
    - `earnings-breakdown.tsx` contains `expandedShifts` state
  </acceptance_criteria>
  <done>EarningsBreakdown groups by shifts, PayrollHistory component renders table with status badges and PDF download</done>
</task>

<task type="auto">
  <name>Task 2: Integrate PayrollHistory + shift breakdown into /my/motivation page</name>
  <files>src/app/(dashboard)/my/motivation/my-motivation-client.tsx</files>
  <read_first>
    - src/app/(dashboard)/my/motivation/my-motivation-client.tsx (full file — current page structure)
    - src/components/motivation/payroll-history.tsx (from Task 1 — props interface)
    - src/components/motivation/earnings-breakdown.tsx (from Task 1 — updated with shift grouping)
    - src/actions/motivation-payroll.ts (getMyPayrolls, getPayrollPdfData)
  </read_first>
  <action>
Integrate the PayrollHistory component and updated EarningsBreakdown into the employee dashboard page.

1. **Add imports:**
```typescript
import { getMyPayrolls, getPayrollPdfData } from "@/actions/motivation-payroll"
import { PayrollHistory } from "@/components/motivation/payroll-history"
```

Remove the old `getPayrolls` import (was using manager permission — line 17).

2. **Add state for payroll history:**
```typescript
const [payrolls, setPayrolls] = useState<Awaited<ReturnType<typeof getMyPayrolls>>>([])
const [payrollsLoading, setPayrollsLoading] = useState(false)
```

3. **Load payrolls when store changes:**
Add a useEffect that calls `getMyPayrolls(selectedStoreId)` when `selectedStoreId` changes:
```typescript
useEffect(() => {
  if (!selectedStoreId) return
  setPayrollsLoading(true)
  getMyPayrolls(selectedStoreId)
    .then(setPayrolls)
    .catch(() => toast.error("Не удалось загрузить историю начислений"))
    .finally(() => setPayrollsLoading(false))
}, [selectedStoreId])
```

4. **PDF download handler:**
```typescript
async function handleDownloadPdf(payrollId: string) {
  try {
    const data = await getPayrollPdfData(payrollId)
    const blob = await pdf(<PayrollPdfDocument data={data} />).toBlob()
    const url = URL.createObjectURL(blob)
    const a = document.createElement("a")
    a.href = url
    a.download = `payroll-${payrollId}.pdf`
    a.click()
    URL.revokeObjectURL(url)
  } catch {
    toast.error("Не удалось скачать PDF")
  }
}
```

5. **Add PayrollHistory section to JSX:**
After the existing EarningsBreakdown section, add a new section:
```tsx
{/* История начислений */}
<div className="mt-8">
  <h2 className="text-lg font-semibold mb-4">История начислений</h2>
  <PayrollHistory
    payrolls={payrolls}
    onDownloadPdf={handleDownloadPdf}
    loading={payrollsLoading}
  />
</div>
```

6. **Remove old latestPayrollId logic** that used to call `getPayrolls` with manager permission. The old code tried `getPayrolls(storeId, dateFrom, dateTo)` which requires `motivation.payroll.view` — replace with the new `getMyPayrolls` pattern.

7. The EarningsBreakdown component already handles shift grouping from Task 1 — no changes needed here for the breakdown rendering. It will automatically show shift-grouped data because the `commissions` array from `getMyEarnings` now includes `shiftId`, `shiftDate`, `shiftNumber` fields (from Plan 02).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit --pretty 2>&1 | grep "my-motivation" | head -10; echo "---"; grep -c "PayrollHistory" src/app/\(dashboard\)/my/motivation/my-motivation-client.tsx; grep -c "getMyPayrolls" src/app/\(dashboard\)/my/motivation/my-motivation-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `my-motivation-client.tsx` contains `import { getMyPayrolls` (not getPayrolls)
    - `my-motivation-client.tsx` contains `import { PayrollHistory`
    - `my-motivation-client.tsx` contains `getMyPayrolls(selectedStoreId)`
    - `my-motivation-client.tsx` contains `handleDownloadPdf`
    - `my-motivation-client.tsx` contains `<PayrollHistory` JSX element
    - `my-motivation-client.tsx` contains `История начислений` heading
    - `my-motivation-client.tsx` does NOT contain `getPayrolls(` call (old manager permission)
    - No TypeScript errors in the three modified files
  </acceptance_criteria>
  <done>Employee dashboard shows payroll history table with PDF download and shift-grouped commission breakdown</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of employee dashboard</name>
  <files>src/app/(dashboard)/my/motivation/my-motivation-client.tsx</files>
  <action>Visual verification of the complete employee dashboard. No code changes — human reviews the UI by following the steps below.</action>
  <what-built>
    Complete employee dashboard with:
    1. Commission breakdown grouped by shifts (collapsible sections)
    2. Payroll history table with period, type, amount, status
    3. PDF download from history table
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Log in as an employee (not admin) who has motivation assignments
    3. Navigate to /my/motivation
    4. Select a store and date range that includes sales
    5. Click "Рассчитать" to load earnings
    6. Verify commission breakdown shows shift sections:
       - Each shift has date, number of sales, total commission
       - Click to expand shows individual sales with item details
       - Sales without a shift are in "Вне смен" group
    7. Scroll down to "История начислений":
       - Table shows payroll records with period, type (Аванс/Итого), amount, status badge
       - Click on a row expands summary details
       - PDF download button works for CONFIRMED/PAID records
    8. Check that no other employee's data is visible
  </how-to-verify>
  <verify>Human visual verification — see how-to-verify steps above</verify>
  <done>User confirms shift-grouped breakdown and payroll history render correctly</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Employee sees shift-grouped commissions (accordion sections)
- Each shift section shows: date, sales count, total commission
- Expanding a shift shows per-sale items with: product, price, cost, profit, rate, commission
- PayrollHistory table shows payroll records with correct status badges
- PDF download works for confirmed/paid records
- "Вне смен" group shown for sales without shift
- No TypeScript compilation errors
</verification>

<success_criteria>
- Shift-grouped accordion renders correctly with real data
- PayrollHistory table shows own payroll records only
- PDF download produces correct document
- No visual regressions in existing motivation page functionality
</success_criteria>

<output>
After completion, create `.planning/phases/14-payroll-employee-dashboard/14-03-SUMMARY.md`
</output>
