---
phase: 01-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/lib/validations/sales.ts
  - src/actions/sales.ts
  - src/__tests__/sales-validation.test.ts
  - src/__tests__/rate-limit.test.ts
  - src/__tests__/password-validation.test.ts
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06]

must_haves:
  truths:
    - "Продавец не может передать произвольную цену или себестоимость через DevTools/API — сервер всегда берёт sellPrice и costPrice из StoreProduct/SerialUnit"
    - "Скидка ограничена: >= 0 и <= sellPrice; скидка > 30% от цены требует permission pos.discount_high"
    - "quantity валидируется: > 0, целое, <= остаток на складе"
    - "Продажа невозможна без открытой кассовой смены — возвращается ошибка 'Откройте кассовую смену перед продажей'"
    - "Возврат проверяет что продажа принадлежит текущему магазину (storeId)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with @/ path alias"
      contains: "resolve"
    - path: "src/lib/validations/sales.ts"
      provides: "Zod schema for createSale input (no price/costPrice fields)"
      exports: ["createSaleSchema", "CreateSaleInput"]
    - path: "src/actions/sales.ts"
      provides: "Secure createSale/createReturn with server-side price lookup"
      contains: "sp.sellPrice"
  key_links:
    - from: "src/actions/sales.ts"
      to: "src/lib/validations/sales.ts"
      via: "import createSaleSchema"
      pattern: "createSaleSchema\\.parse"
    - from: "src/actions/sales.ts"
      to: "prisma StoreProduct"
      via: "server-side price lookup in transaction"
      pattern: "tx\\.storeProduct\\.findUnique"
---

<objective>
Закрыть критические дыры в серверной валидации POS: цены берутся из БД, не от клиента. Клиент передаёт только productId, quantity, discount, serialUnitId. Также — проверка открытой смены, валидация скидок и количества, проверка storeId при возврате.

Purpose: Бизнес теряет деньги из-за возможности подмены цен через DevTools. Это самая опасная дыра в системе.
Output: Защищённые server actions createSale/createReturn + Zod-схема + минимальная Vitest-инфраструктура.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security/01-CONTEXT.md
@.planning/phases/01-security/01-RESEARCH.md
@.planning/phases/01-security/01-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/permissions.ts:
```typescript
export async function requirePermission(permissionCode: PermissionCode, storeId?: string): Promise<void>
```

From src/lib/permissions-list.ts:
```typescript
POS_SELL: { code: "pos.sell", module: "pos", name: "Продажа" },
POS_RETURN: { code: "pos.return", module: "pos", name: "Возврат товара" },
POS_DISCOUNT: { code: "pos.discount", module: "pos", name: "Применение скидки" },
```

From src/actions/sales.ts (CURRENT createSale signature — to be replaced):
```typescript
export async function createSale(data: {
  storeId: string
  items: Array<{
    productId: string
    quantity: number
    price: number        // REMOVE — server looks up
    costPrice: number    // REMOVE — server looks up
    discount: number
    serialUnitId?: string | null
  }>
  payments: Array<{ method: PaymentMethod; amount: number }>
  comment?: string
})
```

From src/actions/sales.ts (CURRENT createReturn — line 538):
```typescript
export async function createReturn(data: {
  saleId: string
  items: Array<{ saleItemId: string; quantity: number }>
  reason: string
  refundMethod?: "CASH" | "CARD" | "SBP" | "TRANSFER" | "CREDIT"
})
// Line 538: await requirePermission("pos.return")  // NO storeId!
```

Prisma StoreProduct composite key:
```
@@unique([storeId, productId], name: "storeId_productId")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest Wave 0 + Zod-схема для createSale</name>
  <files>vitest.config.ts, package.json, src/lib/validations/sales.ts, src/__tests__/sales-validation.test.ts, src/__tests__/rate-limit.test.ts, src/__tests__/password-validation.test.ts</files>
  <read_first>
    - src/lib/validations/trade-in.ts (existing Zod schema pattern in project)
    - src/lib/validations/catalog.ts (existing Zod schema pattern)
    - tsconfig.json (path aliases: @/* -> ./src/*)
    - package.json (current devDependencies)
  </read_first>
  <action>
1. Установить vitest минимально:
   ```bash
   npm install -D vitest
   ```

2. Создать `vitest.config.ts`:
   ```typescript
   import { defineConfig } from "vitest/config"
   import path from "path"

   export default defineConfig({
     test: {
       globals: true,
       environment: "node",
     },
     resolve: {
       alias: {
         "@": path.resolve(__dirname, "./src"),
       },
     },
   })
   ```

3. Создать `src/lib/validations/sales.ts` — Zod-схема для безопасного createSale input. Клиент НЕ передаёт price/costPrice:
   ```typescript
   import { z } from "zod"

   export const createSaleItemSchema = z.object({
     productId: z.string().min(1, "productId обязателен"),
     quantity: z.number().int("Количество должно быть целым числом").positive("Количество должно быть > 0"),
     discount: z.number().min(0, "Скидка не может быть отрицательной"),
     serialUnitId: z.string().nullable().optional(),
   })

   export const createSaleSchema = z.object({
     storeId: z.string().min(1, "storeId обязателен"),
     items: z.array(createSaleItemSchema).min(1, "Добавьте товары"),
     payments: z.array(z.object({
       method: z.enum(["CASH", "CARD", "SBP", "TRANSFER", "CREDIT"]),
       amount: z.number().positive("Сумма оплаты должна быть > 0"),
     })).min(1, "Укажите способ оплаты"),
     comment: z.string().optional(),
   })

   export type CreateSaleInput = z.infer<typeof createSaleSchema>
   ```

4. Создать `src/__tests__/sales-validation.test.ts` — тесты-заглушки для SEC-01..06 (RED — должны падать до Task 2):
   ```typescript
   import { describe, it, expect } from "vitest"
   import { createSaleSchema } from "@/lib/validations/sales"

   describe("createSaleSchema", () => {
     it("does NOT accept price field from client (SEC-01)", () => {
       const input = {
         storeId: "store-1",
         items: [{ productId: "p1", quantity: 1, discount: 0, price: 999 }],
         payments: [{ method: "CASH" as const, amount: 100 }],
       }
       const result = createSaleSchema.safeParse(input)
       // price field should be stripped (not in schema)
       expect(result.success).toBe(true)
       if (result.success) {
         expect((result.data.items[0] as any).price).toBeUndefined()
       }
     })

     it("does NOT accept costPrice field from client (SEC-02)", () => {
       const input = {
         storeId: "store-1",
         items: [{ productId: "p1", quantity: 1, discount: 0, costPrice: 50 }],
         payments: [{ method: "CASH" as const, amount: 100 }],
       }
       const result = createSaleSchema.safeParse(input)
       expect(result.success).toBe(true)
       if (result.success) {
         expect((result.data.items[0] as any).costPrice).toBeUndefined()
       }
     })

     it("rejects negative discount (SEC-03)", () => {
       const input = {
         storeId: "store-1",
         items: [{ productId: "p1", quantity: 1, discount: -10 }],
         payments: [{ method: "CASH" as const, amount: 100 }],
       }
       const result = createSaleSchema.safeParse(input)
       expect(result.success).toBe(false)
     })

     it("rejects non-integer quantity (SEC-04)", () => {
       const input = {
         storeId: "store-1",
         items: [{ productId: "p1", quantity: 1.5, discount: 0 }],
         payments: [{ method: "CASH" as const, amount: 100 }],
       }
       const result = createSaleSchema.safeParse(input)
       expect(result.success).toBe(false)
     })

     it("rejects quantity <= 0 (SEC-04)", () => {
       const input = {
         storeId: "store-1",
         items: [{ productId: "p1", quantity: 0, discount: 0 }],
         payments: [{ method: "CASH" as const, amount: 100 }],
       }
       const result = createSaleSchema.safeParse(input)
       expect(result.success).toBe(false)
     })

     it("accepts valid sale input", () => {
       const input = {
         storeId: "store-1",
         items: [{ productId: "p1", quantity: 2, discount: 10 }],
         payments: [{ method: "CASH" as const, amount: 200 }],
       }
       const result = createSaleSchema.safeParse(input)
       expect(result.success).toBe(true)
     })
   })
   ```

5. Создать `src/__tests__/rate-limit.test.ts` — заглушка (реализация в Plan 02):
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("rate-limit", () => {
     it.todo("blocks after 5 failed attempts (AUTH-02)")
     it.todo("unlocks after 15 minutes (AUTH-02)")
     it.todo("resets counter on successful login (AUTH-02)")
   })
   ```

6. Создать `src/__tests__/password-validation.test.ts` — заглушка (реализация в Plan 02):
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("password-validation", () => {
     it.todo("rejects password shorter than 8 characters (AUTH-03)")
     it.todo("accepts password of 8+ characters (AUTH-03)")
   })
   ```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists and contains `alias: { "@": path.resolve(__dirname, "./src") }`
    - src/lib/validations/sales.ts exports createSaleSchema and CreateSaleInput
    - src/lib/validations/sales.ts does NOT contain `price:` or `costPrice:` fields in createSaleItemSchema
    - src/__tests__/sales-validation.test.ts contains "SEC-01", "SEC-02", "SEC-03", "SEC-04"
    - src/__tests__/rate-limit.test.ts contains "AUTH-02"
    - src/__tests__/password-validation.test.ts contains "AUTH-03"
    - `npx vitest run` exits with code 0 (all Zod schema tests pass, todo tests are skipped)
  </acceptance_criteria>
  <done>Vitest запускается, Zod-схема для createSale валидирует input без price/costPrice, тесты для schema проходят зелёным</done>
</task>

<task type="auto">
  <name>Task 2: Защита createSale — серверная загрузка цен, проверка смены, валидация скидок (SEC-01..05)</name>
  <files>src/actions/sales.ts, src/lib/permissions-list.ts</files>
  <read_first>
    - src/actions/sales.ts (полный файл — текущая реализация createSale строки 122-279)
    - src/lib/validations/sales.ts (создано в Task 1 — Zod-схема)
    - src/lib/permissions-list.ts (текущие permission codes)
    - prisma/schema.prisma (модели StoreProduct, SerialUnit, Shift)
  </read_first>
  <action>
1. В `src/lib/permissions-list.ts` добавить новый permission code ПОСЛЕ POS_DISCOUNT:
   ```typescript
   POS_DISCOUNT_HIGH: { code: "pos.discount_high", module: "pos", name: "Скидка свыше 30%" },
   ```
   Также добавить его в ROLE_PRESETS.owner и ROLE_PRESETS.director (в массив permissions).

2. Переписать `createSale` в `src/actions/sales.ts`:

   a) Изменить сигнатуру — убрать price и costPrice из items:
   ```typescript
   import { createSaleSchema, type CreateSaleInput } from "@/lib/validations/sales"

   export async function createSale(rawData: CreateSaleInput) {
     // 1. Zod validation
     const data = createSaleSchema.parse(rawData)

     // 2. Auth
     await requirePermission("pos.sell", data.storeId)
     const session = await auth()
     if (!session?.user?.id) throw new Error("Не авторизован")
     const sellerId = session.user.id
   ```

   b) Внутри `db.$transaction` — ПЕРВЫМ ДЕЛОМ проверить открытую смену (SEC-05):
   ```typescript
   const openShift = await tx.shift.findFirst({
     where: { storeId: data.storeId, status: "OPEN" },
     select: { id: true },
   })
   if (!openShift) {
     throw new Error("Откройте кассовую смену перед продажей")
   }
   const shiftId = openShift.id
   ```

   c) Для каждого item — загружать цены из БД (SEC-01, SEC-02):
   ```typescript
   for (const item of data.items) {
     let price: number
     let costPrice: number
     let productName: string

     if (item.serialUnitId) {
       const unit = await tx.serialUnit.findUnique({
         where: { id: item.serialUnitId },
         include: { product: { select: { name: true } } },
       })
       if (!unit || unit.status !== "IN_STOCK" || unit.storeId !== data.storeId || unit.productId !== item.productId) {
         throw new Error("Серийная единица недоступна для продажи")
       }
       const sp = await tx.storeProduct.findUnique({
         where: { storeId_productId: { storeId: data.storeId, productId: item.productId } },
       })
       if (!sp) throw new Error("Товар не найден в магазине")
       price = Number(sp.sellPrice)
       costPrice = Number(unit.costPrice)
       productName = unit.product.name
     } else {
       const sp = await tx.storeProduct.findUnique({
         where: { storeId_productId: { storeId: data.storeId, productId: item.productId } },
         include: { product: { select: { name: true } } },
       })
       if (!sp) throw new Error("Товар не найден в магазине")
       if (sp.quantity < item.quantity) throw new Error(`Недостаточно товара "${sp.product.name}" на складе`)
       price = Number(sp.sellPrice)
       costPrice = Number(sp.costPrice)
       productName = sp.product.name
     }
   ```

   d) Валидация скидки (SEC-03) — внутри цикла после получения price:
   ```typescript
     // SEC-03: discount bounds
     if (item.discount < 0) throw new Error("Скидка не может быть отрицательной")
     if (item.discount > price) throw new Error("Скидка не может превышать цену товара")

     // High discount check (> 30% of price)
     if (price > 0 && item.discount / price > 0.3) {
       const hasHighDiscountPerm = session.user.permissions?.includes("pos.discount_high")
       if (!hasHighDiscountPerm) {
         throw new Error(`Скидка ${Math.round(item.discount / price * 100)}% превышает 30%. Требуется разрешение "Скидка свыше 30%"`)
       }
     }
   ```

   e) Рассчитать totalAmount, discountAmount, finalAmount на СЕРВЕРЕ из загруженных цен (не от клиента):
   ```typescript
   // After collecting all resolved items:
   const totalAmount = resolvedItems.reduce((sum, ri) => sum + ri.price * ri.quantity, 0)
   const discountAmount = resolvedItems.reduce((sum, ri) => sum + ri.discount * ri.quantity, 0)
   const finalAmount = totalAmount - discountAmount
   ```

   f) Остальная логика (create sale, update serial units, decrement stock) остаётся прежней, но использует серверные price/costPrice вместо клиентских.

3. Проверить что клиентский код (POS компоненты) по-прежнему работает. Если клиент отправляет price/costPrice — они будут проигнорированы Zod-схемой (strict() не нужен, лишние поля просто отбрасываются).
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/sales-validation.test.ts --reporter=verbose 2>&1 && grep -c "sp.sellPrice\|sp\.costPrice\|unit\.costPrice" src/actions/sales.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/sales.ts createSale signature uses `CreateSaleInput` type (not inline type with price/costPrice)
    - src/actions/sales.ts contains `createSaleSchema.parse(rawData)`
    - src/actions/sales.ts contains `Number(sp.sellPrice)` for server-side price lookup
    - src/actions/sales.ts contains `if (!openShift)` followed by `throw new Error("Откройте кассовую смену перед продажей")`
    - src/actions/sales.ts contains `item.discount > price` check
    - src/actions/sales.ts contains `pos.discount_high` string
    - src/lib/permissions-list.ts contains `POS_DISCOUNT_HIGH`
    - src/actions/sales.ts does NOT accept `price: number` or `costPrice: number` in items type
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>createSale загружает все цены из БД, валидирует скидки и количество серверно, требует открытую смену. Клиентские price/costPrice игнорируются.</done>
</task>

<task type="auto">
  <name>Task 3: Защита createReturn — проверка storeId продажи (SEC-06)</name>
  <files>src/actions/sales.ts</files>
  <read_first>
    - src/actions/sales.ts (createReturn — строки 532-580+)
  </read_first>
  <action>
1. В `createReturn` (строка 532+) изменить порядок: СНАЧАЛА загрузить sale, получить storeId, ПОТОМ проверить permission:

   БЫЛО:
   ```typescript
   export async function createReturn(data: { ... }) {
     await requirePermission("pos.return")  // БЕЗ storeId!
     ...
     const sale = await tx.sale.findUnique({ ... })
   ```

   СТАЛО:
   ```typescript
   export async function createReturn(data: {
     saleId: string
     items: Array<{ saleItemId: string; quantity: number }>
     reason: string
     refundMethod?: "CASH" | "CARD" | "SBP" | "TRANSFER" | "CREDIT"
   }) {
     const session = await auth()
     if (!session?.user?.id) throw new Error("Не авторизован")

     if (!data.reason.trim()) throw new Error("Укажите причину возврата")
     if (data.items.length === 0) throw new Error("Выберите товары для возврата")

     // Загрузить продажу ДО permission check, чтобы получить storeId
     const saleForAuth = await db.sale.findUnique({
       where: { id: data.saleId },
       select: { storeId: true },
     })
     if (!saleForAuth) throw new Error("Продажа не найдена")

     // SEC-06: проверка permission с storeId продажи
     await requirePermission("pos.return", saleForAuth.storeId)
     ...
   ```

2. Внутри транзакции — sale уже загружена для auth, но нужна полная загрузка с items. Оставить `tx.sale.findUnique` как есть внутри транзакции для получения items.

3. Также в `searchSaleByNumber` (строка 394) — изменить поиск с `contains` на `equals` (точное совпадение, связано с безопасностью — не показывать чужие продажи по частичному номеру):
   ```typescript
   // БЫЛО:
   number: { contains: number, mode: "insensitive" },

   // СТАЛО:
   number: { equals: number.trim(), mode: "insensitive" },
   ```
   Примечание: это связано с DATA-09 (Phase 2), но поскольку мы уже модифицируем файл, включаем минимальное исправление.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && grep -n 'requirePermission("pos.return"' src/actions/sales.ts && grep -n 'saleForAuth.storeId' src/actions/sales.ts && grep -n 'equals:.*number' src/actions/sales.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/sales.ts createReturn contains `requirePermission("pos.return", saleForAuth.storeId)`
    - src/actions/sales.ts createReturn loads sale BEFORE requirePermission call (line with db.sale.findUnique before requirePermission)
    - src/actions/sales.ts searchSaleByNumber contains `equals:` instead of `contains:` for number field
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>createReturn проверяет storeId продажи в requirePermission. Продавец магазина А не может оформить возврат по продаже магазина Б.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — все тесты зелёные
2. `grep -c "price: number" src/actions/sales.ts` — должно быть 0 в сигнатуре createSale items
3. `grep "pos.discount_high" src/lib/permissions-list.ts` — permission существует
4. `grep "saleForAuth.storeId" src/actions/sales.ts` — storeId передаётся в requirePermission
5. `grep "Откройте кассовую смену" src/actions/sales.ts` — проверка открытой смены
6. `npx tsc --noEmit` — TypeScript компилируется без ошибок
</verification>

<success_criteria>
- createSale не принимает price/costPrice от клиента (SEC-01, SEC-02)
- Скидка валидируется серверно: >= 0, <= price, > 30% требует pos.discount_high (SEC-03)
- quantity > 0, целое, <= остаток (SEC-04)
- Продажа без открытой смены выбрасывает ошибку (SEC-05)
- createReturn проверяет storeId продажи через requirePermission (SEC-06)
- Vitest работает, тесты проходят
</success_criteria>

<output>
After completion, create `.planning/phases/01-security/01-01-SUMMARY.md`
</output>
