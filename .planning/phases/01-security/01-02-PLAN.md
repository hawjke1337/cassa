---
phase: 01-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/auth.ts
  - src/lib/auth.config.ts
  - src/lib/rate-limit.ts
  - src/lib/serial-history.ts
  - src/actions/serial-units.ts
  - src/actions/settings.ts
  - src/__tests__/rate-limit.test.ts
  - src/__tests__/password-validation.test.ts
  - src/__tests__/auth-jwt.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "После изменения ролей пользователя его permissions обновляются при следующем запросе (без перелогина)"
    - "После 5 неудачных попыток логина аккаунт временно блокируется на 15 минут"
    - "Пароль короче 8 символов отклоняется при создании/смене пользователя"
    - "writeSerialHistory не доступна как server action — это internal helper"
  artifacts:
    - path: "src/lib/rate-limit.ts"
      provides: "In-memory rate limiter: checkRateLimit, recordFailedAttempt, clearAttempts"
      exports: ["checkRateLimit", "recordFailedAttempt", "clearAttempts"]
    - path: "src/lib/serial-history.ts"
      provides: "writeSerialHistory extracted from serial-units.ts without 'use server'"
      exports: ["writeSerialHistory"]
    - path: "src/lib/auth.ts"
      provides: "JWT callback with version-based permission reload"
      contains: "permissionsVersion"
    - path: "src/lib/auth.config.ts"
      provides: "session.maxAge: 900 (15 min)"
      contains: "maxAge: 900"
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/lib/rate-limit.ts"
      via: "import checkRateLimit, recordFailedAttempt, clearAttempts in authorize"
      pattern: "checkRateLimit"
    - from: "src/lib/auth.ts"
      to: "prisma User.permissionsVersion"
      via: "version comparison in jwt callback"
      pattern: "permissionsVersion"
    - from: "src/actions/serial-units.ts"
      to: "src/lib/serial-history.ts"
      via: "import writeSerialHistory (not server action)"
      pattern: "from.*serial-history"
---

<objective>
Усилить аутентификацию: JWT обновляет permissions при каждом запросе (version-based), rate limiting на логин (5 попыток / 15 мин), минимум 8 символов пароля, writeSerialHistory вынесена из server actions.

Purpose: Администратор должен иметь возможность немедленно отозвать доступ. Логин должен быть защищён от brute force. Internal helpers не должны быть доступны как server actions.
Output: Обновлённые auth.ts, auth.config.ts, rate-limit.ts, serial-history.ts + тесты.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security/01-CONTEXT.md
@.planning/phases/01-security/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/auth.ts (current — to be rewritten):
```typescript
export const { handlers, auth, signIn, signOut } = NextAuth({ ... })
// jwt callback loads permissions ONLY on first login (user truthy)
// No reload on subsequent requests
```

From src/lib/auth.config.ts (current):
```typescript
export const authConfig = {
  session: { strategy: "jwt" },  // NO maxAge!
  // ...
} satisfies NextAuthConfig
```

From src/lib/permissions.ts:
```typescript
export async function getUserPermissions(userId: string, storeId?: string): Promise<string[]>
```

From src/actions/serial-units.ts (line 188):
```typescript
// This is in a "use server" file — writeSerialHistory is exposed as server action
export async function writeSerialHistory(tx: any, data: {
  serialUnitId: string
  event: string
  storeId: string
  comment: string
  performedById: string
})
```

From src/actions/settings.ts (password validation points):
```typescript
// Line 251: createUser — `data.password.length < 4`
// Line 349: resetUserPassword — `newPassword.length < 4`
// Line 505: changePassword — `data.newPassword.length < 4`
```

From prisma/schema.prisma User model (lines 44-80):
```prisma
model User {
  id        String   @id @default(cuid())
  login     String   @unique
  password  String
  // ... NO permissionsVersion field currently
}
```

From src/actions/settings.ts toggleUserActive (line 327):
```typescript
export async function toggleUserActive(id: string) {
  // Does NOT increment permissionsVersion
}
```

From src/actions/settings.ts updateUserRoles (line 384):
```typescript
export async function updateUserRoles(userId: string, assignments: ...) {
  // Deletes and recreates roles
  // Does NOT increment permissionsVersion
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema + rate-limit + serial-history + password hardening (AUTH-02, AUTH-03, AUTH-04)</name>
  <files>prisma/schema.prisma, src/lib/rate-limit.ts, src/lib/serial-history.ts, src/actions/serial-units.ts, src/actions/settings.ts, src/__tests__/rate-limit.test.ts, src/__tests__/password-validation.test.ts</files>
  <read_first>
    - prisma/schema.prisma (model User — строки 44-80, нужно добавить permissionsVersion)
    - src/actions/serial-units.ts (полный файл — найти writeSerialHistory и все её вызовы)
    - src/actions/settings.ts (строки 239-260 createUser, 346-362 resetUserPassword, 497-523 changePassword, 327-344 toggleUserActive, 384-407 updateUserRoles)
    - src/__tests__/rate-limit.test.ts (заглушка из Plan 01)
    - src/__tests__/password-validation.test.ts (заглушка из Plan 01)
  </read_first>
  <action>
1. В `prisma/schema.prisma` добавить поле в model User (после `updatedAt`):
   ```prisma
   permissionsVersion Int @default(1)
   ```
   Затем выполнить: `npx prisma db push` (для dev — без миграции).

2. Создать `src/lib/rate-limit.ts`:
   ```typescript
   const loginAttempts = new Map<string, { count: number; firstAttempt: number; lockedUntil: number | null }>()

   const MAX_ATTEMPTS = 5
   const LOCK_DURATION_MS = 15 * 60 * 1000  // 15 minutes
   const WINDOW_MS = 15 * 60 * 1000

   export function checkRateLimit(username: string): { allowed: boolean; retryAfterMs?: number } {
     const now = Date.now()
     const entry = loginAttempts.get(username)

     if (entry?.lockedUntil && now < entry.lockedUntil) {
       return { allowed: false, retryAfterMs: entry.lockedUntil - now }
     }

     if (entry?.lockedUntil && now >= entry.lockedUntil) {
       loginAttempts.delete(username)
       return { allowed: true }
     }

     if (entry && now - entry.firstAttempt > WINDOW_MS) {
       loginAttempts.delete(username)
     }

     return { allowed: true }
   }

   export function recordFailedAttempt(username: string): void {
     const now = Date.now()
     const entry = loginAttempts.get(username) ?? { count: 0, firstAttempt: now, lockedUntil: null }
     entry.count++
     if (entry.count >= MAX_ATTEMPTS) {
       entry.lockedUntil = now + LOCK_DURATION_MS
     }
     loginAttempts.set(username, entry)
   }

   export function clearAttempts(username: string): void {
     loginAttempts.delete(username)
   }

   // Cleanup stale entries every 30 minutes
   if (typeof setInterval !== "undefined") {
     setInterval(() => {
       const now = Date.now()
       for (const [key, entry] of loginAttempts) {
         if (entry.lockedUntil && now >= entry.lockedUntil) {
           loginAttempts.delete(key)
         } else if (now - entry.firstAttempt > WINDOW_MS) {
           loginAttempts.delete(key)
         }
       }
     }, 30 * 60 * 1000)
   }
   ```

3. Создать `src/lib/serial-history.ts` — вынести writeSerialHistory из serial-units.ts:
   - НЕ добавлять "use server" в файл!
   - Скопировать функцию writeSerialHistory из src/actions/serial-units.ts
   - Экспортировать как обычную функцию (не server action)

4. В `src/actions/serial-units.ts`:
   - Удалить export writeSerialHistory
   - Добавить: `import { writeSerialHistory } from "@/lib/serial-history"`
   - Все вызовы writeSerialHistory внутри файла должны использовать импорт

5. В `src/actions/settings.ts` — изменить минимальную длину пароля 4 -> 8 в трёх местах:
   - Строка 251: `data.password.length < 4` -> `data.password.length < 8`
   - Сообщение: "Пароль должен быть не менее 4 символов" -> "Пароль должен быть не менее 8 символов"
   - Строка 349: `newPassword.length < 4` -> `newPassword.length < 8`
   - Сообщение: аналогично
   - Строка 505: `data.newPassword.length < 4` -> `data.newPassword.length < 8`
   - Сообщение: аналогично
   ВАЖНО: НЕ менять валидацию в authorize callback (src/lib/auth.ts) — существующие короткие пароли должны работать при логине.

6. В `src/actions/settings.ts` — после toggleUserActive и updateUserRoles добавить инкремент permissionsVersion:
   
   В `toggleUserActive` (после db.user.update):
   ```typescript
   // Если деактивируем — инкрементить версию permissions для инвалидации JWT
   await db.user.update({
     where: { id },
     data: { 
       isActive: !user.isActive,
       permissionsVersion: { increment: 1 },
     },
   })
   ```
   
   В `updateUserRoles` (после createMany):
   ```typescript
   // Инкрементить версию permissions для инвалидации JWT
   await db.user.update({
     where: { id: userId },
     data: { permissionsVersion: { increment: 1 } },
   })
   ```

7. Обновить `src/__tests__/rate-limit.test.ts` — заменить todo на реальные тесты:
   ```typescript
   import { describe, it, expect, beforeEach } from "vitest"
   import { checkRateLimit, recordFailedAttempt, clearAttempts } from "@/lib/rate-limit"

   describe("rate-limit", () => {
     beforeEach(() => {
       clearAttempts("testuser")
     })

     it("allows first attempt", () => {
       const result = checkRateLimit("testuser")
       expect(result.allowed).toBe(true)
     })

     it("blocks after 5 failed attempts (AUTH-02)", () => {
       for (let i = 0; i < 5; i++) {
         recordFailedAttempt("testuser")
       }
       const result = checkRateLimit("testuser")
       expect(result.allowed).toBe(false)
       expect(result.retryAfterMs).toBeGreaterThan(0)
     })

     it("allows 4 failed attempts without blocking", () => {
       for (let i = 0; i < 4; i++) {
         recordFailedAttempt("testuser")
       }
       const result = checkRateLimit("testuser")
       expect(result.allowed).toBe(true)
     })

     it("resets counter on clearAttempts", () => {
       for (let i = 0; i < 5; i++) {
         recordFailedAttempt("testuser")
       }
       clearAttempts("testuser")
       const result = checkRateLimit("testuser")
       expect(result.allowed).toBe(true)
     })

     it("isolates different usernames", () => {
       for (let i = 0; i < 5; i++) {
         recordFailedAttempt("user-a")
       }
       const resultA = checkRateLimit("user-a")
       const resultB = checkRateLimit("user-b")
       expect(resultA.allowed).toBe(false)
       expect(resultB.allowed).toBe(true)
     })
   })
   ```

8. Обновить `src/__tests__/password-validation.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   // Testing the validation logic directly (not the server action)
   function validatePassword(password: string): boolean {
     return !!password && password.length >= 8
   }

   describe("password-validation (AUTH-03)", () => {
     it("rejects password shorter than 8 characters", () => {
       expect(validatePassword("1234")).toBe(false)
       expect(validatePassword("1234567")).toBe(false)
       expect(validatePassword("")).toBe(false)
     })

     it("accepts password of exactly 8 characters", () => {
       expect(validatePassword("12345678")).toBe(true)
     })

     it("accepts password longer than 8 characters", () => {
       expect(validatePassword("MyPassword123")).toBe(true)
     })
   })
   ```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/rate-limit.test.ts src/__tests__/password-validation.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma User model contains `permissionsVersion Int @default(1)`
    - src/lib/rate-limit.ts exports checkRateLimit, recordFailedAttempt, clearAttempts
    - src/lib/rate-limit.ts contains `MAX_ATTEMPTS = 5` and `LOCK_DURATION_MS = 15 * 60 * 1000`
    - src/lib/serial-history.ts exists and does NOT contain "use server"
    - src/lib/serial-history.ts exports writeSerialHistory
    - src/actions/serial-units.ts contains `import { writeSerialHistory } from "@/lib/serial-history"`
    - src/actions/serial-units.ts does NOT export writeSerialHistory directly
    - src/actions/settings.ts contains `length < 8` (three occurrences)
    - src/actions/settings.ts contains `не менее 8 символов` (three occurrences)
    - src/actions/settings.ts toggleUserActive contains `permissionsVersion: { increment: 1 }`
    - src/actions/settings.ts updateUserRoles contains `permissionsVersion: { increment: 1 }`
    - `npx vitest run src/__tests__/rate-limit.test.ts` exits 0
    - `npx vitest run src/__tests__/password-validation.test.ts` exits 0
  </acceptance_criteria>
  <done>Rate limiter работает (тесты зелёные), пароли минимум 8 символов, writeSerialHistory — internal helper, permissionsVersion добавлен в схему и инкрементируется при смене ролей/деактивации</done>
</task>

<task type="auto">
  <name>Task 2: JWT callback с перезагрузкой permissions + rate limiting в authorize (AUTH-01, AUTH-02)</name>
  <files>src/lib/auth.ts, src/lib/auth.config.ts, src/__tests__/auth-jwt.test.ts</files>
  <read_first>
    - src/lib/auth.ts (полный файл — текущий authorize и jwt callback)
    - src/lib/auth.config.ts (полный файл — текущая конфигурация session)
    - src/lib/rate-limit.ts (создано в Task 1)
    - src/lib/permissions.ts (getUserPermissions signature)
  </read_first>
  <action>
1. Обновить `src/lib/auth.config.ts` — добавить maxAge: 900 (15 минут):
   ```typescript
   import type { NextAuthConfig } from "next-auth"

   export const authConfig = {
     session: { strategy: "jwt", maxAge: 900 },  // 15 minutes
     pages: { signIn: "/login" },
     providers: [],
     callbacks: {
       async jwt({ token, user }) {
         if (user) {
           token.id = user.id as string
           token.login = (user as any).login ?? ""
         }
         return token
       },
       async session({ session, token }) {
         if (session.user) {
           session.user.id = token.id as string
           session.user.login = token.login as string
         }
         return session
       },
       authorized({ auth, request: { nextUrl } }) {
         const isLoggedIn = !!auth?.user
         const isOnLogin = nextUrl.pathname.startsWith("/login")
         const isAuthApi = nextUrl.pathname.startsWith("/api/auth")

         if (isAuthApi) return true
         if (!isLoggedIn && !isOnLogin) {
           return Response.redirect(new URL("/login", nextUrl))
         }
         if (isLoggedIn && isOnLogin) {
           return Response.redirect(new URL("/", nextUrl))
         }
         return true
       },
     },
   } satisfies NextAuthConfig
   ```

2. Переписать `src/lib/auth.ts` — добавить rate limiting в authorize и version-based permission reload в jwt callback:
   ```typescript
   import NextAuth from "next-auth"
   import Credentials from "next-auth/providers/credentials"
   import { compare } from "bcryptjs"
   import { db } from "@/lib/db"
   import { authConfig } from "@/lib/auth.config"
   import { getUserPermissions } from "@/lib/permissions"
   import { checkRateLimit, recordFailedAttempt, clearAttempts } from "@/lib/rate-limit"

   export const { handlers, auth, signIn, signOut } = NextAuth({
     ...authConfig,
     providers: [
       Credentials({
         credentials: {
           login: { label: "Login", type: "text" },
           password: { label: "Password", type: "password" },
         },
         async authorize(credentials) {
           if (!credentials?.login || !credentials?.password) return null

           const username = (credentials.login as string).toLowerCase()

           // AUTH-02: Rate limiting
           const rateCheck = checkRateLimit(username)
           if (!rateCheck.allowed) {
             throw new Error(
               `Слишком много попыток. Повторите через ${Math.ceil(rateCheck.retryAfterMs! / 60000)} мин.`
             )
           }

           const user = await db.user.findUnique({
             where: { login: credentials.login as string },
           })

           if (!user || !user.isActive) {
             recordFailedAttempt(username)
             return null
           }

           const isValid = await compare(
             credentials.password as string,
             user.password
           )
           if (!isValid) {
             recordFailedAttempt(username)
             return null
           }

           clearAttempts(username)
           return {
             id: user.id,
             name: `${user.firstName} ${user.lastName}`,
             login: user.login,
           }
         },
       }),
     ],
     callbacks: {
       ...authConfig.callbacks,
       async jwt({ token, user }) {
         if (user) {
           // First login: load everything
           token.id = user.id as string
           token.login = (user as any).login ?? ""

           const dbUser = await db.user.findUnique({
             where: { id: user.id! },
             select: { permissionsVersion: true },
           })

           const userRoles = await db.userRole.findMany({
             where: { userId: user.id! },
             include: { role: true },
           })
           token.roles = userRoles.map(ur => ur.role.name)
           token.permissions = await getUserPermissions(user.id!)
           token.permissionsVersion = dbUser?.permissionsVersion ?? 1
         } else if (token.id) {
           // AUTH-01: Subsequent requests — check if permissions changed
           const dbUser = await db.user.findUnique({
             where: { id: token.id as string },
             select: { permissionsVersion: true, isActive: true },
           })

           // If user was deactivated, invalidate token
           if (!dbUser || !dbUser.isActive) {
             token.permissions = []
             token.roles = []
             return token
           }

           if (dbUser.permissionsVersion !== token.permissionsVersion) {
             const userRoles = await db.userRole.findMany({
               where: { userId: token.id as string },
               include: { role: true },
             })
             token.roles = userRoles.map(ur => ur.role.name)
             token.permissions = await getUserPermissions(token.id as string)
             token.permissionsVersion = dbUser.permissionsVersion
           }
         }
         return token
       },
       async session({ session, token }) {
         if (session.user) {
           session.user.id = token.id as string
           session.user.login = token.login as string
           session.user.roles = token.roles as string[]
           session.user.permissions = token.permissions as string[]
         }
         return session
       },
     },
   })
   ```

3. Обновить `src/__tests__/auth-jwt.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   // AUTH-01: Version-based permission reload concept test
   // Full integration test requires DB — this tests the logic concept
   describe("auth-jwt permission reload (AUTH-01)", () => {
     it("detects version mismatch requiring reload", () => {
       const tokenVersion = 1
       const dbVersion = 2
       const needsReload = dbVersion !== tokenVersion
       expect(needsReload).toBe(true)
     })

     it("skips reload when versions match", () => {
       const tokenVersion = 3
       const dbVersion = 3
       const needsReload = dbVersion !== tokenVersion
       expect(needsReload).toBe(false)
     })

     it("invalidates deactivated user", () => {
       const dbUser = { isActive: false, permissionsVersion: 1 }
       const shouldInvalidate = !dbUser || !dbUser.isActive
       expect(shouldInvalidate).toBe(true)
     })
   })
   ```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run --reporter=verbose 2>&1 && grep "maxAge: 900" src/lib/auth.config.ts && grep "checkRateLimit" src/lib/auth.ts && grep "permissionsVersion" src/lib/auth.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/auth.config.ts contains `maxAge: 900`
    - src/lib/auth.ts authorize contains `checkRateLimit(username)`
    - src/lib/auth.ts authorize contains `recordFailedAttempt(username)`
    - src/lib/auth.ts authorize contains `clearAttempts(username)`
    - src/lib/auth.ts jwt callback contains `permissionsVersion` comparison
    - src/lib/auth.ts jwt callback contains `if (!dbUser || !dbUser.isActive)` with empty permissions
    - src/lib/auth.ts jwt callback contains `getUserPermissions(token.id` for reload
    - `npx vitest run` exits 0 (all tests green)
    - `npx tsc --noEmit` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>JWT callback перезагружает permissions при изменении версии, rate limiting блокирует после 5 попыток, maxAge = 15 минут. При деактивации пользователя permissions обнуляются.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — все тесты зелёные
2. `grep "maxAge: 900" src/lib/auth.config.ts` — JWT expires in 15 min
3. `grep "checkRateLimit" src/lib/auth.ts` — rate limiting integrated
4. `grep "permissionsVersion" src/lib/auth.ts` — version-based reload
5. `grep "permissionsVersion" prisma/schema.prisma` — field exists
6. `grep "use server" src/lib/serial-history.ts` — should return nothing
7. `grep 'length < 8' src/actions/settings.ts` — three occurrences
8. `npx tsc --noEmit` — TypeScript compiles
</verification>

<success_criteria>
- JWT обновляет permissions при каждом запросе, если permissionsVersion изменилась (AUTH-01)
- Rate limiting: 5 попыток -> блокировка 15 минут, по username (AUTH-02)
- Пароль минимум 8 символов в createUser, resetUserPassword, changePassword (AUTH-03)
- writeSerialHistory — обычный импорт из src/lib/serial-history.ts, не server action (AUTH-04)
- Деактивация пользователя и смена ролей инкрементируют permissionsVersion
- Все тесты зелёные
</success_criteria>

<output>
After completion, create `.planning/phases/01-security/01-02-SUMMARY.md`
</output>
