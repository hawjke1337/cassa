---
phase: 13-suppliers-debts
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/orders/order-detail.tsx
  - src/components/suppliers/supplier-detail.tsx
  - src/components/suppliers/debt-payment-dialog.tsx
  - src/app/(dashboard)/suppliers/debts/page.tsx
  - src/components/dashboard/dashboard-content.tsx
  - src/actions/dashboard.ts
  - src/actions/suppliers.ts
  - src/components/layout/app-sidebar.tsx
  - src/app/(dashboard)/reports/supplier-debts/page.tsx
autonomous: false
requirements: [SUP-03, SUP-04, SUP-07, SUP-08, SUP-09]

must_haves:
  truths:
    - "Order card shows three amounts (Client price / Purchase / Profit) visible only with orders.costs permission"
    - "Supplier city displayed in order card from Supplier.city"
    - "Profit shows 'Не рассчитана' (grey text) when purchasePrice not entered"
    - "Debt payment dialog (AlertDialog) shows amount, remaining, comment field before payment"
    - "/suppliers/debts page shows debts with filters and totals"
    - "Dashboard shows 'Долги поставщикам: X rub (N неоплаченных)' card with orders.costs permission"
    - "Supplier card shows payment history table (date, amount, order #, comment)"
    - "Sidebar has 'Долги' sub-item under 'Поставщики'"
  artifacts:
    - path: "src/components/suppliers/debt-payment-dialog.tsx"
      provides: "AlertDialog for partial debt payment"
      contains: "AlertDialog"
    - path: "src/app/(dashboard)/suppliers/debts/page.tsx"
      provides: "New debts page under suppliers section"
      contains: "SupplierDebts"
    - path: "src/components/orders/order-detail.tsx"
      provides: "Three amounts display with permission guard"
      contains: "Не рассчитана"
    - path: "src/components/suppliers/supplier-detail.tsx"
      provides: "Payment history table"
      contains: "SupplierPayment"
  key_links:
    - from: "src/components/suppliers/debt-payment-dialog.tsx"
      to: "src/actions/orders.ts"
      via: "paySupplierDebt server action call"
      pattern: "paySupplierDebt"
    - from: "src/components/dashboard/dashboard-content.tsx"
      to: "src/actions/dashboard.ts"
      via: "getDashboardData includes supplierDebts aggregate"
      pattern: "supplierDebts"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/app/(dashboard)/suppliers/debts/page.tsx"
      via: "Sidebar nav item href=/suppliers/debts"
      pattern: "/suppliers/debts"
---

<objective>
UI updates across 5 areas: order detail card (3 amounts + supplier city), debt payment AlertDialog, /suppliers/debts page, dashboard card, supplier card payment history, sidebar navigation.

Purpose: Users can see financial breakdown, pay debts partially, view debt summaries, and navigate to debts page.
Output: Complete UI layer for supplier debt management.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-suppliers-debts/13-CONTEXT.md
@.planning/phases/13-suppliers-debts/13-RESEARCH.md
@.planning/phases/13-suppliers-debts/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: paySupplierDebt signature -->
```typescript
export async function paySupplierDebt(
  debtId: string,
  paymentAmount: number | string,
  comment?: string,
): Promise<{ paymentId: string; fullyPaid: boolean; remaining: string }>
```

<!-- SupplierPayment model fields -->
```prisma
model SupplierPayment {
  id, debtId, amount (Decimal 12,2), comment, cashOperationId, userId, paidAt, createdAt, updatedAt
}
```

<!-- StatCard component (src/components/dashboard/stat-card.tsx): -->
```typescript
interface StatCardProps {
  title: string; value: string | number; icon: LucideIcon;
  description?: string; trend?: { value: number; label: string };
  href?: string;
}
```

<!-- Dashboard data action (src/actions/dashboard.ts): -->
```typescript
export async function getDashboardData(storeId: string)
// Returns: { todaySales, todayRevenue, activeOrders, pendingRepairs, ... }
```

<!-- Sidebar nav structure (src/components/layout/app-sidebar.tsx line 111-115): -->
```typescript
{
  title: "Поставщики",
  href: "/suppliers",
  icon: Truck,
  requiredPermissions: ["suppliers.view"],
},
```

<!-- Existing debts report (src/app/(dashboard)/reports/supplier-debts/): -->
<!-- Has SupplierDebtsClient component with filters -->

<!-- formatMoney helper: -->
```typescript
import { formatMoney } from "@/lib/format"
// formatMoney(1234.56) => "1 234,56 ₽"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order detail card (3 amounts + city) + DebtPaymentDialog + supplier detail payment history</name>
  <files>
    src/components/orders/order-detail.tsx,
    src/components/suppliers/debt-payment-dialog.tsx,
    src/components/suppliers/supplier-detail.tsx,
    src/actions/suppliers.ts
  </files>
  <read_first>
    - src/components/orders/order-detail.tsx (full file — find finance section, understand current layout)
    - src/components/suppliers/supplier-detail.tsx (full file — find unpaid debts section)
    - src/actions/suppliers.ts (full file — getSupplier query, understand includes)
    - src/actions/orders.ts (lines 484-510 — verify paySupplierDebt exists from Plan 01)
    - src/components/dashboard/stat-card.tsx (component interface for reference)
    - src/lib/format.ts (formatMoney signature)
  </read_first>
  <action>
**1. Create `src/components/suppliers/debt-payment-dialog.tsx`:**

AlertDialog with:
- Props: `debtId: string, debtAmount: string, totalPaid: string, orderNumber: string, onSuccess: () => void`
- Computed `remaining = sub(debtAmount, totalPaid)` using money helpers
- Input field for payment amount (default: remaining amount)
- Textarea for optional comment
- Display: "Сумма долга: {formatMoney(debtAmount)}", "Уже оплачено: {formatMoney(totalPaid)}", "Остаток: {formatMoney(remaining)}"
- On confirm: call `paySupplierDebt(debtId, amount, comment)` server action
- Toast on success: "Оплата зафиксирована" with `sonner`
- Toast on error with error message
- Uses: AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger from shadcn/ui
- Trigger button: "Оплатить" (variant="outline", size="sm")
- Validate: amount > 0, amount <= remaining

**2. Update `src/components/orders/order-detail.tsx`:**

Find the finance/pricing section. Add three clearly labeled amounts visible ONLY with `orders.costs` permission (use `checkPermission` or conditional rendering based on user permissions prop):

```
Цена клиенту: {formatMoney(order.totalAmount)}
Закуп: {order.purchasePrice ? formatMoney(order.purchasePrice) : "—"}
Прибыль: {order.purchasePrice
  ? formatMoney(sub(order.totalAmount, sum(order.purchasePrice, order.deliveryCost ?? 0)))
  : <span className="text-muted-foreground">Не рассчитана</span>}
```

Also display supplier city: "Город: {order.supplier?.city ?? '—'}" — pull from Supplier.city relation (ensure the query includes supplier).

**3. Update `src/actions/suppliers.ts` — getSupplier to include payment history:**

In `getSupplier(id)`, add to the include:
```typescript
debts: {
  include: {
    payments: {
      orderBy: { paidAt: "desc" },
      include: { user: { select: { name: true } } },
    },
    order: { select: { number: true } },
  },
  orderBy: { createdAt: "desc" },
},
```

**4. Update `src/components/suppliers/supplier-detail.tsx`:**

Add a "История платежей" section after existing debts display. Table columns:
- Дата (payment.paidAt, formatted as dd.MM.yyyy HH:mm)
- Сумма (formatMoney(payment.amount))
- Заказ # (debt.order.number, link to /orders/{orderId})
- Комментарий (payment.comment || "—")
- Оператор (payment.user.name)

Use shadcn/ui Table component. Show "Нет платежей" if empty.

Add DebtPaymentDialog button to each unpaid debt row — pass debtId, debtAmount, totalPaid (sum of debt.payments amounts), orderNumber.
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && test -f src/components/suppliers/debt-payment-dialog.tsx && grep -c "AlertDialog" src/components/suppliers/debt-payment-dialog.tsx && grep -c "Не рассчитана" src/components/orders/order-detail.tsx && grep -c "payments" src/actions/suppliers.ts && grep -c "История платежей" src/components/suppliers/supplier-detail.tsx && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/suppliers/debt-payment-dialog.tsx exists, contains `AlertDialog`, `paySupplierDebt`, `Оплатить`
    - src/components/suppliers/debt-payment-dialog.tsx shows remaining = sub(debtAmount, totalPaid)
    - src/components/orders/order-detail.tsx contains "Не рассчитана" for missing purchasePrice
    - src/components/orders/order-detail.tsx shows three amounts: totalAmount, purchasePrice, profit
    - src/components/orders/order-detail.tsx shows supplier city from Supplier.city relation
    - src/actions/suppliers.ts getSupplier includes `payments` in debts include
    - src/components/suppliers/supplier-detail.tsx contains "История платежей" section with Table
    - src/components/suppliers/supplier-detail.tsx renders DebtPaymentDialog for unpaid debts
    - `pnpm tsc --noEmit` has no new errors in modified files
  </acceptance_criteria>
  <done>Order card shows 3 amounts with permission guard + grey "Не рассчитана"; debt payment dialog works with AlertDialog; supplier card shows payment history table with pay button on unpaid debts</done>
</task>

<task type="auto">
  <name>Task 2: Debts page + dashboard card + sidebar navigation + reports page update</name>
  <files>
    src/app/(dashboard)/suppliers/debts/page.tsx,
    src/components/dashboard/dashboard-content.tsx,
    src/actions/dashboard.ts,
    src/components/layout/app-sidebar.tsx,
    src/app/(dashboard)/reports/supplier-debts/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/reports/supplier-debts/page.tsx (full file — existing debts page structure)
    - src/components/dashboard/dashboard-content.tsx (full file — current stat cards)
    - src/actions/dashboard.ts (full file — getDashboardData query structure)
    - src/components/layout/app-sidebar.tsx (full file — nav structure, how items are defined)
    - src/actions/reports.ts (getSupplierDebtsReport — current query for debts report)
  </read_first>
  <action>
**1. Create `src/app/(dashboard)/suppliers/debts/page.tsx`:**

New page at /suppliers/debts showing all debts. Structure:
- Page title: "Долги поставщикам"
- Use `getSupplierDebtsReport` server action (from src/actions/reports.ts) to fetch data
- Group debts by supplier (Claude's discretion)
- Show totals at top: "Всего: {formatMoney(totalDebt)}", "Неоплачено: {formatMoney(unpaidTotal)} ({unpaidCount})"
- Each debt row: order number (link), debt amount, paid amount (sum of payments), remaining, status badge (Оплачен/Частично/Не оплачен), DebtPaymentDialog button for unpaid debts
- Pagination with standard shadcn/ui pagination (10 per page)
- Permission guard: page requires `orders.costs` (use `requirePermission` in server component or redirect)

The page can reuse `SupplierDebtsClient` component from reports if it fits, or create a new client component.

**2. Update `src/app/(dashboard)/reports/supplier-debts/page.tsx`:**

Update existing reports page to show partial payment info:
- Add "Оплачено" column showing sum(payments) for each debt
- Add "Остаток" column
- Add DebtPaymentDialog button to each unpaid debt row
- Import DebtPaymentDialog from `@/components/suppliers/debt-payment-dialog`

**3. Update `src/actions/dashboard.ts` — add supplier debts aggregate:**

In `getDashboardData`, add query:
```typescript
const supplierDebts = await db.supplierDebt.aggregate({
  where: { isPaid: false, order: { storeId } },
  _sum: { amount: true },
  _count: true,
})
```

Return in result object:
```typescript
supplierDebtsTotal: supplierDebts._sum.amount ? String(supplierDebts._sum.amount) : "0",
supplierDebtsCount: supplierDebts._count,
```

**4. Update `src/components/dashboard/dashboard-content.tsx`:**

Add supplier debts stat card (after existing cards), visible ONLY with `orders.costs` permission. Use `usePermissions()` hook or check from session data — follow existing permission check pattern in the component.

```tsx
{hasPermission("orders.costs") && (
  <Link href="/suppliers/debts">
    <StatCard
      title="Долги поставщикам"
      value={formatMoney(data.supplierDebtsTotal)}
      icon={Truck}
      description={`${data.supplierDebtsCount} неоплаченных`}
    />
  </Link>
)}
```

Import `Truck` from lucide-react (already imported in sidebar, verify in dashboard).

**5. Update `src/components/layout/app-sidebar.tsx`:**

Change the "Поставщики" nav item from a simple link to a group with sub-items. Check if sidebar already supports `items` array (sub-menu pattern). If it does:

```typescript
{
  title: "Поставщики",
  href: "/suppliers",
  icon: Truck,
  requiredPermissions: ["suppliers.view"],
  items: [
    { title: "Все поставщики", href: "/suppliers" },
    { title: "Долги", href: "/suppliers/debts", requiredPermissions: ["orders.costs"] },
  ],
},
```

If sidebar doesn't support sub-items, add "Долги поставщикам" as a separate top-level item right after "Поставщики":
```typescript
{
  title: "Долги поставщикам",
  href: "/suppliers/debts",
  icon: Receipt,
  requiredPermissions: ["orders.costs"],
},
```
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && test -f src/app/\(dashboard\)/suppliers/debts/page.tsx && grep -c "supplierDebts" src/actions/dashboard.ts && grep -c "Долги" src/components/layout/app-sidebar.tsx && grep -c "suppliers/debts" src/components/layout/app-sidebar.tsx && pnpm tsc --noEmit 2>&1 | grep -E "suppliers/debts|dashboard|app-sidebar" | head -10 || pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/suppliers/debts/page.tsx exists with "Долги поставщикам" title
    - src/app/(dashboard)/suppliers/debts/page.tsx shows debts grouped by supplier with totals
    - src/app/(dashboard)/suppliers/debts/page.tsx has permission guard for orders.costs
    - src/actions/dashboard.ts getDashboardData returns `supplierDebtsTotal` and `supplierDebtsCount`
    - src/components/dashboard/dashboard-content.tsx contains StatCard with "Долги поставщикам"
    - src/components/dashboard/dashboard-content.tsx supplier card guarded by orders.costs permission
    - src/components/layout/app-sidebar.tsx contains "/suppliers/debts" href
    - src/components/layout/app-sidebar.tsx contains "Долги" text
    - src/app/(dashboard)/reports/supplier-debts/page.tsx shows payment amount columns
    - `pnpm tsc --noEmit` has no new errors in modified files
  </acceptance_criteria>
  <done>Debts page at /suppliers/debts works with grouped view; dashboard shows supplier debts card; sidebar has "Долги" sub-item; reports page updated with payment columns</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of supplier debt UI</name>
  <files>none</files>
  <action>Human verifies the complete supplier debt management UI across 7 areas: order card amounts, profit calculation, supplier detail payments, debt payment dialog, debts page, dashboard card, sidebar navigation.</action>
  <verify>
    Manual verification:
    1. Open any completed order with supplier - verify three amounts shown (need orders.costs permission)
    2. Open order without purchasePrice - profit shows "Не рассчитана" in grey
    3. Open supplier card - see "История платежей" section
    4. Click "Оплатить" on unpaid debt - AlertDialog shows with remaining amount and comment field
    5. Navigate to /suppliers/debts - see grouped debts with totals
    6. Check dashboard - "Долги поставщикам" card visible
    7. Check sidebar - "Долги" sub-item under "Поставщики"
  </verify>
  <done>User confirms all 7 UI areas render correctly and debt payment flow works</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All new pages render without errors
- Debt payment dialog creates SupplierPayment record
- Dashboard card shows correct totals
- Sidebar navigation works
- Permission guards active on all financial data
</verification>

<success_criteria>
- Order detail shows 3 amounts with permission guard
- "Не рассчитана" displayed for missing purchasePrice
- Supplier city from Supplier.city shown
- DebtPaymentDialog works with AlertDialog
- /suppliers/debts page with grouped debts and totals
- Dashboard supplier debts StatCard with orders.costs guard
- Supplier card payment history table
- Sidebar "Долги" sub-item navigates to /suppliers/debts
- Reports page updated with payment columns
</success_criteria>

<output>
After completion, create `.planning/phases/13-suppliers-debts/13-02-SUMMARY.md`
</output>
