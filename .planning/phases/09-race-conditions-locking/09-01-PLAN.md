---
phase: 09-race-conditions-locking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/sales.ts
  - src/actions/inventory.ts
  - src/lib/stock-helpers.ts
autonomous: true
requirements: [LOCK-01, LOCK-03, LOCK-05]

must_haves:
  truths:
    - "createSale для серийного товара использует SELECT FOR UPDATE на SerialUnit перед изменением статуса"
    - "createSale проверяет SerialUnit.status = IN_STOCK после lock, бросает 'Серийная единица недоступна' при race"
    - "createWriteOff для несерийного товара использует FOR UPDATE на StoreProduct перед decrement"
    - "createWriteOff для серийного товара использует FOR UPDATE на SerialUnit перед WRITTEN_OFF"
    - "Stock decrement в createSale для несерийных использует decrementStockForItems (batch lock + decrement)"
  artifacts:
    - path: "src/actions/sales.ts"
      provides: "createSale with SerialUnit FOR UPDATE lock + batch stock decrement via decrementStockForItems"
      contains: "FOR UPDATE"
    - path: "src/actions/inventory.ts"
      provides: "createWriteOff with FOR UPDATE on StoreProduct and SerialUnit"
      contains: "FOR UPDATE"
    - path: "src/lib/stock-helpers.ts"
      provides: "lockSerialUnits helper for batch FOR UPDATE on SerialUnit rows"
      exports: ["lockSerialUnits"]
  key_links:
    - from: "src/actions/sales.ts"
      to: "src/lib/stock-helpers.ts"
      via: "import { decrementStockForItems, lockSerialUnits }"
      pattern: "lockSerialUnits"
    - from: "src/actions/sales.ts"
      to: "SerialUnit table"
      via: "SELECT ... FOR UPDATE"
      pattern: "FOR UPDATE"
    - from: "src/actions/inventory.ts"
      to: "StoreProduct table"
      via: "SELECT ... FOR UPDATE in createWriteOff"
      pattern: "FOR UPDATE"
---

<objective>
Добавить pessimistic locking (SELECT FOR UPDATE) в createSale для SerialUnit и рефакторнуть stock decrement на использование batch-helper; добавить FOR UPDATE в createWriteOff.

Purpose: Устранить race conditions при одновременных продажах одного серийного товара (BUG-044) и при одновременном списании (BUG-053). Гарантировать атомарный stock decrement (BUG-046).
Output: Hardened createSale и createWriteOff с pessimistic locking.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-race-conditions-locking/09-CONTEXT.md

<interfaces>
<!-- Existing helper in stock-helpers.ts -->
```typescript
export interface StockDecrementItem {
  productId?: string | null
  quantity: number
  serialUnitId?: string | null
}

export async function decrementStockForItems(
tx: Prisma.TransactionClient,
storeId: string,
items: StockDecrementItem[],
): Promise<void>

````

<!-- Existing FOR UPDATE pattern in orders.ts:1062-1070 (to replicate for createSale) -->
```sql
SELECT id FROM "SerialUnit"
WHERE id = ANY($1::text[])
FOR UPDATE
````

<!-- createSale current SerialUnit handling (sales.ts:216-234) — uses findUnique, NO FOR UPDATE -->

```typescript
if (item.serialUnitId) {
  const unit = await tx.serialUnit.findUnique({
    where: { id: item.serialUnitId },
    include: { product: { select: { name: true } } },
  })
  if (
    !unit ||
    unit.status !== "IN_STOCK" ||
    unit.storeId !== data.storeId ||
    unit.productId !== item.productId
  ) {
    throw new Error("Серийная единица недоступна для продажи")
  }
  // ... uses prices from unit
}
```

<!-- createWriteOff current handling (inventory.ts:1279-1299) — findUnique, NO FOR UPDATE -->

```typescript
const sp = await tx.storeProduct.findUnique({
  where: { storeId_productId: { storeId, productId: item.productId } },
})
if (!sp || sp.quantity < item.quantity) {
  throw new Error("Недостаточно товара на складе для списания")
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lockSerialUnits helper + createSale SerialUnit FOR UPDATE + batch stock decrement</name>
  <files>src/lib/stock-helpers.ts, src/actions/sales.ts</files>
  <read_first>
    - src/lib/stock-helpers.ts (existing decrementStockForItems pattern)
    - src/actions/sales.ts (full createSale function, lines 140-400)
    - src/actions/orders.ts lines 1060-1080 (FOR UPDATE pattern for SerialUnit to replicate)
  </read_first>
  <action>
**1. Add `lockSerialUnits` helper to `src/lib/stock-helpers.ts`:**

```typescript
/**
 * LOCK-01: Pessimistic lock на SerialUnit строки через SELECT ... FOR UPDATE.
 * Сортировка по id ASC предотвращает deadlock (единый порядок блокировки).
 * После lock — re-check что все units имеют status = 'IN_STOCK'.
 *
 * @returns Массив locked SerialUnit rows с полями: id, productId, storeId, status, costPrice, imei
 * @throws Error("Серийная единица недоступна для продажи") если хотя бы один unit не IN_STOCK
 */
export async function lockSerialUnits(
  tx: Prisma.TransactionClient,
  serialUnitIds: string[],
  storeId: string,
): Promise<
  {
    id: string
    productId: string
    storeId: string
    status: string
    costPrice: any
    imei: string | null
  }[]
> {
  if (serialUnitIds.length === 0) return []

  const locked = await tx.$queryRaw<
    {
      id: string
      productId: string
      storeId: string
      status: string
      costPrice: any
      imei: string | null
    }[]
  >`
    SELECT id, "productId", "storeId", status, "costPrice", imei
    FROM "SerialUnit"
    WHERE id = ANY(${serialUnitIds}::text[])
    ORDER BY id ASC
    FOR UPDATE
  `

  // Re-check: все units должны быть IN_STOCK и принадлежать storeId
  for (const unit of locked) {
    if (unit.status !== "IN_STOCK") {
      throw new Error("Серийная единица недоступна для продажи")
    }
    if (unit.storeId !== storeId) {
      throw new Error("Серийная единица не принадлежит этому магазину")
    }
  }

  // Проверка что все запрошенные ID найдены
  if (locked.length !== serialUnitIds.length) {
    throw new Error("Серийная единица недоступна для продажи")
  }

  return locked
}
```

**2. Refactor `createSale` in `src/actions/sales.ts`:**

a) **LOCK-01: Replace `findUnique` for SerialUnit with `lockSerialUnits`.**

Before the item loop (around line 210), collect all serialUnitIds from `data.items`:

```typescript
const serialUnitIds = data.items.filter((i) => i.serialUnitId).map((i) => i.serialUnitId as string)
```

Call `lockSerialUnits` BEFORE the item resolution loop (batch lock all serial units at once, BEFORE StoreProduct lock — порядок: SerialUnit first by id ASC is fine since StoreProduct lock is separate query):

```typescript
import { decrementStockForItems, lockSerialUnits } from "@/lib/stock-helpers"

// LOCK-01: Batch lock all SerialUnit rows
const lockedSerials = await lockSerialUnits(tx, serialUnitIds, data.storeId)
const serialMap = new Map(lockedSerials.map((u) => [u.id, u]))
```

b) Inside the item loop, replace the `findUnique` block for serialized items (lines 216-234) with lookup from `serialMap`:

```typescript
if (item.serialUnitId) {
  const unit = serialMap.get(item.serialUnitId)
  if (!unit) throw new Error("Серийная единица недоступна для продажи")
  // productId validation
  if (unit.productId !== item.productId) {
    throw new Error("Серийная единица не принадлежит этому товару")
  }
  // Get product name from StoreProduct (already locked)
  const stock = stockMap.get(item.productId)
  if (!stock) throw new Error("Товар не найден в магазине")
  price = toMoney(stock.sellPrice)
  costPrice = toMoney(unit.costPrice)
  productName = stock.productName
}
```

c) **LOCK-03: Replace manual stock decrement with `decrementStockForItems`.**

Currently createSale does manual `storeProduct.update({ quantity: { decrement } })` somewhere after item resolution. Find and replace that block with:

```typescript
// LOCK-03: Atomic batch stock decrement (reuses Phase 8 helper)
await decrementStockForItems(
  tx,
  data.storeId,
  resolvedItems.map((ri) => ({
    productId: ri.productId,
    quantity: ri.quantity,
    serialUnitId: ri.serialUnitId,
  })),
)
```

Remove the existing individual `storeProduct.update` decrement calls for non-serialized items. The `decrementStockForItems` helper already does FOR UPDATE + sufficiency check + batch decrement.

d) Also add explicit `storeProduct.update({ quantity: { decrement: 1 } })` for serialized items AFTER `decrementStockForItems` (mirror pattern from completeOrder in orders.ts — keeps StoreProduct.quantity counter consistent for serialized products).

  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/lib/stock-helpers.ts src/actions/sales.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/stock-helpers.ts contains `export async function lockSerialUnits(`
    - src/lib/stock-helpers.ts lockSerialUnits body contains `FOR UPDATE`
    - src/lib/stock-helpers.ts lockSerialUnits body contains `ORDER BY id ASC`
    - src/actions/sales.ts createSale contains `lockSerialUnits(tx,` call
    - src/actions/sales.ts createSale does NOT contain `tx.serialUnit.findUnique` inside the item resolution loop
    - src/actions/sales.ts createSale contains `decrementStockForItems(tx,` call
    - src/actions/sales.ts imports both `decrementStockForItems` and `lockSerialUnits` from `@/lib/stock-helpers`
    - TypeScript compilation passes without errors for both files
  </acceptance_criteria>
  <done>createSale locks SerialUnit rows via FOR UPDATE before status change, uses batch decrementStockForItems for stock, preventing double-sale of serial items and race conditions on non-serial stock</done>
</task>

<task type="auto">
  <name>Task 2: createWriteOff FOR UPDATE hardening (LOCK-05)</name>
  <files>src/actions/inventory.ts</files>
  <read_first>
    - src/actions/inventory.ts lines 1209-1323 (createWriteOff full function)
    - src/lib/stock-helpers.ts (lockSerialUnits helper created in Task 1, decrementStockForItems pattern)
  </read_first>
  <action>
**Harden `createWriteOff` in `src/actions/inventory.ts` with FOR UPDATE locking:**

a) **Non-serialized items (lines 1278-1300):** Replace `findUnique` with raw SQL FOR UPDATE:

Replace:

```typescript
const sp = await tx.storeProduct.findUnique({
  where: { storeId_productId: { storeId, productId: item.productId } },
})
```

With:

```typescript
// LOCK-05: FOR UPDATE на StoreProduct перед decrement
const spRows = await tx.$queryRaw<{ quantity: number; costPrice: any }[]>`
  SELECT quantity, "costPrice" FROM "StoreProduct"
  WHERE "storeId" = ${storeId} AND "productId" = ${item.productId}
  FOR UPDATE
`
const sp = spRows[0] ?? null
```

Keep the existing check: `if (!sp || sp.quantity < item.quantity)` — now it reads fresh locked values.

b) **Serialized items (lines 1239-1276):** Replace `findUnique` with `lockSerialUnits` (import from stock-helpers):

Replace:

```typescript
const unit = await tx.serialUnit.findUnique({
  where: { id: item.serialUnitId },
})
if (!unit) throw new Error("Серийная единица не найдена")
if (unit.status !== "IN_STOCK") throw new Error("Серийная единица не на складе")
if (unit.storeId !== storeId) throw new Error("Серийная единица не принадлежит этому магазину")
if (unit.productId !== item.productId)
  throw new Error("Серийная единица не принадлежит этому товару")
```

With:

```typescript
// LOCK-05: FOR UPDATE на SerialUnit перед WRITTEN_OFF
import { lockSerialUnits } from "@/lib/stock-helpers"
const [lockedUnit] = await lockSerialUnits(tx, [item.serialUnitId!], storeId)
if (lockedUnit.productId !== item.productId) {
  throw new Error("Серийная единица не принадлежит этому товару")
}
```

Note: `lockSerialUnits` already validates `status = IN_STOCK` and `storeId` match. The `costPrice` for the writeoff item comes from `lockedUnit.costPrice`.

Update the `writeOffItems.push` to use `Number(lockedUnit.costPrice)` instead of `Number(unit.costPrice)`.

c) Add the import at the top of inventory.ts (if not already there):

```typescript
import { lockSerialUnits } from "@/lib/stock-helpers"
```

  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/actions/inventory.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/inventory.ts createWriteOff non-serialized block contains `FOR UPDATE` raw SQL query
    - src/actions/inventory.ts createWriteOff serialized block contains `lockSerialUnits(tx,`
    - src/actions/inventory.ts createWriteOff does NOT use `tx.serialUnit.findUnique` for unit validation (replaced by lockSerialUnits)
    - src/actions/inventory.ts imports `lockSerialUnits` from `@/lib/stock-helpers`
    - TypeScript compilation passes for inventory.ts
  </acceptance_criteria>
  <done>createWriteOff uses FOR UPDATE on StoreProduct (non-serialized) and lockSerialUnits (serialized) before any stock modification, preventing race conditions during concurrent write-offs</done>
</task>

</tasks>

<verification>
- `grep -n "FOR UPDATE" src/actions/sales.ts` shows at least 2 FOR UPDATE usages (StoreProduct + SerialUnit via lockSerialUnits)
- `grep -n "FOR UPDATE" src/actions/inventory.ts | grep -i writeoff` shows FOR UPDATE in createWriteOff
- `grep -n "lockSerialUnits" src/lib/stock-helpers.ts src/actions/sales.ts src/actions/inventory.ts` shows export + 2 usages
- `grep -n "decrementStockForItems" src/actions/sales.ts` shows usage in createSale
- `npx tsc --noEmit` passes for all modified files
</verification>

<success_criteria>

- createSale: SerialUnit locked via FOR UPDATE before status change (LOCK-01)
- createSale: stock decrement uses batch decrementStockForItems helper (LOCK-03)
- createWriteOff: StoreProduct and SerialUnit locked via FOR UPDATE before modification (LOCK-05)
- All locks follow ordering convention: StoreProduct by productId, SerialUnit by id
- TypeScript compilation passes
  </success_criteria>

<output>
After completion, create `.planning/phases/09-race-conditions-locking/09-01-SUMMARY.md`
</output>
