---
phase: 09-race-conditions-locking
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/__tests__/e2e/concurrency-locking.e2e.test.ts
  - src/__tests__/e2e/transfer-reservation.e2e.test.ts
  - src/__tests__/helpers/fixtures.ts
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03, LOCK-04, LOCK-05, LOCK-06]

must_haves:
  truths:
    - "E2E тест: 2 параллельных createSale на одну SerialUnit — ровно 1 успех, 1 ошибка"
    - "E2E тест: 2 параллельных createSale на последний несерийный товар — ровно 1 успех, 1 ошибка"
    - "E2E тест: confirmTransferSent + createSale concurrent на один StoreProduct — transfer wins или sale wins, нет отрицательного stock"
    - "E2E тест: confirmReceive с duplicate IMEI откатывает StoreProduct increment"
    - "E2E тест: createWriteOff concurrent — ровно 1 успех"
    - "E2E тест: createTransfer reserves stock, createSale на reserved stock fails"
    - "E2E тест: cancelTransfer releases reservation, createSale на этот stock succeeds"
  artifacts:
    - path: "src/__tests__/e2e/concurrency-locking.e2e.test.ts"
      provides: "Concurrency E2E tests for LOCK-01, LOCK-02, LOCK-03, LOCK-05"
      min_lines: 100
    - path: "src/__tests__/e2e/transfer-reservation.e2e.test.ts"
      provides: "Transfer reservation E2E tests for LOCK-04, LOCK-06"
      min_lines: 80
  key_links:
    - from: "src/__tests__/e2e/concurrency-locking.e2e.test.ts"
      to: "src/actions/sales.ts"
      via: "direct import of createSale"
      pattern: "createSale"
    - from: "src/__tests__/e2e/transfer-reservation.e2e.test.ts"
      to: "src/actions/inventory.ts"
      via: "direct import of transfer functions"
      pattern: "createTransfer|confirmTransferSent|cancelTransfer"
---

<objective>
Написать E2E concurrency тесты на реальной БД для всех 6 LOCK-* requirements + зафиксировать deferred failures из Phase 8.

Purpose: Доказать что pessimistic locking работает при параллельных операциях. Без concurrency тестов на реальной БД невозможно верифицировать race condition fixes.
Output: 2 E2E test файла с concurrency-тестами + hotfix deferred failures.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-race-conditions-locking/09-CONTEXT.md
@.planning/phases/09-race-conditions-locking/09-01-SUMMARY.md
@.planning/phases/09-race-conditions-locking/09-02-SUMMARY.md

<interfaces>
<!-- E2E test pattern (from _template.e2e.test.ts) -->
```typescript
import { describe, it, expect } from "vitest"
import { db } from "../helpers/db"
import {
  createTestStore, createTestUser, createTestProduct,
  createTestStoreProduct, createTestShift, createTestCategory,
} from "../helpers/fixtures"
```

<!-- Concurrency test pattern: Promise.allSettled for parallel execution -->

```typescript
const results = await Promise.allSettled([
  actionFn(/* args for operation 1 */),
  actionFn(/* args for operation 2 */),
])
const fulfilled = results.filter((r) => r.status === "fulfilled")
const rejected = results.filter((r) => r.status === "rejected")
expect(fulfilled).toHaveLength(1)
expect(rejected).toHaveLength(1)
```

<!-- Server action signatures needed for tests -->

```typescript
// sales.ts
export async function createSale(data: {
  storeId: string
  items: Array<{ productId: string; quantity: number; discount: number; serialUnitId?: string | null }>
  payments: Array<{ method: string; amount: number }>
  comment?: string
}): Promise<{ id: string; number: string }>

// inventory.ts
export async function createTransfer(
  fromStoreId: string, toStoreId: string,
  items: Array<{ productId: string; quantity: number; serialUnitIds?: string[] }>
): Promise<{ id: string; number: string }>

export async function confirmTransferSent(transferId: string): Promise<{ success: true }>
export async function cancelTransfer(transferId: string): Promise<{ success: true }>
export async function confirmReceive(receiveId: string, serialData?: Record<string, ...>): Promise<{ success: true }>
export async function createWriteOff(
  storeId: string,
  items: Array<{ productId: string; quantity: number; serialUnitId?: string; writeOffReason?: string }>,
  reason: string
): Promise<{ success: true }>
```

<!-- Auth mock pattern for E2E (used in order-completion.e2e.test.ts) -->

```typescript
import { vi } from "vitest"
vi.mock("next-auth", () => ({
  default: () => ({ handlers: {}, signIn: vi.fn(), signOut: vi.fn() }),
}))
vi.mock("@/lib/auth", () => ({
  auth: vi.fn().mockResolvedValue({ user: { id: "test-user-id" } }),
  requirePermission: vi.fn().mockResolvedValue(true),
  checkPermission: vi.fn().mockResolvedValue(true),
}))
```

<!-- Invariants helper -->

```typescript
import { assertStockConservation, assertSerialConsistency } from "../helpers/invariants"
```

<!-- Deferred items from Phase 8 (from deferred-items.md):
1. partial-return-per-unit.e2e.test.ts — assertMoneyConservation failure (ledger re-entry interaction)
2. order-return-sync.e2e.test.ts — assertOrderSaleLink full-return edge
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E concurrency tests for LOCK-01/02/03/05 (createSale + createWriteOff + confirmTransferSent)</name>
  <files>src/__tests__/e2e/concurrency-locking.e2e.test.ts, src/__tests__/helpers/fixtures.ts</files>
  <read_first>
    - src/__tests__/e2e/_template.e2e.test.ts (test structure pattern)
    - src/__tests__/e2e/order-completion.e2e.test.ts lines 1-50 (auth mock pattern + imports)
    - src/__tests__/helpers/fixtures.ts (available fixture helpers — check for createTestSerialUnit, createTestShift)
    - src/actions/sales.ts lines 140-170 (createSale signature and auth requirements)
    - src/actions/inventory.ts lines 497-549 (confirmTransferSent)
    - src/actions/inventory.ts lines 1209-1323 (createWriteOff)
  </read_first>
  <action>
**Create `src/__tests__/e2e/concurrency-locking.e2e.test.ts`:**

File structure:

```typescript
/**
 * Phase 9: E2E Concurrency Locking Tests
 *
 * Тесты на реальной БД проверяющие pessimistic locking (SELECT FOR UPDATE)
 * при параллельных операциях. Каждый тест запускает 2 конкурентных операции
 * через Promise.allSettled — ожидается ровно 1 fulfilled + 1 rejected.
 *
 * Requirements: LOCK-01, LOCK-02, LOCK-03, LOCK-05
 */
import { describe, it, expect, vi } from "vitest"
import { db } from "../helpers/db"
import {
  createTestStore,
  createTestUser,
  createTestCategory,
  createTestProduct,
  createTestStoreProduct,
  createTestShift,
} from "../helpers/fixtures"

// Auth mock — ALL server actions require auth
vi.mock("next-auth", () => ({
  default: () => ({ handlers: {}, signIn: vi.fn(), signOut: vi.fn() }),
}))
vi.mock("@/lib/auth", () => ({
  auth: vi.fn().mockResolvedValue({ user: { id: "FIXTURE_USER_ID" } }),
  requirePermission: vi.fn().mockResolvedValue(true),
  checkPermission: vi.fn().mockResolvedValue(true),
}))
```

**IMPORTANT:** The `auth` mock must return a user ID that exists in the DB. In beforeEach, create a test user and update the mock to return that user's ID:

```typescript
import { auth } from "@/lib/auth"

let testUserId: string

beforeEach(async () => {
  const store = await createTestStore()
  const user = await createTestUser({ storeId: store.id })
  testUserId = user.id
  ;(auth as any).mockResolvedValue({ user: { id: testUserId } })
  // ... create other test data
})
```

**Test 1: LOCK-01 — Двойная продажа серийного товара:**

```
describe("LOCK-01: SerialUnit FOR UPDATE в createSale")
  it("2 параллельных createSale на одну SerialUnit — ровно 1 успех")
```

- Arrange: create store, user, category (isSerialized: true), product, storeProduct, serialUnit (status: IN_STOCK), shift (OPEN)
- Act: `Promise.allSettled([createSale({...serial...}), createSale({...serial...})])`
- Assert: exactly 1 fulfilled, 1 rejected with message containing "недоступна"
- Post-check: `db.serialUnit.findUnique` — status === "SOLD"
- Post-check: `db.sale.count()` === 1

**Test 2: LOCK-03 — Двойная продажа последнего несерийного товара:**

```
describe("LOCK-03: Atomic stock decrement в createSale")
  it("2 параллельных createSale на последний несерийный — ровно 1 успех")
```

- Arrange: store, user, category (isSerialized: false), product, storeProduct (quantity: 1), shift
- Act: `Promise.allSettled([createSale({qty:1}), createSale({qty:1})])`
- Assert: exactly 1 fulfilled, 1 rejected with "Недостаточно"
- Post-check: `db.storeProduct` quantity === 0 (not -1)
- Post-check: `db.sale.count()` === 1

**Test 3: LOCK-05 — Двойное списание последнего товара:**

```
describe("LOCK-05: createWriteOff FOR UPDATE")
  it("2 параллельных createWriteOff на последний товар — ровно 1 успех")
```

- Arrange: store, user, product, storeProduct (quantity: 1)
- Act: `Promise.allSettled([createWriteOff(storeId, [{productId, qty:1}], "test1"), createWriteOff(storeId, [{productId, qty:1}], "test2")])`
- Assert: exactly 1 fulfilled, 1 rejected
- Post-check: storeProduct quantity === 0

**Test 4: LOCK-02 — confirmTransferSent + createSale concurrent:**

```
describe("LOCK-02: confirmTransferSent FOR UPDATE")
  it("confirmTransferSent и createSale concurrent — нет отрицательного stock")
```

- Arrange: store1, store2, product, storeProduct (quantity: 2, reservedQuantity: 1), transfer (PENDING, qty: 1), shift
- Act: `Promise.allSettled([confirmTransferSent(transferId), createSale({qty: 2})])`
- Assert: at least 1 rejected (sale should fail because available = 2 - 1 = 1 < 2)
- Post-check: storeProduct quantity >= 0

**IMPORTANT:** If fixtures don't have `createTestSerialUnit` or `createTestShift`, add them to `src/__tests__/helpers/fixtures.ts`:

- `createTestSerialUnit({ productId, storeId, imei?, costPrice?, status? })` — creates SerialUnit with defaults
- Verify `createTestShift` exists; if not, add `createTestShift({ storeId, userId, status? })` returning Shift with OPEN status

Each test should be isolated (TRUNCATE happens in global beforeEach per template).
</action>
<verify>
<automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/**tests**/e2e/concurrency-locking.e2e.test.ts --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - src/**tests**/e2e/concurrency-locking.e2e.test.ts exists and contains at least 4 test cases - Test file contains `Promise.allSettled` for concurrency testing - Test file contains assertions `toHaveLength(1)` for fulfilled/rejected results - Test file contains post-checks on DB state (quantity === 0, sale.count === 1, etc.) - Test file imports createSale from `@/actions/sales` and createWriteOff/confirmTransferSent from `@/actions/inventory` - All 4 tests pass GREEN (vitest exit code 0) - Test "LOCK-01" verifies SerialUnit.status === SOLD after concurrent createSale - Test "LOCK-03" verifies StoreProduct.quantity === 0 (not negative) after concurrent createSale
</acceptance_criteria>
<done>4 E2E concurrency tests prove LOCK-01/02/03/05 locking works: parallel createSale on same serial unit yields exactly 1 sale; parallel createSale on last non-serial item yields exactly 1 sale; concurrent writeoffs yield exactly 1 success; concurrent transfer+sale doesn't produce negative stock</done>
</task>

<task type="auto">
  <name>Task 2: E2E tests for LOCK-04/06 (confirmReceive atomicity + transfer reservation) + Phase 8 deferred hotfix</name>
  <files>src/__tests__/e2e/transfer-reservation.e2e.test.ts, src/__tests__/e2e/partial-return-per-unit.e2e.test.ts, src/__tests__/e2e/order-return-sync.e2e.test.ts</files>
  <read_first>
    - src/__tests__/e2e/order-completion.e2e.test.ts lines 1-60 (auth mock + fixture pattern)
    - src/__tests__/e2e/partial-return-per-unit.e2e.test.ts (Phase 8 deferred failure — read FULL file to understand assertMoneyConservation failure)
    - src/__tests__/e2e/order-return-sync.e2e.test.ts (Phase 8 deferred failure — assertOrderSaleLink)
    - src/__tests__/helpers/invariants.ts (assertMoneyConservation, assertStockConservation, assertSerialConsistency)
    - src/actions/inventory.ts lines 208-339 (confirmReceive — for LOCK-04 test)
    - .planning/phases/08-order-sale-flow/deferred-items.md (detailed failure analysis)
  </read_first>
  <action>
**1. Create `src/__tests__/e2e/transfer-reservation.e2e.test.ts`:**

```typescript
/**
 * Phase 9: Transfer Reservation E2E Tests
 *
 * LOCK-04: confirmReceive atomicity (SerialUnit failure rolls back StoreProduct)
 * LOCK-06: Stock reservation при PENDING transfer
 */
```

Auth mock same pattern as concurrency-locking test.

**Test 1: LOCK-06 — createTransfer reserves stock, createSale on reserved stock fails:**

- Arrange: store1, store2, product, storeProduct in store1 (quantity: 3), shift
- Act: createTransfer(store1, store2, [{productId, qty: 2}])
- Assert: storeProduct.reservedQuantity === 2
- Act: try createSale({storeId: store1, items: [{productId, qty: 2}]}) — should fail because available = 3 - 2 = 1 < 2
- Assert: error contains "Недостаточно остатка"
- Act: createSale({storeId: store1, items: [{productId, qty: 1}]}) — should succeed (1 available)
- Assert: success, storeProduct quantity === 2 (decremented by 1)

**Test 2: LOCK-06 — cancelTransfer releases reservation:**

- Arrange: store1, store2, product, storeProduct (quantity: 2), create transfer (qty: 2)
- Assert: reservedQuantity === 2, available = 0
- Act: cancelTransfer(transferId)
- Assert: reservedQuantity === 0
- Act: createSale({qty: 2}) — should succeed now
- Assert: storeProduct quantity === 0

**Test 3: LOCK-06 — confirmTransferSent decrements both quantity and reservedQuantity:**

- Arrange: store1, store2, product, storeProduct (quantity: 5), create transfer (qty: 3)
- Assert: reservedQuantity === 3
- Act: confirmTransferSent(transferId)
- Assert: storeProduct quantity === 2, reservedQuantity === 0

**Test 4: LOCK-04 — confirmReceive with duplicate IMEI rolls back StoreProduct:**

- Arrange: store, category (isSerialized: true), product, storeProduct
- Create a receive with 2 serial items, one having a duplicate IMEI (same IMEI already exists in SerialUnit table)
- Pre-insert a SerialUnit with IMEI "123456789012345" to cause duplicate
- Act: confirmReceive(receiveId, serialData with duplicate IMEI) — should throw
- Assert: storeProduct.quantity unchanged (transaction rolled back)
- Assert: no new SerialUnit created (both rolled back, not just the duplicate)

**2. Phase 8 Deferred Hotfixes:**

Read the deferred test files fully to understand the failures:

a) `partial-return-per-unit.e2e.test.ts` — Test "3 позиции × 100₽ со скидкой 99₽":

- Root cause per deferred-items.md: assertMoneyConservation breaks because prepayment is re-entered as both `isExpense=true` (tombstone) AND new inflow Payment. The invariant double-counts.
- **Fix:** Update `assertMoneyConservation` in `src/__tests__/helpers/invariants.ts` to exclude `isExpense=true` payments from inflow sum (they are tombstone audit entries, not real inflows).
- Alternative: If the invariant logic is more complex, adjust the test assertion to account for the re-entry pattern. Read the full invariant helper to determine the right fix.

b) `order-return-sync.e2e.test.ts` — assertOrderSaleLink full-return edge:

- Read the failing test to understand the exact assertion. Fix the edge case.

**IMPORTANT:** These are diagnostic fixes. Read the full test files and invariant helpers before making changes. The deferred-items.md has detailed analysis. Do not blindly change — understand the ledger re-entry pattern from Phase 8 decisions (STATE.md: "Ledger re-entry pattern для prepayment на completeOrder").

If the fix is non-trivial and would take >30 min, document the findings and mark as "needs separate hotfix plan" in the SUMMARY.
</action>
<verify>
<automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/**tests**/e2e/transfer-reservation.e2e.test.ts --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - src/**tests**/e2e/transfer-reservation.e2e.test.ts exists with at least 4 test cases - Test "LOCK-06 reservation" verifies reservedQuantity increment on createTransfer - Test "LOCK-06 cancelTransfer" verifies reservedQuantity decrement to 0 - Test "LOCK-06 confirmTransferSent" verifies both quantity and reservedQuantity decrement - Test "LOCK-04 confirmReceive" verifies rollback of StoreProduct on SerialUnit failure - All transfer-reservation tests pass GREEN - Phase 8 deferred failures investigated: either fixed (tests pass) or documented with root cause in SUMMARY
</acceptance_criteria>
<done>E2E tests prove LOCK-04 (confirmReceive atomicity) and LOCK-06 (transfer reservation) work correctly; Phase 8 deferred failures investigated and fixed or documented</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/e2e/concurrency-locking.e2e.test.ts` — all tests GREEN
- `npx vitest run src/__tests__/e2e/transfer-reservation.e2e.test.ts` — all tests GREEN
- `npx vitest run src/__tests__/e2e/ --reporter=verbose` — no new regressions in existing E2E tests
- Total: at least 8 new E2E test cases covering all 6 LOCK-* requirements
</verification>

<success_criteria>

- Concurrency test: 2 parallel createSale on same SerialUnit — exactly 1 success (LOCK-01)
- Concurrency test: 2 parallel createSale on last non-serial item — exactly 1 success (LOCK-03)
- Concurrency test: parallel createWriteOff — exactly 1 success (LOCK-05)
- Concurrency test: confirmTransferSent + createSale — no negative stock (LOCK-02)
- Reservation test: createTransfer reserves stock, createSale on reserved fails (LOCK-06)
- Atomicity test: confirmReceive failure rolls back StoreProduct (LOCK-04)
- All E2E tests GREEN on real PostgreSQL
- Phase 8 deferred failures: resolved or documented
  </success_criteria>

<output>
After completion, create `.planning/phases/09-race-conditions-locking/09-03-SUMMARY.md`
</output>
