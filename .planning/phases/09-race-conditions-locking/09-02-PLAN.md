---
phase: 09-race-conditions-locking
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - prisma/schema.prisma
  - src/actions/inventory.ts
  - src/actions/sales.ts
autonomous: true
requirements: [LOCK-02, LOCK-04, LOCK-06]

must_haves:
  truths:
    - "confirmTransferSent использует FOR UPDATE на StoreProduct источника перед decrement"
    - "confirmReceive при failure в SerialUnit-цикле откатывает всю транзакцию включая StoreProduct increment"
    - "При создании transfer с PENDING status reservedQuantity увеличивается в источнике"
    - "POS и createSale используют (quantity - reservedQuantity) как доступный остаток"
    - "cancelTransfer уменьшает reservedQuantity обратно"
    - "confirmTransferSent уменьшает и quantity и reservedQuantity"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "StoreProduct.reservedQuantity column with default 0"
      contains: "reservedQuantity"
    - path: "src/actions/inventory.ts"
      provides: "Transfer functions with reservation logic + FOR UPDATE locking"
      contains: "reservedQuantity"
    - path: "src/actions/sales.ts"
      provides: "POS search using availableQuantity = quantity - reservedQuantity"
      contains: "reservedQuantity"
  key_links:
    - from: "src/actions/inventory.ts createTransfer"
      to: "StoreProduct.reservedQuantity"
      via: "increment reservedQuantity on PENDING"
      pattern: "reservedQuantity.*increment"
    - from: "src/actions/inventory.ts confirmTransferSent"
      to: "StoreProduct"
      via: "FOR UPDATE + decrement both quantity and reservedQuantity"
      pattern: "FOR UPDATE"
    - from: "src/actions/sales.ts searchProducts"
      to: "StoreProduct"
      via: "availableQuantity = quantity - reservedQuantity"
      pattern: "reservedQuantity"
---

<objective>
Добавить reservedQuantity на StoreProduct для резервирования stock при PENDING transfer; добавить FOR UPDATE в confirmTransferSent; верифицировать atomicity confirmReceive.

Purpose: Устранить возможность продажи зарезервированного товара (BUG-080), race condition при подтверждении отправки (BUG-045), и orphaned записей при failure в confirmReceive (BUG-047).
Output: Schema migration + hardened transfer functions + POS availableQuantity.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-race-conditions-locking/09-CONTEXT.md
@.planning/phases/09-race-conditions-locking/09-01-SUMMARY.md

<interfaces>
<!-- StoreProduct model (prisma/schema.prisma:201-214) — NO reservedQuantity yet -->
```prisma
model StoreProduct {
  id         String  @id @default(cuid())
  store      Store   @relation(fields: [storeId], references: [id], onDelete: Restrict)
  storeId    String
  product    Product @relation(fields: [productId], references: [id], onDelete: Restrict)
  productId  String
  quantity   Int     @default(0)
  minQty     Int     @default(0)
  sellPrice  Decimal @db.Decimal(12, 2)
  costPrice  Decimal @db.Decimal(12, 2)
  updatedAt  DateTime @updatedAt
  @@unique([storeId, productId])
}
```

<!-- createTransfer (inventory.ts:396-495) — creates transfer, sets SerialUnit IN_TRANSFER, NO reservation -->
<!-- confirmTransferSent (inventory.ts:497-549) — uses findUnique (NO FOR UPDATE), decrements quantity -->
<!-- cancelTransfer (inventory.ts:654-700) — reverts SerialUnit to IN_STOCK, NO reservation release -->
<!-- confirmReceive (inventory.ts:208-339) — SerialUnit loop inside $transaction -->

<!-- POS search (sales.ts:99-119) — uses sp.quantity for maxStock (non-serialized) -->

```typescript
const maxStock = isSerialized ? (serialCounts[sp.productId] ?? 0) : sp.quantity
if (maxStock <= 0) return null
```

<!-- decrementStockForItems (stock-helpers.ts) — checks sp.quantity, not availableQuantity -->
<!-- lockSerialUnits (stock-helpers.ts) — from Plan 09-01 -->

<!-- stock-helpers.ts decrementStockForItems check (line 73): -->

```typescript
if (row.quantity < decrementQty) {
  throw new Error(`Недостаточно остатка: ${row.productName}`)
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + confirmTransferSent FOR UPDATE + createTransfer reservation</name>
  <files>prisma/schema.prisma, src/actions/inventory.ts</files>
  <read_first>
    - prisma/schema.prisma (StoreProduct model around line 201)
    - src/actions/inventory.ts lines 396-549 (createTransfer + confirmTransferSent)
    - src/actions/inventory.ts lines 654-700 (cancelTransfer)
    - src/actions/inventory.ts lines 551-652 (confirmTransferReceived)
    - src/lib/stock-helpers.ts (decrementStockForItems — needs reservedQuantity awareness)
  </read_first>
  <action>
**1. Schema migration — add `reservedQuantity` to StoreProduct:**

In `prisma/schema.prisma`, add to StoreProduct model after `quantity`:

```prisma
reservedQuantity Int @default(0)
```

Run migration:

```bash
cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp"
npx prisma db push
```

**2. `createTransfer` (inventory.ts:396-495) — add stock reservation:**

Inside the `db.$transaction` block, AFTER creating the transfer and BEFORE/DURING the serial unit loop, add reservation logic for non-serialized items:

```typescript
// LOCK-06: Reserve stock in source store for non-serialized items
for (const item of items) {
  const isSerialized = item.serialUnitIds && item.serialUnitIds.length > 0
  if (!isSerialized) {
    // FOR UPDATE lock + check available stock
    const spRows = await tx.$queryRaw<{ quantity: number; reservedQuantity: number }[]>`
      SELECT quantity, "reservedQuantity" FROM "StoreProduct"
      WHERE "storeId" = ${fromStoreId} AND "productId" = ${item.productId}
      FOR UPDATE
    `
    const sp = spRows[0]
    if (!sp) throw new Error("Товар не найден в магазине-источнике")
    const available = sp.quantity - sp.reservedQuantity
    if (available < item.quantity) {
      throw new Error("Недостаточно доступного товара для перемещения")
    }
    await tx.storeProduct.update({
      where: { storeId_productId: { storeId: fromStoreId, productId: item.productId } },
      data: { reservedQuantity: { increment: item.quantity } },
    })
  }
}
```

This block goes BEFORE the serial unit handling loop (serial units are already marked IN_TRANSFER which prevents sale, so no reservedQuantity needed for them).

**3. `confirmTransferSent` (inventory.ts:497-549) — add FOR UPDATE + release reservation:**

Replace the non-serialized block (lines 515-538):

```typescript
if (!isSerialized) {
  // LOCK-02: FOR UPDATE на StoreProduct источника перед decrement
  const spRows = await tx.$queryRaw<{ quantity: number; reservedQuantity: number }[]>`
    SELECT quantity, "reservedQuantity" FROM "StoreProduct"
    WHERE "storeId" = ${transfer.fromStoreId} AND "productId" = ${item.productId}
    FOR UPDATE
  `
  const sp = spRows[0]
  if (!sp || sp.quantity < item.quantity) {
    throw new Error("Недостаточно товара на складе для перемещения")
  }

  // Decrement both quantity and reservedQuantity
  await tx.storeProduct.update({
    where: {
      storeId_productId: {
        storeId: transfer.fromStoreId,
        productId: item.productId,
      },
    },
    data: {
      quantity: { decrement: item.quantity },
      reservedQuantity: { decrement: item.quantity },
    },
  })
}
```

**4. `cancelTransfer` (inventory.ts:654-700) — release reservation:**

Inside the `db.$transaction` block, BEFORE the serial unit revert loop, add reservation release for non-serialized:

```typescript
// LOCK-06: Release reservation for non-serialized items
for (const item of transfer.items) {
  const isSerialized = item.serialUnits.length > 0
  if (!isSerialized) {
    await tx.storeProduct.update({
      where: {
        storeId_productId: {
          storeId: transfer.fromStoreId,
          productId: item.productId,
        },
      },
      data: { reservedQuantity: { decrement: item.quantity } },
    })
  }
}
```

**5. Update `decrementStockForItems` in `src/lib/stock-helpers.ts`:**

Change the sufficiency check (line 73) from `row.quantity < decrementQty` to account for reservedQuantity:

```typescript
// Read reservedQuantity in the FOR UPDATE query (add to SELECT)
const locked = await tx.$queryRaw<
  { productId: string; quantity: number; reservedQuantity: number; productName: string }[]
>`
  SELECT sp."productId", sp.quantity, sp."reservedQuantity", p.name as "productName"
  FROM "StoreProduct" sp
  JOIN "Product" p ON p.id = sp."productId"
  WHERE sp."storeId" = ${storeId} AND sp."productId" = ANY(${productIds}::text[])
  FOR UPDATE OF sp
`

// Sufficiency check: available = quantity - reservedQuantity
for (const [productId, decrementQty] of decrements) {
  const row = stockMap.get(productId)
  if (!row) throw new Error(`Товар не найден в магазине: ${productId}`)
  const available = row.quantity - (row.reservedQuantity ?? 0)
  if (available < decrementQty) {
    throw new Error(`Недостаточно остатка: ${row.productName}`)
  }
}
```

  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx prisma validate && npx tsc --noEmit src/actions/inventory.ts src/lib/stock-helpers.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma StoreProduct model contains `reservedQuantity Int @default(0)`
    - src/actions/inventory.ts createTransfer contains `reservedQuantity.*increment` for non-serialized items
    - src/actions/inventory.ts confirmTransferSent contains `FOR UPDATE` raw SQL query on StoreProduct
    - src/actions/inventory.ts confirmTransferSent contains `reservedQuantity.*decrement`
    - src/actions/inventory.ts cancelTransfer contains `reservedQuantity.*decrement`
    - src/lib/stock-helpers.ts decrementStockForItems SELECT query contains `"reservedQuantity"`
    - src/lib/stock-helpers.ts sufficiency check uses `quantity - reservedQuantity` (or equivalent)
    - `npx prisma validate` passes
    - TypeScript compilation passes for inventory.ts and stock-helpers.ts
  </acceptance_criteria>
  <done>StoreProduct has reservedQuantity field; createTransfer reserves stock at PENDING; confirmTransferSent uses FOR UPDATE and releases reservation; cancelTransfer releases reservation; decrementStockForItems respects reservedQuantity</done>
</task>

<task type="auto">
  <name>Task 2: POS availableQuantity + confirmReceive atomicity verification (LOCK-04)</name>
  <files>src/actions/sales.ts, src/actions/inventory.ts</files>
  <read_first>
    - src/actions/sales.ts lines 50-119 (searchProducts function — POS stock display)
    - src/actions/inventory.ts lines 208-339 (confirmReceive — verify transaction boundary)
    - src/actions/sales.ts lines 180-242 (createSale — quantity check after lock)
  </read_first>
  <action>
**1. POS search — use availableQuantity (sales.ts `searchProducts`):**

In the `searchProducts` function (around line 103), change the stock calculation for non-serialized:

Replace:

```typescript
const maxStock = isSerialized ? (serialCounts[sp.productId] ?? 0) : sp.quantity
```

With:

```typescript
// LOCK-06: Available stock excludes reserved quantity (pending transfers)
const reservedQty = (sp as any).reservedQuantity ?? 0
const maxStock = isSerialized ? (serialCounts[sp.productId] ?? 0) : sp.quantity - reservedQty
```

Also ensure the Prisma query in searchProducts selects `reservedQuantity`. Find the `db.storeProduct.findMany` call and add `reservedQuantity: true` to the select clause (or verify it's included via implicit select). If StoreProduct query uses `select`, add `reservedQuantity: true`. If it uses `include` or no select, it's already included.

Check the existing query — it's around lines 50-76. If it has a `select` on storeProduct fields, add `reservedQuantity: true`.

**2. createSale quantity check — use availableQuantity (sales.ts):**

In the createSale FOR UPDATE query (line 191), add `sp."reservedQuantity"` to the SELECT:

```sql
SELECT sp."productId", sp.quantity, sp."reservedQuantity", sp."sellPrice", sp."costPrice", p.name as "productName"
```

In the non-serialized quantity check (around line 240), change:

```typescript
if (stock.quantity < item.quantity) {
```

to:

```typescript
const availableQty = stock.quantity - (stock.reservedQuantity ?? 0)
if (availableQty < item.quantity) {
```

Add `reservedQuantity: number` to the raw query result type.

**3. LOCK-04: Verify confirmReceive atomicity:**

Read `confirmReceive` (inventory.ts lines 208-339). Verify:

- The entire function body (SerialUnit create loop + StoreProduct upsert/update) is inside a single `db.$transaction(async (tx) => { ... })` call
- All database operations use `tx` (not `db`)
- If any SerialUnit.create throws (e.g., duplicate IMEI), Prisma automatically rolls back the entire transaction including StoreProduct changes

If the transaction boundary is already correct (it should be based on code inspection — line 216 starts the transaction), add a code comment:

```typescript
// LOCK-04: Entire confirmReceive is within a single interactive transaction.
// If SerialUnit.create fails (duplicate IMEI, constraint violation), Prisma
// automatically rolls back ALL changes including StoreProduct.quantity increments.
// No manual compensation needed.
```

If the transaction boundary is NOT correct (some operations outside tx), wrap them inside the existing transaction.

  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx tsc --noEmit src/actions/sales.ts src/actions/inventory.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/sales.ts searchProducts computes maxStock as `sp.quantity - reservedQty` (or equivalent) for non-serialized
    - src/actions/sales.ts createSale FOR UPDATE query SELECT includes `"reservedQuantity"`
    - src/actions/sales.ts createSale non-serialized quantity check uses `quantity - reservedQuantity`
    - src/actions/inventory.ts confirmReceive has comment about LOCK-04 atomicity (or was wrapped in transaction)
    - src/actions/inventory.ts confirmReceive — all db operations use `tx` not `db`
    - TypeScript compilation passes for sales.ts and inventory.ts
  </acceptance_criteria>
  <done>POS shows available stock (excluding reserved); createSale validates against available stock; confirmReceive atomicity verified (no orphaned StoreProduct on SerialUnit failure)</done>
</task>

</tasks>

<verification>
- `grep -n "reservedQuantity" prisma/schema.prisma` shows field in StoreProduct
- `grep -n "reservedQuantity" src/actions/inventory.ts` shows usage in createTransfer, confirmTransferSent, cancelTransfer
- `grep -n "reservedQuantity" src/actions/sales.ts` shows usage in searchProducts and createSale
- `grep -n "reservedQuantity" src/lib/stock-helpers.ts` shows usage in decrementStockForItems
- `grep -n "LOCK-04" src/actions/inventory.ts` shows atomicity comment in confirmReceive
- `npx prisma validate` passes
- `npx tsc --noEmit` passes for all modified files
</verification>

<success_criteria>

- confirmTransferSent: FOR UPDATE lock on StoreProduct before decrement (LOCK-02)
- confirmReceive: entire SerialUnit-cycle within single transaction, automatic rollback on failure (LOCK-04)
- StoreProduct.reservedQuantity field exists, used by createTransfer/confirmTransferSent/cancelTransfer (LOCK-06)
- POS search and createSale use availableQuantity = quantity - reservedQuantity
- decrementStockForItems checks available stock (quantity - reservedQuantity)
  </success_criteria>

<output>
After completion, create `.planning/phases/09-race-conditions-locking/09-02-SUMMARY.md`
</output>
