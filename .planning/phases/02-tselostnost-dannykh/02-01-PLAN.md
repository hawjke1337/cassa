---
phase: 02-tselostnost-dannykh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/sales.ts
  - src/actions/inventory.ts
  - src/actions/returns.ts
  - src/lib/counters.ts
  - src/__tests__/stock-locking.test.ts
  - src/__tests__/counter-transaction.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Два продавца одновременно продают последний экземпляр — один получает ошибку, а не минусовой остаток"
    - "Номера документов не дублируются при параллельных транзакциях"
    - "getNextNumber вызывается ВНУТРИ транзакции, а не до нее"
  artifacts:
    - path: "src/actions/sales.ts"
      provides: "SELECT FOR UPDATE на StoreProduct.quantity в createSale"
      contains: "FOR UPDATE"
    - path: "src/lib/counters.ts"
      provides: "Транзакционный getNextNumber с опциональным tx"
      exports: ["getNextNumber"]
    - path: "src/__tests__/stock-locking.test.ts"
      provides: "Тесты блокировки остатков"
    - path: "src/__tests__/counter-transaction.test.ts"
      provides: "Тесты транзакционной нумерации"
  key_links:
    - from: "src/actions/sales.ts"
      to: "src/lib/counters.ts"
      via: "getNextNumber(prefix, tx) внутри $transaction"
      pattern: "getNextNumber.*tx"
    - from: "src/actions/sales.ts"
      to: "PostgreSQL"
      via: "$queryRaw SELECT FOR UPDATE"
      pattern: "queryRaw.*FOR UPDATE"
---

<objective>
Устранить race conditions на остатках и нумерации документов через pessimistic locking (SELECT FOR UPDATE) и транзакционный getNextNumber.

Purpose: Без блокировки два параллельных запроса могут продать последний экземпляр товара дважды или создать два документа с одним номером.
Output: Обновленные sales.ts, inventory.ts, returns.ts с FOR UPDATE; обновленный counters.ts с опциональным tx; unit-тесты.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tselostnost-dannykh/02-CONTEXT.md
@.planning/phases/02-tselostnost-dannykh/02-RESEARCH.md

<interfaces>
<!-- Текущая сигнатура getNextNumber -->
From src/lib/counters.ts:
```typescript
import { db } from "@/lib/db"
export async function getNextNumber(prefix: string): Promise<string>
```

<!-- createSale вызывает getNextNumber ДО транзакции (строка 133) — это race condition -->
From src/actions/sales.ts (line 133-135):
```typescript
const number = await getNextNumber("S")
const sale = await db.$transaction(async (tx) => {
```

<!-- Prisma transaction client type -->
```typescript
type PrismaTx = Parameters<Parameters<typeof db.$transaction>[0]>[0]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Транзакционный getNextNumber и тесты нумерации (DATA-02)</name>
  <files>src/lib/counters.ts, src/__tests__/counter-transaction.test.ts</files>
  <read_first>
    - src/lib/counters.ts (текущая реализация — 11 строк)
    - src/lib/db.ts (экспорт db для типа PrismaTx)
    - src/__tests__/sales-validation.test.ts (паттерн существующих тестов)
  </read_first>
  <behavior>
    - Test 1: getNextNumber("S") без tx возвращает строку формата "S-{year}-000001" (обратная совместимость)
    - Test 2: getNextNumber("S") вызванный дважды возвращает разные номера (000001, 000002)
    - Test 3: getNextNumber принимает tx параметр (TypeScript компилируется)
    - Test 4: Формат номера корректен — prefix-year-6 цифр с ведущими нулями
  </behavior>
  <action>
1. Обновить `src/lib/counters.ts`:
   - Добавить тип: `type PrismaTx = Parameters<Parameters<typeof db.$transaction>[0]>[0]`
   - Изменить сигнатуру: `export async function getNextNumber(prefix: string, tx?: PrismaTx): Promise<string>`
   - Внутри: `const client = tx ?? db`
   - Заменить `db.counter.upsert` на raw SQL через client:
     ```
     await client.$queryRaw`INSERT INTO "Counter" (id, current) VALUES (${counterId}, 0) ON CONFLICT (id) DO NOTHING`
     const result = await client.$queryRaw<{ current: number }[]>`UPDATE "Counter" SET current = current + 1 WHERE id = ${counterId} RETURNING current`
     ```
   - Return: `${prefix}-${year}-${String(result[0].current).padStart(6, "0")}`
   - Экспортировать тип: `export type { PrismaTx }`

2. Создать `src/__tests__/counter-transaction.test.ts`:
   - Мокировать `@/lib/db` с `db.$queryRaw` и `db.$transaction`
   - Тест 1: вызов без tx — использует `db.$queryRaw`
   - Тест 2: вызов с tx — использует `tx.$queryRaw`
   - Тест 3: формат возвращаемого номера корректен
   - Тест 4: два вызова возвращают разные номера (mock increment)
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/counter-transaction.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tx?: PrismaTx" src/lib/counters.ts
    - grep -q "FOR UPDATE\|RETURNING current\|ON CONFLICT" src/lib/counters.ts
    - grep -q "const client = tx ?? db" src/lib/counters.ts
    - grep -q "export type.*PrismaTx\|export { PrismaTx }\|export type { PrismaTx }" src/lib/counters.ts
    - test -f src/__tests__/counter-transaction.test.ts
  </acceptance_criteria>
  <done>getNextNumber принимает опциональный tx, использует raw SQL с атомарным INSERT ON CONFLICT + UPDATE RETURNING, все тесты зеленые</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SELECT FOR UPDATE в createSale, confirmReceive, createReturn и тесты (DATA-01)</name>
  <files>src/actions/sales.ts, src/actions/inventory.ts, src/actions/returns.ts, src/__tests__/stock-locking.test.ts</files>
  <read_first>
    - src/actions/sales.ts (полностью — createSale на строках 123-250, getNextNumber вызов на строке 133)
    - src/actions/inventory.ts (confirmReceive на строках 195-325)
    - src/actions/returns.ts (createReturn — найти декремент quantity)
    - src/lib/counters.ts (обновленный из Task 1)
  </read_first>
  <behavior>
    - Test 1: extractForUpdateQuery для одного productId возвращает SQL с FOR UPDATE
    - Test 2: extractForUpdateQuery для нескольких productIds возвращает SQL с ANY() и FOR UPDATE
    - Test 3: validateStockAvailability бросает ошибку если quantity < requested
    - Test 4: validateStockAvailability проходит если quantity >= requested
    - Test 5: validateStockAvailability бросает ошибку если товар не найден (empty result)
  </behavior>
  <action>
1. В `src/actions/sales.ts` — createSale:
   - ПЕРЕМЕСТИТЬ `getNextNumber("S")` ВНУТРЬ `db.$transaction` — `const number = await getNextNumber("S", tx)`
   - ПЕРЕД циклом `for (const item of data.items)` добавить batch SELECT FOR UPDATE:
     ```typescript
     const productIds = data.items.map(i => i.productId)
     const locked = await tx.$queryRaw<{ productId: string; quantity: number; sellPrice: any; costPrice: any }[]>`
       SELECT "productId", quantity, "sellPrice", "costPrice"
       FROM "StoreProduct"
       WHERE "storeId" = ${data.storeId} AND "productId" = ANY(${productIds}::text[])
       FOR UPDATE
     `
     const stockMap = new Map(locked.map(r => [r.productId, r]))
     ```
   - В цикле для regular products: заменить `tx.storeProduct.findUnique` на чтение из stockMap
   - Проверка: `if (!stock || stock.quantity < item.quantity) throw new Error("Недостаточно товара...")`
   - Для serialized products: оставить findUnique на SerialUnit (серийники блокируются по-другому — через status check)
   - Обновить StoreProduct.quantity через `tx.storeProduct.update` с `{ decrement: item.quantity }` (уже есть, не менять)

2. В `src/actions/inventory.ts` — confirmReceive:
   - Для non-serialized products (строки 300-319): ПЕРЕД upsert добавить SELECT FOR UPDATE:
     ```typescript
     const existing = await tx.$queryRaw<{ quantity: number; costPrice: any }[]>`
       SELECT quantity, "costPrice" FROM "StoreProduct"
       WHERE "storeId" = ${receive.storeId} AND "productId" = ${item.productId}
       FOR UPDATE
     `
     ```
   - Если existing[0] — вычислить средневзвешенную (будет детализировано в Plan 02)
   - Пока: заменить upsert.update на `{ increment: item.quantity }` с FOR UPDATE блокировкой (costPrice пока оставить как есть — Plan 02 исправит)
   - Для serialized products (строки 254-272): добавить FOR UPDATE на StoreProduct перед upsert (upsert вызывает конфликт с FOR UPDATE — использовать findFirst + create/update)

3. В `src/actions/returns.ts` — createReturn:
   - Найти место где quantity увеличивается (increment) и добавить SELECT FOR UPDATE перед ним
   - getNextNumber("R", tx) — передать tx

4. Обновить ВСЕ вызовы getNextNumber в sales.ts, inventory.ts, returns.ts, shifts.ts, orders.ts — передать tx:
   - sales.ts: `getNextNumber("S", tx)` (перенести внутрь транзакции)
   - inventory.ts: `getNextNumber("SR", tx)` для StockReceive
   - shifts.ts: `getNextNumber("SH", tx)` (строка 41 — уже внутри транзакции, добавить tx)
   - orders.ts: `getNextNumber("CO", tx)` (найти вызов, передать tx)
   - returns.ts: `getNextNumber("R", tx)` (передать tx)

5. Создать `src/__tests__/stock-locking.test.ts`:
   - Тесты на чистые функции валидации (не DB-зависимые):
     - Проверка что при quantity < requested бросается ошибка
     - Проверка что batch query формирует правильные параметры
     - Проверка что getNextNumber вызывается с tx внутри транзакции (mock-based)
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/stock-locking.test.ts --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "FOR UPDATE" src/actions/sales.ts
    - grep -q 'getNextNumber("S", tx)' src/actions/sales.ts
    - grep -q "FOR UPDATE" src/actions/inventory.ts
    - grep -q 'getNextNumber.*tx)' src/actions/shifts.ts
    - grep -q 'getNextNumber.*tx)' src/actions/returns.ts || echo "returns.ts may not have getNextNumber"
    - test -f src/__tests__/stock-locking.test.ts
    - npx vitest run --reporter=verbose 2>&1 | grep -q "Tests.*passed"
  </acceptance_criteria>
  <done>Все операции уменьшения/увеличения quantity используют SELECT FOR UPDATE; getNextNumber вызывается внутри транзакций с tx; все существующие и новые тесты зеленые</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — все тесты проходят (включая Phase 1)
- `grep -r "FOR UPDATE" src/actions/` показывает sales.ts, inventory.ts, returns.ts
- `grep -r "getNextNumber" src/actions/ src/lib/` — все вызовы внутри транзакций с tx
- `npx tsc --noEmit` — TypeScript компилируется без ошибок
</verification>

<success_criteria>
1. SELECT FOR UPDATE используется в createSale, confirmReceive, createReturn для StoreProduct.quantity
2. getNextNumber принимает опциональный tx и использует raw SQL
3. Все вызовы getNextNumber в actions перемещены внутрь транзакций и передают tx
4. Все unit-тесты зеленые (Phase 1 + Phase 2)
5. TypeScript компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/02-tselostnost-dannykh/02-01-SUMMARY.md`
</output>
