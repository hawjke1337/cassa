---
phase: 02-tselostnost-dannykh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/inventory.ts
  - src/actions/motivation-calculation.ts
  - src/lib/validations/motivation.ts
  - src/__tests__/weighted-cost-price.test.ts
  - src/__tests__/sell-price-fallback.test.ts
  - src/__tests__/partial-return-commission.test.ts
  - src/__tests__/motivation-validation.test.ts
autonomous: true
requirements: [DATA-03, DATA-05, DATA-06, DATA-07]

must_haves:
  truths:
    - "При приемке 10 единиц по 100р к 5 по 80р — costPrice становится ~93.33р"
    - "При создании StoreProduct для серийного товара с sellPrice=0 — fallback на costPrice * 1.3"
    - "Частичный возврат 1 из 3 единиц вычитает комиссию только за 1 единицу, а не за все 3"
    - "PERCENT rate > 1 отклоняется валидацией; FIXED rate > 100000 отклоняется"
  artifacts:
    - path: "src/actions/inventory.ts"
      provides: "Средневзвешенная costPrice в confirmReceive + sellPrice fallback"
      contains: "oldQty * oldCostPrice"
    - path: "src/actions/motivation-calculation.ts"
      provides: "Per-item дедукция комиссий при частичном возврате"
      contains: "returnedQuantityMap"
    - path: "src/lib/validations/motivation.ts"
      provides: "Валидация rate с условным max"
      contains: "refine"
    - path: "src/__tests__/weighted-cost-price.test.ts"
      provides: "Тесты средневзвешенной costPrice"
    - path: "src/__tests__/partial-return-commission.test.ts"
      provides: "Тесты per-item дедукции"
  key_links:
    - from: "src/actions/inventory.ts"
      to: "StoreProduct.costPrice"
      via: "средневзвешенная формула в confirmReceive"
      pattern: "oldQty.*oldCostPrice.*receiveQty.*receiveCostPrice"
    - from: "src/actions/motivation-calculation.ts"
      to: "calculateItemCommission"
      via: "per-item quantity adjustment при returnDeductions"
      pattern: "item\\.quantity.*returnedQuantity"
---

<objective>
Исправить финансовые расчеты: средневзвешенная costPrice при приемке, sellPrice fallback для серийных товаров, per-item дедукция комиссий при частичном возврате, валидация rate в формулах мотивации.

Purpose: Текущий код перезаписывает costPrice последним приходом (теряется реальная себестоимость), исключает всю комиссию за SaleItem при частичном возврате (вместо пропорциональной), и не валидирует rate (можно поставить 200% комиссию).
Output: Исправленные формулы в inventory.ts и motivation-calculation.ts, обновленная Zod-схема, unit-тесты.
</objective>

<execution_context>
@/Users/pushkarev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pushkarev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tselostnost-dannykh/02-CONTEXT.md
@.planning/phases/02-tselostnost-dannykh/02-RESEARCH.md

<interfaces>
<!-- confirmReceive: текущий код перезаписывает costPrice (строки 314-318) -->
From src/actions/inventory.ts (lines 300-319):
```typescript
// Non-serialized: existing quantity-based logic
await tx.storeProduct.upsert({
  where: { storeId_productId: { storeId: receive.storeId, productId: item.productId } },
  create: { storeId: receive.storeId, productId: item.productId, quantity: item.quantity, sellPrice: 0, costPrice: item.costPrice },
  update: { quantity: { increment: item.quantity }, costPrice: item.costPrice },  // <-- BUG: перезаписывает
})
```

<!-- confirmReceive: serialized products создают StoreProduct с sellPrice: 0 (строки 254-272) -->
From src/actions/inventory.ts (lines 255-272):
```typescript
await tx.storeProduct.upsert({
  create: { storeId: receive.storeId, productId: item.productId, quantity: 0, sellPrice: 0, costPrice: item.costPrice },
  update: { costPrice: item.costPrice },  // <-- BUG: sellPrice: 0 при создании
})
```

<!-- calculateItemCommission — чистая функция для расчета комиссии -->
From src/actions/motivation-calculation.ts (lines 90-105):
```typescript
function calculateItemCommission(
  sellPrice: number, costPrice: number, quantity: number,
  rate: number, basis: "PROFIT" | "RETAIL_PRICE", type: "PERCENT" | "FIXED" = "PERCENT"
): number {
  if (type === "FIXED") return rate * quantity
  if (basis === "PROFIT") return (sellPrice - costPrice) * quantity * rate
  return sellPrice * quantity * rate
}
```

<!-- returnedItemIds — Set<saleItemId> вместо Map<saleItemId, returnedQuantity> -->
From src/actions/motivation-calculation.ts (line 292):
```typescript
const saleItems = sale.items.filter((item) => !returnedItemIds.has(item.id))
// BUG: исключает ВЕСЬ item если хотя бы 1 единица возвращена
```

<!-- commissionRuleSchema — нет .max() на rate -->
From src/lib/validations/motivation.ts:
```typescript
export const commissionRuleSchema = z.object({
  groupId: z.string().optional(),
  type: commissionTypeSchema.default("PERCENT"),
  rate: z.coerce.number().min(0, "Значение не может быть отрицательным"),  // <-- нет max
  basis: commissionBasisSchema,
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Средневзвешенная costPrice и sellPrice fallback в confirmReceive (DATA-03, DATA-05)</name>
  <files>src/actions/inventory.ts, src/__tests__/weighted-cost-price.test.ts, src/__tests__/sell-price-fallback.test.ts</files>
  <read_first>
    - src/actions/inventory.ts (полностью — confirmReceive строки 195-325)
    - src/__tests__/sales-validation.test.ts (паттерн тестов)
  </read_first>
  <behavior>
    - Test 1: weightedAvgCostPrice(5, 80, 10, 100) === 93.33
    - Test 2: weightedAvgCostPrice(0, 0, 10, 100) === 100 (первая приемка)
    - Test 3: weightedAvgCostPrice(100, 50, 100, 50) === 50 (одинаковая цена)
    - Test 4: weightedAvgCostPrice(1, 999.99, 1, 0.01) === 500.00 (округление до 2 знаков)
    - Test 5: sellPriceFallback(0, 1000) === 1300 (costPrice * 1.3)
    - Test 6: sellPriceFallback(1500, 1000) === 1500 (sellPrice уже задана, не менять)
    - Test 7: sellPriceFallback(0, 0) === 0 (нулевая costPrice)
  </behavior>
  <action>
1. Извлечь чистые функции в `src/actions/inventory.ts` (перед confirmReceive):

```typescript
/** Средневзвешенная costPrice при приемке */
export function weightedAvgCostPrice(
  oldQty: number, oldCostPrice: number,
  receiveQty: number, receiveCostPrice: number
): number {
  if (oldQty === 0) return +receiveCostPrice.toFixed(2)
  return +((oldQty * oldCostPrice + receiveQty * receiveCostPrice) / (oldQty + receiveQty)).toFixed(2)
}

/** Fallback sellPrice для серийных товаров: costPrice * 1.3 если sellPrice = 0 */
export function sellPriceFallback(currentSellPrice: number, costPrice: number): number {
  if (currentSellPrice > 0) return currentSellPrice
  return +(costPrice * 1.3).toFixed(2)
}
```

2. В `confirmReceive` — блок non-serialized products (строки 300-319):
   - ПЕРЕД upsert: прочитать текущий StoreProduct через `tx.storeProduct.findUnique` (или использовать результат FOR UPDATE из Plan 01, если уже применён)
   - Если StoreProduct существует:
     ```typescript
     const existing = await tx.storeProduct.findUnique({
       where: { storeId_productId: { storeId: receive.storeId, productId: item.productId } },
     })
     if (existing) {
       const newCostPrice = weightedAvgCostPrice(
         existing.quantity, Number(existing.costPrice),
         item.quantity, Number(item.costPrice)
       )
       await tx.storeProduct.update({
         where: { storeId_productId: { storeId: receive.storeId, productId: item.productId } },
         data: { quantity: { increment: item.quantity }, costPrice: newCostPrice },
       })
     } else {
       await tx.storeProduct.create({
         data: { storeId: receive.storeId, productId: item.productId, quantity: item.quantity, sellPrice: 0, costPrice: item.costPrice },
       })
     }
     ```

3. В `confirmReceive` — блок serialized products (строки 254-272):
   - При upsert create: заменить `sellPrice: 0` на `sellPrice: sellPriceFallback(0, Number(item.costPrice))`
   - Это даст sellPrice = costPrice * 1.3 при первом создании StoreProduct для серийного товара
   - При upsert update: НЕ менять sellPrice (администратор мог задать вручную)

4. Создать тесты:
   - `src/__tests__/weighted-cost-price.test.ts` — тесты чистой функции weightedAvgCostPrice
   - `src/__tests__/sell-price-fallback.test.ts` — тесты чистой функции sellPriceFallback
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/weighted-cost-price.test.ts src/__tests__/sell-price-fallback.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "weightedAvgCostPrice" src/actions/inventory.ts
    - grep -q "sellPriceFallback" src/actions/inventory.ts
    - grep -q "export function weightedAvgCostPrice" src/actions/inventory.ts
    - grep -q "export function sellPriceFallback" src/actions/inventory.ts
    - grep -q "oldQty \* oldCostPrice" src/actions/inventory.ts
    - grep -q "costPrice \* 1.3" src/actions/inventory.ts
    - grep -qv "update:.*costPrice: item.costPrice" src/actions/inventory.ts || echo "BUG: costPrice перезапись не исправлена"
    - test -f src/__tests__/weighted-cost-price.test.ts
    - test -f src/__tests__/sell-price-fallback.test.ts
  </acceptance_criteria>
  <done>confirmReceive для non-serialized вычисляет средневзвешенную costPrice; для serialized ставит sellPrice = costPrice * 1.3 при создании; тесты зеленые</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-item дедукция комиссий и валидация rate мотивации (DATA-06, DATA-07)</name>
  <files>src/actions/motivation-calculation.ts, src/lib/validations/motivation.ts, src/__tests__/partial-return-commission.test.ts, src/__tests__/motivation-validation.test.ts</files>
  <read_first>
    - src/actions/motivation-calculation.ts (полностью — особенно returnedItemIds на строке 194, фильтрация на строке 292)
    - src/lib/validations/motivation.ts (commissionRuleSchema)
  </read_first>
  <behavior>
    - Test 1 (partial return): Продажа 3 шт по 1000р (cost 500р), возврат 1 шт — комиссия считается за 2 шт, дедукция за 1 шт
    - Test 2 (full return): Продажа 3 шт, возврат 3 шт — комиссия 0, дедукция за 3 шт
    - Test 3 (no return): Продажа 3 шт, возврат 0 — комиссия за 3 шт, дедукций нет
    - Test 4 (validation): commissionRuleSchema.parse({ type: "PERCENT", rate: 1.5, basis: "PROFIT" }) бросает ошибку
    - Test 5 (validation): commissionRuleSchema.parse({ type: "PERCENT", rate: 0.1, basis: "PROFIT" }) проходит
    - Test 6 (validation): commissionRuleSchema.parse({ type: "FIXED", rate: 200000, basis: "PROFIT" }) бросает ошибку
    - Test 7 (validation): commissionRuleSchema.parse({ type: "FIXED", rate: 500, basis: "RETAIL_PRICE" }) проходит
  </behavior>
  <action>
1. В `src/actions/motivation-calculation.ts` — заменить `returnedItemIds: Set<string>` на `returnedQuantityMap: Map<string, number>`:

   a) В секции "1. Returns nested in period sales" (строки 198-223):
      - Вместо `returnedItemIds.add(retItem.saleItemId)`:
        ```typescript
        const prev = returnedQuantityMap.get(retItem.saleItemId) ?? 0
        returnedQuantityMap.set(retItem.saleItemId, prev + retItem.quantity)
        ```

   b) В секции "2. Cross-period returns" (строки 262-280):
      - Аналогично: аккумулировать quantity в Map

   c) В секции "Calculate commissions per sale" (строка 292):
      - Заменить:
        ```typescript
        const saleItems = sale.items.filter((item) => !returnedItemIds.has(item.id))
        ```
      - На:
        ```typescript
        const saleItems = sale.items
          .map((item) => {
            const returnedQty = returnedQuantityMap.get(item.id) ?? 0
            const effectiveQty = item.quantity - returnedQty
            return { ...item, quantity: effectiveQty }
          })
          .filter((item) => item.quantity > 0)
        ```

   d) Убедиться что calculateItemCommission получает effectiveQty (уже получает item.quantity, который теперь = effectiveQty)

2. В `src/lib/validations/motivation.ts` — обновить commissionRuleSchema:
   - Заменить текущую схему на:
     ```typescript
     export const commissionRuleSchema = z.object({
       groupId: z.string().optional(),
       type: commissionTypeSchema.default("PERCENT"),
       rate: z.coerce.number().min(0, "Значение не может быть отрицательным"),
       basis: commissionBasisSchema,
     }).refine(
       (data) => {
         if (data.type === "PERCENT") return data.rate <= 1
         if (data.type === "FIXED") return data.rate <= 100000
         return true
       },
       {
         message: "Для процента максимум 1 (100%), для фиксированной суммы максимум 100 000",
         path: ["rate"],
       }
     )
     ```
   - Обновить тип CommissionRule: `export type CommissionRule = z.infer<typeof commissionRuleSchema>`

3. Создать `src/__tests__/partial-return-commission.test.ts`:
   - Экспортировать calculateItemCommission из motivation-calculation.ts (добавить export)
   - Тесты на чистую функцию calculateItemCommission с разными quantity
   - Тест: commission за 3 шт по 1000р (cost 500р), rate 0.1 PERCENT PROFIT = (1000-500)*3*0.1 = 150
   - Тест: commission за 2 шт (после возврата 1) = (1000-500)*2*0.1 = 100
   - Тест: commission за 0 шт = 0

4. Создать `src/__tests__/motivation-validation.test.ts`:
   - Тесты на commissionRuleSchema.safeParse с валидными/невалидными rate
  </action>
  <verify>
    <automated>cd "/Users/pushkarev/PROD/astore shop/astore ePRM Программа для учете бизнесса- касса не доделана/astore-erp" && npx vitest run src/__tests__/partial-return-commission.test.ts src/__tests__/motivation-validation.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "returnedQuantityMap" src/actions/motivation-calculation.ts
    - grep -qv "returnedItemIds" src/actions/motivation-calculation.ts || echo "WARNING: returnedItemIds still present"
    - grep -q "effectiveQty\|item.quantity - returnedQty" src/actions/motivation-calculation.ts
    - grep -q "refine" src/lib/validations/motivation.ts
    - grep -q "rate <= 1" src/lib/validations/motivation.ts
    - grep -q "rate <= 100000" src/lib/validations/motivation.ts
    - test -f src/__tests__/partial-return-commission.test.ts
    - test -f src/__tests__/motivation-validation.test.ts
  </acceptance_criteria>
  <done>Частичный возврат вычитает комиссию per-item (не per-sale); rate валидируется: PERCENT max=1, FIXED max=100000; все тесты зеленые</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — все тесты проходят
- `npx tsc --noEmit` — TypeScript компилируется
- `grep -q "weightedAvgCostPrice" src/actions/inventory.ts` — средневзвешенная costPrice
- `grep -q "returnedQuantityMap" src/actions/motivation-calculation.ts` — per-item дедукция
- `grep -q "refine" src/lib/validations/motivation.ts` — валидация rate
</verification>

<success_criteria>
1. confirmReceive вычисляет средневзвешенную costPrice для non-serialized products
2. confirmReceive ставит sellPrice = costPrice * 1.3 для новых серийных StoreProduct
3. motivation-calculation использует Map<saleItemId, returnedQuantity> вместо Set<saleItemId>
4. commissionRuleSchema.refine валидирует PERCENT <= 1 и FIXED <= 100000
5. Все тесты зеленые, TypeScript компилируется
</success_criteria>

<output>
After completion, create `.planning/phases/02-tselostnost-dannykh/02-02-SUMMARY.md`
</output>
